---
quick_task: 260409-i0d
title: Fix all documentation to match actual runtime and CLI behavior
status: planned
created: 2026-04-09T11:12:39.000Z
---

# Quick Task 260409-i0d Plan

## Goal

Bring every repo-owned documentation page in sync with actual `agent-badge` behavior, command contracts, and support guidance so docs remain a trustworthy source of truth.

## Tasks

1. Build a documentation drift checklist by comparing user-facing behavior in `packages/agent-badge/src/cli/main.ts` and `packages/agent-badge/src/commands/*.ts` against current docs, and explicitly list every mismatch before editing.
2. Update all mismatched docs across `README.md`, user docs under `docs/*.md`, and maintainer docs (`docs/RELEASE.md`, `docs/maintainers/RELEASE.md`) so command examples, runtime/hook language, auth flows, uninstall semantics, and cross-links reflect actual behavior.
3. Extend `scripts/verify-docs.sh` with assertions for the newly corrected invariants, then run `npm run docs:check` and resolve failures until the documentation verification gate passes cleanly.

---
phase: 04-publish-and-readme-badge-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/core/src/publish/badge-payload.ts
  - packages/core/src/publish/badge-payload.test.ts
  - packages/core/src/publish/publish-service.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/core/src/publish/index.ts
  - packages/core/src/index.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/agent-badge/src/cli/main.ts
  - packages/agent-badge/src/cli/main.test.ts
  - packages/agent-badge/src/index.ts
autonomous: true
requirements:
  - PUBL-03
user_setup: []
must_haves:
  truths:
    - "The first publish flow writes exactly one aggregate-only Shields endpoint JSON document to the deterministic gist file `agent-badge.json`."
    - "Publish totals come only from the existing full-backfill plus attribution pipeline; Phase 4 does not introduce a second aggregation path."
    - "Successful publish updates `state.publish.lastPublishedHash` from the exact JSON payload content but does not implement skip-if-unchanged logic yet."
    - "Unsupported `badge.mode === \"cost\"` fails or degrades explicitly; it never fabricates cost values from token totals."
  artifacts:
    - path: packages/core/src/publish/badge-payload.ts
      provides: Shields endpoint payload serializer
      contains: "schemaVersion: 1"
    - path: packages/core/src/publish/publish-service.ts
      provides: Core gist-publish orchestration
      contains: "publishBadgeToGist"
    - path: packages/agent-badge/src/commands/publish.ts
      provides: First publish command
      contains: "runPublishCommand"
    - path: packages/agent-badge/src/commands/publish.test.ts
      provides: PUBL-03 regression coverage
      contains: "lastPublishedHash"
  key_links:
    - from: packages/agent-badge/src/commands/publish.ts
      to: packages/core/src/scan/full-backfill.ts
      via: "publish reusing historical scan totals"
      pattern: "runFullBackfillScan"
    - from: packages/agent-badge/src/commands/publish.ts
      to: packages/core/src/attribution/attribution-engine.ts
      via: "publish reusing attributed repo totals"
      pattern: "attributeBackfillSessions"
    - from: packages/core/src/publish/publish-service.ts
      to: packages/core/src/publish/badge-payload.ts
      via: "publishing serialized endpoint JSON"
      pattern: "buildEndpointBadgePayload"
    - from: packages/core/src/publish/publish-service.ts
      to: packages/core/src/publish/github-gist-client.ts
      via: "updating the deterministic gist file"
      pattern: "updateGistFile"
---

<objective>
Implement the first real publish path: aggregate-only Shields payload serialization, deterministic Gist file updates, and the `agent-badge publish` command.

Purpose: Phase 4 needs a concrete publish operation before init can safely point README badges at a live remote JSON target.
Output: core payload/publish services, a CLI publish command, persisted publish hash/status updates, and tests proving only aggregate values leave the machine.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md
@.planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md
@.planning/phases/04-publish-and-readme-badge-integration/04-01-PLAN.md
@packages/core/src/publish/badge-url.ts
@packages/core/src/publish/publish-target.ts
@packages/core/src/publish/github-gist-client.ts
@packages/core/src/scan/full-backfill.ts
@packages/core/src/scan/scan-report.ts
@packages/core/src/state/state-schema.ts
@packages/core/src/config/config-schema.ts
@packages/agent-badge/src/commands/scan.ts
@packages/agent-badge/src/cli/main.ts
@packages/agent-badge/src/index.ts

<interfaces>
From `packages/core/src/config/config-schema.ts`:

```ts
badge: {
  label: string;
  mode: "sessions" | "tokens" | "cost";
}
```

From `packages/core/src/scan/full-backfill.ts`:

```ts
export async function runFullBackfillScan(
  options: RunFullBackfillScanOptions
): Promise<RunFullBackfillScanResult>;
```

From `packages/core/src/attribution/attribution-engine.ts`:

```ts
export function attributeBackfillSessions(
  options: AttributeBackfillSessionsOptions
): AttributeBackfillSessionsResult;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the aggregate-only Shields endpoint payload and core publish service</name>
  <files>packages/core/src/publish/badge-payload.ts, packages/core/src/publish/badge-payload.test.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/publish/badge-payload.ts, packages/core/src/publish/badge-payload.test.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/index.ts, packages/core/src/index.ts, packages/core/src/publish/badge-url.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/github-gist-client.ts, packages/core/src/scan/full-backfill.ts, packages/core/src/scan/scan-report.ts, packages/core/src/state/state-schema.ts, packages/core/src/config/config-schema.ts, .planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md, .planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md</read_first>
  <action>Create `packages/core/src/publish/badge-payload.ts` exporting `buildEndpointBadgePayload({ label, mode, includedTotals })`. The function must return exactly `{ schemaVersion: 1, label, message, color }`, where `message` is `${includedTotals.sessions} sessions` for `mode === "sessions"` and `${includedTotals.tokens} tokens` for `mode === "tokens"`, and `color` is `brightgreen` when the selected numeric total is greater than 0 or `lightgrey` when it is 0. When `mode === "cost"`, throw the exact error message `Badge mode "cost" is not yet supported because scan results do not include cost totals.`. Create `packages/core/src/publish/publish-service.ts` exporting `publishBadgeToGist({ config, state, scan, attribution, client })` that computes included totals from attributed sessions, builds the endpoint payload, serializes it with `JSON.stringify(payload, null, 2) + "\\n"`, updates the deterministic file `agent-badge.json` through `client.updateGistFile`, and returns the next state with `publish.status = "published"` and `publish.lastPublishedHash` set from `createHash("sha256").update(serializedPayload).digest("hex")`. Keep all payload fields aggregate-only. Export the new modules from `packages/core/src/publish/index.ts` and `packages/core/src/index.ts`.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/badge-payload.test.ts packages/core/src/publish/publish-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/publish/badge-payload.ts` contains `schemaVersion: 1`
    - `packages/core/src/publish/badge-payload.ts` contains `sessions`
    - `packages/core/src/publish/badge-payload.ts` contains `tokens`
    - `packages/core/src/publish/badge-payload.ts` contains `Badge mode "cost" is not yet supported because scan results do not include cost totals.`
    - `packages/core/src/publish/publish-service.ts` contains `publishBadgeToGist`
    - `packages/core/src/publish/publish-service.ts` contains `createHash("sha256")`
    - `packages/core/src/publish/publish-service.ts` contains `agent-badge.json`
    - `packages/core/src/publish/publish-service.test.ts` contains `aggregate-only`
  </acceptance_criteria>
  <done>The core package can now serialize one deterministic endpoint payload and publish it to the configured gist target without leaking local evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add the `publish` command and wire it to the existing scan plus attribution pipeline</name>
  <files>packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts, packages/agent-badge/src/index.ts</files>
  <read_first>packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/scan.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts, packages/agent-badge/src/index.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-target.ts, packages/core/src/scan/full-backfill.ts, packages/core/src/scan/scan-state.ts, .planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md, .planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md</read_first>
  <action>Create `packages/agent-badge/src/commands/publish.ts` exporting `runPublishCommand(options)` with the same `cwd`, `homeRoot`, and `stdout` seams used by `runScanCommand()`. The command must read `.agent-badge/config.json` and `.agent-badge/state.json`, require `config.publish.gistId` and `config.publish.badgeUrl`, call `runFullBackfillScan()` plus `attributeBackfillSessions()` to compute current totals, invoke `publishBadgeToGist()`, write the next state back to `.agent-badge/state.json`, and print a summary that starts with `agent-badge publish`. If no gist is configured, throw the exact error `Publish is not configured. Run \`agent-badge init\` or re-run init with \`--gist-id <id>\` first.`. Register `publish` in `packages/agent-badge/src/cli/main.ts`, add a CLI main test that the command is registered, and export `runPublishCommand` from `packages/agent-badge/src/index.ts`. Keep the publish path separate from `scan`; do not embed publish side effects into `runScanCommand()`.</action>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/cli/main.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/publish.ts` contains `export async function runPublishCommand`
    - `packages/agent-badge/src/commands/publish.ts` contains `runFullBackfillScan`
    - `packages/agent-badge/src/commands/publish.ts` contains `attributeBackfillSessions`
    - `packages/agent-badge/src/commands/publish.ts` contains `publishBadgeToGist`
    - `packages/agent-badge/src/commands/publish.ts` contains `Publish is not configured. Run \`agent-badge init\` or re-run init with \`--gist-id <id>\` first.`
    - `packages/agent-badge/src/cli/main.ts` contains `.command("publish")`
    - `packages/agent-badge/src/index.ts` contains `runPublishCommand`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `lastPublishedHash`
  </acceptance_criteria>
  <done>The runtime now has a dedicated `publish` command that reuses scan/attribution data and writes one aggregate endpoint payload to the configured gist.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --run packages/core/src/publish/badge-payload.test.ts packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/cli/main.test.ts` succeeds
- [ ] `packages/core/src/publish/badge-payload.ts` throws the explicit unsupported-cost message instead of fabricating cost values
- [ ] `packages/agent-badge/src/commands/publish.ts` uses `runFullBackfillScan` and `attributeBackfillSessions`
- [ ] Successful publish updates `state.publish.status` and `state.publish.lastPublishedHash`
</verification>

<success_criteria>
- PUBL-03 is satisfied by a real aggregate-only publish flow
- The repo has a first-class `agent-badge publish` command
- The deterministic gist file `agent-badge.json` is updated in place
- Publish state is persisted for later diff-aware logic in Phase 5
</success_criteria>

<output>
After completion, create `.planning/phases/04-publish-and-readme-badge-integration/04-02-SUMMARY.md`
</output>

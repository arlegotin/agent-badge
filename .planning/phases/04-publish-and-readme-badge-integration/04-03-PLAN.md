---
phase: 04-publish-and-readme-badge-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - packages/core/src/publish/readme-badge.ts
  - packages/core/src/publish/readme-badge.test.ts
  - packages/core/src/publish/index.ts
  - packages/core/src/index.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
autonomous: true
requirements:
  - BOOT-05
  - PUBL-04
user_setup: []
must_haves:
  truths:
    - "Init inserts exactly one managed README badge block when a stable badge URL exists and reuses that block on re-run instead of appending duplicates."
    - "When no README exists, init prints one pasteable markdown snippet and does not silently create a README file."
    - "Init performs the first publish before mutating README content, so the inserted badge points at a live remote JSON target rather than a broken placeholder."
    - "If publish target setup is deferred or the first publish fails, init leaves README content untouched and prints actionable next steps instead."
  artifacts:
    - path: packages/core/src/publish/readme-badge.ts
      provides: Managed README badge insertion and snippet helpers
      contains: "agent-badge:start"
    - path: packages/agent-badge/src/commands/init.ts
      provides: End-to-end init publish plus README orchestration
      contains: "buildReadmeBadgeSnippet"
    - path: packages/agent-badge/src/commands/init.test.ts
      provides: BOOT-05 and PUBL-04 regression coverage
      contains: "does not duplicate the badge"
  key_links:
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/publish/publish-service.ts
      via: "init performing first publish before README mutation"
      pattern: "publishBadgeToGist"
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/publish/readme-badge.ts
      via: "managed README block insertion and snippet generation"
      pattern: "upsertReadmeBadge|buildReadmeBadgeSnippet"
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/init/preflight.ts
      via: "README file selection from preflight result"
      pattern: "readme.fileName"
---

<objective>
Finish Phase 4 with stable README badge integration and end-to-end init orchestration that performs the first publish before inserting the badge.

Purpose: BOOT-05 and PUBL-04 are the visible Phase 4 outcome, and they only count if init mutates README content idempotently after a successful first publish or emits a clean snippet when no README exists.
Output: a managed README badge helper, init-time first publish orchestration, no-README snippet behavior, and tests proving re-runs do not duplicate the badge URL.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md
@.planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md
@.planning/phases/04-publish-and-readme-badge-integration/04-01-PLAN.md
@.planning/phases/04-publish-and-readme-badge-integration/04-02-PLAN.md
@packages/core/src/publish/badge-url.ts
@packages/core/src/publish/publish-target.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/init/preflight.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/init.test.ts
@packages/testkit/src/repo-fixtures.ts

<interfaces>
From `packages/core/src/init/preflight.ts`:

```ts
export interface InitReadmeStatus {
  readonly exists: boolean;
  readonly fileName: string | null;
}
```

From `packages/core/src/publish/publish-service.ts`:

```ts
export async function publishBadgeToGist(
  options: PublishBadgeToGistOptions
): Promise<PublishBadgeToGistResult>;
```

From `packages/agent-badge/src/commands/init.ts` after `04-01`:

```ts
export interface RunInitCommandOptions {
  readonly gistId?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a managed README badge helper with a no-README snippet path</name>
  <files>packages/core/src/publish/readme-badge.ts, packages/core/src/publish/readme-badge.test.ts, packages/core/src/publish/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/publish/readme-badge.ts, packages/core/src/publish/readme-badge.test.ts, packages/core/src/publish/badge-url.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/index.ts, packages/core/src/index.ts, .planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md, .planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md</read_first>
  <action>Create `packages/core/src/publish/readme-badge.ts` with these exact exports: `AGENT_BADGE_README_START_MARKER = "<!-- agent-badge:start -->"`, `AGENT_BADGE_README_END_MARKER = "<!-- agent-badge:end -->"`, `buildReadmeBadgeMarkdown({ label, badgeUrl })`, `buildReadmeBadgeSnippet({ label, badgeUrl })`, and `upsertReadmeBadge(content, badgeMarkdown)`. `buildReadmeBadgeMarkdown()` and `buildReadmeBadgeSnippet()` must both produce the exact markdown image form `![${label}](${badgeUrl})`. `upsertReadmeBadge()` must remove any existing managed block between the markers, then write one managed block containing the badge markdown, preserving all other README content and ensuring the final content ends with a newline. Add `packages/core/src/publish/readme-badge.test.ts` covering the exact scenarios `inserts one managed badge block`, `reuses the managed block on re-run`, and `builds a pasteable snippet when no README exists`. Export the helper from `packages/core/src/publish/index.ts` and `packages/core/src/index.ts`.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/readme-badge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/publish/readme-badge.ts` contains `<!-- agent-badge:start -->`
    - `packages/core/src/publish/readme-badge.ts` contains `<!-- agent-badge:end -->`
    - `packages/core/src/publish/readme-badge.ts` contains `buildReadmeBadgeSnippet`
    - `packages/core/src/publish/readme-badge.ts` contains `![${label}](${badgeUrl})`
    - `packages/core/src/publish/readme-badge.test.ts` contains `inserts one managed badge block`
    - `packages/core/src/publish/readme-badge.test.ts` contains `reuses the managed block on re-run`
    - `packages/core/src/index.ts` contains `./publish/index.js`
  </acceptance_criteria>
  <done>The core package now has one deterministic way to insert or emit the badge markup without duplicating README content.</done>
</task>

<task type="auto">
  <name>Task 2: Extend init to perform first publish, then insert the badge or emit a snippet</name>
  <files>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts</files>
  <read_first>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/readme-badge.ts, packages/core/src/init/preflight.ts, packages/agent-badge/src/commands/publish.ts, packages/testkit/src/repo-fixtures.ts, .planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md, .planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md</read_first>
  <action>Update `packages/agent-badge/src/commands/init.ts` so that after scaffold/runtime wiring and publish-target setup complete, it follows this exact order when `config.publish.gistId` and `config.publish.badgeUrl` are both non-null: read config/state, run `runFullBackfillScan()`, compute attribution with `attributeBackfillSessions()`, call `publishBadgeToGist()`, persist the updated state, then handle README output. If `preflight.readme.exists === true`, read the detected README file from `preflight.readme.fileName`, build the markdown with `buildReadmeBadgeMarkdown({ label: config.badge.label, badgeUrl: config.publish.badgeUrl })`, upsert the managed block, and write the file back. If `preflight.readme.exists === false`, print a line starting with `- Badge snippet:` followed by `buildReadmeBadgeSnippet({ label: config.badge.label, badgeUrl: config.publish.badgeUrl })`. If publish target setup is deferred or the first publish throws, do not write README content and print one actionable line starting with `- Badge setup deferred:`. Update `packages/agent-badge/src/commands/init.test.ts` with coverage for these exact behaviors: `prints a pasteable snippet when README is missing`, `does not duplicate the badge on re-running init`, and `does not insert a broken badge when publish target is deferred`.</action>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/init.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/init.ts` contains `runFullBackfillScan`
    - `packages/agent-badge/src/commands/init.ts` contains `attributeBackfillSessions`
    - `packages/agent-badge/src/commands/init.ts` contains `publishBadgeToGist`
    - `packages/agent-badge/src/commands/init.ts` contains `buildReadmeBadgeMarkdown`
    - `packages/agent-badge/src/commands/init.ts` contains `buildReadmeBadgeSnippet`
    - `packages/agent-badge/src/commands/init.ts` contains `- Badge snippet:`
    - `packages/agent-badge/src/commands/init.ts` contains `- Badge setup deferred:`
    - `packages/agent-badge/src/commands/init.test.ts` contains `prints a pasteable snippet when README is missing`
    - `packages/agent-badge/src/commands/init.test.ts` contains `does not duplicate the badge on re-running init`
    - `packages/agent-badge/src/commands/init.test.ts` contains `does not insert a broken badge when publish target is deferred`
  </acceptance_criteria>
  <done>Init now completes the Phase 4 onboarding path: first publish, then one stable README badge insert or snippet output, with deferred paths leaving the repo untouched.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --run packages/core/src/publish/readme-badge.test.ts packages/agent-badge/src/commands/init.test.ts` succeeds
- [ ] `packages/core/src/publish/readme-badge.ts` manages one marker block bounded by `<!-- agent-badge:start -->` and `<!-- agent-badge:end -->`
- [ ] `packages/agent-badge/src/commands/init.ts` performs the first publish before mutating README content
- [ ] Re-running init does not duplicate the badge line or marker block
- [ ] Missing README prints one pasteable snippet instead of creating a file silently
</verification>

<success_criteria>
- BOOT-05 is satisfied by README insertion or snippet output from init
- PUBL-04 is satisfied by one stable badge URL that later publishes reuse remotely
- Init leaves README untouched when publish target setup is deferred or publishing fails
- Phase 4 is complete end to end: target setup, publish, and repo badge integration
</success_criteria>

<output>
After completion, create `.planning/phases/04-publish-and-readme-badge-integration/04-03-SUMMARY.md`
</output>

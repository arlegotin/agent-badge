---
phase: 04-publish-and-readme-badge-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/src/config/config-schema.ts
  - packages/core/src/config/config-schema.test.ts
  - packages/core/src/state/state-schema.ts
  - packages/core/src/state/state-schema.test.ts
  - packages/core/src/init/scaffold.ts
  - packages/core/src/publish/badge-url.ts
  - packages/core/src/publish/github-gist-client.ts
  - packages/core/src/publish/github-gist-client.test.ts
  - packages/core/src/publish/publish-state.ts
  - packages/core/src/publish/publish-state.test.ts
  - packages/core/src/publish/publish-target.ts
  - packages/core/src/publish/index.ts
  - packages/core/src/index.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
  - packages/agent-badge/src/cli/main.ts
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
user_setup: []
must_haves:
  truths:
    - "Init can create exactly one public gist when auth exists, reuse an explicitly provided gist ID when one is supplied, and avoid creating duplicate publish targets on re-run."
    - "A missing or failing GitHub publish target leaves the repo in explicit deferred mode with actionable next-step output instead of broken README or publish state."
    - "The configured gist ID and badge URL are derived once from a deterministic gist target plus the fixed filename `agent-badge.json`; they are not copied from revision-specific Gist API `raw_url` values."
    - "Publish config/state persistence remains aggregate-only and never stores transcript text, local paths, or provider session details."
  artifacts:
    - path: packages/core/src/publish/publish-target.ts
      provides: Init-time create/connect/defer orchestration
      contains: "ensurePublishTarget"
    - path: packages/core/src/publish/github-gist-client.ts
      provides: GitHub Gist transport boundary
      contains: "createGitHubGistClient"
    - path: packages/core/src/publish/badge-url.ts
      provides: Stable badge URL derivation
      contains: "agent-badge.json"
    - path: packages/agent-badge/src/commands/init.ts
      provides: Init wiring for publish-target setup
      contains: "gistId"
    - path: packages/core/src/publish/publish-state.test.ts
      provides: PUBL-01 and PUBL-02 regression coverage
      contains: "deferred"
  key_links:
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/publish/publish-target.ts
      via: "init establishing or reusing the publish target"
      pattern: "ensurePublishTarget"
    - from: packages/core/src/publish/publish-target.ts
      to: packages/core/src/publish/github-gist-client.ts
      via: "GitHub Gist create/get operations"
      pattern: "createGitHubGistClient|GitHubGistClient"
    - from: packages/core/src/publish/publish-target.ts
      to: packages/core/src/publish/badge-url.ts
      via: "stable badge URL derivation from gist identity"
      pattern: "buildStableBadgeUrl"
    - from: packages/core/src/init/scaffold.ts
      to: packages/core/src/state/state-schema.ts
      via: "schema-safe publish-state reconciliation"
      pattern: "deferred|lastPublishedHash"
---

<objective>
Add the publish-target foundation: deterministic badge URL derivation, GitHub Gist create/connect/defer services, and init wiring that persists one stable target without duplicating gists.

Purpose: Phase 4 cannot publish JSON or touch README safely until init can establish one stable public Gist target and persist that decision idempotently.
Output: a core publish namespace with a GitHub client seam, publish-target orchestration, stable badge URL helper, explicit deferred mode, and init CLI coverage for create/connect/defer flows.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md
@.planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md
@packages/core/package.json
@packages/core/src/config/config-schema.ts
@packages/core/src/config/config-schema.test.ts
@packages/core/src/state/state-schema.ts
@packages/core/src/state/state-schema.test.ts
@packages/core/src/init/scaffold.ts
@packages/core/src/init/github-auth.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/init.test.ts
@packages/agent-badge/src/cli/main.ts

<interfaces>
From `packages/core/src/config/config-schema.ts`:

```ts
publish: {
  provider: "github-gist";
  gistId: string | null;
  badgeUrl: string | null;
}
```

From `packages/core/src/state/state-schema.ts`:

```ts
publish: {
  status: "idle" | "pending" | "published" | "error";
  gistId: string | null;
  lastPublishedHash: string | null;
}
```

From `packages/agent-badge/src/commands/init.ts`:

```ts
export async function runInitCommand(
  options: RunInitCommandOptions = {}
): Promise<InitCommandResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend publish config/state defaults for explicit target management</name>
  <files>packages/core/src/config/config-schema.ts, packages/core/src/config/config-schema.test.ts, packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/init/scaffold.ts</files>
  <read_first>packages/core/src/config/config-schema.ts, packages/core/src/config/config-schema.test.ts, packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/init/scaffold.ts, .planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md, .planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md</read_first>
  <action>Keep `config.publish` limited to exactly `provider`, `gistId`, and `badgeUrl`, but update `packages/core/src/state/state-schema.ts` so the publish status enum becomes exactly `"idle" | "deferred" | "pending" | "published" | "error"`. Preserve `gistId` and `lastPublishedHash` in state and update `defaultAgentBadgeState.publish.status` to `"idle"`. In `packages/core/src/init/scaffold.ts`, reconcile the new `"deferred"` state value without dropping valid existing `gistId` or `lastPublishedHash` fields. Add tests in `packages/core/src/config/config-schema.test.ts` and `packages/core/src/state/state-schema.test.ts` that prove config remains aggregate-only, the new `deferred` status parses successfully, and transcript-like or path-like publish fields are still rejected.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/config/config-schema.test.ts packages/core/src/state/state-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/state/state-schema.ts` contains `"deferred"`
    - `packages/core/src/state/state-schema.test.ts` contains `deferred`
    - `packages/core/src/config/config-schema.test.ts` contains `publish.badgeUrl`
    - `packages/core/src/init/scaffold.ts` contains `publishStatuses`
    - `packages/core/src/init/scaffold.ts` contains `"deferred"`
  </acceptance_criteria>
  <done>Publish target persistence can now represent a deliberate unpublished/deferred state without losing the existing gist/hash bookkeeping seam.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic badge URL and GitHub Gist target services in core</name>
  <files>packages/core/package.json, packages/core/src/publish/badge-url.ts, packages/core/src/publish/github-gist-client.ts, packages/core/src/publish/github-gist-client.test.ts, packages/core/src/publish/publish-state.ts, packages/core/src/publish/publish-state.test.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/package.json, packages/core/src/publish/badge-url.ts, packages/core/src/publish/github-gist-client.ts, packages/core/src/publish/github-gist-client.test.ts, packages/core/src/publish/publish-state.ts, packages/core/src/publish/publish-state.test.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/index.ts, packages/core/src/index.ts, packages/core/src/config/config-schema.ts, packages/core/src/state/state-schema.ts, packages/core/src/init/github-auth.ts, .planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md, .planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md</read_first>
  <action>Add `octokit` version `^5.0.0` to `packages/core/package.json`. Create `packages/core/src/publish/badge-url.ts` with a fixed exported filename constant `AGENT_BADGE_GIST_FILE = "agent-badge.json"` and an exported `buildStableBadgeUrl({ ownerLogin, gistId })` helper that returns a Shields endpoint URL rooted at `https://img.shields.io/endpoint`, points `url` at the stable raw gist path for `agent-badge.json`, and appends `cacheSeconds=3600`. Create `packages/core/src/publish/github-gist-client.ts` defining a `GitHubGistClient` interface with exactly `getGist(gistId)`, `createPublicGist(input)`, and `updateGistFile(input)` methods plus a `createGitHubGistClient()` factory. Create `packages/core/src/publish/publish-state.ts` helpers that atomically apply publish-target results back into config/state without storing any local provider data. Create `packages/core/src/publish/publish-target.ts` exporting `ensurePublishTarget({ config, state, githubAuth, gistId, client })` that follows this exact order: validate and reuse an explicit `gistId` when provided, otherwise reuse `config.publish.gistId` when already configured, otherwise create one public gist when `githubAuth.available === true`, otherwise return `status: "deferred"`. When creating a gist, seed the deterministic file `agent-badge.json` with the exact placeholder JSON `{"schemaVersion":1,"label":"AI Usage","message":"pending","color":"lightgrey"}` and derive `badgeUrl` from `buildStableBadgeUrl()` instead of any API `raw_url` field. Export the publish namespace from `packages/core/src/publish/index.ts` and re-export it from `packages/core/src/index.ts`.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/github-gist-client.test.ts packages/core/src/publish/publish-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/package.json` contains `"octokit": "^5.0.0"`
    - `packages/core/src/publish/badge-url.ts` contains `AGENT_BADGE_GIST_FILE`
    - `packages/core/src/publish/badge-url.ts` contains `agent-badge.json`
    - `packages/core/src/publish/badge-url.ts` contains `cacheSeconds=3600`
    - `packages/core/src/publish/github-gist-client.ts` contains `createGitHubGistClient`
    - `packages/core/src/publish/github-gist-client.ts` contains `createPublicGist`
    - `packages/core/src/publish/publish-target.ts` contains `export async function ensurePublishTarget`
    - `packages/core/src/publish/publish-target.ts` contains `status: "deferred"`
    - `packages/core/src/publish/publish-target.ts` contains `"message":"pending"`
    - `packages/core/src/index.ts` contains `./publish/index.js`
  </acceptance_criteria>
  <done>The core package can now create, validate, or defer one deterministic public publish target without relying on revision-specific Gist URLs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire init to create, connect, or defer the publish target idempotently</name>
  <files>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/cli/main.ts</files>
  <read_first>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/cli/main.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/publish-state.ts, packages/core/src/init/github-auth.ts, .planning/phases/04-publish-and-readme-badge-integration/04-RESEARCH.md, .planning/phases/04-publish-and-readme-badge-integration/04-VALIDATION.md</read_first>
  <action>Extend `RunInitCommandOptions` with `gistId?: string`. Update `packages/agent-badge/src/cli/main.ts` so `agent-badge init` accepts `--gist-id <id>` and forwards it to `runInitCommand()`. In `packages/agent-badge/src/commands/init.ts`, after scaffold and runtime wiring succeed, load `.agent-badge/config.json` and `.agent-badge/state.json`, call `ensurePublishTarget()` with the detected `githubAuth` and optional `gistId`, persist the returned config/state through the shared helpers, and print one summary line that starts with `- Publish target:` followed by one of exactly `created public gist`, `connected existing gist`, `reused existing gist`, or `deferred`. Do not touch README content or run publish yet in this plan. Update `packages/agent-badge/src/commands/init.test.ts` with coverage for `--gist-id`, automatic public-gist creation, and deferred-mode re-runs that prove no second gist is created when `config.publish.gistId` is already set.</action>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/cli/main.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-badge/src/cli/main.ts` contains `--gist-id <id>`
    - `packages/agent-badge/src/commands/init.ts` contains `gistId?: string`
    - `packages/agent-badge/src/commands/init.ts` contains `ensurePublishTarget`
    - `packages/agent-badge/src/commands/init.ts` contains `- Publish target:`
    - `packages/agent-badge/src/commands/init.test.ts` contains `connected existing gist`
    - `packages/agent-badge/src/commands/init.test.ts` contains `created public gist`
    - `packages/agent-badge/src/commands/init.test.ts` contains `deferred`
  </acceptance_criteria>
  <done>Init now owns the Phase 4 publish-target decision cleanly: create, connect, reuse, or defer, with deterministic persisted state and no duplicate gists.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --run packages/core/src/config/config-schema.test.ts packages/core/src/state/state-schema.test.ts packages/core/src/publish/github-gist-client.test.ts packages/core/src/publish/publish-state.test.ts packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/cli/main.test.ts` succeeds
- [ ] `packages/core/src/publish/publish-target.ts` seeds `agent-badge.json` and never copies API `raw_url` into config
- [ ] `packages/agent-badge/src/commands/init.ts` accepts `--gist-id` and prints one explicit publish-target outcome
- [ ] Re-running init with an existing configured gist reuses it instead of creating another
</verification>

<success_criteria>
- PUBL-01 is satisfied by one public-gist create flow from init
- PUBL-02 is satisfied by explicit connect and deferred paths without hidden state drift
- The repo stores one stable badge target derived from gist identity plus `agent-badge.json`
- Phase 4 has a reusable core publish namespace for later publish and README work
</success_criteria>

<output>
After completion, create `.planning/phases/04-publish-and-readme-badge-integration/04-01-SUMMARY.md`
</output>

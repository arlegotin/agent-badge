---
phase: 17-publish-failure-visibility-and-state-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/publish/publish-trust.ts
  - packages/core/src/publish/publish-trust.test.ts
  - packages/core/src/publish/index.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
  - packages/agent-badge/src/commands/status.ts
  - packages/agent-badge/src/commands/status.test.ts
autonomous: true
requirements:
  - OPER-01
user_setup: []
must_haves:
  truths:
    - "Operators can see from `refresh` and `status` when a local refresh is newer than the last successful live badge publish instead of reading a generic publish error."
    - "Live-badge trust appears as its own operator concept in `refresh` and `status`, separate from Phase 16 shared-mode health."
    - "No publish attempt, unchanged badge payload, and stale-after-failed-publish states render distinct output before canonical persistence lands in Plan 02."
  artifacts:
    - path: packages/core/src/publish/publish-trust.ts
      provides: reusable live-badge trust derivation from current persisted state
      contains: derivePublishTrustReport
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: immediate live-badge trust visibility in refresh output, including failed-soft runs
      contains: Live badge trust:
    - path: packages/agent-badge/src/commands/status.ts
      provides: status summary of live-badge trust that stays separate from shared-mode lines
      contains: Live badge trust:
  key_links:
    - from: packages/core/src/publish/publish-trust.ts
      to: packages/agent-badge/src/commands/refresh.ts
      via: "refresh prints the canonical live-badge trust summary after success and after failed-soft state persistence"
      pattern: derivePublishTrustReport
    - from: packages/core/src/publish/publish-trust.ts
      to: packages/agent-badge/src/commands/status.ts
      via: "status renders the same live-badge trust vocabulary instead of inventing command-local wording"
      pattern: derivePublishTrustReport
    - from: packages/agent-badge/src/commands/status.ts
      to: packages/core/src/publish/shared-health.ts
      via: "status keeps shared-mode output on separate lines so shared-health does not become badge-trust"
      pattern: Shared mode:
---

<objective>
Add operator-visible live-badge trust messaging to the repo surfaces people already use in Phase 17.

Purpose: Phase 16 introduced shared-mode health, but the normal CLI still does not answer the operator question "did my last local refresh actually update the live badge?" This plan introduces one live-badge trust helper and wires it into `refresh` and `status` without overloading shared-health.
Output: a reusable `publish-trust` helper plus `refresh`/`status` output that distinguishes not-attempted, unchanged, current, and stale-after-failed-publish states.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-publish-failure-visibility-and-state-trust/17-RESEARCH.md
@packages/core/src/state/state-schema.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/publish/shared-health.ts
@packages/agent-badge/src/commands/refresh.ts
@packages/agent-badge/src/commands/status.ts

<interfaces>
From packages/core/src/state/state-schema.ts:
```typescript
publish: z.object({
  status: publishStatusSchema,
  gistId: z.string().min(1).nullable(),
  lastPublishedHash: z.string().min(1).nullable(),
  lastPublishedAt: isoDateTimeSchema.nullable(),
  publisherId: z.string().min(1).nullable().optional().default(null),
  mode: publishModeSchema.optional().default("legacy")
})

refresh: z.object({
  lastRefreshedAt: isoDateTimeSchema.nullable(),
  lastScanMode: refreshScanModeSchema.nullable(),
  lastPublishDecision: refreshPublishDecisionSchema.nullable(),
  summary: refreshSummarySchema.nullable()
})
```

From packages/agent-badge/src/commands/status.ts:
```typescript
const report = [
  "agent-badge status",
  `- Totals: ${formatTotalsLine(state)}`,
  `- Providers: ${formatProvidersLine(config)}`,
  `- Publish: ${formatPublishLine(config, state)}`,
  ...sharedModeLines,
  `- Last refresh: ${formatLastRefreshLine(state)}`,
  `- Checkpoints: ${formatCheckpointsLine(state)}`
].join("\n");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a dedicated live-badge trust helper that derives operator-visible state from persisted refresh and publish facts</name>
  <files>packages/core/src/publish/publish-trust.ts, packages/core/src/publish/publish-trust.test.ts, packages/core/src/publish/index.ts</files>
  <read_first>packages/core/src/state/state-schema.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/shared-health.ts, packages/core/src/publish/index.ts, .planning/phases/17-publish-failure-visibility-and-state-trust/17-RESEARCH.md</read_first>
  <action>Create `packages/core/src/publish/publish-trust.ts` and export it from `packages/core/src/publish/index.ts`. Define one machine-readable report dedicated to live badge trust, not shared-health: `PublishTrustStatus = "current" | "unchanged" | "not-attempted" | "stale-failed-publish" | "unknown"` plus a `derivePublishTrustReport({ state, now })` helper that only uses the currently persisted `publish` and `refresh` facts. In Phase 17 Plan 01 the helper should classify: no publish target or `lastPublishDecision` of `not-configured` / `deferred` as `not-attempted`; `lastPublishDecision === "skipped"` as `unchanged`; `lastPublishDecision === "published"` as `current`; and `lastPublishDecision === "failed"` with `lastRefreshedAt` newer than `lastPublishedAt` (or no successful publish at all) as `stale-failed-publish`. Return timestamps needed by the command layer such as `lastRefreshedAt` and `lastPublishedAt`, but do not read remote gist state and do not import `inspectSharedPublishHealth()`. Add tests first covering: no publish attempt yet, skipped refresh meaning unchanged badge payload, published refresh meaning current badge, and failed publish after a newer local refresh meaning stale badge risk.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-trust.ts` contains `export type PublishTrustStatus =`
    - `packages/core/src/publish/publish-trust.ts` contains `stale-failed-publish`
    - `packages/core/src/publish/publish-trust.ts` contains `not-attempted`
    - `packages/core/src/publish/publish-trust.ts` contains `unchanged`
    - `packages/core/src/publish/publish-trust.ts` contains `derivePublishTrustReport`
    - `packages/core/src/publish/publish-trust.test.ts` contains `returns not-attempted when no publish has been configured`
    - `packages/core/src/publish/publish-trust.test.ts` contains `returns unchanged when the last refresh skipped the badge write`
    - `packages/core/src/publish/publish-trust.test.ts` contains `returns stale-failed-publish when the refresh is newer than the last successful publish`
    - `packages/core/src/publish/index.ts` contains `publish-trust.js`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/publish-trust.test.ts</automated>
  </verify>
  <done>The repo has one explicit, test-locked live-badge trust helper that can be reused by command surfaces without conflating badge trust with shared-mode health.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface live-badge trust in refresh and status while keeping shared-health on separate lines</name>
  <files>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts</files>
  <read_first>packages/core/src/publish/publish-trust.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts, .planning/phases/17-publish-failure-visibility-and-state-trust/17-RESEARCH.md</read_first>
  <action>Wire `derivePublishTrustReport()` into both command surfaces. In `packages/agent-badge/src/commands/status.ts`, add a new line exactly prefixed `- Live badge trust:` immediately after the existing publish line and before any shared-mode output. Render explicit operator wording such as `not attempted`, `unchanged`, `current`, or `stale after failed publish`, and when useful add a separate line exactly prefixed `- Last successful badge update:` using `state.publish.lastPublishedAt`. Keep the existing `- Shared mode:` and `- Shared issues:` lines intact so live badge trust and shared contributor health stay separate concepts. In `packages/agent-badge/src/commands/refresh.ts`, print the same trust line in the normal summary and also in the failed-soft path after the failed state has been written, so a publish failure reports stale badge risk instead of only `- Error:`. Do not query remote shared health from the trust helper and do not rename the shared-mode lines. Add tests first for: status with no configured publish target, status with stale failed publish state, refresh soft-failure output showing stale badge trust, and refresh output showing unchanged trust when publish skipped because the badge payload hash matched.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/status.ts` contains `- Live badge trust:`
    - `packages/agent-badge/src/commands/status.ts` contains `- Last successful badge update:`
    - `packages/agent-badge/src/commands/status.ts` contains `- Shared mode:`
    - `packages/agent-badge/src/commands/refresh.ts` contains `- Live badge trust:`
    - `packages/agent-badge/src/commands/status.test.ts` contains `Live badge trust: not attempted`
    - `packages/agent-badge/src/commands/status.test.ts` contains `Live badge trust: stale after failed publish`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Live badge trust: stale after failed publish`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Live badge trust: unchanged`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Last successful badge update:`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/status.test.ts</automated>
  </verify>
  <done>`refresh` and `status` now expose live-badge trust explicitly and separately from shared-health, so a stale badge risk is visible in normal operator workflows.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/publish-trust.test.ts` succeeds
- `npm test -- --run packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/status.test.ts` succeeds
- `npm test -- --run packages/core/src/publish/publish-trust.test.ts packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/status.test.ts` succeeds after the full plan
</verification>

<success_criteria>
- `refresh` and `status` show a dedicated live-badge trust summary instead of relying on generic publish status alone.
- Failed-soft refresh output makes stale badge risk obvious from the command that just failed.
- Shared-mode health remains visible but semantically separate from live-badge trust.
</success_criteria>

<output>
After completion, create `.planning/phases/17-publish-failure-visibility-and-state-trust/17-01-SUMMARY.md`
</output>

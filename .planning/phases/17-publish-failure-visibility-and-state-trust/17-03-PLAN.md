---
phase: 17-publish-failure-visibility-and-state-trust
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - packages/core/src/publish/publish-trust.ts
  - packages/core/src/publish/publish-trust.test.ts
  - packages/core/src/diagnostics/doctor.ts
  - packages/core/src/diagnostics/doctor.test.ts
  - packages/agent-badge/src/commands/status.ts
  - packages/agent-badge/src/commands/status.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
  - packages/agent-badge/src/commands/doctor.ts
  - packages/agent-badge/src/commands/doctor.test.ts
autonomous: true
requirements:
  - OPER-01
  - OPER-02
user_setup: []
must_haves:
  truths:
    - "`status`, `refresh`, and `doctor` derive one coherent live-badge trust result from the same persisted publish-attempt facts instead of command-specific heuristics."
    - "Operators can distinguish a stale failed publish from a failed publish where the visible badge did not need to change."
    - "Shared contributor health and live badge trust remain separate surfaces, so `shared-health: stale` does not get confused with a stale badge after a failed publish."
  artifacts:
    - path: packages/core/src/publish/publish-trust.ts
      provides: canonical live-badge trust derivation from persisted diagnostics
      contains: derivePublishTrustReport
    - path: packages/core/src/diagnostics/doctor.ts
      provides: doctor-level publish-trust check separate from shared-health
      contains: publish-trust
    - path: packages/agent-badge/src/commands/status.ts
      provides: canonical live-badge trust wording in status output
      contains: Live badge trust:
    - path: packages/agent-badge/src/commands/doctor.ts
      provides: doctor output that renders the same trust vocabulary used by status and refresh
      contains: publish-trust
  key_links:
    - from: packages/core/src/publish/publish-trust.ts
      to: packages/agent-badge/src/commands/status.ts
      via: "status renders the persisted live-badge trust report instead of Phase 17-01 heuristics"
      pattern: derivePublishTrustReport
    - from: packages/core/src/publish/publish-trust.ts
      to: packages/agent-badge/src/commands/refresh.ts
      via: "refresh prints the same persisted trust vocabulary after success and failure"
      pattern: derivePublishTrustReport
    - from: packages/core/src/publish/publish-trust.ts
      to: packages/core/src/diagnostics/doctor.ts
      via: "doctor adds a dedicated publish-trust check while leaving shared-health untouched"
      pattern: derivePublishTrustReport
---

<objective>
Align doctor and command trust output with the canonical persisted Phase 17 diagnostics.

Purpose: Plans 01 and 02 establish the trust surface and persist the attempt facts it needs, but operator workflows still need one final pass that replaces Plan 01's coarse heuristics with the canonical persisted model. This plan upgrades `publish-trust`, wires that report through `status`, `refresh`, and `doctor`, and keeps shared-health semantically separate.
Output: one canonical live-badge trust report reused by `status`, `refresh`, and `doctor`, including the distinction between stale failed publishes and failed-but-unchanged badge state.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-publish-failure-visibility-and-state-trust/17-RESEARCH.md
@.planning/phases/17-publish-failure-visibility-and-state-trust/17-01-PLAN.md
@.planning/phases/17-publish-failure-visibility-and-state-trust/17-02-PLAN.md
@packages/core/src/publish/publish-trust.ts
@packages/core/src/diagnostics/doctor.ts
@packages/agent-badge/src/commands/status.ts
@packages/agent-badge/src/commands/refresh.ts
@packages/agent-badge/src/commands/doctor.ts

<interfaces>
From packages/core/src/publish/publish-trust.ts:
```typescript
export type PublishTrustStatus =
  | "current"
  | "unchanged"
  | "not-attempted"
  | "stale-failed-publish"
  | "unknown";
```

From packages/core/src/state/state-schema.ts:
```typescript
publish: z.object({
  status: publishStatusSchema,
  gistId: z.string().min(1).nullable(),
  lastPublishedHash: z.string().min(1).nullable(),
  lastPublishedAt: isoDateTimeSchema.nullable(),
  publisherId: z.string().min(1).nullable().optional().default(null),
  mode: publishModeSchema.optional().default("legacy"),
  lastAttemptedAt: isoDateTimeSchema.nullable(),
  lastAttemptOutcome: z.enum(["published", "unchanged", "failed", "not-attempted"]).nullable(),
  lastSuccessfulSyncAt: isoDateTimeSchema.nullable(),
  lastAttemptCandidateHash: z.string().min(1).nullable(),
  lastAttemptChangedBadge: z.enum(["yes", "no", "unknown"]).nullable(),
  lastFailureCode: z.string().min(1).nullable()
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade publish-trust to use canonical persisted attempt facts instead of coarse heuristics</name>
  <files>packages/core/src/publish/publish-trust.ts, packages/core/src/publish/publish-trust.test.ts</files>
  <read_first>packages/core/src/publish/publish-trust.ts, packages/core/src/publish/publish-trust.test.ts, packages/core/src/state/state-schema.ts, .planning/phases/17-publish-failure-visibility-and-state-trust/17-RESEARCH.md, .planning/phases/17-publish-failure-visibility-and-state-trust/17-02-PLAN.md</read_first>
  <action>Upgrade `derivePublishTrustReport()` so Plan 01's coarse heuristics are replaced by the new persisted attempt facts from Plan 02. Distinguish exactly these operator-visible states: no publish attempt, current after successful publish, unchanged after successful sync, stale because publish failed and the candidate badge differs from the last published hash, and failed but live badge unchanged because the candidate hash matches the current live hash. Keep this vocabulary separate from `inspectSharedPublishHealth()` and do not reuse `shared-health` statuses or issue ids. Add tests first for: failed publish with an unchanged candidate badge, failed publish with a changed candidate badge, missing attempt facts returning `unknown`, and successful unchanged sync using `lastSuccessfulSyncAt` without mutating `lastPublishedAt` semantics.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-trust.ts` contains `failed-but-unchanged`
    - `packages/core/src/publish/publish-trust.ts` contains `stale-failed-publish`
    - `packages/core/src/publish/publish-trust.ts` contains `derivePublishTrustReport`
    - `packages/core/src/publish/publish-trust.test.ts` contains `failed publish with an unchanged candidate badge is not stale`
    - `packages/core/src/publish/publish-trust.test.ts` contains `failed publish with a changed candidate badge is stale`
    - `packages/core/src/publish/publish-trust.test.ts` contains `returns unknown when persisted attempt facts are incomplete`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/publish-trust.test.ts</automated>
  </verify>
  <done>The repo has one canonical trust helper that interprets persisted attempt facts correctly instead of inferring too much from coarse Plan 01 state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the canonical publish-trust report across status, refresh, and doctor without overloading shared-health</name>
  <files>packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/doctor.ts, packages/agent-badge/src/commands/doctor.test.ts</files>
  <read_first>packages/core/src/publish/publish-trust.ts, packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/doctor.ts, packages/agent-badge/src/commands/doctor.test.ts, .planning/phases/17-publish-failure-visibility-and-state-trust/17-RESEARCH.md</read_first>
  <action>In `packages/core/src/diagnostics/doctor.ts`, add a dedicated check `id: "publish-trust"` after `publish-shields` and before `shared-mode`; have it reuse `derivePublishTrustReport()` and emit privacy-safe fix lines such as rerun `agent-badge refresh`, reconnect the gist target, or retry publish on the machine with current local state. Update `packages/agent-badge/src/commands/status.ts`, `refresh.ts`, and `doctor.ts` to render the same live-badge trust wording from the shared report instead of command-local conditionals. Keep the existing `shared-mode` and `shared-health` checks and lines intact so a repo can report both shared-state staleness and live badge trust independently. Add tests first that lock one vocabulary across all three surfaces, including the case where publish failed but the live badge did not need to change.</action>
  <acceptance_criteria>
    - `packages/core/src/diagnostics/doctor.ts` contains `id: "publish-trust"`
    - `packages/core/src/diagnostics/doctor.ts` contains `derivePublishTrustReport`
    - `packages/core/src/diagnostics/doctor.ts` contains `id: "shared-health"`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `publish-trust`
    - `packages/agent-badge/src/commands/status.test.ts` contains `Live badge trust: publish failed but live badge is unchanged`
    - `packages/agent-badge/src/commands/status.test.ts` contains `Live badge trust: stale after failed publish`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Live badge trust: publish failed but live badge is unchanged`
    - `packages/agent-badge/src/commands/doctor.test.ts` contains `publish-trust`
    - `packages/agent-badge/src/commands/doctor.test.ts` contains `Live badge trust`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/status.test.ts packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/doctor.test.ts</automated>
  </verify>
  <done>The CLI and doctor use one canonical, persisted live-badge trust model that distinguishes stale failed publishes from unchanged failed publishes while keeping shared-health semantics separate.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/publish-trust.test.ts` succeeds
- `npm test -- --run packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/status.test.ts packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/doctor.test.ts` succeeds
- `npm test -- --run packages/core/src/publish/publish-trust.test.ts packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/status.test.ts packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/doctor.test.ts` succeeds after the full plan
</verification>

<success_criteria>
- `status`, `refresh`, and `doctor` all derive the same live-badge trust answer from persisted state.
- Operators can distinguish stale failed publishes from failed-but-unchanged badge state without reading raw diagnostics.
- Shared contributor health and live-badge trust remain distinct concepts in both code and operator output.
</success_criteria>

<output>
After completion, create `.planning/phases/17-publish-failure-visibility-and-state-trust/17-03-SUMMARY.md`
</output>

---
phase: 17-publish-failure-visibility-and-state-trust
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - packages/core/src/state/state-schema.ts
  - packages/core/src/state/state-schema.test.ts
  - packages/core/src/init/scaffold.ts
  - packages/core/src/init/scaffold.test.ts
  - packages/core/src/publish/publish-service.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
autonomous: true
requirements:
  - OPER-01
  - OPER-02
user_setup: []
must_haves:
  truths:
    - "Persisted state records the last publish attempt, whether the live badge would have changed, and the last successful sync without storing raw error strings or private detail."
    - "Init reruns preserve every additive Phase 17 publish diagnostic field so canonical trust facts are not stripped back out of `.agent-badge/state.json`."
    - "Publish and refresh persist the same privacy-safe attempt contract, giving later command surfaces enough data to distinguish changed, unchanged, failed, and not-attempted outcomes."
  artifacts:
    - path: packages/core/src/state/state-schema.ts
      provides: canonical persisted publish-attempt diagnostics and trust markers
      contains: lastAttemptedAt
    - path: packages/core/src/publish/publish-service.ts
      provides: pre-write candidate badge hash and changed-badge facts for publish attempts
      contains: candidateHash
    - path: packages/core/src/init/scaffold.ts
      provides: init rerun reconciliation that preserves Phase 17 publish diagnostics
      contains: lastAttemptedAt
  key_links:
    - from: packages/core/src/publish/publish-service.ts
      to: packages/core/src/state/state-schema.ts
      via: "publish and refresh persist candidate-hash and outcome facts returned by the shared publish flow"
      pattern: lastAttemptedAt
    - from: packages/core/src/state/state-schema.ts
      to: packages/core/src/init/scaffold.ts
      via: "init reruns preserve every additive publish diagnostic field instead of stripping Phase 17 trust facts from state.json"
      pattern: lastAttemptedAt
---

<objective>
Make publish-attempt diagnostics canonical and persist them from every write path.

Purpose: Plan 01 exposes live-badge trust using the coarse facts the repo already has, but the repo still cannot persist the attempt facts needed for canonical stale-vs-unchanged reasoning. This plan adds those additive state fields, teaches publish flows to compute them safely, and ensures init reruns preserve them. A follow-on plan will consume these new facts across `status`, `refresh`, and `doctor`.
Output: additive publish-attempt state fields, init-safe reconciliation, and publish-service candidate-hash facts persisted by both publish write paths.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-publish-failure-visibility-and-state-trust/17-RESEARCH.md
@.planning/phases/17-publish-failure-visibility-and-state-trust/17-01-PLAN.md
@packages/core/src/state/state-schema.ts
@packages/core/src/init/scaffold.ts
@packages/core/src/publish/publish-service.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/commands/refresh.ts

<interfaces>
From packages/core/src/publish/publish-service.ts:
```typescript
export interface PublishBadgeIfChangedResult {
  readonly state: AgentBadgeState;
  readonly decision: "published" | "skipped";
  readonly healthBeforePublish: SharedPublishHealthReport;
  readonly healthAfterPublish: SharedPublishHealthReport;
  readonly migrationPerformed: boolean;
}
```

From packages/core/src/state/state-schema.ts:
```typescript
publish: z.object({
  status: publishStatusSchema,
  gistId: z.string().min(1).nullable(),
  lastPublishedHash: z.string().min(1).nullable(),
  lastPublishedAt: isoDateTimeSchema.nullable(),
  publisherId: z.string().min(1).nullable().optional().default(null),
  mode: publishModeSchema.optional().default("legacy")
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist canonical publish-attempt diagnostics in state and populate them from every publish write path</name>
  <files>packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/scaffold.test.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts</files>
  <read_first>packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/scaffold.test.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, .planning/phases/17-publish-failure-visibility-and-state-trust/17-RESEARCH.md</read_first>
  <action>Extend the `publish` state additively with canonical Phase 17 diagnostics: `lastAttemptedAt`, `lastAttemptOutcome`, `lastSuccessfulSyncAt`, `lastAttemptCandidateHash`, `lastAttemptChangedBadge`, and `lastFailureCode`. Use privacy-safe enums only; do not persist `error.message`, stack traces, local paths, gist response bodies, or any raw provider/session evidence. In `packages/core/src/publish/publish-service.ts`, compute the next badge payload hash before any `updateGistFile()` calls by merging the local contributor record into the current shared contributor set in memory, then return exact attempt facts such as `candidateHash` and `changedBadge` from the publish result. Keep `lastPublishedAt` meaning "the visible badge value changed here" and update `lastSuccessfulSyncAt` on both `published` and `unchanged` outcomes. In `packages/agent-badge/src/commands/refresh.ts`, persist the new fields for every outcome: `not-configured`, `deferred`, `failed`, `published`, and `skipped`/unchanged. In `packages/agent-badge/src/commands/publish.ts`, persist the same diagnostics contract instead of only writing the updated state blob after a successful publish. Update `packages/core/src/init/scaffold.ts` so init reruns preserve every new publish diagnostic field instead of stripping them from `.agent-badge/state.json`, and add the matching regression coverage in `scaffold.test.ts`. Use broad failure codes for Phase 17 such as `not-configured`, `deferred`, `remote-write-failed`, `remote-inspection-failed`, and `unknown`; Phase 18 can refine them later without rewriting the schema. Add tests first for: old state parsing with new defaults, unchanged sync updates `lastSuccessfulSyncAt` without changing `lastPublishedAt`, failed publish persists candidate hash plus changed-badge signal, both publish commands avoid storing raw error text in `.agent-badge/state.json`, and init reruns preserve the new diagnostics.</action>
  <acceptance_criteria>
    - `packages/core/src/state/state-schema.ts` contains `lastAttemptedAt`
    - `packages/core/src/state/state-schema.ts` contains `lastAttemptOutcome`
    - `packages/core/src/state/state-schema.ts` contains `lastSuccessfulSyncAt`
    - `packages/core/src/state/state-schema.ts` contains `lastAttemptCandidateHash`
    - `packages/core/src/state/state-schema.ts` contains `lastAttemptChangedBadge`
    - `packages/core/src/state/state-schema.ts` contains `lastFailureCode`
    - `packages/core/src/state/state-schema.test.ts` contains `parses publish attempt diagnostics without raw error detail`
    - `packages/core/src/publish/publish-service.ts` contains `candidateHash`
    - `packages/core/src/publish/publish-service.ts` contains `changedBadge`
    - `packages/core/src/publish/publish-service.test.ts` contains `computes the next badge hash before remote writes`
    - `packages/core/src/publish/publish-service.test.ts` contains `keeps lastPublishedAt unchanged when the badge payload is unchanged`
    - `packages/core/src/init/scaffold.ts` contains `lastAttemptedAt`
    - `packages/core/src/init/scaffold.test.ts` contains `preserves publish attempt diagnostics on init reruns`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `persists failed publish diagnostics without storing the raw error message`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `persists canonical publish attempt diagnostics after a successful publish`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/state/state-schema.test.ts packages/core/src/init/scaffold.test.ts packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <done>The repo persists one privacy-safe, additive publish-attempt contract that can explain success, unchanged, failed, and not-attempted outcomes without leaking private detail.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/state/state-schema.test.ts packages/core/src/init/scaffold.test.ts packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds
</verification>

<success_criteria>
- `.agent-badge/state.json` can represent the last publish attempt, last successful sync, and badge-change signal without exposing raw error detail.
- Init reruns preserve the new publish diagnostics instead of downgrading the state shape.
- Publish and refresh persist the same candidate-hash and changed-badge facts needed for the follow-on trust-alignment plan.
</success_criteria>

<output>
After completion, create `.planning/phases/17-publish-failure-visibility-and-state-trust/17-02-SUMMARY.md`
</output>

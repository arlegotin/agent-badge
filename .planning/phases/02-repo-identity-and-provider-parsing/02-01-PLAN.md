---
phase: 02-repo-identity-and-provider-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/config/config-schema.ts
  - packages/core/src/config/config-schema.test.ts
  - packages/core/src/init/default-config.ts
  - packages/core/src/repo/repo-fingerprint.ts
  - packages/core/src/repo/repo-fingerprint.test.ts
  - packages/core/src/repo/index.ts
autonomous: true
requirements:
  - ATTR-01
user_setup: []
must_haves:
  truths:
    - "The tool can derive one canonical repo fingerprint from the git root, normalized origin URL, and configured aliases."
    - "Persisted repo aliases are privacy-safe remote and slug values, not absolute local paths."
    - "A no-origin repository still resolves a stable runtime fingerprint using git-root evidence and basename fallback."
  artifacts:
    - path: packages/core/src/repo/repo-fingerprint.ts
      provides: canonical repo fingerprint service and remote normalizer
      contains: "resolveRepoFingerprint"
    - path: packages/core/src/config/config-schema.ts
      provides: privacy-safe repo alias config schema
      contains: "repo"
    - path: packages/core/src/init/default-config.ts
      provides: default repo alias shape for fresh config
      contains: "aliases"
    - path: packages/core/src/repo/repo-fingerprint.test.ts
      provides: ATTR-01 regression coverage
      contains: "supports no-origin repositories"
  key_links:
    - from: packages/core/src/repo/repo-fingerprint.ts
      to: packages/core/src/config/config-schema.ts
      via: "repo.aliases.remotes and repo.aliases.slugs normalization"
      pattern: "aliases\\.remotes|aliases\\.slugs"
    - from: packages/core/src/repo/repo-fingerprint.ts
      to: git origin and root inspection
      via: "git remote get-url origin plus git rev-parse --show-toplevel"
      pattern: "remote get-url origin|rev-parse --show-toplevel"
    - from: packages/core/src/init/default-config.ts
      to: packages/core/src/config/config-schema.ts
      via: "schema-backed default config parsing"
      pattern: "parseAgentBadgeConfig"
---

<objective>
Implement the canonical repo identity layer for Phase 2: privacy-safe alias config, strict remote normalization, and a stable runtime repo fingerprint service.

Purpose: ATTR-01 must exist before attribution can trust repo matching across origin changes, mirrors, or no-origin repos.
Output: Repo alias schema/defaults, `resolveRepoFingerprint()`, and focused tests for normalized remotes, alias matching, and no-origin fallback.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md
@.planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md
@.planning/phases/01-workspace-and-init-foundation/01-03-SUMMARY.md
@packages/core/src/config/config-schema.ts
@packages/core/src/init/default-config.ts
@packages/core/src/repo/git-context.ts

<interfaces>
From `packages/core/src/config/config-schema.ts`:

```ts
export type AgentBadgeConfig = z.infer<typeof agentBadgeConfigSchema>;
export const defaultAgentBadgeConfig: AgentBadgeConfig = { ... };
export function parseAgentBadgeConfig(input: unknown): AgentBadgeConfig;
```

From `packages/core/src/repo/git-context.ts`:

```ts
export interface GetGitContextOptions {
  readonly cwd: string;
  readonly allowGitInit?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend config defaults for privacy-safe repo aliases</name>
  <files>packages/core/src/config/config-schema.ts, packages/core/src/config/config-schema.test.ts, packages/core/src/init/default-config.ts</files>
  <read_first>packages/core/src/config/config-schema.ts, packages/core/src/config/config-schema.test.ts, packages/core/src/init/default-config.ts, packages/core/src/repo/git-context.ts, .planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md, .planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md</read_first>
  <action>Add `repo.aliases.remotes` and `repo.aliases.slugs` to `agentBadgeConfigSchema` in `packages/core/src/config/config-schema.ts`, both as strict string arrays with empty-array defaults in `defaultAgentBadgeConfig`. Update `packages/core/src/init/default-config.ts` so `createDefaultAgentBadgeConfig()` preserves the existing provider availability seam but now returns the full schema-valid `repo.aliases` shape. Extend `packages/core/src/config/config-schema.test.ts` with one positive parse of the new `repo.aliases` shape and one negative assertion that a path-like alias key such as `repo.aliases.paths` is rejected. Do not add any persisted absolute-path alias field.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/config/config-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/config/config-schema.ts` contains `repo:`
    - `packages/core/src/config/config-schema.ts` contains `aliases:`
    - `packages/core/src/config/config-schema.ts` contains `remotes: z.array(z.string().min(1))`
    - `packages/core/src/config/config-schema.ts` contains `slugs: z.array(z.string().min(1))`
    - `packages/core/src/init/default-config.ts` contains `repo: {`
    - `packages/core/src/config/config-schema.test.ts` contains `repo.aliases.paths`
  </acceptance_criteria>
  <done>The persisted config can carry privacy-safe repo aliases and fresh defaults include the new alias structure without redesigning provider defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Build the canonical repo fingerprint and remote normalization service</name>
  <files>packages/core/src/repo/repo-fingerprint.ts, packages/core/src/repo/repo-fingerprint.test.ts, packages/core/src/repo/index.ts</files>
  <read_first>packages/core/src/repo/git-context.ts, packages/core/src/config/config-schema.ts, packages/core/src/init/default-config.ts, .planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md, .planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md</read_first>
  <action>Create `packages/core/src/repo/repo-fingerprint.ts` exporting `normalizeGitRemoteUrl(input)` and `resolveRepoFingerprint(options)`. `normalizeGitRemoteUrl()` must normalize GitHub-style HTTPS, `ssh://`, and SCP remotes into the canonical `https://github.com/owner/repo` form, lower-case the host, trim one trailing `.git`, trim one trailing slash, and reject multi-segment non-GitHub paths from canonical slug derivation. `resolveRepoFingerprint()` must run `git rev-parse --show-toplevel`, `git remote get-url origin`, and `realpath()` to produce `{ gitRoot, gitRootRealPath, gitRootBasename, originUrlRaw, originUrlNormalized, host, owner, repo, canonicalSlug, aliasRemoteUrlsNormalized, aliasSlugs }`. When `origin` is absent, set `originUrlNormalized`, `host`, `owner`, and `canonicalSlug` to `null`, and set `repo` from the git-root basename. Create `packages/core/src/repo/repo-fingerprint.test.ts` with cases for HTTPS/SSH normalization, alias application, and a test named `supports no-origin repositories`. Export the module from `packages/core/src/repo/index.ts`. Keep `packages/core/src/repo/git-context.ts` focused on init/preflight booleans and do not move fingerprint logic into it.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/repo/repo-fingerprint.test.ts packages/core/src/config/config-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/repo/repo-fingerprint.ts` contains `export async function resolveRepoFingerprint`
    - `packages/core/src/repo/repo-fingerprint.ts` contains `export function normalizeGitRemoteUrl`
    - `packages/core/src/repo/repo-fingerprint.ts` contains `realpath`
    - `packages/core/src/repo/repo-fingerprint.ts` contains `canonicalSlug`
    - `packages/core/src/repo/repo-fingerprint.test.ts` contains `normalizes git@github.com:Owner/Repo.git`
    - `packages/core/src/repo/repo-fingerprint.test.ts` contains `supports no-origin repositories`
    - `packages/core/src/repo/index.ts` contains `./repo-fingerprint.js`
  </acceptance_criteria>
  <done>Phase 2 has a stable repo identity service that turns git state plus privacy-safe aliases into one canonical runtime fingerprint.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --run packages/core/src/config/config-schema.test.ts packages/core/src/repo/repo-fingerprint.test.ts` succeeds
- [ ] `packages/core/src/config/config-schema.ts` contains only remote and slug alias persistence, not path aliases
- [ ] `packages/core/src/repo/repo-fingerprint.ts` normalizes GitHub remotes and handles the no-origin path explicitly
</verification>

<success_criteria>
- ATTR-01 is implemented through one canonical repo fingerprint service
- Repo aliases persist only privacy-safe remotes and slugs
- No-origin repositories remain representable without inventing an owner from the local path
</success_criteria>

<output>
After completion, create `.planning/phases/02-repo-identity-and-provider-parsing/02-01-SUMMARY.md`
</output>

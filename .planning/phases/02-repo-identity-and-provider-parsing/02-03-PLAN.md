---
phase: 02-repo-identity-and-provider-parsing
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - packages/core/src/init/default-config.test.ts
  - packages/core/src/init/preflight.test.ts
  - packages/core/src/providers/claude/claude-jsonl.ts
  - packages/core/src/providers/claude/claude-adapter.ts
  - packages/core/src/providers/claude/claude-adapter.test.ts
  - packages/core/src/providers/index.ts
  - packages/core/src/index.ts
  - packages/testkit/src/claude-fixtures.ts
  - packages/testkit/src/index.ts
  - packages/testkit/fixtures/claude/projects/project-with-dedupe/session-main.jsonl
  - packages/testkit/fixtures/claude/projects/project-no-index/session-secondary.jsonl
  - packages/testkit/fixtures/claude/projects/project-with-index/session-tertiary.jsonl
  - packages/testkit/fixtures/claude/projects/project-with-index/sessions-index.json
autonomous: true
requirements:
  - SCAN-01
  - SCAN-03
user_setup: []
must_haves:
  truths:
    - "Claude scanning walks `~/.claude/projects/**/*.jsonl` and emits the same normalized session shape as the Codex adapter."
    - "Claude token totals dedupe assistant rows by `message.id`, keeping the latest row for each assistant message."
    - "Fresh config defaults enable only detected providers while preserving the existing availability-based seam from Phase 1."
    - "The core package exports repo identity plus both provider adapters from one public entrypoint."
  artifacts:
    - path: packages/core/src/providers/claude/claude-adapter.ts
      provides: Claude session scanner
      contains: "scanClaudeSessions"
    - path: packages/core/src/providers/claude/claude-adapter.test.ts
      provides: SCAN-03 regression coverage
      contains: "dedupes repeated assistant message.id rows"
    - path: packages/core/src/init/default-config.test.ts
      provides: SCAN-01 default-provider coverage
      contains: "enables only detected providers by default"
    - path: packages/core/src/providers/index.ts
      provides: public provider exports
      contains: "./claude/claude-adapter.js"
  key_links:
    - from: packages/core/src/providers/claude/claude-adapter.ts
      to: packages/core/src/providers/session-summary.ts
      via: "provider-neutral summary parsing"
      pattern: "parseNormalizedSessionSummary|normalizedSessionSummarySchema"
    - from: packages/core/src/init/default-config.test.ts
      to: packages/core/src/init/default-config.ts
      via: "availability-based provider defaults"
      pattern: "createDefaultAgentBadgeConfig"
    - from: packages/core/src/index.ts
      to: packages/core/src/providers/index.ts
      via: "root export surface"
      pattern: "./providers/index.js"
---

<objective>
Finish Phase 2 by implementing the Claude adapter, hardening provider-default coverage, and exposing the Phase 2 modules through the core public API.

Purpose: SCAN-03 must reuse the shared session model from `02-02`, and SCAN-01 needs explicit coverage without redesigning the already-correct provider-availability seam.
Output: Claude JSONL parser and adapter, default-config/preflight coverage for provider availability, sanitized Claude fixtures, and root exports for repo/provider services.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md
@.planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md
@.planning/phases/01-workspace-and-init-foundation/01-03-SUMMARY.md
@packages/core/src/init/default-config.ts
@packages/core/src/init/provider-detection.ts
@packages/core/src/init/preflight.test.ts
@packages/core/src/providers/session-summary.ts
@packages/testkit/src/provider-fixtures.ts

<interfaces>
From `packages/core/src/init/provider-detection.ts`:

```ts
export interface ProviderDetectionResult {
  readonly codex: ProviderAvailability;
  readonly claude: ProviderAvailability;
}

export function detectProviderAvailability(
  options?: DetectProviderAvailabilityOptions
): ProviderDetectionResult;
```

From `packages/core/src/providers/session-summary.ts` (created in `02-02`):

```ts
export interface NormalizedSessionSummary {
  provider: "codex" | "claude";
  providerSessionId: string;
  tokenUsage: { total: number; ... };
  lineage: { parentSessionId: string | null; kind: "root" | "child" | "unknown" };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock SCAN-01 with explicit provider-default tests</name>
  <files>packages/core/src/init/default-config.test.ts, packages/core/src/init/preflight.test.ts</files>
  <read_first>packages/core/src/init/default-config.ts, packages/core/src/init/provider-detection.ts, packages/core/src/init/preflight.test.ts, .planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md, .planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md</read_first>
  <action>Create `packages/core/src/init/default-config.test.ts` that covers the four availability combinations and includes a test named `enables only detected providers by default`. Extend `packages/core/src/init/preflight.test.ts` with one assertion that provider detection still reports only `~/.codex` and `~/.claude` labels, not absolute paths. Keep `detectProviderAvailability()` and `createDefaultAgentBadgeConfig()` as the existing low-level/defaulting seam from Phase 1; only modify production code if these tests expose a real bug.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/init/default-config.test.ts packages/core/src/init/preflight.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/init/default-config.test.ts` contains `enables only detected providers by default`
    - `packages/core/src/init/default-config.test.ts` contains `codex`
    - `packages/core/src/init/default-config.test.ts` contains `claude`
    - `packages/core/src/init/preflight.test.ts` contains `~/.codex`
    - `packages/core/src/init/preflight.test.ts` contains `~/.claude`
  </acceptance_criteria>
  <done>SCAN-01 is explicitly protected by tests without redesigning the provider-availability logic that Phase 1 already established.</done>
</task>

<task type="auto">
  <name>Task 2: Add sanitized Claude fixtures and SCAN-03 tests</name>
  <files>packages/testkit/src/claude-fixtures.ts, packages/testkit/src/index.ts, packages/testkit/fixtures/claude/projects/project-with-dedupe/session-main.jsonl, packages/testkit/fixtures/claude/projects/project-no-index/session-secondary.jsonl, packages/testkit/fixtures/claude/projects/project-with-index/session-tertiary.jsonl, packages/testkit/fixtures/claude/projects/project-with-index/sessions-index.json</files>
  <read_first>packages/testkit/src/provider-fixtures.ts, packages/testkit/src/index.ts, packages/core/src/providers/session-summary.ts, .planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md, .planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md</read_first>
  <action>Create `packages/testkit/src/claude-fixtures.ts` exporting `createClaudeFixtureHome()` that copies a sanitized `projects/` tree into `.claude/`. Export the helper from `packages/testkit/src/index.ts`. Add JSONL fixtures that cover one project with repeated assistant `message.id` rows, one project directory with JSONL and no `sessions-index.json`, and one project directory where `sessions-index.json` exists but should be ignored as a correctness input. Use placeholder cwd values such as `/repo/main` and fake public-safe remotes only.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/testkit/src/claude-fixtures.ts` contains `export async function createClaudeFixtureHome`
    - `packages/testkit/src/index.ts` contains `createClaudeFixtureHome`
    - `packages/testkit/fixtures/claude/projects/project-with-dedupe/session-main.jsonl` contains `/repo/main`
    - `packages/testkit/fixtures/claude/projects/project-with-index/sessions-index.json` contains `ignoredByAdapter`
  </acceptance_criteria>
  <done>Claude parsing has committed fixture coverage for the real JSONL and message-dedupe edge cases called out in research.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the Claude adapter and expose the Phase 2 API surface</name>
  <files>packages/core/src/providers/claude/claude-jsonl.ts, packages/core/src/providers/claude/claude-adapter.ts, packages/core/src/providers/claude/claude-adapter.test.ts, packages/core/src/providers/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/providers/session-summary.ts, packages/core/src/providers/codex/codex-adapter.ts, packages/core/src/index.ts, packages/testkit/src/claude-fixtures.ts, .planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md, .planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md</read_first>
  <action>Create `packages/core/src/providers/claude/claude-jsonl.ts` with `readClaudeProjectJsonlSessions(claudeRoot)` that walks `projects/**/*.jsonl`, parses JSONL safely, and groups rows by file/session. Create `packages/core/src/providers/claude/claude-adapter.ts` exporting `scanClaudeSessions({ homeRoot })` that keeps only assistant `message.usage` rows, dedupes by `message.id` using the latest timestamped row per assistant message, maps usage fields (`input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens`) into the shared `NormalizedSessionSummary`, and ignores `progress`, `file-history-snapshot`, `last-prompt`, and raw transcript text. Create `packages/core/src/providers/claude/claude-adapter.test.ts` with test names that include `dedupes repeated assistant message.id rows`, `ignores sessions-index.json as a canonical source`, and `ignores non-assistant rows for totals`. Create `packages/core/src/providers/index.ts` exporting `./session-summary.js`, `./codex/codex-adapter.js`, and `./claude/claude-adapter.js`. Update `packages/core/src/index.ts` to export `./repo/index.js` and `./providers/index.js` so the public API closes Phase 2 cleanly.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/init/default-config.test.ts packages/core/src/init/preflight.test.ts packages/core/src/providers/claude/claude-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/providers/claude/claude-jsonl.ts` contains `projects`
    - `packages/core/src/providers/claude/claude-jsonl.ts` contains `.jsonl`
    - `packages/core/src/providers/claude/claude-adapter.ts` contains `export async function scanClaudeSessions`
    - `packages/core/src/providers/claude/claude-adapter.ts` contains `message.id`
    - `packages/core/src/providers/claude/claude-adapter.ts` contains `provider: "claude"`
    - `packages/core/src/providers/claude/claude-adapter.ts` contains `cache_creation_input_tokens`
    - `packages/core/src/providers/claude/claude-adapter.ts` contains `cache_read_input_tokens`
    - `packages/core/src/providers/claude/claude-adapter.test.ts` contains `dedupes repeated assistant message.id rows`
    - `packages/core/src/providers/claude/claude-adapter.test.ts` contains `ignores sessions-index.json as a canonical source`
    - `packages/core/src/providers/claude/claude-adapter.test.ts` contains `ignores non-assistant rows for totals`
    - `packages/core/src/providers/index.ts` contains `./claude/claude-adapter.js`
    - `packages/core/src/providers/index.ts` contains `./codex/codex-adapter.js`
    - `packages/core/src/index.ts` contains `./providers/index.js`
    - `packages/core/src/index.ts` contains `./repo/index.js`
  </acceptance_criteria>
  <done>SCAN-03 is implemented against the canonical Claude JSONL files, and the core package now exports the complete Phase 2 repo/provider API surface.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --run packages/core/src/init/default-config.test.ts packages/core/src/init/preflight.test.ts packages/core/src/providers/claude/claude-adapter.test.ts` succeeds
- [ ] `packages/core/src/providers/claude/claude-adapter.ts` dedupes assistant usage by `message.id`
- [ ] `packages/core/src/providers/index.ts` and `packages/core/src/index.ts` expose the Phase 2 modules publicly
- [ ] Provider-default tests confirm missing homes stay disabled without changing saved config semantics
</verification>

<success_criteria>
- SCAN-01 is explicitly covered by default-config and preflight tests
- SCAN-03 is implemented from `~/.claude/projects/**/*.jsonl` with message-level dedupe
- The shared Phase 2 repo/provider modules are exported from `@agent-badge/core`
</success_criteria>

<output>
After completion, create `.planning/phases/02-repo-identity-and-provider-parsing/02-03-SUMMARY.md`
</output>

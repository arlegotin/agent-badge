---
phase: 02-repo-identity-and-provider-parsing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/core/package.json
  - package-lock.json
  - packages/core/src/providers/session-summary.ts
  - packages/core/src/providers/codex/codex-sql.ts
  - packages/core/src/providers/codex/codex-adapter.ts
  - packages/core/src/providers/codex/codex-adapter.test.ts
  - packages/testkit/src/codex-fixtures.ts
  - packages/testkit/src/index.ts
  - packages/testkit/fixtures/codex/state-5.sql
  - packages/testkit/fixtures/codex/history.jsonl
autonomous: true
requirements:
  - SCAN-02
user_setup: []
must_haves:
  truths:
    - "Codex scanning reads `~/.codex/state_*.sqlite` as the primary source of truth and emits one normalized summary per `threads.id`."
    - "Codex normalized summaries preserve `thread_spawn_edges` lineage without deciding parent-child roll-up policy yet."
    - "Codex parsing only emits allowlisted metadata and token totals; it never pulls transcript text or unsafe thread fields into normalized output."
  artifacts:
    - path: packages/core/src/providers/session-summary.ts
      provides: provider-neutral normalized session schema
      contains: "normalizedSessionSummarySchema"
    - path: packages/core/src/providers/codex/codex-sql.ts
      provides: allowlisted SQLite query boundary
      contains: "thread_spawn_edges"
    - path: packages/core/src/providers/codex/codex-adapter.ts
      provides: Codex session scanner
      contains: "scanCodexSessions"
    - path: packages/core/src/providers/codex/codex-adapter.test.ts
      provides: SCAN-02 coverage for dedupe, lineage, and privacy
      contains: "dedupes by threads.id"
    - path: packages/testkit/src/codex-fixtures.ts
      provides: sanitized Codex fixture home builder
      contains: "createCodexFixtureHome"
  key_links:
    - from: packages/core/src/providers/codex/codex-adapter.ts
      to: packages/core/src/providers/codex/codex-sql.ts
      via: "allowlisted thread and edge query helpers"
      pattern: "loadCodexThreadRows|loadCodexSpawnEdges"
    - from: packages/core/src/providers/codex/codex-adapter.ts
      to: packages/core/src/providers/session-summary.ts
      via: "normalized session parsing"
      pattern: "parseNormalizedSessionSummary|normalizedSessionSummarySchema"
    - from: packages/core/src/providers/codex/codex-adapter.test.ts
      to: packages/testkit/src/codex-fixtures.ts
      via: "fixture home copied into `.codex`"
      pattern: "createCodexFixtureHome"
---

<objective>
Define the shared normalized session model and implement the first provider adapter against Codex SQLite data.

Purpose: SCAN-02 needs one trustworthy, privacy-safe source of truth before Phase 3 can attribute usage across providers.
Output: Shared session-summary schema, sanitized Codex fixtures, SQLite access helpers, and a Codex adapter that normalizes one summary per thread.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md
@.planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md
@.planning/phases/01-workspace-and-init-foundation/01-03-SUMMARY.md
@package.json
@packages/core/package.json
@packages/core/src/index.ts
@packages/testkit/src/provider-fixtures.ts

<interfaces>
From `packages/testkit/src/provider-fixtures.ts`:

```ts
export interface ProviderFixture {
  readonly homeRoot: string;
  readonly codexRoot: string | null;
  readonly claudeRoot: string | null;
  cleanup(): Promise<void>;
  writeProviderFile(provider: ProviderName, relativePath: string, content: string): Promise<string>;
}
```

From `packages/core/src/index.ts`:

```ts
export * from "./config/index.js";
export * from "./init/index.js";
export * from "./repo/git-context.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared normalized session schema and sanitized Codex fixtures</name>
  <files>packages/core/src/providers/session-summary.ts, packages/testkit/src/codex-fixtures.ts, packages/testkit/src/index.ts, packages/testkit/fixtures/codex/state-5.sql, packages/testkit/fixtures/codex/history.jsonl</files>
  <read_first>packages/core/src/index.ts, packages/testkit/src/provider-fixtures.ts, packages/testkit/src/index.ts, .planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md, .planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md</read_first>
  <action>Create `packages/core/src/providers/session-summary.ts` exporting `normalizedSessionSummarySchema`, `NormalizedSessionSummary`, and `parseNormalizedSessionSummary()` with the Phase 2 shape from research: `provider`, `providerSessionId`, `startedAt`, `updatedAt`, `cwd`, `gitBranch`, `observedRemoteUrl`, `observedRemoteUrlNormalized`, `tokenUsage`, `lineage`, and `metadata`. Create `packages/testkit/src/codex-fixtures.ts` exporting `createCodexFixtureHome()` that copies sanitized fixture assets into `.codex/` under a temp home root. Export the helper from `packages/testkit/src/index.ts`. Add `packages/testkit/fixtures/codex/state-5.sql` with allowlisted schema/data for `threads` and `thread_spawn_edges`, plus `packages/testkit/fixtures/codex/history.jsonl` for fallback-path tests only. Use placeholder cwd values such as `/repo/main` and fake public-safe remotes only.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/providers/session-summary.ts` contains `export const normalizedSessionSummarySchema`
    - `packages/core/src/providers/session-summary.ts` contains `provider: z.enum(["codex", "claude"])`
    - `packages/core/src/providers/session-summary.ts` contains `reasoningOutput`
    - `packages/testkit/src/codex-fixtures.ts` contains `export async function createCodexFixtureHome`
    - `packages/testkit/src/index.ts` contains `createCodexFixtureHome`
    - `packages/testkit/fixtures/codex/state-5.sql` contains `CREATE TABLE threads`
    - `packages/testkit/fixtures/codex/state-5.sql` contains `CREATE TABLE thread_spawn_edges`
    - `packages/testkit/fixtures/codex/history.jsonl` contains `/repo/main`
  </acceptance_criteria>
  <done>The repo now has one provider-neutral session schema and sanitized Codex fixture assets that future adapters can target directly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the SQLite query boundary and Codex adapter</name>
  <files>packages/core/package.json, package-lock.json, packages/core/src/providers/codex/codex-sql.ts, packages/core/src/providers/codex/codex-adapter.ts</files>
  <read_first>packages/core/package.json, package-lock.json, packages/core/src/providers/session-summary.ts, packages/core/src/repo/repo-fingerprint.ts, .planning/phases/02-repo-identity-and-provider-parsing/02-RESEARCH.md, .planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md</read_first>
  <action>Add one deliberate SQLite runtime dependency to `packages/core/package.json` and `package-lock.json` for `packages/core/src/providers/codex/codex-sql.ts`; do not rely on `node:sqlite` or the host `sqlite3` CLI at runtime because the project supports Node 20+. Create `packages/core/src/providers/codex/codex-sql.ts` with `findLatestCodexStateDatabase(codexRoot)`, `loadCodexThreadRows(dbPath)`, and `loadCodexSpawnEdges(dbPath)`. Query only the safe `threads` columns named in research (`id`, `created_at`, `updated_at`, `source`, `model_provider`, `cwd`, `tokens_used`, `git_sha`, `git_branch`, `git_origin_url`, `cli_version`, `agent_nickname`, `agent_role`, `model`) and the `thread_spawn_edges` columns; never `SELECT *`. Create `packages/core/src/providers/codex/codex-adapter.ts` exporting `scanCodexSessions({ homeRoot })` that picks the newest `state_*.sqlite`, maps one `NormalizedSessionSummary` per `threads.id`, preserves lineage from `thread_spawn_edges`, sets `tokenUsage.total` from `tokens_used`, reuses `normalizeGitRemoteUrl()` from `packages/core/src/repo/repo-fingerprint.ts` when normalizing `git_origin_url`, and uses `history.jsonl` only as a non-blocking fallback when SQLite cannot be read. Do not read or emit `logs.message`, `threads.title`, or `threads.first_user_message`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/package.json` contains `better-sqlite3`
    - `packages/core/src/providers/codex/codex-sql.ts` contains `SELECT id, created_at, updated_at`
    - `packages/core/src/providers/codex/codex-sql.ts` contains `thread_spawn_edges`
    - `packages/core/src/providers/codex/codex-sql.ts` does not contain `SELECT *`
    - `packages/core/src/providers/codex/codex-adapter.ts` contains `export async function scanCodexSessions`
    - `packages/core/src/providers/codex/codex-adapter.ts` contains `provider: "codex"`
    - `packages/core/src/providers/codex/codex-adapter.ts` contains `history.jsonl`
    - `packages/core/src/providers/codex/codex-adapter.ts` contains `normalizeGitRemoteUrl`
    - `packages/core/src/providers/codex/codex-adapter.ts` does not contain `first_user_message`
  </acceptance_criteria>
  <done>Codex scanning is implemented against the authoritative SQLite state file and keeps lineage plus safe metadata for each thread.</done>
</task>

<task type="auto">
  <name>Task 3: Prove SCAN-02 with fixture-backed Codex adapter tests</name>
  <files>packages/core/src/providers/codex/codex-adapter.test.ts</files>
  <read_first>packages/core/src/providers/codex/codex-adapter.ts, packages/core/src/providers/codex/codex-sql.ts, packages/core/src/providers/session-summary.ts, packages/testkit/src/codex-fixtures.ts, packages/testkit/fixtures/codex/state-5.sql, packages/testkit/fixtures/codex/history.jsonl, .planning/phases/02-repo-identity-and-provider-parsing/02-VALIDATION.md</read_first>
  <action>Create `packages/core/src/providers/codex/codex-adapter.test.ts` that uses `createCodexFixtureHome()` and proves four behaviors: one normalized summary per `threads.id`, lineage preserved from `thread_spawn_edges`, no unsafe text fields emitted in normalized outputs, and `history.jsonl` is consulted only when SQLite is unreadable and never as the totals source. Use test names that include the exact strings `dedupes by threads.id`, `preserves thread_spawn_edges lineage`, `does not emit first_user_message`, and `falls back to history.jsonl only when sqlite is unreadable` so the plan remains grep-verifiable.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/providers/codex/codex-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/providers/codex/codex-adapter.test.ts` contains `dedupes by threads.id`
    - `packages/core/src/providers/codex/codex-adapter.test.ts` contains `preserves thread_spawn_edges lineage`
    - `packages/core/src/providers/codex/codex-adapter.test.ts` contains `does not emit first_user_message`
    - `packages/core/src/providers/codex/codex-adapter.test.ts` contains `falls back to history.jsonl only when sqlite is unreadable`
  </acceptance_criteria>
  <done>SCAN-02 is protected by fixtures that prove the Codex adapter reads the right artifact, preserves lineage, and avoids privacy regressions.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` succeeds after the SQLite dependency and adapter land
- [ ] `npm test -- --run packages/core/src/providers/codex/codex-adapter.test.ts` succeeds
- [ ] `packages/core/src/providers/codex/codex-sql.ts` contains allowlisted column selection and no `SELECT *`
- [ ] `packages/core/src/providers/codex/codex-adapter.ts` never emits transcript-like fields
</verification>

<success_criteria>
- SCAN-02 is implemented against `~/.codex/state_*.sqlite`
- The shared normalized session model is established for later providers
- Codex lineage is preserved without settling aggregation policy early
- Fixture-backed tests prove safe parsing and fallback boundaries
</success_criteria>

<output>
After completion, create `.planning/phases/02-repo-identity-and-provider-parsing/02-02-SUMMARY.md`
</output>

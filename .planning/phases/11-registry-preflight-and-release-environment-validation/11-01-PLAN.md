---
phase: 11-registry-preflight-and-release-environment-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/release/preflight.ts
  - scripts/release/preflight.test.ts
autonomous: true
requirements:
  - REL-07
user_setup: []
must_haves:
  truths:
    - "The repo exposes one maintainer-owned `npm run release:preflight` entrypoint before any real publish step."
    - "The preflight derives the exact publishable package names and versions from workspace manifests instead of duplicated hardcoded lists."
    - "The preflight classifies live registry results for `agent-badge`, `create-agent-badge`, and `@agent-badge/core` into an unambiguous overall decision that can stop Phase 12 before publish."
  artifacts:
    - path: package.json
      provides: root maintainer entrypoint for release preflight
      contains: '"release:preflight":'
    - path: scripts/release/preflight.ts
      provides: manifest-derived live registry preflight and decision output
      contains: 'dist-tags.latest'
    - path: scripts/release/preflight.test.ts
      provides: deterministic tests for registry classification and package inventory
      contains: 'describe("release preflight"'
  key_links:
    - from: package.json
      to: scripts/release/preflight.ts
      via: "root release-preflight script delegates to the repo-owned preflight implementation"
      pattern: 'release:preflight'
    - from: scripts/release/preflight.ts
      to: packages/agent-badge/package.json
      via: "derives the runtime package name and intended version from the publishable workspace manifest"
      pattern: 'agent-badge'
    - from: scripts/release/preflight.ts
      to: packages/create-agent-badge/package.json
      via: "derives the initializer package name and intended version from the publishable workspace manifest"
      pattern: 'create-agent-badge'
    - from: scripts/release/preflight.ts
      to: packages/core/package.json
      via: "derives the scoped core package identity before live registry lookup"
      pattern: '@agent-badge/core'
---

<objective>
Add a repo-owned live registry preflight and decision record for publish readiness.

Purpose: REL-07 requires a maintainer to verify the real npm registry state for the three publishable packages immediately before publish and get a clear safe-vs-blocked decision instead of manually interpreting scattered `npm view` output.
Output: one `npm run release:preflight` entrypoint plus one tested implementation that resolves package inventory from local manifests, queries live registry state, and prints package-by-package plus overall status.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-registry-preflight-and-release-environment-validation/11-RESEARCH.md
@.planning/phases/11-registry-preflight-and-release-environment-validation/11-VALIDATION.md
@package.json
@docs/RELEASE.md
@.changeset/config.json
@packages/core/package.json
@packages/agent-badge/package.json
@packages/create-agent-badge/package.json
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the manifest-derived live registry preflight engine and status report</name>
  <files>scripts/release/preflight.ts</files>
  <read_first>.planning/STATE.md, .planning/phases/11-registry-preflight-and-release-environment-validation/11-RESEARCH.md, package.json, .changeset/config.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json, docs/RELEASE.md, .github/workflows/release.yml</read_first>
  <action>Create `scripts/release/preflight.ts` as a repo-owned TypeScript entrypoint executed from the repo root. The script must resolve the publishable package inventory from `packages/core/package.json`, `packages/agent-badge/package.json`, and `packages/create-agent-badge/package.json`, capturing the exact names `@agent-badge/core`, `agent-badge`, and `create-agent-badge` plus the intended local version from each manifest. For each package, run a live registry read with `npm view <package> name version dist-tags.latest --json` and normalize the outcome into a stable result shape with statuses such as `safe`, `warn`, or `blocked`. Treat a missing package response as a first-publish-safe state, treat an unexpected existing package/version state as at least `warn`, and treat a concrete version/name conflict that would stop the intended publish as `blocked`. Print a human-readable report that includes the package name, intended local version, observed registry state, and one overall decision line such as `OVERALL: safe`, `OVERALL: warn`, or `OVERALL: blocked`. Add `--json` support so maintainers can capture the same decision record in machine-readable form without mutating repo state.</action>
  <acceptance_criteria>
    - `scripts/release/preflight.ts` contains `npm`
    - `scripts/release/preflight.ts` contains `view`
    - `scripts/release/preflight.ts` contains `dist-tags.latest`
    - `scripts/release/preflight.ts` contains `--json`
    - `scripts/release/preflight.ts` contains `OVERALL:`
    - `scripts/release/preflight.ts` contains `safe`
    - `scripts/release/preflight.ts` contains `warn`
    - `scripts/release/preflight.ts` contains `blocked`
    - `scripts/release/preflight.ts` contains `@agent-badge/core`
    - `scripts/release/preflight.ts` contains `agent-badge`
    - `scripts/release/preflight.ts` contains `create-agent-badge`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'dist-tags\\.latest|OVERALL:|safe|warn|blocked|@agent-badge/core|agent-badge|create-agent-badge|--json' scripts/release/preflight.ts</automated>
  </verify>
  <done>The repo has one read-only preflight implementation that converts live npm package state into a package-by-package and overall release-readiness decision.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the repo command and add deterministic tests for registry-state classification</name>
  <files>package.json, scripts/release/preflight.test.ts</files>
  <read_first>package.json, scripts/release/preflight.ts, .planning/phases/11-registry-preflight-and-release-environment-validation/11-VALIDATION.md, packages/agent-badge/src/commands/release-readiness-matrix.test.ts</read_first>
  <action>Add a root script entry such as `"release:preflight": "tsx scripts/release/preflight.ts"` to `package.json`. Create `scripts/release/preflight.test.ts` with `describe("release preflight", ...)` and deterministic tests that stub child-process registry responses instead of calling the live network. Cover at least these cases with exact assertions on the normalized output: all three packages missing from the registry -> overall `safe`; one package already exists at an unexpected state -> overall `blocked`; registry metadata is partial or ambiguous -> overall `warn`; the manifest-derived package inventory includes exactly `@agent-badge/core`, `agent-badge`, and `create-agent-badge`. Keep the tests focused on pure result parsing and overall decision logic so they run without auth or network access.</action>
  <acceptance_criteria>
    - `package.json` contains `"release:preflight":`
    - `package.json` contains `tsx scripts/release/preflight.ts`
    - `scripts/release/preflight.test.ts` contains `describe("release preflight"`
    - `scripts/release/preflight.test.ts` contains `safe`
    - `scripts/release/preflight.test.ts` contains `warn`
    - `scripts/release/preflight.test.ts` contains `blocked`
    - `scripts/release/preflight.test.ts` contains `@agent-badge/core`
    - `scripts/release/preflight.test.ts` contains `agent-badge`
    - `scripts/release/preflight.test.ts` contains `create-agent-badge`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run scripts/release/preflight.test.ts</automated>
  </verify>
  <done>The preflight is runnable through a stable repo command and has deterministic unit coverage for the registry-state decision logic.</done>
</task>

</tasks>

<verification>
- `npm test -- --run scripts/release/preflight.test.ts` succeeds
- `package.json` exposes `npm run release:preflight`
- `npm run release:preflight -- --json` is available for machine-readable decision capture without publishing anything
</verification>

<success_criteria>
- REL-07 has a repo-owned live registry preflight instead of manual `npm view` interpretation.
- Maintainers can see a single overall decision plus package-by-package evidence before attempting production publish.
</success_criteria>

<output>
After completion, create `.planning/phases/11-registry-preflight-and-release-environment-validation/11-01-SUMMARY.md`
</output>

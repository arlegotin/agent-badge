---
phase: 11-registry-preflight-and-release-environment-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - scripts/release/preflight.ts
  - scripts/release/preflight.test.ts
  - docs/RELEASE.md
  - scripts/verify-docs.sh
autonomous: true
requirements:
  - REL-07
user_setup: []
must_haves:
  truths:
    - "The same repo-owned `npm run release:preflight` command checks npm auth, release-input coherence, and release workflow prerequisites before Phase 12."
    - "The preflight surfaces exact blockers for missing npm auth, release workflow drift, or unresolved publish prerequisites instead of requiring the maintainer to infer them from docs."
    - "The maintainer release checklist requires `npm run release:preflight` before `npm run release`, and the docs gate enforces that contract."
  artifacts:
    - path: scripts/release/preflight.ts
      provides: release-environment and workflow-prerequisite checks layered onto the registry preflight
      contains: 'npm whoami'
    - path: docs/RELEASE.md
      provides: maintainer-facing release sequence that includes the repo-owned preflight command
      contains: 'npm run release:preflight'
    - path: scripts/verify-docs.sh
      provides: deterministic docs enforcement for the new preflight step
      contains: 'npm run release:preflight'
  key_links:
    - from: scripts/release/preflight.ts
      to: .github/workflows/release.yml
      via: "preflight validates the expected workflow contract before production publish"
      pattern: 'NPM_TOKEN'
    - from: docs/RELEASE.md
      to: package.json
      via: "maintainer release checklist points directly at the repo-owned preflight command"
      pattern: 'npm run release:preflight'
    - from: scripts/verify-docs.sh
      to: docs/RELEASE.md
      via: "docs verification enforces the new preflight command and release sequence"
      pattern: 'npm run release:preflight'
---

<objective>
Validate release credentials, secrets, and GitHub Actions prerequisites before production publish.

Purpose: REL-07 is not complete until the maintainer can prove the release environment itself is ready, not only that the registry package names can be looked up. Phase 12 should not start if npm auth is missing, if the workflow contract has drifted, or if the release inputs are inconsistent with the intended publish path.
Output: an extended preflight command plus docs and docs-gate updates that make the environment checks part of the required release sequence.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-registry-preflight-and-release-environment-validation/11-RESEARCH.md
@.planning/phases/11-registry-preflight-and-release-environment-validation/11-VALIDATION.md
@.planning/phases/11-registry-preflight-and-release-environment-validation/11-01-PLAN.md
@package.json
@docs/RELEASE.md
@scripts/verify-docs.sh
@.changeset/config.json
@.github/workflows/release.yml
@.github/workflows/ci.yml
@packages/core/package.json
@packages/agent-badge/package.json
@packages/create-agent-badge/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the preflight with npm auth, workflow-contract, and release-input checks</name>
  <files>scripts/release/preflight.ts, scripts/release/preflight.test.ts</files>
  <read_first>scripts/release/preflight.ts, scripts/release/preflight.test.ts, .planning/phases/11-registry-preflight-and-release-environment-validation/11-RESEARCH.md, .changeset/config.json, .github/workflows/release.yml, package.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json</read_first>
  <action>Extend `scripts/release/preflight.ts` so the same command validates the operator environment before publish. Add an npm auth section that runs `npm ping` and `npm whoami` and reports exact failures when registry access or authenticated identity is missing. Add a release-input section that reads `.changeset/config.json` plus the three publishable package manifests and verifies the repo still intends a public publish path with coherent package names and versions. Add a workflow-contract section that reads `.github/workflows/release.yml` and verifies the current production path still contains `changesets/action@v1`, `publish: npm run release`, and `NPM_TOKEN`. Keep these checks read-only. Update `scripts/release/preflight.test.ts` to cover exact blocked outcomes for missing npm auth, missing workflow markers, and inconsistent release-input state, with explicit assertions on the overall status and check IDs.</action>
  <acceptance_criteria>
    - `scripts/release/preflight.ts` contains `npm ping`
    - `scripts/release/preflight.ts` contains `npm whoami`
    - `scripts/release/preflight.ts` contains `.changeset/config.json`
    - `scripts/release/preflight.ts` contains `.github/workflows/release.yml`
    - `scripts/release/preflight.ts` contains `changesets/action@v1`
    - `scripts/release/preflight.ts` contains `publish: npm run release`
    - `scripts/release/preflight.ts` contains `NPM_TOKEN`
    - `scripts/release/preflight.test.ts` contains `npm-auth`
    - `scripts/release/preflight.test.ts` contains `workflow-contract`
    - `scripts/release/preflight.test.ts` contains `release-inputs`
    - `scripts/release/preflight.test.ts` contains `blocked`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run scripts/release/preflight.test.ts</automated>
  </verify>
  <done>The preflight blocks on missing npm auth, inconsistent release inputs, or workflow-contract drift before any real publish is attempted.</done>
</task>

<task type="auto">
  <name>Task 2: Make release preflight mandatory in the maintainer checklist and enforce it in docs verification</name>
  <files>docs/RELEASE.md, scripts/verify-docs.sh</files>
  <read_first>docs/RELEASE.md, scripts/verify-docs.sh, package.json, scripts/release/preflight.ts, .github/workflows/release.yml, .planning/phases/10-release-rehearsal-and-checklist/10-02-SUMMARY.md</read_first>
  <action>Update `docs/RELEASE.md` so the required release sequence becomes `npm run docs:check`, `npm run typecheck`, `npm run verify:clean-checkout`, `npm run release:preflight`, and only then `npm run release`. Document that `npm run release:preflight` is the gate for live registry, npm auth, and workflow-prerequisite checks. Keep the existing `/tmp` and `npm_config_cache` guidance intact, and add exact maintainer follow-up text for any GitHub Actions prerequisites that the local command cannot prove automatically. Update `scripts/verify-docs.sh` to enforce the presence of `npm run release:preflight`, `npm whoami`, `npm ping`, and the existing `NPM_TOKEN`-relevant release guidance in `docs/RELEASE.md`. Keep the current docs checks additive and deterministic; do not add new docs tooling.</action>
  <acceptance_criteria>
    - `docs/RELEASE.md` contains `npm run release:preflight`
    - `docs/RELEASE.md` contains `npm whoami`
    - `docs/RELEASE.md` contains `npm ping`
    - `docs/RELEASE.md` contains `npm run release`
    - `scripts/verify-docs.sh` contains `npm run release:preflight`
    - `scripts/verify-docs.sh` contains `npm whoami`
    - `scripts/verify-docs.sh` contains `npm ping`
    - `scripts/verify-docs.sh` still contains `npm run verify:clean-checkout`
    - `scripts/verify-docs.sh` still contains `npm view agent-badge`
    - `scripts/verify-docs.sh` still contains `npm view create-agent-badge`
    - `scripts/verify-docs.sh` still contains `npm view @agent-badge/core`
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check</automated>
  </verify>
  <done>The maintainer release checklist now requires the repo-owned preflight command before publish, and the docs gate prevents that step from drifting away.</done>
</task>

</tasks>

<verification>
- `npm test -- --run scripts/release/preflight.test.ts` succeeds
- `npm run docs:check` succeeds
- `npm run release:preflight` reports registry state, npm auth state, release-input state, and workflow-contract state before `npm run release`
</verification>

<success_criteria>
- REL-07 is satisfied by one repo-owned preflight flow that covers both live package-state checks and release-environment readiness.
- Phase 12 can begin only after a maintainer has an explicit pass/warn/block report instead of inferred release confidence.
</success_criteria>

<output>
After completion, create `.planning/phases/11-registry-preflight-and-release-environment-validation/11-02-SUMMARY.md`
</output>

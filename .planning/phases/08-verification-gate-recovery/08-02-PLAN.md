---
phase: 08-verification-gate-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/diagnostics/doctor.test.ts
  - packages/core/src/providers/claude/claude-adapter.test.ts
  - packages/testkit/src/claude-fixtures.ts
autonomous: true
requirements:
  - REL-05
user_setup: []
must_haves:
  truths:
    - "`npm test -- --run` passes the doctor fixture coverage against the current state schema."
    - "Claude incremental refresh coverage is deterministic on copied fixtures and does not depend on a fixed timestamp being newer than the host filesystem watermark."
    - "Fixture repairs follow current config/state/runtime contracts instead of freezing obsolete shapes into tests."
  artifacts:
    - path: packages/core/src/diagnostics/doctor.test.ts
      provides: doctor command regression coverage against current scaffold/state contracts
      contains: 'describe("runDoctorChecks"'
    - path: packages/core/src/providers/claude/claude-adapter.test.ts
      provides: deterministic incremental Claude coverage
      contains: 'returns only sessions whose project files changed after the cursor watermark'
    - path: packages/testkit/src/claude-fixtures.ts
      provides: Claude fixture copy behavior used by incremental tests
      contains: 'export async function createClaudeFixtureHome()'
  key_links:
    - from: packages/core/src/diagnostics/doctor.test.ts
      to: packages/core/src/state/state-schema.ts
      via: "doctor fixtures must serialize current state schema fields"
      pattern: 'export function parseAgentBadgeState('
    - from: packages/core/src/providers/claude/claude-adapter.test.ts
      to: packages/core/src/providers/claude/claude-adapter.ts
      via: "incremental test must prove the real watermark contract"
      pattern: 'function isClaudeFileChanged('
---

<objective>
Repair the failing test gate by rebasing stale fixtures to current schema and current incremental-file semantics.

Purpose: REL-05 is blocked by three doctor-test failures and one Claude incremental regression.
Output: updated doctor fixtures, deterministic Claude incremental coverage, and passing targeted tests for the current runtime contract.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-verification-gate-recovery/08-RESEARCH.md
@.planning/phases/08-verification-gate-recovery/08-VALIDATION.md
@packages/core/src/diagnostics/doctor.test.ts
@packages/core/src/diagnostics/doctor.ts
@packages/core/src/state/state-schema.ts
@packages/core/src/init/default-state.ts
@packages/core/src/providers/claude/claude-adapter.ts
@packages/core/src/providers/claude/claude-adapter.test.ts
@packages/testkit/src/claude-fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild doctor fixtures from the current state schema instead of config-shaped data</name>
  <files>packages/core/src/diagnostics/doctor.test.ts</files>
  <read_first>packages/core/src/diagnostics/doctor.test.ts, packages/core/src/state/state-schema.ts, packages/core/src/init/default-state.ts, packages/core/src/config/config-schema.ts, packages/core/src/diagnostics/doctor.ts</read_first>
  <action>Update `packages/core/src/diagnostics/doctor.test.ts` so `.agent-badge/state.json` is created from the current state source of truth rather than `{ ...parseAgentBadgeConfig(config), publish: ... }`. Build fixture state with the fields required by `agentBadgeStateSchema`: `version`, `init`, `checkpoints`, `publish`, `refresh`, and `overrides`. Preserve the scenario-specific publish values (`status`, `gistId`, `lastPublishedHash`, `lastPublishedAt`) while leaving the rest of the state valid for current runtime parsing. Remove the config-shaped keys from the persisted state fixture path entirely.</action>
  <acceptance_criteria>
    - `packages/core/src/diagnostics/doctor.test.ts` contains `parseAgentBadgeState(`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `init:`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `checkpoints:`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `overrides:`
    - `packages/core/src/diagnostics/doctor.test.ts` does not contain `...parseAgentBadgeConfig(config)`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/diagnostics/doctor.test.ts</automated>
  </verify>
  <done>The doctor fixture writes current-schema state and the doctor regression tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Make the Claude incremental test advance a file beyond the real cursor watermark deterministically</name>
  <files>packages/core/src/providers/claude/claude-adapter.test.ts, packages/testkit/src/claude-fixtures.ts</files>
  <read_first>packages/core/src/providers/claude/claude-adapter.test.ts, packages/core/src/providers/claude/claude-adapter.ts, packages/testkit/src/claude-fixtures.ts</read_first>
  <action>Update `packages/core/src/providers/claude/claude-adapter.test.ts` so the changed-file scenario derives a definitely-newer timestamp from the runtime fixture state instead of hard-coding `2026-03-31T00:00:00Z`. After creating the cursor, compute a timestamp greater than the copied fixture watermark (for example by reading the touched file stat or using `Date.now()` after the cursor is built), apply it with `utimes`, and keep the append payload for `session-main`. If the fixture copier in `packages/testkit/src/claude-fixtures.ts` preserves source mtimes in a way that makes the test ambiguous across hosts, update the helper so copied project files get deterministic local mtimes that the test can advance reliably.</action>
  <acceptance_criteria>
    - `packages/core/src/providers/claude/claude-adapter.test.ts` contains `returns only sessions whose project files changed after the cursor watermark`
    - `packages/core/src/providers/claude/claude-adapter.test.ts` does not contain `2026-03-31T00:00:00Z`
    - `packages/core/src/providers/claude/claude-adapter.test.ts` contains `utimes(`
    - `packages/core/src/providers/claude/claude-adapter.test.ts` contains `session-main`
    - `packages/testkit/src/claude-fixtures.ts` contains `createClaudeFixtureHome`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/providers/claude/claude-adapter.test.ts && npm test -- --run packages/core/src/diagnostics/doctor.test.ts</automated>
  </verify>
  <done>The remaining targeted failing tests are deterministic and green on current fixtures.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/diagnostics/doctor.test.ts` succeeds
- `npm test -- --run packages/core/src/providers/claude/claude-adapter.test.ts` succeeds
- `npm test -- --run` no longer fails on doctor or Claude incremental coverage
</verification>

<success_criteria>
- REL-05 is restored for the currently failing doctor and Claude incremental paths.
- The repaired tests prove current runtime/schema behavior rather than stale fixture assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-gate-recovery/08-02-SUMMARY.md`
</output>

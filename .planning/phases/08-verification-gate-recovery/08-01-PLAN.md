---
phase: 08-verification-gate-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/publish/github-gist-client.ts
  - packages/core/src/publish/github-gist-client.test.ts
autonomous: true
requirements:
  - REL-04
user_setup: []
must_haves:
  truths:
    - "`npm run build` succeeds from current source without TypeScript errors at the Octokit seam."
    - "The GitHub Gist client still supports get, create, update, and delete behavior through the local transport abstraction."
    - "The publish layer still issues gist_id-based requests and normalizes Gist metadata correctly after the build fix."
  artifacts:
    - path: packages/core/src/publish/github-gist-client.ts
      provides: compile-safe Octokit transport seam
      contains: 'export function createGitHubGistClient('
    - path: packages/core/src/publish/github-gist-client.test.ts
      provides: unit coverage for gist get/create/update/delete transport mapping
      contains: 'it("deletes a gist through the transport seam"'
  key_links:
    - from: packages/core/src/publish/github-gist-client.ts
      to: packages/core/src/publish/github-gist-client.test.ts
      via: "transport seam and delete behavior are locked by unit tests"
      pattern: 'remove: vi.fn()'
---

<objective>
Recover the TypeScript build gate by fixing the Octokit transport seam used by the GitHub Gist client.

Purpose: REL-04 is currently blocked by a compiler failure in the dynamic Octokit import path.
Output: one compile-safe gist client implementation plus test coverage that locks get/create/update/delete transport behavior.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-verification-gate-recovery/08-RESEARCH.md
@.planning/phases/08-verification-gate-recovery/08-VALIDATION.md
@package.json
@packages/core/src/publish/github-gist-client.ts
@packages/core/src/publish/github-gist-client.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the brittle Octokit module cast with a compile-safe constructor seam</name>
  <files>packages/core/src/publish/github-gist-client.ts</files>
  <read_first>packages/core/src/publish/github-gist-client.ts, packages/core/src/publish/github-gist-client.test.ts, packages/core/package.json</read_first>
  <action>Update `packages/core/src/publish/github-gist-client.ts` so `loadOctokit()` no longer force-casts `await import("octokit")` to a full `{ readonly Octokit: new (...) => OctokitLike }` module shape. Extract `Octokit` through a narrowed helper that validates the exported constructor before instantiation, then adapt only the gist methods the repo actually uses. Preserve `gist_id` request payloads and keep `createGitHubGistClient()` returning the same `GitHubGistClient` interface. If the installed Octokit type surface exposes gist deletion through a different method name than `remove`, map that upstream method into the local `deleteGist()` behavior instead of broadening local types with `any`.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/github-gist-client.ts` contains `await import("octokit")`
    - `packages/core/src/publish/github-gist-client.ts` does not contain `as {`
    - `packages/core/src/publish/github-gist-client.ts` contains `createGitHubGistClient(`
    - `packages/core/src/publish/github-gist-client.ts` contains `deleteGist(input)`
    - `packages/core/src/publish/github-gist-client.ts` contains `gist_id: input.gistId`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The workspace build no longer fails on the Octokit import seam.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the corrected gist transport behavior with explicit seam tests</name>
  <files>packages/core/src/publish/github-gist-client.test.ts</files>
  <read_first>packages/core/src/publish/github-gist-client.test.ts, packages/core/src/publish/github-gist-client.ts</read_first>
  <action>Update `packages/core/src/publish/github-gist-client.test.ts` so the seam tests cover the exact constructor/transport behavior preserved by Task 1: one test each for `get`, `create`, `update`, and `delete`, all driven through the injected `octokit` transport object. Keep the delete assertion aligned with the actual local transport method name used after Task 1 while preserving the public `deleteGist({ gistId })` contract. Make the tests assert the exact `gist_id` payloads and response normalization the runtime depends on.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/github-gist-client.test.ts` contains `maps gist metadata from the get endpoint`
    - `packages/core/src/publish/github-gist-client.test.ts` contains `creates public gists through the transport seam`
    - `packages/core/src/publish/github-gist-client.test.ts` contains `updates a single gist file by gist id`
    - `packages/core/src/publish/github-gist-client.test.ts` contains `deletes a gist through the transport seam`
    - `packages/core/src/publish/github-gist-client.test.ts` contains `gist_id: "gist_123"`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/github-gist-client.test.ts && npm run build</automated>
  </verify>
  <done>The Gist client seam is compile-safe and behaviorally locked by unit tests.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npm test -- --run packages/core/src/publish/github-gist-client.test.ts` succeeds
- The TypeScript compiler no longer reports `TS2352` in `packages/core/src/publish/github-gist-client.ts`
</verification>

<success_criteria>
- REL-04 build failure is removed at the Octokit seam.
- Gist transport behavior remains covered for get/create/update/delete operations.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-gate-recovery/08-01-SUMMARY.md`
</output>

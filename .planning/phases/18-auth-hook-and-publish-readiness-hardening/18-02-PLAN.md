---
phase: 18-auth-hook-and-publish-readiness-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - packages/core/src/diagnostics/doctor.ts
  - packages/core/src/diagnostics/doctor.test.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "`init` and `doctor` point operators to the exact supported auth env vars or gist-target recovery path using one canonical readiness vocabulary instead of command-local copy."
    - "`publish` and normal `refresh` print `Publish readiness:` lines derived from the canonical readiness report before trust or failure output diverges."
    - "Operator-facing readiness wording stays aligned across init, doctor, publish, and refresh because the commands render the same core remediation contract."
  artifacts:
    - path: packages/core/src/diagnostics/doctor.ts
      provides: doctor checks that consume the canonical readiness report and remediation text
      contains: inspectPublishReadiness
    - path: packages/agent-badge/src/commands/init.ts
      provides: deferred setup and first-publish guidance sourced from the canonical readiness contract
      contains: GH_TOKEN, GITHUB_TOKEN, or GITHUB_PAT
    - path: packages/agent-badge/src/commands/publish.ts
      provides: canonical publish readiness reporting before operator-facing failure handling
      contains: Publish readiness:
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: standard refresh readiness reporting that stays aligned with publish and doctor
      contains: Publish readiness:
  key_links:
    - from: packages/core/src/publish/publish-readiness.ts
      to: packages/core/src/diagnostics/doctor.ts
      via: "doctor reuses the same readiness classification and fix lines already produced by the canonical contract"
      pattern: inspectPublishReadiness
    - from: packages/core/src/publish/publish-readiness.ts
      to: packages/agent-badge/src/commands/init.ts
      via: "init surfaces the same auth and gist remediation text instead of maintaining separate deferred-setup copy"
      pattern: GH_TOKEN, GITHUB_TOKEN, or GITHUB_PAT
    - from: packages/core/src/publish/publish-readiness.ts
      to: packages/agent-badge/src/commands/publish.ts
      via: "publish renders the canonical readiness classification before command-specific exit handling"
      pattern: Publish readiness:
    - from: packages/core/src/publish/publish-readiness.ts
      to: packages/agent-badge/src/commands/refresh.ts
      via: "normal refresh renders the same readiness classification instead of inventing a second vocabulary"
      pattern: Publish readiness:
---

<objective>
Wire the canonical readiness contract from Plan 18-01 into the operator-facing command surfaces that need it.

Purpose: The core runtime can classify failures, but operators only benefit if init, doctor, publish, and refresh all render the same explanations and fixes. This plan is only about those command surfaces, not hook automation policy.
Output: init, doctor, publish, and refresh all render canonical readiness classifications and environment-specific remediation sourced from one core contract.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-01-PLAN.md
@packages/core/src/publish/publish-readiness.ts
@packages/core/src/diagnostics/doctor.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/commands/refresh.ts

<interfaces>
From packages/core/src/publish/publish-readiness.ts:
```typescript
export function inspectPublishReadiness(
  options?: InspectPublishReadinessOptions
): PublishReadinessReport;

export function formatPublishReadinessStatus(
  status: PublishReadinessStatus
): string;
```

From packages/core/src/diagnostics/doctor.ts:
```typescript
export interface DoctorCheckResult {
  readonly id: string;
  readonly status: "ok" | "warn" | "fail";
  readonly summary: string;
  readonly fix?: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse canonical readiness fixes in init and doctor</name>
  <files>packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts</files>
  <read_first>packages/core/src/publish/publish-readiness.ts, packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md</read_first>
  <action>Wire `inspectPublishReadiness()` into `packages/core/src/diagnostics/doctor.ts` and `packages/agent-badge/src/commands/init.ts`. In doctor, keep the existing `publish-auth` and `publish-write` checks but make them consume the canonical readiness report and fix text instead of duplicating logic; preserve read-only default behavior and keep `--probe-write` as the only mutating path. In init, reuse the same readiness-fix wording for deferred setup and first-publish failure messages so operators see the same supported env vars (`GH_TOKEN`, `GITHUB_TOKEN`, `GITHUB_PAT`) and the same `agent-badge init --gist-id <id>` recovery path everywhere.</action>
  <acceptance_criteria>
    - `packages/core/src/diagnostics/doctor.ts` contains `inspectPublishReadiness`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `reuses canonical publish readiness fixes for gist-unreachable`
    - `packages/agent-badge/src/commands/init.ts` contains `GH_TOKEN, GITHUB_TOKEN, or GITHUB_PAT`
    - `packages/agent-badge/src/commands/init.test.ts` contains `prints the canonical auth-missing remediation when badge setup is deferred`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/init.test.ts</automated>
  </verify>
  <done>Init and doctor use the canonical readiness vocabulary and environment-specific fixes instead of maintaining divergent copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse canonical readiness reporting in publish and normal refresh</name>
  <files>packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts</files>
  <read_first>packages/core/src/publish/publish-readiness.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md</read_first>
  <action>Update `packages/agent-badge/src/commands/publish.ts` and the normal non-hook path in `packages/agent-badge/src/commands/refresh.ts` so both commands render a line exactly prefixed `- Publish readiness:` using the canonical formatter from Plan 18-01. The wording must cover `auth missing`, `gist unreachable`, `gist not public`, `remote write failed`, and `remote readback mismatch`, and must stay sourced from the readiness contract rather than command-local strings. Do not add hook-policy warning or blocking behavior here; that belongs in Plan 18-03.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/publish.ts` contains `- Publish readiness:`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `renders Publish readiness: remote readback mismatch`
    - `packages/agent-badge/src/commands/refresh.ts` contains `- Publish readiness:`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `renders Publish readiness: auth missing`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `renders Publish readiness: gist unreachable`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <done>Publish and normal refresh render the same readiness classification as init and doctor without yet changing hook-policy UX.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/init.test.ts` succeeds
- `npm test -- --run packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds
- `npm test -- --run packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds after the full plan
</verification>

<success_criteria>
- Init and doctor tell the operator exactly how to fix missing auth or broken gist readiness using one shared remediation contract.
- Publish and normal refresh render canonical readiness states before command-specific trust or failure handling diverges.
- Operator-facing readiness wording does not drift between init, doctor, publish, and refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-02-SUMMARY.md`
</output>

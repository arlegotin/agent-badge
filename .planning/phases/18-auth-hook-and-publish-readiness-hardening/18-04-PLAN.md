---
phase: 18-auth-hook-and-publish-readiness-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - packages/core/src/publish/shared-badge-aggregation.test.ts
  - packages/core/src/publish/publish-service.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "The shared publish regression no longer assumes `agent-badge.json` is the final gist write, so automated validation matches the real badge -> contributor -> overrides update sequence."
    - "A successful remote write is not treated as trustworthy completion until publish execution reads the gist back and verifies the expected badge payload and shared files."
    - "A live `agent-badge publish` run with missing or invalid GitHub auth reports `Publish readiness: auth missing` with canonical remediation instead of surfacing raw GitHub REST auth text."
    - "A normal non-hook `agent-badge refresh` run that fails during publish still prints `Publish readiness` and `Live badge trust` lines before exiting non-zero."
  artifacts:
    - path: packages/core/src/publish/shared-badge-aggregation.test.ts
      provides: sequence-aware shared publish assertions aligned with the current multi-write gist update order
      contains: updateGistFile.mock.calls
    - path: packages/core/src/publish/publish-service.ts
      provides: post-write gist readback verification plus auth-aware publish failure classification
      contains: remote-readback-mismatch
    - path: packages/agent-badge/src/commands/publish.ts
      provides: sanitized canonical publish failure output that does not leak raw GitHub REST auth text
      contains: Publish readiness:
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: normal-run degraded publish summary output on failure
      contains: Live badge trust:
  key_links:
    - from: packages/core/src/publish/shared-badge-aggregation.test.ts
      to: packages/core/src/publish/publish-service.ts
      via: "sequence-aware assertions match the real badge -> contributor -> overrides gist write order"
      pattern: AGENT_BADGE_OVERRIDES_GIST_FILE
    - from: packages/core/src/publish/publish-service.ts
      to: packages/agent-badge/src/commands/publish.ts
      via: "typed auth failures map unauthenticated GitHub errors to `auth-missing`, which publish renders through the canonical readiness line"
      pattern: auth-missing
    - from: packages/core/src/publish/publish-service.ts
      to: packages/core/src/publish/shared-badge-aggregation.test.ts
      via: "readback-aware publish execution and sequence-aware regression coverage stay aligned on the real badge -> contributor -> overrides write order"
      pattern: remote-readback-mismatch
    - from: packages/agent-badge/src/commands/refresh.ts
      to: packages/core/src/publish/publish-trust.ts
      via: "normal refresh failure output prints the same trust summary already used on healthy and hook-mode paths"
      pattern: printPublishTrustLines
---

<objective>
Close the remaining automated-validation and live-UAT gaps after the main Phase 18 readiness and automation work lands.

Purpose: The earlier plans establish the intended readiness and automation contracts. This final gap-closure plan adds publish readback verification where it becomes operationally relevant, fixes the known failing regression, closes the live missing-auth publish misclassification, and makes normal refresh failure output catch up to the canonical readiness/trust summary.
Output: green shared publish regression coverage, typed post-write publish verification, auth-aware live publish failure handling, and standard refresh failure output that matches the canonical readiness/trust vocabulary.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-HUMAN-UAT.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-01-PLAN.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-02-PLAN.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-03-PLAN.md
@packages/core/src/publish/shared-badge-aggregation.test.ts
@packages/core/src/publish/publish-service.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/commands/refresh.ts

<interfaces>
From packages/core/src/publish/publish-service.ts:
```typescript
export class PublishBadgeError extends Error {
  readonly attemptedAt: string;
  readonly failureCode: AgentBadgePublishFailureCode;
  readonly candidateHash: string | null;
  readonly changedBadge: boolean | null;
}
```

From packages/agent-badge/src/commands/refresh.ts:
```typescript
function printSoftFailure(
  stdout: OutputWriter,
  error: Error,
  state: AgentBadgeState | null,
  config: ReturnType<typeof parseAgentBadgeConfig> | null,
  hookPolicy?: AgentBadgeRefreshMode,
  failureStatus: "failed-soft" | "failed" = "failed-soft"
): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the shared publish regression so automated validation matches the real gist write sequence</name>
  <files>packages/core/src/publish/shared-badge-aggregation.test.ts</files>
  <behavior>
    - Test 1: the merged-remote-totals case still proves one `updateGistFile()` call writes `agent-badge.json` with `50 tokens` even though later calls write the local contributor and overrides files.
    - Test 2: the partial-local-snapshot case still proves the badge payload write contains `100 tokens` without requiring that badge call to be last.
    - Test 3: the duplicate-session case still proves the badge payload write contains `30 tokens` and does not regress dedupe coverage when overrides are written afterward.
  </behavior>
  <action>Update the stale expectations called out in the existing Phase 18 UAT notes. Replace badge-payload `toHaveBeenLastCalledWith(...)` assertions with sequence-aware checks against `updateGistFile.mock.calls` so the regression matches the current badge -> contributor -> overrides write order. Keep the assertions strong enough to catch regressions in both payload content and write sequencing; do not weaken them to a generic `toHaveBeenCalled()` or substring-only check.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/shared-badge-aggregation.test.ts` no longer contains `toHaveBeenLastCalledWith({` for the badge payload expectations
    - the file contains a sequence-aware assertion against `updateGistFile.mock.calls`
    - the file still asserts badge payload messages `50 tokens`, `100 tokens`, and `30 tokens`
    - the file still references `AGENT_BADGE_OVERRIDES_GIST_FILE`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/shared-badge-aggregation.test.ts</automated>
  </verify>
  <done>The shared aggregation regression matches the real gist write order and no longer blocks the automated validation gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add publish readback verification and reclassify live unauthenticated publish failures</name>
  <files>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts</files>
  <read_first>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/publish-readiness.ts, packages/core/src/publish/publish-state.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-HUMAN-UAT.md</read_first>
  <action>Update `packages/core/src/publish/publish-service.ts` so successful writes are verified by remote readback before the runtime trusts them. After `updateGistFile()` succeeds, re-read the gist, ensure `agent-badge.json` exists, verify its payload hash matches `candidateHash`, and ensure required shared files are present and parseable. Throw a typed `PublishBadgeError` with `failureCode` set to `remote-readback-failed`, `remote-readback-mismatch`, or `remote-state-invalid` when verification fails. In the same publish-service change set, map GitHub auth failures encountered during gist reads, writes, or readback verification to `failureCode: "auth-missing"` when the upstream error clearly signals missing or invalid credentials, preserving the original error as `cause` while normalizing the surfaced message to operator-safe wording. Update `packages/agent-badge/src/commands/publish.ts` so the typed publish-error branch renders the canonical `auth missing` wording and does not add raw GitHub REST auth text back into command output.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-service.ts` contains `remote-readback-failed`
    - `packages/core/src/publish/publish-service.ts` contains `remote-readback-mismatch`
    - `packages/core/src/publish/publish-service.ts` contains `remote-state-invalid`
    - `packages/core/src/publish/publish-service.ts` contains `auth-missing`
    - `packages/core/src/publish/publish-service.test.ts` contains `records remote-readback-mismatch when the remote badge payload hash differs`
    - `packages/core/src/publish/publish-service.test.ts` contains `records remote-readback-failed when gist readback fails after a successful write`
    - `packages/core/src/publish/publish-service.test.ts` contains `classifies unauthenticated GitHub write failures as auth-missing`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `renders Publish readiness: auth missing before rethrowing a typed auth failure`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `not.toContain(\"Requires authentication - https://docs.github.com/rest\")`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts</automated>
  </verify>
  <done>Publish execution distinguishes target, write, and readback failures, verifies successful writes by remote readback, and surfaces live auth failures as canonical `auth missing` output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make normal refresh failure output print the canonical readiness and trust summary before exiting non-zero</name>
  <files>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts</files>
  <read_first>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/core/src/publish/publish-trust.ts, packages/core/src/publish/publish-readiness.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-HUMAN-UAT.md</read_first>
  <behavior>
    - Test 1: a standard non-hook refresh that fails during publish still prints `- Publish readiness:` and `- Live badge trust:` before exiting non-zero.
    - Test 2: the standard refresh auth-failure output uses the same canonical wording as the existing hook-mode branch.
    - Test 3: hook-mode `fail-soft` and `strict` output remain unchanged while the normal refresh path catches up to the same degraded-state vocabulary.
  </behavior>
  <action>Refactor the normal non-hook failure path in `packages/agent-badge/src/commands/refresh.ts` so it prints the same degraded publish summary currently used by hook-mode refresh before rethrowing. Reuse the existing readiness formatter and trust formatter so the output lines are exactly prefixed `- Publish readiness:` and `- Live badge trust:` in both paths, and keep the current `Pre-push policy`, `Warning`, and `Blocking` behavior unchanged for hook-mode runs. When the underlying publish error was the live auth failure from Task 2, ensure the normal refresh output also avoids echoing raw `Requires authentication - https://docs.github.com/rest` text. Do not convert normal refresh into fail-soft mode; it should still exit non-zero after printing the canonical degraded summary.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `renders Publish readiness and Live badge trust in standard refresh failure output`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `renders Publish readiness: auth missing in standard refresh failure output`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `not.toContain(\"Requires authentication - https://docs.github.com/rest\")`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Warning: live badge may be stale; push continues because pre-push policy is fail-soft.`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Blocking: push stopped because pre-push policy is strict.`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <done>Normal refresh failures now print the canonical readiness and trust summary before exiting, without regressing hook-mode fail-soft and strict behavior.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/shared-badge-aggregation.test.ts` succeeds
- `npm test -- --run packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds
- `npm test -- --run` succeeds after the full plan
</verification>

<success_criteria>
- Automated validation no longer fails because the shared publish regression assumes the badge payload is the last gist write.
- Publish completion is verified by gist readback instead of assuming a successful remote write is sufficient.
- Live unauthenticated publish failures classify as `auth-missing` and surface canonical readiness wording instead of raw GitHub REST auth text.
- Normal non-hook refresh prints `Publish readiness` and `Live badge trust` lines even when publish fails and the command exits non-zero.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-04-SUMMARY.md`
</output>

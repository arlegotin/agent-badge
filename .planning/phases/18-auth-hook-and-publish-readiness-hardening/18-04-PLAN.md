---
phase: 18-auth-hook-and-publish-readiness-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - packages/core/src/publish/publish-service.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
autonomous: true
requirements:
  - OPER-03
  - AUTH-01
  - AUTH-02
  - CTRL-01
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "A live `agent-badge publish` run with missing or invalid GitHub auth reports `Publish readiness: auth missing` with canonical remediation instead of `remote write failed` plus raw GitHub REST auth text."
    - "A normal non-hook `agent-badge refresh` run that hits a publish failure still prints `- Publish readiness:` and `- Live badge trust:` before exiting non-zero."
    - "Normal refresh failure output uses the same degraded publish vocabulary as hook-mode refresh without regressing the explicit pre-push policy behavior from Plan 18-02."
  artifacts:
    - path: packages/core/src/publish/publish-service.ts
      provides: auth-aware publish failure classification for live GitHub write failures
      contains: auth-missing
    - path: packages/agent-badge/src/commands/publish.ts
      provides: canonical publish failure output that does not leak raw GitHub REST auth text
      contains: Publish readiness:
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: normal-run degraded publish summary output on failure
      contains: Live badge trust:
    - path: packages/agent-badge/src/commands/refresh.test.ts
      provides: regression coverage for standard refresh failure summaries and vocabulary parity
      contains: standard refresh failure output
  key_links:
    - from: packages/core/src/publish/publish-service.ts
      to: packages/agent-badge/src/commands/publish.ts
      via: "typed publish failures map unauthenticated GitHub errors to `auth-missing`, which publish renders through the canonical readiness line"
      pattern: auth-missing
    - from: packages/agent-badge/src/commands/refresh.ts
      to: packages/core/src/publish/publish-trust.ts
      via: "normal refresh failure output prints the same trust summary already used on healthy and hook-mode paths"
      pattern: printPublishTrustLines
    - from: packages/agent-badge/src/commands/refresh.ts
      to: packages/core/src/publish/pre-push-policy.ts
      via: "normal refresh failure output stays vocabulary-compatible with hook-mode degraded reporting rather than inventing new wording"
      pattern: live badge may be stale; push continues because pre-push policy is fail-soft.
---

<objective>
Close the live Phase 18 UAT failures where auth-related publish errors still bypass the canonical readiness/trust output.

Purpose: Phase 18's mocked tests passed, but live GitHub auth failures showed two remaining runtime gaps: `publish` still collapses unauthenticated writes into `remote write failed` and leaks raw Octokit auth text, while normal `refresh` exits before printing the canonical readiness and trust summary lines that hook-mode already shows. This plan fixes those runtime surfaces without reopening the already-passing doctor and hook-policy work.
Output: auth-aware publish failure classification, sanitized publish failure output, normal-refresh degraded summary parity, and regression tests locked to the live UAT failures.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-VERIFICATION.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-HUMAN-UAT.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-01-SUMMARY.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-02-SUMMARY.md
@packages/core/src/publish/publish-service.ts
@packages/core/src/publish/publish-readiness.ts
@packages/core/src/publish/pre-push-policy.ts
@packages/core/src/publish/publish-trust.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/commands/refresh.ts

<interfaces>
From packages/core/src/publish/publish-service.ts:
```typescript
export class PublishBadgeError extends Error {
  readonly attemptedAt: string;
  readonly failureCode: AgentBadgePublishFailureCode;
  readonly candidateHash: string | null;
  readonly changedBadge: boolean | null;
}
```

From packages/core/src/publish/publish-readiness.ts:
```typescript
export function formatPublishReadinessStatus(
  status: PublishReadinessStatus
): string;

export function inspectPublishReadiness(
  options?: InspectPublishReadinessOptions
): PublishReadinessReport;
```

From packages/agent-badge/src/commands/refresh.ts:
```typescript
function printSoftFailure(
  stdout: OutputWriter,
  error: Error,
  state: AgentBadgeState | null,
  config: ReturnType<typeof parseAgentBadgeConfig> | null,
  hookPolicy?: AgentBadgeRefreshMode,
  failureStatus: "failed-soft" | "failed" = "failed-soft"
): void;
```

From packages/core/src/publish/pre-push-policy.ts:
```typescript
export function derivePrePushPolicyConsequence(
  report: PrePushPolicyReport
): {
  readonly level: "warning" | "blocking" | null;
  readonly message: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reclassify unauthenticated GitHub publish failures and sanitize publish output</name>
  <files>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts</files>
  <read_first>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/publish-readiness.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-HUMAN-UAT.md, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-01-SUMMARY.md</read_first>
  <behavior>
    - Test 1: a typed publish failure triggered by an unauthenticated GitHub write is classified as `auth-missing`, not `remote-write-failed`.
    - Test 2: `runPublishCommand()` prints `- Publish readiness: auth missing` before rethrowing.
    - Test 3: the surfaced publish output no longer includes raw `Requires authentication - https://docs.github.com/rest` text when canonical readiness output is available.
  </behavior>
  <action>Update `packages/core/src/publish/publish-service.ts` so GitHub auth failures encountered during gist reads, writes, or readback verification are mapped to `failureCode: "auth-missing"` when the upstream error clearly signals missing or invalid credentials. Preserve the original error as `cause`, but normalize the thrown `PublishBadgeError.message` to operator-safe wording instead of forwarding raw GitHub REST auth text. In `packages/agent-badge/src/commands/publish.ts`, keep the existing `- Publish readiness:` line, but ensure the typed publish-error branch renders the canonical `auth missing` wording and does not add raw GitHub REST auth text back into command output. Do not weaken or change the existing gist-unreachable, remote-write-failed, or readback-mismatch classifications from Plan 18-01; this task is only for the live missing-auth collapse reported in 18-HUMAN-UAT.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-service.ts` contains `auth-missing`
    - `packages/core/src/publish/publish-service.test.ts` contains `classifies unauthenticated GitHub write failures as auth-missing`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `renders Publish readiness: auth missing before rethrowing a typed auth failure`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `Requires authentication - https://docs.github.com/rest`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `not.toContain(\"Requires authentication - https://docs.github.com/rest\")`
    - `packages/agent-badge/src/commands/publish.ts` contains `- Publish readiness:`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts</automated>
  </verify>
  <done>Live unauthenticated publish failures surface the canonical `auth missing` readiness state and no longer echo raw GitHub REST auth text back to the operator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make normal refresh failure output print the same readiness and trust summary as hook mode</name>
  <files>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts</files>
  <read_first>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/core/src/publish/pre-push-policy.ts, packages/core/src/publish/publish-trust.ts, packages/core/src/publish/publish-readiness.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-HUMAN-UAT.md, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-02-SUMMARY.md</read_first>
  <behavior>
    - Test 1: a standard non-hook refresh that fails during publish still prints `- Publish readiness:` and `- Live badge trust:` before exiting non-zero.
    - Test 2: the standard refresh auth-failure output uses the same canonical wording as the existing hook-mode branch.
    - Test 3: hook-mode `fail-soft` and `strict` output remain unchanged while the normal refresh path catches up to the same degraded-state vocabulary.
  </behavior>
  <action>Refactor `packages/agent-badge/src/commands/refresh.ts` so the normal non-hook failure path prints the same degraded publish summary currently used by hook-mode refresh before the command rethrows. Reuse the existing readiness formatter and trust formatter so the output lines are exactly prefixed `- Publish readiness:` and `- Live badge trust:` in both paths, and keep the current `Pre-push policy`, `Warning`, and `Blocking` behavior unchanged for hook-mode runs per the passing 18-HUMAN-UAT results. When the underlying publish error was the live auth failure from Task 1, ensure the normal refresh output also avoids echoing raw `Requires authentication - https://docs.github.com/rest` text. Do not convert normal refresh into fail-soft mode; it should still exit non-zero after printing the canonical degraded summary.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `renders Publish readiness and Live badge trust in standard refresh failure output`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `renders Publish readiness: auth missing in standard refresh failure output`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `not.toContain(\"Requires authentication - https://docs.github.com/rest\")`
    - `packages/agent-badge/src/commands/refresh.ts` contains `- Publish readiness:`
    - `packages/agent-badge/src/commands/refresh.ts` contains `- Live badge trust:`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Warning: live badge may be stale; push continues because pre-push policy is fail-soft.`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Blocking: push stopped because pre-push policy is strict.`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <done>Normal refresh failures now print the same canonical readiness and trust summary as hook-mode refresh, while hook fail-soft and strict behavior remain unchanged.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts` succeeds
- `npm test -- --run packages/agent-badge/src/commands/refresh.test.ts` succeeds
- `npm test -- --run packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds after the full plan
</verification>

<success_criteria>
- Live unauthenticated publish failures classify as `auth-missing` and surface canonical readiness wording instead of `remote write failed`.
- Normal non-hook refresh prints `Publish readiness` and `Live badge trust` lines even when publish fails and the command exits non-zero.
- Hook-mode fail-soft versus strict behavior stays intact while normal refresh output catches up to the same degraded-state vocabulary.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-04-SUMMARY.md`
</output>

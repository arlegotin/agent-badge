---
phase: 18-auth-hook-and-publish-readiness-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - packages/core/src/publish/shared-badge-aggregation.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
gap_closure: true
must_haves:
  truths:
    - "Running `npm test -- --run` passes cleanly again so Phase 18 manual UAT can resume."
    - "`packages/core/src/publish/shared-badge-aggregation.test.ts` proves the badge payload write without assuming `agent-badge.json` is the final gist update."
    - "The shared publish regression still locks the expected token totals for merged contributors and duplicate-session dedupe under the current badge -> contributor -> overrides write sequence."
  artifacts:
    - path: packages/core/src/publish/shared-badge-aggregation.test.ts
      provides: sequence-aware shared aggregation assertions aligned with the current publish-service write order
      contains: updateGistFile.mock.calls
  key_links:
    - from: packages/core/src/publish/shared-badge-aggregation.test.ts
      to: packages/core/src/publish/publish-service.ts
      via: "the regression assertions match the current publish path where the badge payload is written before the contributor and overrides snapshots"
      pattern: AGENT_BADGE_OVERRIDES_GIST_FILE
---

<objective>
Close the automated Phase 18 validation blocker caused by a stale shared-aggregation test expectation.

Purpose: Phase 18 implementation is already complete, but manual UAT cannot continue until the full test suite is green. The failing regression still expects the badge payload write to be the last `updateGistFile()` call even though `publish-service.ts` now performs later contributor and overrides writes.
Output: updated shared aggregation assertions plus a green full-suite rerun that reopens the Phase 18 UAT gate.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-UAT.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-01-SUMMARY.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-02-SUMMARY.md
@packages/core/src/publish/shared-badge-aggregation.test.ts
@packages/core/src/publish/publish-service.ts

<interfaces>
From packages/core/src/publish/publish-service.ts:
```typescript
await client.updateGistFile({
  gistId: config.publish.gistId,
  files: serializedFiles
});
await client.updateGistFile({
  gistId: config.publish.gistId,
  files: {
    [localContributorFileName]: {
      content: serializeJsonFile(contributorRecord)
    }
  }
});
await client.updateGistFile({
  gistId: config.publish.gistId,
  files: {
    [AGENT_BADGE_OVERRIDES_GIST_FILE]: {
      content: serializeJsonFile(authoritativeOverrides)
    }
  }
});
```

From packages/core/src/publish/shared-badge-aggregation.test.ts:
```typescript
expect(updateGistFile).toHaveBeenLastCalledWith({
  gistId: "gist_123",
  files: {
    [AGENT_BADGE_GIST_FILE]: {
      content: `...`
    }
  }
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace stale last-call badge assertions with sequence-aware shared publish checks</name>
  <files>packages/core/src/publish/shared-badge-aggregation.test.ts</files>
  <behavior>
    - Test 1: the merged-remote-totals case still proves one `updateGistFile()` call writes `agent-badge.json` with `50 tokens` even though later calls write the local contributor and overrides files.
    - Test 2: the partial-local-snapshot case still proves the badge payload write contains `100 tokens` without requiring that badge call to be last.
    - Test 3: the duplicate-session case still proves the badge payload write contains `30 tokens` and does not regress dedupe coverage when overrides are written afterward.
  </behavior>
  <action>Update the three stale expectations called out in `18-UAT.md`. Replace `toHaveBeenLastCalledWith(...)` with assertions that match the current publish-service behavior: inspect `updateGistFile.mock.calls` and verify the badge payload write by exact call content and/or exact call index in the publish path instead of assuming it is the final write. Keep the assertions strong enough to catch regressions in both payload content and write sequencing; do not weaken them to a generic `toHaveBeenCalled()` or substring-only check. Update all three failing sites in this file, and keep the contributor and overrides writes visible in the test so the sequence stays documented.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/shared-badge-aggregation.test.ts` no longer contains `toHaveBeenLastCalledWith({` for the badge payload expectations
    - the file contains a sequence-aware assertion against `updateGistFile.mock.calls`
    - the file still asserts badge payload messages `50 tokens`, `100 tokens`, and `30 tokens`
    - the file still references `AGENT_BADGE_OVERRIDES_GIST_FILE` so the later overrides write remains part of the regression contract
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>The full Vitest suite passes again, and the shared aggregation regression matches the current badge -> contributor -> overrides write order.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` succeeds
- the failure in `packages/core/src/publish/shared-badge-aggregation.test.ts` no longer depends on `agent-badge.json` being the final gist write
</verification>

<success_criteria>
- Phase 18's automated validation gate is green again.
- Manual Phase 18 UAT can resume without a stale test expectation blocking it.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-03-SUMMARY.md`
</output>

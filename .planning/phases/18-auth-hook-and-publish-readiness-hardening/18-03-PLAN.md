---
phase: 18-auth-hook-and-publish-readiness-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - packages/core/src/runtime/local-cli.ts
  - packages/core/src/runtime/local-cli.test.ts
  - packages/core/src/init/runtime-wiring.ts
  - packages/core/src/init/runtime-wiring.test.ts
  - packages/agent-badge/src/cli/main.ts
  - packages/agent-badge/src/cli/main.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
  - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
autonomous: true
requirements:
  - OPER-03
  - CTRL-01
must_haves:
  truths:
    - "The managed pre-push command encodes the selected automation policy explicitly as `fail-soft` or `strict` instead of relying on shell inference."
    - "Hook-mode refresh prints the active pre-push policy plus an explicit warning or blocking consequence when degraded publish health means the live badge may be stale."
    - "Release-readiness scenarios lock the no-auth, fail-soft, and strict hook behaviors so a degraded pre-push run cannot be mistaken for a successful badge update."
  artifacts:
    - path: packages/core/src/runtime/local-cli.ts
      provides: canonical explicit pre-push command strings for both supported policies
      contains: --hook-policy
    - path: packages/core/src/init/runtime-wiring.ts
      provides: managed hook wiring that writes the explicit policy command idempotently
      contains: agent-badge refresh --hook pre-push --hook-policy
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: loud hook-mode degraded output with explicit warning or blocking consequences
      contains: Pre-push policy:
    - path: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      provides: scenario coverage for no-auth, fail-soft, and strict automation behavior
      contains: fail-soft
  key_links:
    - from: packages/core/src/runtime/local-cli.ts
      to: packages/core/src/init/runtime-wiring.ts
      via: "managed hooks write exactly the explicit pre-push command emitted by the local CLI helper"
      pattern: --hook-policy
    - from: packages/agent-badge/src/cli/main.ts
      to: packages/agent-badge/src/commands/refresh.ts
      via: "CLI parsing forwards the explicit hook policy into hook-mode refresh execution"
      pattern: hookPolicy
    - from: packages/agent-badge/src/commands/refresh.ts
      to: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      via: "matrix scenarios lock the warning and blocking semantics that make degraded hook behavior unmistakable"
      pattern: live badge may be stale
---

<objective>
Make pre-push automation policy explicit in the managed runtime and make degraded hook-mode behavior impossible to mistake for a healthy badge update.

Purpose: Phase 18 needs a distinct automation slice after the readiness work is in place. This plan handles only pre-push policy plumbing and hook-mode degraded UX, leaving core readiness to Plans 18-01 and 18-02.
Output: explicit `--hook-policy` plumbing, idempotent managed hook wiring, and loud hook-mode degraded output with release-readiness scenario coverage.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-01-PLAN.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-02-PLAN.md
@packages/core/src/runtime/local-cli.ts
@packages/core/src/init/runtime-wiring.ts
@packages/agent-badge/src/cli/main.ts
@packages/agent-badge/src/commands/refresh.ts
@packages/agent-badge/src/commands/release-readiness-matrix.test.ts

<interfaces>
From packages/core/src/runtime/local-cli.ts:
```typescript
export function getAgentBadgeRefreshScriptCommand(
  mode: AgentBadgeRefreshMode = "fail-soft"
): string;
```

From packages/agent-badge/src/commands/refresh.ts:
```typescript
export interface RunRefreshCommandOptions {
  readonly hook?: "pre-push";
  readonly failSoft?: boolean;
  readonly forceFull?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the selected pre-push policy explicit in CLI parsing and managed hook wiring</name>
  <files>packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/local-cli.test.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts</files>
  <read_first>packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/local-cli.test.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md</read_first>
  <action>Add a canonical refresh CLI option `--hook-policy <mode>` that accepts exactly `fail-soft` or `strict`; keep `--fail-soft` as a backward-compatible alias that maps to `hookPolicy: "fail-soft"` but stop relying on the absence of a flag to mean strict mode. Update `packages/core/src/runtime/local-cli.ts` so the managed command strings are exactly `agent-badge refresh --hook pre-push --hook-policy fail-soft` and `agent-badge refresh --hook pre-push --hook-policy strict`. Update `packages/core/src/init/runtime-wiring.ts` and its tests so managed hook blocks write those exact commands and remain idempotent.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/cli/main.ts` contains `--hook-policy <mode>`
    - `packages/core/src/runtime/local-cli.ts` contains `agent-badge refresh --hook pre-push --hook-policy fail-soft`
    - `packages/core/src/runtime/local-cli.ts` contains `agent-badge refresh --hook pre-push --hook-policy strict`
    - `packages/core/src/init/runtime-wiring.test.ts` contains `agent-badge refresh --hook pre-push --hook-policy fail-soft`
    - `packages/core/src/init/runtime-wiring.test.ts` contains `agent-badge refresh --hook pre-push --hook-policy strict`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/cli/main.test.ts</automated>
  </verify>
  <done>The selected automation policy is explicit in the CLI contract and the managed pre-push hook command string.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make degraded hook-mode refresh loud and lock the semantics in scenario tests</name>
  <files>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/release-readiness-matrix.test.ts</files>
  <read_first>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/release-readiness-matrix.test.ts, packages/core/src/publish/publish-readiness.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md</read_first>
  <action>Update hook-mode `agent-badge refresh` output so the policy and degraded state are unmistakable. In concise pre-push mode, always print `- Pre-push policy: fail-soft` or `- Pre-push policy: strict` before the trust lines. When readiness or publish trust is degraded and policy is fail-soft, print a line exactly prefixed `- Warning:` with the concrete message `live badge may be stale; push continues because pre-push policy is fail-soft.` When policy is strict and the same degraded state occurs, print a line exactly prefixed `- Blocking:` with the concrete message `push stopped because pre-push policy is strict.` Keep the existing `Publish readiness:` and `Live badge trust:` lines visible in hook mode so the operator sees both the cause and the consequence. Extend `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` with degraded scenarios that prove no-auth repos still wire a fail-soft hook explicitly, fail-soft output includes the warning line and does not look like success, and strict policy scenarios block on degraded publish state with the explicit blocking message.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/refresh.ts` contains `- Pre-push policy:`
    - `packages/agent-badge/src/commands/refresh.ts` contains `live badge may be stale; push continues because pre-push policy is fail-soft.`
    - `packages/agent-badge/src/commands/refresh.ts` contains `push stopped because pre-push policy is strict.`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Warning: live badge may be stale; push continues because pre-push policy is fail-soft.`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Blocking: push stopped because pre-push policy is strict.`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `no-auth`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `fail-soft`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `strict`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/release-readiness-matrix.test.ts</automated>
  </verify>
  <done>Hook-mode refresh makes degraded publish health unmistakable, and regression scenarios lock the explicit fail-soft versus strict semantics.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/cli/main.test.ts` succeeds
- `npm test -- --run packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/release-readiness-matrix.test.ts` succeeds
- `npm test -- --run packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/cli/main.test.ts packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/release-readiness-matrix.test.ts` succeeds after the full plan
</verification>

<success_criteria>
- Managed pre-push automation encodes `fail-soft` or `strict` explicitly instead of relying on hidden shell behavior.
- Hook-mode refresh warns loudly when degraded publish health still allows the push, and blocks with an explicit message when policy is strict.
- Scenario coverage proves degraded pre-push runs cannot be mistaken for successful badge updates.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-03-SUMMARY.md`
</output>

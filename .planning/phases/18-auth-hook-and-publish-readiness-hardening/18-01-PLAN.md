---
phase: 18-auth-hook-and-publish-readiness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/state/state-schema.ts
  - packages/core/src/state/state-schema.test.ts
  - packages/core/src/init/github-auth.ts
  - packages/core/src/init/scaffold.ts
  - packages/core/src/init/scaffold.test.ts
  - packages/core/src/publish/publish-readiness.ts
  - packages/core/src/publish/publish-readiness.test.ts
  - packages/core/src/publish/publish-target.ts
  - packages/core/src/publish/publish-state.ts
  - packages/core/src/publish/publish-state.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "Publish attempts persist machine-readable failure codes that distinguish missing auth, broken gist reachability or ownership, write failure, and readback divergence instead of collapsing them into one generic state."
    - "The core readiness report produces environment-specific remediation for auth and gist-target problems without persisting raw GitHub responses, stack traces, or local evidence."
    - "Init reruns and persisted publish state stay migration-safe while the expanded readiness vocabulary lands."
  artifacts:
    - path: packages/core/src/publish/publish-readiness.ts
      provides: canonical readiness inspection, failure-code mapping, and remediation text
      contains: inspectPublishReadiness
    - path: packages/core/src/state/state-schema.ts
      provides: additive persisted failure-code vocabulary that remains migration-safe
      contains: auth-missing
    - path: packages/core/src/init/scaffold.ts
      provides: init rerun compatibility for the expanded publish-state schema
      contains: lastFailureCode
    - path: packages/core/src/publish/publish-target.ts
      provides: mapping from auth and gist-target setup failures into the canonical readiness vocabulary
      contains: gist-unreachable
  key_links:
    - from: packages/core/src/publish/publish-target.ts
      to: packages/core/src/publish/publish-readiness.ts
      via: "target setup reasons map into the canonical readiness failure vocabulary instead of command-local aliases"
      pattern: gist-unreachable
    - from: packages/core/src/state/state-schema.ts
      to: packages/core/src/init/scaffold.ts
      via: "expanded failure-code vocabulary remains parseable and preserved across init reruns"
      pattern: auth-missing
    - from: packages/core/src/publish/publish-readiness.ts
      to: packages/core/src/publish/publish-state.ts
      via: "canonical readiness classifications are the same machine-readable values persisted in publish state"
      pattern: auth-missing
---

<objective>
Create the core publish-readiness contract and failure-classification machinery before command-level UX builds on it.

Purpose: Phase 18 needs one durable readiness vocabulary that explains why a badge is stale or at risk. This plan keeps scope in the foundational core runtime: expanded failure codes, migration-safe persisted state, and canonical readiness inspection that later plans can wire into command surfaces and publish execution.
Output: one canonical readiness module, additive publish failure codes, and migration-safe schema/scaffold updates.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md
@packages/core/src/state/state-schema.ts
@packages/core/src/init/github-auth.ts
@packages/core/src/init/scaffold.ts
@packages/core/src/publish/publish-target.ts
@packages/core/src/publish/publish-state.ts

<interfaces>
From packages/core/src/state/state-schema.ts:
```typescript
const publishFailureCodeSchema = z.enum([
  "not-configured",
  "deferred",
  "remote-write-failed",
  "remote-inspection-failed",
  "unknown"
]);
```

From packages/core/src/init/github-auth.ts:
```typescript
export interface GitHubAuthStatus {
  readonly available: boolean;
  readonly source: GitHubAuthSource;
}
```

From packages/core/src/publish/publish-target.ts:
```typescript
export interface PublishTargetResult {
  readonly status: PublishTargetStatus;
  readonly gistId: string | null;
  readonly badgeUrl: string | null;
  readonly reason?:
    | "auth-unavailable"
    | "gist-not-public"
    | "gist-missing-owner"
    | "gist-unreachable"
    | "gist-create-failed";
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the canonical readiness contract and map auth/gist-target problems into it</name>
  <files>packages/core/src/init/github-auth.ts, packages/core/src/publish/publish-readiness.ts, packages/core/src/publish/publish-readiness.test.ts, packages/core/src/publish/publish-target.ts</files>
  <read_first>packages/core/src/init/github-auth.ts, packages/core/src/publish/publish-target.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md</read_first>
  <action>Create `packages/core/src/publish/publish-readiness.ts` plus tests and move the canonical readiness and remediation vocabulary there. Export `inspectPublishReadiness()` and helpers that classify exactly these machine-readable failure codes: `auth-missing`, `gist-unreachable`, `gist-not-public`, `gist-missing-owner`, `remote-write-failed`, `remote-readback-failed`, `remote-readback-mismatch`, `remote-state-invalid`, `not-configured`, `deferred`, and `unknown`. Update `packages/core/src/init/github-auth.ts` so auth-source detail is available to the readiness contract, and update `packages/core/src/publish/publish-target.ts` so `auth-unavailable` and gist-target problems map into the new canonical codes instead of command-local wording.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-readiness.ts` contains `export function inspectPublishReadiness`
    - `packages/core/src/publish/publish-readiness.ts` contains `auth-missing`
    - `packages/core/src/publish/publish-readiness.ts` contains `gist-unreachable`
    - `packages/core/src/publish/publish-target.ts` contains `auth-unavailable`
    - `packages/core/src/init/github-auth.ts` contains `GitHubAuthSource`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/publish-readiness.test.ts packages/core/src/publish/publish-target.test.ts</automated>
  </verify>
  <done>The repo has one canonical readiness contract that explains auth and gist-target problems without leaking private detail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make persisted publish state and init reruns migration-safe for the expanded readiness vocabulary</name>
  <files>packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/scaffold.test.ts, packages/core/src/publish/publish-state.ts, packages/core/src/publish/publish-state.test.ts</files>
  <read_first>packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/scaffold.test.ts, packages/core/src/publish/publish-state.ts, packages/core/src/publish/publish-state.test.ts, packages/core/src/publish/publish-readiness.ts, .planning/phases/18-auth-hook-and-publish-readiness-hardening/18-RESEARCH.md</read_first>
  <action>Update `packages/core/src/state/state-schema.ts`, `packages/core/src/publish/publish-state.ts`, and `packages/core/src/init/scaffold.ts` so the expanded readiness vocabulary persists cleanly and survives init reruns. Preserve the aggregate-only boundary by persisting only the machine-readable failure codes plus existing attempt facts; do not store raw error messages, HTTP bodies, stack traces, local paths, or provider session ids. Ensure old state still parses while the new codes and canonical readiness classifications survive migration-safe scaffold rewrites.</action>
  <acceptance_criteria>
    - `packages/core/src/state/state-schema.ts` contains `auth-missing`
    - `packages/core/src/state/state-schema.ts` contains `remote-readback-mismatch`
    - `packages/core/src/state/state-schema.ts` contains `remote-state-invalid`
    - `packages/core/src/publish/publish-state.test.ts` contains `persists auth-missing without raw error detail`
    - `packages/core/src/init/scaffold.test.ts` contains `preserves expanded publish failure codes on init reruns`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/state/state-schema.test.ts packages/core/src/init/scaffold.test.ts packages/core/src/publish/publish-state.test.ts</automated>
  </verify>
  <done>The expanded readiness vocabulary is persisted safely and survives init reruns without leaking raw failure detail.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/publish-readiness.test.ts packages/core/src/publish/publish-target.test.ts` succeeds
- `npm test -- --run packages/core/src/state/state-schema.test.ts packages/core/src/init/scaffold.test.ts packages/core/src/publish/publish-state.test.ts` succeeds
- Later plans consume this readiness vocabulary and expanded persisted state without reintroducing command-local failure classifications
</verification>

<success_criteria>
- The core runtime persists precise publish failure codes for missing auth, broken gist target state, write failure, and readback divergence.
- Readiness classification and persisted diagnostics remain privacy-safe and migration-safe across init reruns.
- Later plans can build command surfaces and publish verification on one canonical readiness contract instead of re-deriving failure causes.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-01-SUMMARY.md`
</output>

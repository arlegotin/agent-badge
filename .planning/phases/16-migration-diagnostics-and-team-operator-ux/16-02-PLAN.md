---
phase: 16-migration-diagnostics-and-team-operator-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - packages/core/src/diagnostics/doctor.ts
  - packages/core/src/diagnostics/doctor.test.ts
  - packages/agent-badge/src/commands/status.ts
  - packages/agent-badge/src/commands/status.test.ts
  - packages/agent-badge/src/commands/doctor.ts
  - packages/agent-badge/src/commands/doctor.test.ts
  - README.md
  - docs/HOW-IT-WORKS.md
  - docs/PRIVACY.md
  - docs/TROUBLESHOOTING.md
  - docs/MANUAL-GIST.md
  - scripts/verify-docs.sh
autonomous: true
requirements:
  - MIGR-02
  - MIGR-03
user_setup: []
must_haves:
  truths:
    - "Operators can see whether a repo is legacy, healthy shared, stale, conflicting, partially migrated, or orphaned from normal CLI surfaces without reading raw gist files."
    - "Doctor remediation and status summaries use the same shared-health vocabulary, so migration and recovery guidance do not drift between commands."
    - "Docs explain the actual shared observation model and the migration limit that continuity still depends on local machine data at transition time."
  artifacts:
    - path: packages/core/src/diagnostics/doctor.ts
      provides: shared-state diagnostics checks and remediation guidance
      contains: shared-health
    - path: packages/agent-badge/src/commands/status.ts
      provides: operator-facing shared mode and health summary
      contains: Shared mode:
    - path: README.md
      provides: migration and team-publish overview for repo operators
      contains: migrate existing single-writer repos
    - path: docs/HOW-IT-WORKS.md
      provides: corrected description of shared contributor observation files
      contains: per-session observations
    - path: docs/TROUBLESHOOTING.md
      provides: stale/conflict/orphaned/partial-migration recovery steps
      contains: orphaned local publisher
  key_links:
    - from: packages/core/src/publish/shared-health.ts
      to: packages/core/src/diagnostics/doctor.ts
      via: "doctor turns the canonical shared-health report into actionable checks"
      pattern: inspectSharedPublishHealth
    - from: packages/core/src/publish/shared-health.ts
      to: packages/agent-badge/src/commands/status.ts
      via: "status prints the same mode and health classification used by doctor"
      pattern: inspectSharedPublishHealth
    - from: scripts/verify-docs.sh
      to: docs/HOW-IT-WORKS.md
      via: "docs gate enforces the shared observation terminology"
      pattern: per-session observations
    - from: scripts/verify-docs.sh
      to: docs/TROUBLESHOOTING.md
      via: "docs gate enforces operator recovery guidance for shared-state failures"
      pattern: orphaned local publisher
---

<objective>
Finish the operator-facing Phase 16 experience for shared-mode health, recovery, and docs.

Purpose: Plan 01 introduces the migration and shared-health foundation. This plan turns that foundation into trustworthy operator UX by surfacing shared-health in `status` and `doctor`, then aligning the public docs and docs gate with the real shared observation model and migration limits.
Output: shared-health-aware CLI surfaces plus migration/recovery documentation that is locked by automated docs verification.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-migration-diagnostics-and-team-operator-ux/16-RESEARCH.md
@.planning/phases/16-migration-diagnostics-and-team-operator-ux/16-VALIDATION.md
@.planning/phases/16-migration-diagnostics-and-team-operator-ux/16-01-PLAN.md
@packages/core/src/publish/shared-health.ts
@packages/core/src/diagnostics/doctor.ts
@packages/agent-badge/src/commands/status.ts
@packages/agent-badge/src/commands/doctor.ts
@README.md
@docs/HOW-IT-WORKS.md
@docs/PRIVACY.md
@docs/TROUBLESHOOTING.md
@docs/MANUAL-GIST.md
@scripts/verify-docs.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface shared mode and shared-health clearly in status and doctor</name>
  <files>packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts, packages/agent-badge/src/commands/doctor.ts, packages/agent-badge/src/commands/doctor.test.ts</files>
  <read_first>packages/core/src/publish/shared-health.ts, packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts, packages/agent-badge/src/commands/doctor.ts, packages/agent-badge/src/commands/doctor.test.ts, .planning/phases/16-migration-diagnostics-and-team-operator-ux/16-RESEARCH.md</read_first>
  <action>Reuse `inspectSharedPublishHealth()` instead of adding ad-hoc logic in the command layer. In `packages/agent-badge/src/commands/status.ts`, extend the report with a new line exactly prefixed `- Shared mode:` that prints the canonical mode and health, for example `- Shared mode: shared | health=healthy | contributors=2`. When issues exist, add one privacy-safe line exactly prefixed `- Shared issues:` containing issue ids and counts, never raw digests or local paths. In `packages/core/src/diagnostics/doctor.ts`, add explicit shared-state checks named `shared-mode` and `shared-health` after the existing publish checks. `shared-mode` should explain whether the repo is still legacy-only or already shared; `shared-health` should warn/fail for stale, conflict, partial, or orphaned conditions and include concrete fix lines such as rerun `agent-badge init`, rerun `agent-badge refresh`, or migrate from the original publisher machine. Keep `packages/agent-badge/src/commands/doctor.ts` as a thin renderer over the richer core results. Add tests first so `status.test.ts` covers healthy shared and orphaned shared output, and `doctor.test.ts` / core doctor tests cover the new `shared-mode` and `shared-health` check ids plus actionable fix guidance.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/status.ts` contains `- Shared mode:`
    - `packages/agent-badge/src/commands/status.ts` contains `- Shared issues:`
    - `packages/core/src/diagnostics/doctor.ts` contains `id: "shared-mode"`
    - `packages/core/src/diagnostics/doctor.ts` contains `id: "shared-health"`
    - `packages/core/src/diagnostics/doctor.ts` contains `inspectSharedPublishHealth`
    - `packages/agent-badge/src/commands/status.test.ts` contains `Shared mode: shared | health=healthy | contributors=`
    - `packages/agent-badge/src/commands/status.test.ts` contains `Shared issues: missing-local-contributor`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `shared-mode`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `shared-health`
    - `packages/agent-badge/src/commands/doctor.test.ts` contains `shared-health`
    - `packages/agent-badge/src/commands/doctor.test.ts` contains `migrate from the original publisher machine`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/status.test.ts packages/agent-badge/src/commands/doctor.test.ts</automated>
  </verify>
  <done>Operators can inspect shared-mode health and remediation paths directly from the normal CLI without opening gist JSON manually.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update docs and docs checks for migration, shared observations, and recovery guidance</name>
  <files>README.md, docs/HOW-IT-WORKS.md, docs/PRIVACY.md, docs/TROUBLESHOOTING.md, docs/MANUAL-GIST.md, scripts/verify-docs.sh</files>
  <read_first>README.md, docs/HOW-IT-WORKS.md, docs/PRIVACY.md, docs/TROUBLESHOOTING.md, docs/MANUAL-GIST.md, scripts/verify-docs.sh, .planning/phases/16-migration-diagnostics-and-team-operator-ux/16-RESEARCH.md</read_first>
  <action>Bring the docs up to the real Phase 15/16 model and lock them with `scripts/verify-docs.sh`. In `README.md`, add a short migration note that existing single-writer repos should rerun `agent-badge init` on the original publisher machine before normal team publishing. In `docs/HOW-IT-WORKS.md`, replace the outdated contributor-file description with exact wording that shared contributor files store aggregate-only per-session observations keyed by opaque digests, not per-publisher total snapshots. In `docs/PRIVACY.md`, clarify that diagnostics stay aggregate-only and do not expose raw session ids even when reporting conflicts or orphaned publishers. In `docs/TROUBLESHOOTING.md`, add dedicated sections for `shared mode is stale`, `shared mode reports conflicts`, and `orphaned local publisher`. In `docs/MANUAL-GIST.md`, add the rerun-`init` migration path and post-migration verification steps using `agent-badge status` and `agent-badge doctor`. Extend `scripts/verify-docs.sh` with exact required strings for `per-session observations`, `migrate existing single-writer repos`, `original publisher machine`, and `orphaned local publisher`, then keep the existing doc gate passing.</action>
  <acceptance_criteria>
    - `README.md` contains `migrate existing single-writer repos`
    - `README.md` contains `original publisher machine`
    - `docs/HOW-IT-WORKS.md` contains `per-session observations`
    - `docs/HOW-IT-WORKS.md` contains `opaque digests`
    - `docs/PRIVACY.md` contains `Diagnostics stay aggregate-only`
    - `docs/TROUBLESHOOTING.md` contains `shared mode is stale`
    - `docs/TROUBLESHOOTING.md` contains `shared mode reports conflicts`
    - `docs/TROUBLESHOOTING.md` contains `orphaned local publisher`
    - `docs/MANUAL-GIST.md` contains `agent-badge status`
    - `docs/MANUAL-GIST.md` contains `agent-badge doctor`
    - `scripts/verify-docs.sh` contains `per-session observations`
    - `scripts/verify-docs.sh` contains `migrate existing single-writer repos`
    - `scripts/verify-docs.sh` contains `original publisher machine`
    - `scripts/verify-docs.sh` contains `orphaned local publisher`
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check</automated>
  </verify>
  <done>The operator docs match the actual shared model, migration path, and recovery workflow, and the docs gate prevents drift.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/status.test.ts packages/agent-badge/src/commands/doctor.test.ts` succeeds
- `npm run docs:check` succeeds
- `npm test -- --run` succeeds after both plans complete
</verification>

<success_criteria>
- `status` and `doctor` expose one coherent shared-health model to operators.
- Docs explain the real shared observation model and migration limits instead of the old totals-only wording.
- Shared-mode remediation guidance is test-locked rather than dependent on memory.
</success_criteria>

<output>
After completion, create `.planning/phases/16-migration-diagnostics-and-team-operator-ux/16-02-SUMMARY.md`
</output>

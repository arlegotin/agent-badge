---
phase: 16-migration-diagnostics-and-team-operator-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/publish/shared-health.ts
  - packages/core/src/publish/shared-health.test.ts
  - packages/core/src/publish/publish-service.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/core/src/publish/index.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02
user_setup: []
must_haves:
  truths:
    - "Legacy repos migrate to the shared contributor model on the same gist id and stable badge files instead of rotating the README badge URL."
    - "The runtime can classify remote/local publish state as legacy, healthy shared, stale shared, conflicting shared, partially migrated, or orphaned-local-publisher from one shared-health report."
    - "Init, publish, and refresh all use the same shared-health and migration contract so the first shared transition is explicit rather than command-specific behavior."
  artifacts:
    - path: packages/core/src/publish/shared-health.ts
      provides: canonical shared-health inspection and migration classification helpers
      contains: inspectSharedPublishHealth
    - path: packages/core/src/publish/publish-service.ts
      provides: shared publish result enriched with migration and health context
      contains: migrationPerformed
    - path: packages/agent-badge/src/commands/init.ts
      provides: init-time migration through the existing gist target
      contains: Migration:
    - path: packages/agent-badge/src/commands/publish.ts
      provides: publish command migration summary and shared-health-aware output
      contains: Migration:
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: refresh command migration summary and shared-health-aware publish result wiring
      contains: Migration:
  key_links:
    - from: packages/core/src/publish/shared-health.ts
      to: packages/core/src/publish/publish-service.ts
      via: "publish-service inspects remote/local shared state before and after the shared write"
      pattern: inspectSharedPublishHealth
    - from: packages/core/src/publish/publish-service.ts
      to: packages/agent-badge/src/commands/init.ts
      via: "init reuses the publish-service migration result instead of inventing its own migration detection"
      pattern: migrationPerformed
    - from: packages/core/src/publish/publish-service.ts
      to: packages/agent-badge/src/commands/publish.ts
      via: "publish surfaces the same migration and health result returned by the shared service"
      pattern: migrationPerformed
    - from: packages/core/src/publish/publish-service.ts
      to: packages/agent-badge/src/commands/refresh.ts
      via: "refresh uses the same shared transition contract as init and publish"
      pattern: migrationPerformed
---

<objective>
Implement the safe migration and shared-health foundation for Phase 16.

Purpose: Phase 15 made shared publishing semantically correct, but there is still no explicit upgrade path for legacy repos and no canonical shared-health report. This plan adds the core inspection layer and wires the existing write commands onto it so migration happens on the existing gist target with one explicit contract.
Output: shared-health inspection helpers plus init/publish/refresh migration wiring that preserves stable badge URLs and exposes whether a legacy-to-shared transition just occurred.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-migration-diagnostics-and-team-operator-ux/16-RESEARCH.md
@.planning/phases/16-migration-diagnostics-and-team-operator-ux/16-VALIDATION.md
@packages/core/src/publish/shared-model.ts
@packages/core/src/publish/shared-merge.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/diagnostics/doctor.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/commands/refresh.ts

<interfaces>
From packages/core/src/publish/publish-service.ts:
```typescript
export interface PublishBadgeIfChangedResult {
  readonly state: AgentBadgeState;
  readonly decision: "published" | "skipped";
}
```

From packages/core/src/state/state-schema.ts:
```typescript
publish: z.object({
  status: publishStatusSchema,
  gistId: z.string().min(1).nullable(),
  lastPublishedHash: z.string().min(1).nullable(),
  lastPublishedAt: isoDateTimeSchema.nullable(),
  publisherId: z.string().min(1).nullable().optional().default(null),
  mode: publishModeSchema.optional().default("legacy")
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a canonical shared-health inspection layer for legacy, stale, conflicting, partial, and orphaned publish states</name>
  <files>packages/core/src/publish/shared-health.ts, packages/core/src/publish/shared-health.test.ts, packages/core/src/publish/index.ts, packages/core/src/publish/publish-service.test.ts</files>
  <read_first>packages/core/src/publish/shared-model.ts, packages/core/src/publish/shared-merge.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, .planning/phases/16-migration-diagnostics-and-team-operator-ux/16-RESEARCH.md, .planning/phases/16-migration-diagnostics-and-team-operator-ux/16-VALIDATION.md</read_first>
  <action>Create `packages/core/src/publish/shared-health.ts` and export it from `packages/core/src/publish/index.ts`. Define exact machine-readable types instead of freeform strings: `SharedPublishHealthMode = "legacy" | "shared"`, `SharedPublishHealthStatus = "healthy" | "stale" | "conflict" | "partial" | "orphaned"`, and `SharedPublishIssueId = "legacy-no-contributors" | "missing-shared-overrides" | "missing-local-contributor" | "stale-contributor" | "conflicting-session-observations"`. Implement `inspectSharedPublishHealth({ gist, state, now })` so it reuses the existing shared contributor parsing rules, counts remote contributor files, detects whether the local `publisherId` is missing remotely, detects whether duplicate session digests disagree on `attributionStatus` or `overrideDecision`, and marks a contributor as stale when its `updatedAt` is more than exactly 7 days older than `now`. Return one structured report with `mode`, `status`, `remoteContributorCount`, `hasSharedOverrides`, `conflictingSessionCount`, `stalePublisherIds`, `orphanedLocalPublisher`, and `issues`. Add tests first in `shared-health.test.ts` for: legacy gist with only badge payloads, healthy shared gist with two contributor files, orphaned local publisher, stale contributor older than 7 days, and conflicting observations on one shared digest. Update `publish-service.test.ts` helpers if needed so the new module uses the same contributor fixtures already established there.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/shared-health.ts` contains `export type SharedPublishHealthMode = "legacy" | "shared"`
    - `packages/core/src/publish/shared-health.ts` contains `export type SharedPublishHealthStatus = "healthy" | "stale" | "conflict" | "partial" | "orphaned"`
    - `packages/core/src/publish/shared-health.ts` contains `legacy-no-contributors`
    - `packages/core/src/publish/shared-health.ts` contains `missing-local-contributor`
    - `packages/core/src/publish/shared-health.ts` contains `conflicting-session-observations`
    - `packages/core/src/publish/shared-health.ts` contains `7 * 24 * 60 * 60 * 1000`
    - `packages/core/src/publish/shared-health.ts` contains `inspectSharedPublishHealth`
    - `packages/core/src/publish/shared-health.test.ts` contains `classifies a gist with badge payloads but no contributor files as legacy`
    - `packages/core/src/publish/shared-health.test.ts` contains `marks the local publisher as orphaned when its contributor file is missing`
    - `packages/core/src/publish/shared-health.test.ts` contains `marks contributors older than seven days as stale`
    - `packages/core/src/publish/shared-health.test.ts` contains `marks disagreeing shared observations as conflict`
    - `packages/core/src/publish/index.ts` contains `shared-health.js`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/shared-health.test.ts packages/core/src/publish/publish-service.test.ts</automated>
  </verify>
  <done>The repo has one canonical, test-locked shared-health classification layer that every operator surface can reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire init, publish, and refresh through the shared-health report and explicit legacy-to-shared migration result</name>
  <files>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts</files>
  <read_first>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/shared-health.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, .planning/phases/16-migration-diagnostics-and-team-operator-ux/16-RESEARCH.md</read_first>
  <action>Extend the publish-service result instead of hiding migration inside side effects. Add `healthBeforePublish`, `healthAfterPublish`, and `migrationPerformed: boolean` to the shared publish result returned by `publishBadgeIfChanged()` and `publishBadgeToGist()`. Use `inspectSharedPublishHealth()` on the existing gist before the write and on the authoritative shared gist after the contributor write + overrides derivation. Set `migrationPerformed` to `true` exactly when the pre-write report is `mode: "legacy"` and the post-write report is `mode: "shared"`. Keep the same configured gist id and badge files; do not rotate the gist or regenerate a new badge URL. In `init.ts`, `publish.ts`, and `refresh.ts`, print the same privacy-safe publish summary contract after a successful shared publish: `- Publish mode: shared` and `- Migration: legacy -> shared` when `migrationPerformed` is true, otherwise `- Migration: none`. Keep the current publish/refresh wording compatible and add the matching lines to init output instead of inventing a fourth format. Add regression tests first for: init migrates an existing legacy gist without changing the badge url, init prints `Publish mode: shared`, publish reports `Migration: legacy -> shared` on first shared write, refresh reports the same migration string when it performs the first shared publish, and reruns after migration report `Migration: none` instead of repeating the transition.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-service.ts` contains `healthBeforePublish`
    - `packages/core/src/publish/publish-service.ts` contains `healthAfterPublish`
    - `packages/core/src/publish/publish-service.ts` contains `migrationPerformed`
    - `packages/core/src/publish/publish-service.ts` contains `inspectSharedPublishHealth`
    - `packages/core/src/publish/publish-service.test.ts` contains `marks the first shared publish on a legacy gist as a migration`
    - `packages/core/src/publish/publish-service.test.ts` contains `does not rotate the gist id or badge payload file names during migration`
    - `packages/agent-badge/src/commands/init.ts` contains `Publish mode: shared`
    - `packages/agent-badge/src/commands/init.ts` contains `Migration: legacy -> shared`
    - `packages/agent-badge/src/commands/publish.ts` contains `Migration: legacy -> shared`
    - `packages/agent-badge/src/commands/refresh.ts` contains `Migration: legacy -> shared`
    - `packages/agent-badge/src/commands/init.test.ts` contains `migrates an existing legacy gist without changing the badge URL`
    - `packages/agent-badge/src/commands/init.test.ts` contains `Publish mode: shared`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `Migration: legacy -> shared`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Migration: legacy -> shared`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `Migration: none`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `Migration: none`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <done>Legacy repos now move onto the shared contributor model through the existing publish paths with one explicit, test-locked migration result instead of silent behavior.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/shared-health.test.ts packages/core/src/publish/publish-service.test.ts` succeeds
- `npm test -- --run packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds
</verification>

<success_criteria>
- The codebase has one canonical shared-health classifier for legacy/shared publish state.
- The first shared write on an existing gist is surfaced as an explicit migration without changing the stable badge URL.
- Init, publish, and refresh all expose the same migration transition semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/16-migration-diagnostics-and-team-operator-ux/16-01-SUMMARY.md`
</output>

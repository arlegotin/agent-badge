---
phase: 26-minimal-repo-scaffold-and-init-rewire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/create-agent-badge/src/index.ts
  - packages/create-agent-badge/src/index.test.ts
  - packages/core/src/init/runtime-wiring.ts
  - packages/core/src/init/runtime-wiring.test.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - ART-01
must_haves:
  truths:
    - "`npm init agent-badge@latest` no longer shells out to install `@legotin/agent-badge` into the target repo after `runInitCommand()` completes."
    - "Default init on a repo without a preexisting manifest does not create `package.json`, add `@legotin/agent-badge`, or create repo-local runtime scripts just to satisfy agent-badge."
    - "The default scaffold still owns `.agent-badge/*`, managed `.gitignore` entries, and the optional shared-runtime `pre-push` hook."
    - "Init continues to report the Phase 25 shared-runtime prerequisite instead of falling back silently to repo-local runtime ownership."
  artifacts:
    - path: packages/create-agent-badge/src/index.ts
      provides: "pure initializer entrypoint with no post-init package-manager install step"
      contains: "runInitCommand"
    - path: packages/core/src/init/runtime-wiring.ts
      provides: "minimal repo-scaffold reconciliation for hook/gitignore plus legacy managed package.json cleanup"
      contains: "command -v agent-badge"
    - path: packages/agent-badge/src/commands/init.ts
      provides: "init integration that uses the minimal scaffold contract instead of repo-local runtime ownership"
      contains: "writeRuntimeWiringSummary"
  key_links:
    - from: packages/create-agent-badge/src/index.ts
      to: packages/agent-badge/src/commands/init.ts
      via: runCreateAgentBadge -> runInitCommand
      pattern: "runInitCommand"
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/init/runtime-wiring.ts
      via: minimal scaffold reconciliation
      pattern: "command -v agent-badge|package.json#devDependencies.@legotin/agent-badge"
---

<objective>
Establish the Phase 26 default contract: the initializer and init command leave only repo-owned scaffold behind and never vendor the runtime into the target repository by default.

Purpose: satisfy the initializer-facing half of the milestone by removing repo-local runtime installation and manifest mutation from the default init path while preserving the shared-runtime contract from Phase 25.
Output: a pure initializer entrypoint, a minimal init-time scaffold reconciler, and focused regression coverage for manifest-free default setup.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/git/legotin/agent-badge/AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-global-runtime-contract-and-command-resolution/25-02-SUMMARY.md
@.planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-RESEARCH.md
@.planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-VALIDATION.md
@packages/create-agent-badge/src/index.ts
@packages/create-agent-badge/src/index.test.ts
@packages/core/src/init/scaffold.ts
@packages/core/src/init/runtime-wiring.ts
@packages/core/src/init/runtime-wiring.test.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/init.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove repo-local runtime installation from the npm initializer entrypoint</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-RESEARCH.md
    - packages/create-agent-badge/src/index.ts
    - packages/create-agent-badge/src/index.test.ts
    - packages/agent-badge/src/commands/init.ts
  </read_first>
  <files>packages/create-agent-badge/src/index.ts, packages/create-agent-badge/src/index.test.ts</files>
  <behavior>
    - Test 1: `runCreateAgentBadge()` delegates to `runInitCommand()` with the GitHub CLI token resolver and does not shell out to install `@legotin/agent-badge` after init completes.
    - Test 2: the initializer no longer depends on reading a target-repo `package.json` to discover a runtime dependency specifier after setup.
    - Test 3: direct-execution error handling remains unchanged for the published bin entry.
  </behavior>
  <action>Delete the repo-local runtime install path from `packages/create-agent-badge/src/index.ts`. Remove `resolveRepoLocalRuntimeSpecifier()`, `buildRuntimeInstallCommand()`, and `installRepoLocalRuntime()` plus any `spawnSync`, `readFile`, or `PackageManager` plumbing that exists only to support the post-init install step. Keep `runCreateAgentBadge()` as a thin delegator that calls `runInitCommand({ ...options, cwd: process.cwd(), ghCliTokenResolver: resolveGitHubCliToken })` and returns the result. Rewrite `packages/create-agent-badge/src/index.test.ts` so the core contract is "pure initializer entrypoint": no fixture package.json is required, no package-manager install call is expected, and the stale failure assertion about `Failed to install repo-local @legotin/agent-badge` is removed entirely. Preserve the shebang, symlinked direct-execution detection, and console-error exit-path coverage.</action>
  <acceptance_criteria>
    - `rg -n "installRepoLocalRuntime|buildRuntimeInstallCommand|resolveRepoLocalRuntimeSpecifier|Failed to install repo-local @legotin/agent-badge" packages/create-agent-badge/src/index.ts packages/create-agent-badge/src/index.test.ts` returns no matches.
    - `rg -n "runInitCommand\\(" packages/create-agent-badge/src/index.ts` returns at least one match.
    - `rg -n "spawnSync" packages/create-agent-badge/src/index.ts packages/create-agent-badge/src/index.test.ts` returns no matches.
    - `packages/create-agent-badge/src/index.test.ts` still contains the direct-execution error assertion for `process.exitCode`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/create-agent-badge/src/index.test.ts</automated>
  </verify>
  <done>The npm initializer is now a pure repo bootstrap entrypoint and no longer installs the runtime into the target repository after init.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace init-time repo-local runtime wiring with the minimal scaffold contract</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-RESEARCH.md
    - .planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-VALIDATION.md
    - packages/core/src/init/scaffold.ts
    - packages/core/src/init/runtime-wiring.ts
    - packages/core/src/init/runtime-wiring.test.ts
    - packages/agent-badge/src/commands/init.ts
    - packages/agent-badge/src/commands/init.test.ts
  </read_first>
  <files>packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts</files>
  <behavior>
    - Test 1: default init on a repo with no `package.json` leaves `package.json` absent after setup while still creating `.agent-badge/*`, the managed `.gitignore` block, and the direct shared-runtime `pre-push` hook when enabled.
    - Test 2: a repo that already has user-owned manifest content preserves that content while managed `@legotin/agent-badge`, `agent-badge:init`, and `agent-badge:refresh` entries are removed or left absent under the new default contract.
    - Test 3: the shared-runtime `pre-push` hook still contains the `command -v agent-badge >/dev/null 2>&1` guard and direct `agent-badge refresh --hook pre-push --hook-policy ...` command introduced in Phase 25.
    - Test 4: init output still reports shared-runtime availability/remediation, but the runtime wiring summary for a fresh repo no longer claims managed package.json dependency or script creation.
  </behavior>
  <action>Refactor `packages/core/src/init/runtime-wiring.ts` away from "repo-local runtime wiring" as the default init abstraction. Introduce a minimal scaffold reconciliation path that manages only repo-owned artifacts for the Phase 26 default model: keep `.git/hooks/pre-push` and `.gitignore` reconciliation, preserve the direct shared-runtime hook contract, and add targeted cleanup for the exact managed legacy manifest keys `devDependencies.@legotin/agent-badge`, `scripts[\"agent-badge:init\"]`, and `scripts[\"agent-badge:refresh\"]` when a manifest already exists. Never create `package.json` solely for agent-badge. If a legacy manifest becomes empty after removing managed agent-badge content, delete it only when no user-owned keys remain; otherwise preserve the file and its unrelated content. Update `runInitCommand()` to call this minimal reconciler instead of writing repo-local runtime ownership, and revise `packages/agent-badge/src/commands/init.test.ts` plus `packages/core/src/init/runtime-wiring.test.ts` to prove package-json-free fresh init, safe legacy cleanup, unchanged README badge behavior, and unchanged shared-runtime hook generation.</action>
  <acceptance_criteria>
    - `rg -n "command -v agent-badge >/dev/null 2>&1" packages/core/src/init/runtime-wiring.ts packages/core/src/init/runtime-wiring.test.ts` returns at least one match.
    - `packages/agent-badge/src/commands/init.test.ts` contains an assertion that `package.json` is absent after default init in a repo that started without a manifest.
    - `packages/core/src/init/runtime-wiring.test.ts` contains a legacy cleanup scenario that preserves unrelated manifest content while removing managed agent-badge entries.
    - `rg -n "package.json#devDependencies.@legotin/agent-badge|package.json#scripts.agent-badge:init|package.json#scripts.agent-badge:refresh" packages/agent-badge/src/commands/init.test.ts packages/core/src/init/runtime-wiring.test.ts` returns matches only in legacy-cleanup expectations, not in the fresh-init success path.
    - `packages/agent-badge/src/commands/init.test.ts` still asserts one managed README marker block and one managed hook block across reruns.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/commands/init.test.ts packages/create-agent-badge/src/index.test.ts</automated>
  </verify>
  <done>Default init is now package-json-free and runtime-install-free while still producing the repo-owned scaffold required for agent-badge to operate under the shared-runtime model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| initializer entrypoint -> target repo | `npm init agent-badge@latest` writes into arbitrary repositories and must not create extra runtime ownership artifacts. |
| legacy manifest content -> cleanup logic | Init reconciliation may remove stale managed keys from package.json and must preserve user-owned content. |
| shared runtime contract -> generated hook | The direct Phase 25 hook contract must survive the artifact-footprint cleanup unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | T | `packages/core/src/init/runtime-wiring.ts` | mitigate | Remove only exact managed agent-badge manifest keys during init reconciliation and preserve unrelated manifest content by default. |
| T-26-01-02 | D | `packages/create-agent-badge/src/index.ts`, `packages/agent-badge/src/commands/init.ts` | mitigate | Keep the shared-runtime remediation/reporting path intact so removing repo-local fallback does not leave operators with silent failures. |
| T-26-01-03 | T | `.git/hooks/pre-push` generation | mitigate | Preserve the direct shared hook contract and guard from Phase 25; Phase 26 must not regress to wrapper runners or repo-local binaries. |
</threat_model>

<verification>
Run the focused Phase 26 initializer and init-contract suite:

- `npm test -- --run packages/create-agent-badge/src/index.test.ts`
- `npm test -- --run packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/commands/init.test.ts packages/create-agent-badge/src/index.test.ts`
</verification>

<success_criteria>
- The npm initializer no longer installs `@legotin/agent-badge` into the target repo after init.
- Fresh default init does not create package.json or managed runtime dependency/script entries solely for agent-badge.
- The default scaffold still manages `.agent-badge/*`, `.gitignore`, and the direct shared-runtime hook contract.
- Init reruns can start converging legacy repos toward the minimal-artifact model without deleting user-owned manifest content.
</success_criteria>

<output>
After completion, create `.planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-minimal-repo-scaffold-and-init-rewire-01-SUMMARY.md`
</output>

---
phase: 26-minimal-repo-scaffold-and-init-rewire
plan: 02
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - packages/agent-badge/src/commands/config.ts
  - packages/agent-badge/src/commands/config.test.ts
  - packages/agent-badge/src/commands/init.test.ts
  - packages/core/src/init/runtime-wiring.test.ts
  - packages/agent-badge/src/commands/uninstall.test.ts
  - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - ART-01
  - ART-02
must_haves:
  truths:
    - "`config set refresh.prePush.*` rewrites only repo-owned hook/scaffold state and never recreates repo-local runtime dependency or script ownership after Phase 26."
    - "Rerunning init preserves one README badge block, one managed gitignore block, one managed hook block, and zero default repo-local runtime package entries."
    - "Legacy repos converge to the minimal-artifact model through normal init/config flows while uninstall compatibility for old managed hook shapes remains intact."
    - "Repo-owned proof surfaces stop asserting repo-local runtime manifest ownership as the default Phase 26 contract."
  artifacts:
    - path: packages/agent-badge/src/commands/config.ts
      provides: "refresh-policy rewrites on top of the minimal scaffold contract"
      contains: "refresh.prePush.mode"
    - path: packages/agent-badge/src/commands/init.test.ts
      provides: "idempotent README and legacy-rerun proof for the minimal-artifact model"
      contains: "agent-badge:start"
    - path: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      provides: "scenario-matrix proof that Phase 26 defaults no longer depend on repo-local runtime manifest ownership"
      contains: "command -v agent-badge >/dev/null 2>&1"
  key_links:
    - from: packages/agent-badge/src/commands/config.ts
      to: packages/core/src/init/runtime-wiring.ts
      via: refresh-policy scaffold reconciliation
      pattern: "refresh.prePush.enabled|refresh.prePush.mode"
    - from: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      to: packages/agent-badge/src/commands/init.ts
      via: scenario-matrix contract assertions
      pattern: "agent-badge:start|command -v agent-badge"
---

<objective>
Propagate the minimal-artifact contract through config-driven rewrites, rerun semantics, and repo-owned proof surfaces so Phase 26 finishes as an idempotent operator model instead of a one-off initializer change.

Purpose: satisfy the repo-artifact and rerun half of the milestone by ensuring the new minimal scaffold survives init reruns, config mutations, and regression coverage without reintroducing repo-local runtime ownership.
Output: config integration on the new scaffold contract, legacy-rerun convergence proof, and updated readiness tests for the minimal-artifact model.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/git/legotin/agent-badge/AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-RESEARCH.md
@.planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-VALIDATION.md
@.planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-01-PLAN.md
@packages/core/src/init/runtime-wiring.ts
@packages/core/src/init/runtime-wiring.test.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/init.test.ts
@packages/agent-badge/src/commands/config.ts
@packages/agent-badge/src/commands/config.test.ts
@packages/agent-badge/src/commands/uninstall.ts
@packages/agent-badge/src/commands/uninstall.test.ts
@packages/agent-badge/src/commands/release-readiness-matrix.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire config mutations and legacy reruns to preserve the minimal-artifact contract</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-RESEARCH.md
    - packages/core/src/init/runtime-wiring.ts
    - packages/core/src/init/runtime-wiring.test.ts
    - packages/agent-badge/src/commands/config.ts
    - packages/agent-badge/src/commands/config.test.ts
    - packages/agent-badge/src/commands/init.test.ts
    - packages/agent-badge/src/commands/uninstall.test.ts
  </read_first>
  <files>packages/agent-badge/src/commands/config.ts, packages/agent-badge/src/commands/config.test.ts, packages/agent-badge/src/commands/init.test.ts, packages/core/src/init/runtime-wiring.test.ts, packages/agent-badge/src/commands/uninstall.test.ts</files>
  <behavior>
    - Test 1: `config set refresh.prePush.mode` and `config set refresh.prePush.enabled` update the managed hook contract but do not create or recreate `package.json`, `@legotin/agent-badge`, or managed package scripts.
    - Test 2: init reruns on a legacy repo leave exactly one README badge marker block, one managed gitignore block, and one managed hook block while removing default managed runtime entries from package.json.
    - Test 3: uninstall compatibility tests still prove that old shared-hook and legacy-hook marker blocks are removable even though Phase 26 stops creating managed runtime manifest ownership by default.
  </behavior>
  <action>Update `packages/agent-badge/src/commands/config.ts` so refresh-setting mutations use the minimal scaffold reconciler introduced in Plan 26-01 instead of `applyRepoLocalRuntimeWiring()` plus `detectPackageManager()` and `resolveRuntimeDependencySpecifier()`. Rewrite `packages/agent-badge/src/commands/config.test.ts` so strict/fail-soft and enable/disable scenarios assert hook rewrites only and explicitly prove no package.json recreation. Extend `packages/agent-badge/src/commands/init.test.ts` and `packages/core/src/init/runtime-wiring.test.ts` with legacy-rerun scenarios that start from old managed manifest entries, converge to the minimal-artifact model, and preserve custom README or hook content. Keep `packages/agent-badge/src/commands/uninstall.test.ts` focused on compatibility cleanup of shared-hook and legacy-hook blocks; do not make uninstall depend on new default package.json ownership that should no longer exist after Phase 26.</action>
  <acceptance_criteria>
    - `rg -n "applyRepoLocalRuntimeWiring|resolveRuntimeDependencySpecifier|detectPackageManager" packages/agent-badge/src/commands/config.ts` returns no matches in the refresh-setting rewrite path.
    - `packages/agent-badge/src/commands/config.test.ts` contains at least one assertion that `package.json` stays absent or unchanged while refresh-policy settings are updated.
    - `packages/agent-badge/src/commands/init.test.ts` contains a legacy-rerun scenario that proves one README marker block and no default managed runtime package entries after rerun.
    - `packages/core/src/init/runtime-wiring.test.ts` contains a legacy cleanup scenario that preserves custom hook or gitignore content while removing managed runtime manifest keys.
    - `packages/agent-badge/src/commands/uninstall.test.ts` still contains both shared-hook and legacy-hook cleanup regressions with preserved custom `echo custom-check` lines.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/commands/init.test.ts packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/commands/uninstall.test.ts</automated>
  </verify>
  <done>Config rewrites and init reruns now converge on the same minimal-artifact contract, and legacy compatibility cleanup remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update scenario-matrix proof to pin the Phase 26 minimal-artifact contract</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-RESEARCH.md
    - packages/agent-badge/src/commands/init.ts
    - packages/agent-badge/src/commands/init.test.ts
    - packages/agent-badge/src/commands/config.ts
    - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
  </read_first>
  <files>packages/agent-badge/src/commands/release-readiness-matrix.test.ts, packages/agent-badge/src/commands/init.test.ts</files>
  <behavior>
    - Test 1: release-readiness scenarios assert the shared-runtime hook contract, `.agent-badge` scaffold, and README badge insertion without expecting `package.json#scripts.agent-badge:refresh` or repo-local `@legotin/agent-badge` ownership by default.
    - Test 2: idempotent re-init scenarios prove the minimal-artifact model survives repeated init runs with one README marker block and no default runtime manifest entries.
    - Test 3: legacy wrapper strings such as `npm run --silent agent-badge:refresh`, `pnpm run --silent agent-badge:refresh`, `yarn run agent-badge:refresh`, and `bun run agent-badge:refresh` remain absent from the managed hook contract.
  </behavior>
  <action>Rewrite `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` so the scenario matrix validates the real Phase 26 contract: hook content still uses the shared Phase 25 guard and direct refresh command, README badge insertion remains single-block and idempotent, publish state still behaves the same, and default repo-local runtime manifest ownership is gone. Remove helper logic that assumes `package.json` exists solely to read `agent-badge:refresh` scripts, and replace it with artifact checks over `.agent-badge/*`, `.git/hooks/pre-push`, README markers, and the absence of managed agent-badge dependency/script ownership by default. Keep lockfile and provider-matrix coverage if already useful, but do not reintroduce repo-local runtime expectations into the readiness proof. Extend `packages/agent-badge/src/commands/init.test.ts` only where needed to keep the matrix and command-level idempotency proof aligned on the same README/gitignore/hook semantics.</action>
  <acceptance_criteria>
    - `rg -n "readPackageScripts|packageScripts\\[\"agent-badge:refresh\"\\]" packages/agent-badge/src/commands/release-readiness-matrix.test.ts` returns no matches.
    - `rg -n "command -v agent-badge >/dev/null 2>&1|agent-badge refresh --hook pre-push --hook-policy" packages/agent-badge/src/commands/release-readiness-matrix.test.ts` returns at least one match for the shared hook contract.
    - `rg -n "npm run --silent agent-badge:refresh|pnpm run --silent agent-badge:refresh|yarn run agent-badge:refresh|bun run agent-badge:refresh" packages/agent-badge/src/commands/release-readiness-matrix.test.ts` returns matches only in the legacy-runner denylist, not as expected hook output.
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains at least one assertion that managed agent-badge manifest ownership is absent by default after init.
    - `packages/agent-badge/src/commands/init.test.ts` still contains idempotent re-init coverage with one README marker block.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts packages/agent-badge/src/commands/init.test.ts</automated>
  </verify>
  <done>Repo-owned readiness proof now encodes the minimal-artifact model instead of the old repo-local runtime ownership contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config mutation -> repo scaffold | Operator config changes rewrite repo automation and must not recreate deprecated runtime ownership artifacts. |
| legacy repos -> rerun cleanup | Existing repos may contain managed manifest entries from the old model and must converge safely without user-content loss. |
| scenario-matrix proof -> future planning/execution | Tests define the contract for later phases, so stale repo-local expectations become a planning risk if not corrected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | T | `packages/agent-badge/src/commands/config.ts` | mitigate | Route refresh-setting rewrites through the minimal scaffold reconciler so config cannot silently recreate managed runtime manifest ownership. |
| T-26-02-02 | T | `packages/agent-badge/src/commands/init.test.ts`, `packages/core/src/init/runtime-wiring.test.ts` | mitigate | Add explicit legacy-rerun coverage that proves managed cleanup preserves user-owned content and idempotent README/hook/gitignore markers. |
| T-26-02-03 | D | `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` | mitigate | Replace stale proof assumptions about package scripts with assertions over the actual minimal-artifact contract so later reviews do not validate the wrong behavior. |
</threat_model>

<verification>
Run the Phase 26 convergence and readiness suite:

- `npm test -- --run packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/commands/init.test.ts packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/commands/uninstall.test.ts`
- `npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts packages/agent-badge/src/commands/init.test.ts`
</verification>

<success_criteria>
- Config-driven hook updates no longer recreate repo-local runtime dependency or script ownership.
- Legacy init reruns converge to one README badge block, one managed gitignore block, one managed hook block, and no default runtime manifest ownership.
- Uninstall compatibility coverage remains intact for old hook shapes.
- Scenario-matrix proof encodes the Phase 26 minimal-artifact contract rather than the old repo-local model.
</success_criteria>

<output>
After completion, create `.planning/phases/26-minimal-repo-scaffold-and-init-rewire/26-minimal-repo-scaffold-and-init-rewire-02-SUMMARY.md`
</output>

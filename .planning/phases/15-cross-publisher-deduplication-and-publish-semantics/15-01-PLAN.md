---
phase: 15-cross-publisher-deduplication-and-publish-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/publish/shared-model.ts
  - packages/core/src/publish/shared-merge.ts
  - packages/core/src/publish/shared-model.test.ts
  - packages/core/src/publish/shared-merge.test.ts
  - packages/core/src/publish/index.ts
  - packages/core/src/scan/refresh-cache.ts
  - packages/core/src/scan/refresh-cache.test.ts
  - packages/core/src/scan/incremental-refresh.ts
  - packages/core/src/scan/incremental-refresh.test.ts
autonomous: true
requirements:
  - CONS-01
  - CONS-02
  - CONS-03
user_setup: []
must_haves:
  truths:
    - "Shared contributor files publish one opaque observation per stable provider session identity, so shared totals are derived by session rather than by per-publisher total summation."
    - "The same underlying session observed by multiple publishers is counted once through a deterministic canonical reducer instead of last-writer-wins or duplicate summation."
    - "Incremental refresh retains enough aggregate-only per-session state for later publish and pre-push flows to reproduce the shared reducer without forcing a full rescan."
  artifacts:
    - path: packages/core/src/publish/shared-model.ts
      provides: schema-version-2 contributor observation model keyed by opaque digests
      contains: schemaVersion: z.literal(2)
    - path: packages/core/src/publish/shared-merge.ts
      provides: canonical session reducer and resolved shared override derivation
      contains: deriveResolvedSharedOverrides
    - path: packages/core/src/scan/refresh-cache.ts
      provides: publishable per-session cache entries for included and ambiguous sessions
      contains: overrideDecision
    - path: packages/core/src/scan/incremental-refresh.ts
      provides: refresh-cache population that preserves candidate-session usage and override intent
      contains: state.overrides.ambiguousSessions
  key_links:
    - from: packages/core/src/scan/refresh-cache.ts
      to: packages/core/src/scan/incremental-refresh.ts
      via: "incremental refresh populates the cache with the same per-session fields later publish wiring will consume"
      pattern: overrideDecision
    - from: packages/core/src/publish/shared-model.ts
      to: packages/core/src/publish/shared-merge.ts
      via: "the reducer consumes schema-version-2 observation records keyed by opaque digests"
      pattern: observations
    - from: packages/core/src/scan/incremental-refresh.ts
      to: packages/core/src/publish/shared-model.ts
      via: "cache entries preserve the status, digest inputs, and override intent needed to build schema-version-2 publisher observations in the next plan"
      pattern: providerSessionId
---

<objective>
Implement the Phase 15 shared-state foundation for deterministic cross-publisher deduplication.

Purpose: Phase 14 made remote writes merge-safe, but the current contributor schema still stores only per-publisher totals. That cannot satisfy `CONS-01`, `CONS-02`, or the shared-state foundation of `CONS-03`. This plan stops at the shared model, reducer, and refresh-cache layer so Plan 02 can wire publish-service and the CLI flows onto a smaller, already-tested contract.
Output: schema-version-2 contributor files, deterministic duplicate-session reduction, and cache-backed per-session observations ready for publish-service and CLI wiring.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-RESEARCH.md
@.planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-VALIDATION.md
@.planning/phases/14-shared-remote-contribution-model/14-VERIFICATION.md
@.planning/phases/14-shared-remote-contribution-model/14-02-SUMMARY.md
@packages/core/src/publish/shared-model.ts
@packages/core/src/publish/shared-merge.ts
@packages/core/src/scan/refresh-cache.ts
@packages/core/src/scan/incremental-refresh.ts
@packages/core/src/attribution/override-store.ts

<interfaces>
From packages/core/src/publish/shared-model.ts:
```typescript
export const sharedContributorRecordSchema = z.object({
  schemaVersion: z.literal(1),
  publisherId: z.string(),
  updatedAt: isoDateTimeSchema,
  totals: z.object({
    sessions: nonnegativeIntSchema,
    tokens: nonnegativeIntSchema,
    estimatedCostUsdMicros: nonnegativeIntSchema.nullable()
  }).strict()
}).strict();
```

From packages/core/src/publish/publish-service.ts:
```typescript
export interface PublishBadgeIfChangedOptions {
  readonly config: Pick<AgentBadgeConfig, "badge" | "publish">;
  readonly state: AgentBadgeState;
  readonly includedTotals: IncludedTotals;
  readonly client: GitHubGistClient;
  readonly now: string;
  readonly skipIfUnchanged: boolean;
}
```

From packages/core/src/scan/refresh-cache.ts:
```typescript
const refreshCacheEntrySchema = z.object({
  provider: z.enum(["codex", "claude"]),
  providerSessionId: z.string().min(1),
  updatedAt: z.string().min(1).nullable(),
  status: z.enum(["included", "ambiguous", "excluded"]),
  includedSessions: z.number().int().nonnegative(),
  includedTokens: z.number().int().nonnegative(),
  includedEstimatedCostUsdMicros: z.number().int().nonnegative().nullable().optional().default(null)
}).strict();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace totals-only contributor files with schema-version-2 session observation records and deterministic reducers</name>
  <files>packages/core/src/publish/shared-model.ts, packages/core/src/publish/shared-merge.ts, packages/core/src/publish/shared-model.test.ts, packages/core/src/publish/shared-merge.test.ts, packages/core/src/publish/index.ts</files>
  <read_first>packages/core/src/publish/shared-model.ts, packages/core/src/publish/shared-merge.ts, packages/core/src/publish/index.ts, packages/core/src/attribution/override-store.ts, .planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-RESEARCH.md, .planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-VALIDATION.md, .planning/phases/14-shared-remote-contribution-model/14-VERIFICATION.md</read_first>
  <action>Upgrade `sharedContributorRecordSchema` per `CONS-01` and `CONS-02` from `schemaVersion: 1` totals-only files to `schemaVersion: 2` observation files. Replace `totals` with `observations: Record<string, { sessionUpdatedAt: string | null; attributionStatus: "included" | "ambiguous" | "excluded"; overrideDecision: "include" | "exclude" | null; tokens: int >= 0; estimatedCostUsdMicros: int >= 0 | null }>` keyed by the existing opaque digest format from `buildSharedOverrideDigest()`; do not introduce raw `provider:providerSessionId` values or a second digest scheme. Keep `publisherId` and `updatedAt`. In `shared-merge.ts`, remove totals summation as the primary model and add pure reducer helpers that: flatten contributor observation maps; group by digest; choose one canonical winner per digest using the exact comparator from research (latest non-null `sessionUpdatedAt`, then larger `tokens`, then larger non-null `estimatedCostUsdMicros`, then lexicographically smallest `publisherId`); resolve the repo-level ambiguous-session decision order-independently; derive badge totals from canonical included sessions only; and derive `agent-badge-overrides.json` from resolved shared ambiguous outcomes instead of blind overwrite merging. Keep the overrides file aggregate-only and opaque. Update `shared-model.test.ts` and `shared-merge.test.ts` first so they cover schema-version `2`, rejection of raw session keys, deterministic duplicate reduction, tie-break stability, and order-independent shared include/exclude resolution. Export the new reducer helpers from `packages/core/src/publish/index.ts`.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/shared-model.ts` contains `schemaVersion: z.literal(2)`
    - `packages/core/src/publish/shared-model.ts` contains `observations: z.record`
    - `packages/core/src/publish/shared-model.ts` contains `attributionStatus`
    - `packages/core/src/publish/shared-model.ts` contains `overrideDecision`
    - `packages/core/src/publish/shared-merge.ts` contains `compareSharedObservationWatermark`
    - `packages/core/src/publish/shared-merge.ts` contains `deriveResolvedSharedOverrides`
    - `packages/core/src/publish/shared-merge.ts` contains `publisherId.localeCompare`
    - `packages/core/src/publish/shared-model.test.ts` contains `schemaVersion: 2`
    - `packages/core/src/publish/shared-model.test.ts` contains `rejects raw provider:providerSessionId`
    - `packages/core/src/publish/shared-merge.test.ts` contains `chooses the latest sessionUpdatedAt before token count`
    - `packages/core/src/publish/shared-merge.test.ts` contains `breaks final ties with the lexicographically smallest publisherId`
    - `packages/core/src/publish/shared-merge.test.ts` contains `resolves shared ambiguous include and exclude order-independently`
    - `packages/core/src/publish/index.ts` contains `deriveResolvedSharedOverrides`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/shared-model.test.ts packages/core/src/publish/shared-merge.test.ts</automated>
  </verify>
  <done>`CONS-01` and the reducer half of `CONS-02` are encoded in strict public schemas and pure deterministic helpers rather than publish-order side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend refresh cache and incremental refresh to persist publishable per-session observations</name>
  <files>packages/core/src/scan/refresh-cache.ts, packages/core/src/scan/refresh-cache.test.ts, packages/core/src/scan/incremental-refresh.ts, packages/core/src/scan/incremental-refresh.test.ts</files>
  <read_first>packages/core/src/scan/refresh-cache.ts, packages/core/src/scan/refresh-cache.test.ts, packages/core/src/scan/incremental-refresh.ts, packages/core/src/scan/incremental-refresh.test.ts, packages/core/src/attribution/override-store.ts, .planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-RESEARCH.md, .planning/phases/05-incremental-refresh-and-operator-commands/05-01-SUMMARY.md</read_first>
  <action>Update the refresh cache per `CONS-01` and `CONS-03` so incremental publish no longer depends on pre-summed included totals. Replace the included-only cache contract with a per-session observation-ready shape: bump the cache version, keep `provider` and `providerSessionId`, and store `sessionUpdatedAt`, `status`, `overrideDecision`, `tokens`, and `estimatedCostUsdMicros` for both included and ambiguous sessions. Keep private evidence out of the cache. Update `buildRefreshCacheEntry()` and `mergeAttributedSessionsIntoCache()` so ambiguous sessions retain their token and optional cost aggregates, and local explicit overrides from `state.overrides.ambiguousSessions` are copied into cache entries. Update `summarizeRefreshCache()` so included totals are derived from `status === "included"` while ambiguous and excluded counts still report separately. Extend `incremental-refresh.ts` tests so changed ambiguous sessions survive into the cache with tokens intact, override decisions persist across merges, and full-refresh fallback still produces the new cache shape.</action>
  <acceptance_criteria>
    - `packages/core/src/scan/refresh-cache.ts` contains `version: z.literal(2)`
    - `packages/core/src/scan/refresh-cache.ts` contains `overrideDecision`
    - `packages/core/src/scan/refresh-cache.ts` contains `tokens: z.number().int().nonnegative()`
    - `packages/core/src/scan/refresh-cache.ts` contains `estimatedCostUsdMicros`
    - `packages/core/src/scan/refresh-cache.test.ts` contains `preserves ambiguous session tokens for shared publish`
    - `packages/core/src/scan/refresh-cache.test.ts` contains `overrideDecision`
    - `packages/core/src/scan/incremental-refresh.ts` contains `state.overrides.ambiguousSessions`
    - `packages/core/src/scan/incremental-refresh.ts` contains `status === "included"`
    - `packages/core/src/scan/incremental-refresh.test.ts` contains `merges ambiguous sessions into the cache without zeroing their tokens`
    - `packages/core/src/scan/incremental-refresh.test.ts` contains `persists explicit include or exclude overrides into cache entries`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/scan/refresh-cache.test.ts packages/core/src/scan/incremental-refresh.test.ts</automated>
  </verify>
  <done>The refresh path preserves enough aggregate-only session state to feed the shared reducer from cache without forcing a full backfill on every publish.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/shared-model.test.ts packages/core/src/publish/shared-merge.test.ts` succeeds
- `npm test -- --run packages/core/src/scan/refresh-cache.test.ts packages/core/src/scan/incremental-refresh.test.ts` succeeds
</verification>

<success_criteria>
- Shared contributor files stop publishing opaque local totals as the authoritative unit of merge.
- Duplicate remote observations converge to one canonical session contribution no matter which publisher writes first.
- Refresh and pre-push have a cache-backed per-session foundation ready for shared publish wiring without a mandatory full rescan.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-01-SUMMARY.md`
</output>

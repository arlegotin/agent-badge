---
phase: 15-cross-publisher-deduplication-and-publish-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - packages/core/src/publish/shared-merge.ts
  - packages/core/src/publish/shared-merge.test.ts
  - packages/core/src/publish/publish-service.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/core/src/publish/shared-badge-aggregation.test.ts
  - packages/core/src/scan/incremental-refresh.ts
  - packages/core/src/scan/incremental-refresh.test.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
autonomous: true
requirements:
  - CONS-02
  - CONS-03
user_setup: []
must_haves:
  truths:
    - "Both `publish` and `refresh` send the same session-level observation contract into shared publish, so CLI path choice does not change shared totals."
    - "Repeated publishes from different contributors converge to the same badge payload and shared override output for the same logical session set."
    - "Watermark conflicts for one duplicated session resolve deterministically by session data, not by publish order or gist write order."
    - "Shared include and exclude outcomes for ambiguous sessions are visible in automated publish and refresh regressions, so the repo-level outcome does not depend on one developer's local machine."
  artifacts:
    - path: packages/core/src/publish/publish-service.ts
      provides: shared publish wiring from publisher observation maps to canonical totals and resolved shared overrides
      contains: publisherObservations
    - path: packages/agent-badge/src/commands/publish.ts
      provides: full-publish CLI wiring that passes publisherObservations instead of includedTotals
      contains: publisherObservations
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: refresh CLI wiring that converts cache entries into publisherObservations
      contains: publisherObservations
    - path: packages/core/src/publish/publish-service.test.ts
      provides: convergence and mixed-publisher duplicate integration coverage
      contains: repeated publishes converge to the same badge payload
    - path: packages/core/src/publish/shared-badge-aggregation.test.ts
      provides: authoritative badge derivation coverage for canonical session winners
      contains: higher-watermark duplicate session is counted once
    - path: packages/core/src/scan/incremental-refresh.test.ts
      provides: refresh-path regression coverage for ambiguous candidate retention
      contains: shared include decisions can promote cached ambiguous usage
    - path: packages/agent-badge/src/commands/refresh.test.ts
      provides: command-level proof that refresh publishes the same shared totals as full publish
      contains: refresh publish matches full publish for duplicate sessions
  key_links:
    - from: packages/agent-badge/src/commands/publish.ts
      to: packages/core/src/publish/publish-service.ts
      via: "full publish builds one publisher observation map and hands it to shared publish"
      pattern: publisherObservations
    - from: packages/core/src/publish/publish-service.test.ts
      to: packages/core/src/publish/publish-service.ts
      via: "integration tests prove publish order cannot change canonical shared totals"
      pattern: repeated publishes converge
    - from: packages/core/src/publish/shared-badge-aggregation.test.ts
      to: packages/core/src/publish/shared-merge.ts
      via: "aggregation tests assert the canonical reducer counts duplicate sessions once"
      pattern: higher-watermark duplicate session
    - from: packages/agent-badge/src/commands/refresh.ts
      to: packages/core/src/scan/incremental-refresh.ts
      via: "refresh converts cached per-session entries into the same publisher observation contract used by full publish"
      pattern: publisherObservations
---

<objective>
Wire the shared-state foundation into publish-service and both CLI flows, then prove convergence under duplicates and conflicting overrides.

Purpose: Plan 01 now stops at the shared model, reducer, and refresh-cache foundation. This plan completes `CONS-02` and `CONS-03` by wiring publish-service, `agent-badge publish`, and `agent-badge refresh` onto that foundation, then locking the behavior down with the high-value regressions from `15-VALIDATION.md`.
Output: publish-service and CLI observation-map wiring plus convergence, conflict, and command-parity regression coverage for the shared reducer.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-RESEARCH.md
@.planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-VALIDATION.md
@.planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-01-PLAN.md
@packages/core/src/publish/shared-merge.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/publish/shared-badge-aggregation.test.ts
@packages/core/src/scan/incremental-refresh.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/commands/refresh.ts
@packages/agent-badge/src/commands/publish.test.ts
@packages/agent-badge/src/commands/refresh.test.ts

<interfaces>
From packages/core/src/publish/shared-merge.ts after Plan 01:
```typescript
export function compareSharedObservationWatermark(...): number;
export function deriveSharedIncludedTotals(records: readonly SharedContributorRecord[]): IncludedTotals;
export function deriveResolvedSharedOverrides(records: readonly SharedContributorRecord[]): SharedOverridesRecord;
```

From packages/core/src/publish/publish-service.ts after Plan 01:
```typescript
export interface PublishBadgeIfChangedOptions {
  readonly config: Pick<AgentBadgeConfig, "badge" | "publish">;
  readonly state: AgentBadgeState;
  readonly publisherObservations: SharedContributorObservationMap;
  readonly client: GitHubGistClient;
  readonly now: string;
  readonly skipIfUnchanged: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire publish-service, publish, and refresh onto the shared observation-map contract</name>
  <files>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/core/src/publish/shared-badge-aggregation.test.ts</files>
  <read_first>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/shared-merge.ts, packages/core/src/scan/incremental-refresh.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, .planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-01-PLAN.md, .planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-VALIDATION.md</read_first>
  <action>Move the publish wiring from old Plan 01 into this plan. Change `publish-service.ts` so `publishBadgeIfChanged()` and `publishBadgeToGist()` accept `publisherObservations` instead of `includedTotals`, write schema-version-2 contributor observation maps, derive authoritative totals from canonical winners, and derive `agent-badge-overrides.json` from resolved shared outcomes. In `packages/agent-badge/src/commands/publish.ts`, stop using `collectIncludedTotals()` as the publish input and instead build `publisherObservations` from the full scan and attribution result. In `packages/agent-badge/src/commands/refresh.ts`, convert `refresh.cache.entries` into the same `publisherObservations` contract before calling `publishBadgeIfChanged()`. Update the service and command tests so they assert the new input shape, `publisherId` / `mode: "shared"` persistence, schema-version-1 contributor-file isolation for authoritative totals, and aggregate-only command output. Extend `shared-badge-aggregation.test.ts` here as needed so the rendered badge payload is derived from canonical observation winners after the new wiring lands.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-service.ts` contains `publisherObservations`
    - `packages/core/src/publish/publish-service.ts` contains `deriveResolvedSharedOverrides`
    - `packages/core/src/publish/publish-service.test.ts` contains `schema-version-1 contributor files are ignored for authoritative totals`
    - `packages/core/src/publish/publish-service.test.ts` contains `writes the local publisher observation map with schemaVersion 2`
    - `packages/agent-badge/src/commands/publish.ts` contains `publisherObservations`
    - `packages/agent-badge/src/commands/refresh.ts` contains `publisherObservations`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `Publish mode: shared`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `publisherObservations`
    - `packages/core/src/publish/shared-badge-aggregation.test.ts` contains `agent-badge.json`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/publish-service.test.ts packages/core/src/publish/shared-badge-aggregation.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <done>The publish-service and both CLI entrypoints now consume the shared observation-map foundation from Plan 01, completing the implementation path for `CONS-02` and `CONS-03`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add convergence, duplicate-watermark, and refresh-parity regressions around the wired shared reducer</name>
  <files>packages/core/src/publish/shared-merge.ts, packages/core/src/publish/shared-merge.test.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/shared-badge-aggregation.test.ts, packages/core/src/scan/incremental-refresh.ts, packages/core/src/scan/incremental-refresh.test.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.test.ts</files>
  <read_first>packages/core/src/publish/shared-merge.ts, packages/core/src/publish/shared-merge.test.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/shared-badge-aggregation.test.ts, packages/core/src/scan/incremental-refresh.ts, packages/core/src/scan/incremental-refresh.test.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/refresh.test.ts, .planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-VALIDATION.md</read_first>
  <action>Add the Phase 15 convergence cases from `15-VALIDATION.md` after Task 1 lands. In `shared-merge.test.ts`, add explicit reducer-level cases for the ordered watermark rule: newer `sessionUpdatedAt` beats older data; equal timestamps fall back to higher `tokens`; equal tokens fall back to higher non-null cost; exact ties fall back to lexicographically smaller `publisherId`. In `publish-service.test.ts`, add integration cases named `repeated publishes converge to the same badge payload` and `duplicate sessions from two publishers are counted once`. In `shared-badge-aggregation.test.ts`, add `higher-watermark duplicate session is counted once`. In `incremental-refresh.test.ts`, add `shared include decisions can promote cached ambiguous usage`. In `refresh.test.ts`, add `refresh publish matches full publish for duplicate sessions`. In `publish.test.ts`, add `publish output stays aggregate-only while shared override decisions converge`. If any of these tests expose a mismatch, patch only `shared-merge.ts`, `publish-service.ts`, or `incremental-refresh.ts` to satisfy the comparator, parity, and privacy rules rather than weakening the tests.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/shared-merge.test.ts` contains `equal timestamps fall back to higher tokens`
    - `packages/core/src/publish/shared-merge.test.ts` contains `exact ties fall back to the lexicographically smaller publisherId`
    - `packages/core/src/publish/publish-service.test.ts` contains `repeated publishes converge to the same badge payload`
    - `packages/core/src/publish/publish-service.test.ts` contains `duplicate sessions from two publishers are counted once`
    - `packages/core/src/publish/shared-badge-aggregation.test.ts` contains `higher-watermark duplicate session is counted once`
    - `packages/core/src/scan/incremental-refresh.test.ts` contains `shared include decisions can promote cached ambiguous usage`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `refresh publish matches full publish for duplicate sessions`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `publish output stays aggregate-only while shared override decisions converge`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/shared-merge.test.ts packages/core/src/publish/publish-service.test.ts packages/core/src/publish/shared-badge-aggregation.test.ts packages/core/src/scan/incremental-refresh.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <done>`CONS-02` and `CONS-03` are locked by regressions that prove duplicate shared observations converge deterministically and that refresh and full publish stay in parity.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/publish-service.test.ts packages/core/src/publish/shared-badge-aggregation.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds
- `npm test -- --run packages/core/src/publish/shared-merge.test.ts packages/core/src/publish/publish-service.test.ts packages/core/src/publish/shared-badge-aggregation.test.ts packages/core/src/scan/incremental-refresh.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds
- `npm test -- --run` succeeds after both tasks complete
</verification>

<success_criteria>
- Both CLI entrypoints are wired to the shared observation-map contract from Plan 01.
- Duplicate cross-publisher session observations converge under deterministic tests instead of relying on manual reasoning.
- Shared ambiguous include/exclude behavior is proven at reducer, publish-service, and CLI boundaries.
- The refresh path demonstrates parity with full publish for the shared reducer contract.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cross-publisher-deduplication-and-publish-semantics/15-02-SUMMARY.md`
</output>

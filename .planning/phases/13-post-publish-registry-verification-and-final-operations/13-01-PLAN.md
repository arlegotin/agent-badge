---
phase: 13-post-publish-registry-verification-and-final-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/smoke/verify-registry-install.sh
  - packages/create-agent-badge/src/index.ts
  - packages/create-agent-badge/src/index.test.ts
autonomous: true
requirements:
  - REL-09
user_setup: []
must_haves:
  truths:
    - "Maintainers have one repo-owned command that installs the published `@legotin/agent-badge`, `@legotin/agent-badge-core`, and `create-agent-badge` artifacts from npm in a clean temp workspace and checks real file-system effects."
    - "The initializer-first contract is guarded by direct-execution tests that catch a symlinked-bin no-op before another release ships."
    - "Registry smoke can record exact-version runtime and initializer outcomes for the Phase 12 publish without requiring GitHub auth or live gist side effects."
  artifacts:
    - path: package.json
      provides: root maintainer entrypoint for exact-version registry smoke
      contains: '"smoke:registry": "bash scripts/smoke/verify-registry-install.sh"'
    - path: scripts/smoke/verify-registry-install.sh
      provides: exact-version clean-room registry smoke with optional initializer and evidence modes
      contains: '--check-initializer'
    - path: packages/create-agent-badge/src/index.ts
      provides: realpath-aware direct-execution guard for the published initializer bin
      contains: 'export function isDirectExecution'
    - path: packages/create-agent-badge/src/index.test.ts
      provides: regression coverage for symlinked-bin direct execution in the initializer package
      contains: 'treats a symlinked bin path as direct execution'
  key_links:
    - from: package.json
      to: scripts/smoke/verify-registry-install.sh
      via: "root maintainer script delegates registry smoke to the repo-owned shell entrypoint"
      pattern: 'smoke:registry'
    - from: scripts/smoke/verify-registry-install.sh
      to: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
      via: "registry smoke uses the Phase 12 published version `1.1.1` as the exact install target for live npm proof"
      pattern: '1.1.1'
    - from: packages/create-agent-badge/src/index.test.ts
      to: packages/create-agent-badge/src/index.ts
      via: "initializer tests exercise the exported direct-execution helper against symlinked bin paths"
      pattern: 'isDirectExecution'
---

<objective>
Create the exact-version registry smoke contract and initializer execution guard for post-publish proof.

Purpose: REL-09 requires proof against the actual npm registry artifacts, and Phase 13 research already found that exit-code-only initializer checks can miss a shipped no-op. The repo needs one exact-version smoke entrypoint plus direct-execution regression coverage before it can record trustworthy live-install evidence.
Output: a root `smoke:registry` command, `scripts/smoke/verify-registry-install.sh`, and initializer direct-execution coverage that mirrors the runtime CLI’s symlink-safe guard.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-VALIDATION.md
@.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
@package.json
@scripts/smoke/verify-packed-install.sh
@packages/create-agent-badge/src/index.ts
@packages/agent-badge/src/cli/main.ts
@packages/agent-badge/src/cli/main.test.ts
@packages/core/package.json
@packages/agent-badge/package.json
@packages/create-agent-badge/package.json

<interfaces>
From packages/create-agent-badge/src/index.ts:
```typescript
export interface RunCreateAgentBadgeOptions
  extends Omit<RunInitCommandOptions, "cwd"> {}

export async function runCreateAgentBadge(
  options: RunCreateAgentBadgeOptions = {}
): Promise<InitCommandResult>;

export const main = runCreateAgentBadge;
```

From packages/agent-badge/src/cli/main.ts:
```typescript
export function isDirectExecution(
  argv: readonly string[] = process.argv
): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the exact-version registry smoke entrypoint and clean-room verification script</name>
  <files>package.json, scripts/smoke/verify-registry-install.sh</files>
  <read_first>package.json, scripts/smoke/verify-packed-install.sh, docs/RELEASE.md, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-VALIDATION.md, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json</read_first>
  <action>Add `"smoke:registry": "bash scripts/smoke/verify-registry-install.sh"` to the root `package.json` scripts block. Create `scripts/smoke/verify-registry-install.sh` as a repo-owned bash entrypoint modeled on `scripts/smoke/verify-packed-install.sh`, with `set -euo pipefail`, `mktemp`, `trap cleanup EXIT`, isolated `npm_config_cache`, and exact long-option parsing for `--version <semver>`, `--check-initializer`, `--write-evidence`, and `--phase-dir <path>`. Make `--version` required and use the Phase 12 published value `1.1.1` as the first execution target. The script must:

1. Create one clean temp runtime project and one separate temp initializer project under `${TMPDIR:-/tmp}`.
2. In the runtime project, run `npm init -y`, `git init --quiet`, then `npm install @legotin/agent-badge@${VERSION} @legotin/agent-badge-core@${VERSION} create-agent-badge@${VERSION}`.
3. Mask `GH_TOKEN`, `GITHUB_TOKEN`, and `GITHUB_PAT` while running `./node_modules/.bin/agent-badge init` so smoke never creates a real gist.
4. Assert exact runtime effects: `.agent-badge/config.json` exists, `.agent-badge/state.json` exists, `.git/hooks/pre-push` exists, `node --input-type=module -e "import('@legotin/agent-badge')"` succeeds, `node --input-type=module -e "import('@legotin/agent-badge-core')"` succeeds, `node --input-type=module -e "import('create-agent-badge')"` succeeds, and the captured init log contains `Badge setup deferred`.
5. When `--check-initializer` is present, run `npm_config_yes=true npm init agent-badge@${VERSION}` in the initializer project with GitHub auth env vars unset, then assert `.agent-badge/config.json`, `.agent-badge/state.json`, and `.git/hooks/pre-push` exist there too. If the initializer exits `0` but fails any file assertion, treat that as a blocked smoke result and persist the failing assertion text.
6. Print scratch-directory diagnostics and exact package coordinates, but do not fail on stderr deprecation warnings alone.
7. When `--write-evidence` is present, write `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` and `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` (or the supplied `--phase-dir`) with exact fields/lines for `status`, `version`, `packages`, `runtime`, `initializer`, `runtimeProject`, `initializerProject`, and `blockingIssue`. The script must always write those evidence files before exiting, even when smoke status is `blocked`.</action>
  <acceptance_criteria>
    - `package.json` contains `"smoke:registry": "bash scripts/smoke/verify-registry-install.sh"`
    - `scripts/smoke/verify-registry-install.sh` contains `--version`
    - `scripts/smoke/verify-registry-install.sh` contains `--check-initializer`
    - `scripts/smoke/verify-registry-install.sh` contains `--write-evidence`
    - `scripts/smoke/verify-registry-install.sh` contains `--phase-dir`
    - `scripts/smoke/verify-registry-install.sh` contains `@legotin/agent-badge@${VERSION}`
    - `scripts/smoke/verify-registry-install.sh` contains `@legotin/agent-badge-core@${VERSION}`
    - `scripts/smoke/verify-registry-install.sh` contains `create-agent-badge@${VERSION}`
    - `scripts/smoke/verify-registry-install.sh` contains `Badge setup deferred`
    - `scripts/smoke/verify-registry-install.sh` contains `.agent-badge/config.json`
    - `scripts/smoke/verify-registry-install.sh` contains `.git/hooks/pre-push`
    - `scripts/smoke/verify-registry-install.sh` contains `13-REGISTRY-SMOKE.json`
    - `scripts/smoke/verify-registry-install.sh` contains `13-REGISTRY-SMOKE.md`
    - `scripts/smoke/verify-registry-install.sh` contains `blockingIssue`
    - `scripts/smoke/verify-registry-install.sh` contains `npm init agent-badge@${VERSION}`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke/verify-registry-install.sh --version 1.1.1</automated>
    <automated>bash scripts/smoke/verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence || true; test -f .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json && test -f .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md</automated>
  </verify>
  <done>The repo has one exact-version registry smoke command that can prove runtime install behavior, optionally prove initializer behavior, and persist Phase 13 evidence even when a live registry smoke reveals a blocker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the initializer bin direct-execution guard and cover it with symlink-path tests</name>
  <files>packages/create-agent-badge/src/index.ts, packages/create-agent-badge/src/index.test.ts</files>
  <read_first>packages/create-agent-badge/src/index.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `packages/create-agent-badge/src/index.ts` starts with `#!/usr/bin/env node`.
    - Test 2: `isDirectExecution([process.execPath, symlinkPath])` returns `true` when the symlink points at `packages/create-agent-badge/src/index.ts`.
    - Test 3: `isDirectExecution([process.execPath])` returns `false` when no entry path is supplied.
    - Test 4: direct execution errors are printed and set `process.exitCode = 1` instead of failing silently.
  </behavior>
  <action>Update `packages/create-agent-badge/src/index.ts` to match the runtime CLI’s symlink-safe direct-execution pattern. Replace the current `pathToFileURL(process.argv[1]).href` comparison with an exported `isDirectExecution(argv: readonly string[] = process.argv): boolean` helper that imports `realpathSync` from `node:fs` and `fileURLToPath` from `node:url`, returns `false` for a missing `argv[1]`, and otherwise compares `fileURLToPath(import.meta.url)` to `realpathSync(argv[1])` inside a `try/catch`. Keep `runCreateAgentBadge()` delegating to `runInitCommand({ ...options, cwd: process.cwd() })`, keep `main = runCreateAgentBadge`, and change the module footer to `if (isDirectExecution()) { void runCreateAgentBadge().catch(...) }` so thrown errors print `error.message` and set `process.exitCode = 1`. Add `packages/create-agent-badge/src/index.test.ts` with the same temp-dir/symlink pattern used in `packages/agent-badge/src/cli/main.test.ts`, plus the shebang assertion and the missing-entry-path false case.</action>
  <acceptance_criteria>
    - `packages/create-agent-badge/src/index.ts` contains `#!/usr/bin/env node`
    - `packages/create-agent-badge/src/index.ts` contains `export function isDirectExecution`
    - `packages/create-agent-badge/src/index.ts` contains `realpathSync`
    - `packages/create-agent-badge/src/index.ts` contains `fileURLToPath`
    - `packages/create-agent-badge/src/index.ts` contains `process.exitCode = 1`
    - `packages/create-agent-badge/src/index.ts` contains `runCreateAgentBadge().catch`
    - `packages/create-agent-badge/src/index.test.ts` contains `treats a symlinked bin path as direct execution`
    - `packages/create-agent-badge/src/index.test.ts` contains `starts with(\"#!/usr/bin/env node\\n\")`
    - `packages/create-agent-badge/src/index.test.ts` contains `isDirectExecution([process.execPath])`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/create-agent-badge/src/index.test.ts</automated>
  </verify>
  <done>The published initializer entrypoint now uses the same realpath-aware direct-execution guard as the runtime CLI, and a dedicated test file will catch the symlinked-bin no-op before another release ships.</done>
</task>

</tasks>

<verification>
- `bash scripts/smoke/verify-registry-install.sh --version 1.1.1` succeeds for the runtime-only path
- `bash scripts/smoke/verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence` writes `13-REGISTRY-SMOKE.json` and `13-REGISTRY-SMOKE.md` even when the initializer smoke is blocked
- `npm test -- --run packages/create-agent-badge/src/index.test.ts` succeeds
</verification>

<success_criteria>
- REL-09 has a repo-owned registry smoke contract that installs the exact published packages from npm rather than relying on tarballs alone.
- The initializer package has explicit direct-execution regression coverage instead of an untested bin-path assumption.
</success_criteria>

<output>
After completion, create `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-01-SUMMARY.md`
</output>

---
phase: 13-post-publish-registry-verification-and-final-operations
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .changeset/13-registry-repair.md
  - .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
  - .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md
  - .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json
  - .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md
  - docs/RELEASE.md
  - packages/core/package.json
  - packages/agent-badge/package.json
  - packages/create-agent-badge/package.json
  - scripts/verify-docs.sh
autonomous: true
requirements:
  - REL-09
  - OPER-07
user_setup:
  - service: npm
    why: "If the published `1.1.1` initializer smoke is blocked, the repair release path needs real npm publish authorization."
    env_vars:
      - name: NPM_TOKEN
        source: "GitHub repository secret `NPM_TOKEN` or maintainer npm auth configured for the chosen publish path"
    dashboard_config:
      - task: "Confirm the authenticated npm identity can publish `@legotin/agent-badge`, `@legotin/agent-badge-core`, and `create-agent-badge` patch versions"
        location: "npmjs.com -> package access / organization ownership"
  - service: GitHub
    why: "The preferred repair release path remains `.github/workflows/release.yml` via `workflow_dispatch`."
    env_vars:
      - name: GITHUB_TOKEN
        source: "Maintainer GitHub auth with permission to trigger/view Actions runs when the workflow path is used"
    dashboard_config:
      - task: "Confirm repository Actions access and the publish-capable `NPM_TOKEN` secret before attempting a repair release"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "Maintainers can run one checked-in post-publish smoke step against the exact published version `1.1.1` and review durable evidence of runtime and initializer outcomes."
    - "The production release checklist covers registry preflight, publish execution, GitHub Actions confirmation, and post-publish registry smoke in one maintained document."
    - "If the published `1.1.1` initializer or runtime fails live smoke, the phase includes a concrete repair-release path that bumps the publishable packages, republishes, and reruns registry smoke before REL-09 can close."
  artifacts:
    - path: .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json
      provides: machine-readable post-publish registry smoke result for the Phase 12 release
      contains: '"status"'
    - path: .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md
      provides: human-readable post-publish registry smoke report
      contains: 'Runtime smoke:'
    - path: .changeset/13-registry-repair.md
      provides: exact patch-release input for a registry repair if `1.1.1` smoke is blocked
      contains: '"@legotin/agent-badge": patch'
    - path: docs/RELEASE.md
      provides: single production runbook including the Phase 13 post-publish smoke command and evidence review
      contains: '13-REGISTRY-SMOKE.md'
    - path: scripts/verify-docs.sh
      provides: exact-string enforcement for the final release checklist content
      contains: '13-REGISTRY-SMOKE.md'
  key_links:
    - from: docs/RELEASE.md
      to: scripts/smoke/verify-registry-install.sh
      via: "the runbook requires the repo-owned registry smoke command after publish and workflow confirmation"
      pattern: 'verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence'
    - from: .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json
      to: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
      via: "Phase 13 evidence starts from the exact published version recorded in Phase 12 evidence and must be rerun against the repaired patch version if `1.1.1` is blocked"
      pattern: '"version": "1.1.1"'
    - from: .changeset/13-registry-repair.md
      to: packages/create-agent-badge/package.json
      via: "the contingent repair release bumps the publishable package versions before the rerun publish path executes"
      pattern: '1.1.2'
    - from: scripts/verify-docs.sh
      to: docs/RELEASE.md
      via: "docs verification enforces the final operator checklist and smoke-evidence references"
      pattern: '13-REGISTRY-SMOKE.md'
---

<objective>
Persist live registry smoke evidence for the published release and finalize the single production runbook.

Purpose: REL-09 is only credible when Phase 12’s published version is installed from npm and its real runtime/initializer effects are recorded, while OPER-07 requires the same repo-owned release checklist to include that final post-publish proof.
Output: `13-REGISTRY-SMOKE.json`, `13-REGISTRY-SMOKE.md`, and an updated `docs/RELEASE.md` plus `scripts/verify-docs.sh` that make Phase 13 smoke verification mandatory, with a contingent `1.1.2` repair-release branch if the published `1.1.1` initializer smoke is blocked.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-VALIDATION.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-01-PLAN.md
@.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
@docs/RELEASE.md
@scripts/verify-docs.sh
@scripts/smoke/verify-registry-install.sh
@.github/workflows/release.yml

<interfaces>
Required smoke command from Plan 13-01:
```bash
bash scripts/smoke/verify-registry-install.sh \
  --version 1.1.1 \
  --check-initializer \
  --write-evidence \
  --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations
```

Required evidence fields:
```json
{
  "status": "passed|blocked",
  "version": "1.1.1",
  "packages": ["@legotin/agent-badge@1.1.1", "@legotin/agent-badge-core@1.1.1", "create-agent-badge@1.1.1"],
  "runtime": {},
  "initializer": {},
  "blockingIssue": null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run exact-version registry smoke for the published release and persist Phase 13 evidence</name>
  <files>.changeset/13-registry-repair.md, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json</files>
  <read_first>.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, docs/RELEASE.md, scripts/smoke/verify-registry-install.sh, package.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-VALIDATION.md</read_first>
  <action>Use the Phase 12 publish evidence as the authority for the first exact published version and run `bash scripts/smoke/verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations`. Do not substitute `latest`; the first smoke must target `1.1.1` specifically because that is the version recorded in `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json`. Persist `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` and `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` from that run. If both surfaces pass, leave the version at `1.1.1`, skip the repair branch, and proceed to Task 2.

If the evidence file reports `"status": "blocked"` because the initializer or runtime does not create the required files, do not finish the phase with a blocked smoke report. Instead execute this concrete repair-release branch:

1. Create `.changeset/13-registry-repair.md` with exactly these frontmatter entries: `"@legotin/agent-badge": patch`, `"@legotin/agent-badge-core": patch`, and `"create-agent-badge": patch`, plus body text stating the patch release repairs the published initializer registry behavior found by Phase 13 smoke.
2. Run `npm run changeset version` so `packages/core/package.json`, `packages/agent-badge/package.json`, and `packages/create-agent-badge/package.json` all move from `1.1.1` to `1.1.2`, and `packages/create-agent-badge/package.json` updates `"@legotin/agent-badge": "^1.1.2"`.
3. Re-run the release gates from the repaired source: `npm run docs:check`, `npm run typecheck`, `npm run verify:clean-checkout`, and `npm --silent run release:preflight -- --json > .planning/phases/12-production-publish-execution/12-preflight.json`.
4. Publish the repaired `1.1.2` packages using the same operator contract in `docs/RELEASE.md`: prefer `.github/workflows/release.yml` via `workflow_dispatch`, and use local `npm run release` only if the workflow path is unavailable.
5. Refresh `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` and `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md` with `npm run release:evidence`, using `--publish-path github-actions --workflow-run-url <exact URL> --workflow-run-id <exact ID> --workflow-run-conclusion success --published-at <ISO8601>` for the workflow path or `--publish-path local-cli --fallback-reason "<exact reason>" --published-at <ISO8601>` for local fallback.
6. Re-run `bash scripts/smoke/verify-registry-install.sh --version 1.1.2 --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations` and require the rewritten `13-REGISTRY-SMOKE.json` to report `"version": "1.1.2"` and `"status": "passed"` before considering REL-09 complete.

If the repair release cannot be published or the `1.1.2` rerun smoke still reports `"status": "blocked"`, leave the evidence blocked and stop without marking the phase complete.</action>
  <acceptance_criteria>
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` exists
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"version": "1.1.1"` or `"version": "1.1.2"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"status"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"runtime"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"initializer"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` contains `Status:`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` contains `Version: 1.1.1` or `Version: 1.1.2`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` contains `Runtime smoke:`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` contains `Initializer smoke:`
    - if `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"status": "blocked"` after the first `1.1.1` run, then `.changeset/13-registry-repair.md` exists and contains `"@legotin/agent-badge": patch`, `"@legotin/agent-badge-core": patch`, and `"create-agent-badge": patch`
    - if the repair branch runs, `packages/core/package.json` contains `"version": "1.1.2"`
    - if the repair branch runs, `packages/agent-badge/package.json` contains `"version": "1.1.2"`
    - if the repair branch runs, `packages/create-agent-badge/package.json` contains `"version": "1.1.2"` and `"@legotin/agent-badge": "^1.1.2"`
    - final phase completion requires `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` to contain `"status": "passed"` for the final smoked version
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke/verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations || true; test -f .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json && test -f .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md && rg -n '"version": "1.1.1"|"version": "1.1.2"|"status"|"runtime"|"initializer"' .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json && rg -n 'Status:|Version: 1.1.1|Version: 1.1.2|Runtime smoke:|Initializer smoke:' .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md</automated>
  </verify>
  <done>Phase 13 has durable JSON and Markdown evidence for the final smoked release, and if the original `1.1.1` publish was blocked the plan forces a concrete `1.1.2` repair-release branch before REL-09 can be considered complete.</done>
</task>

<task type="auto">
  <name>Task 2: Make the final release runbook require post-publish registry smoke and evidence review</name>
  <files>docs/RELEASE.md, scripts/verify-docs.sh</files>
  <read_first>docs/RELEASE.md, scripts/verify-docs.sh, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md, .github/workflows/release.yml</read_first>
  <action>Update `docs/RELEASE.md` so it becomes the single production release checklist for OPER-07, with the existing order preserved and one new post-publish section after workflow/fallback evidence capture. The updated runbook must include all of these exact points:

1. The required local gates remain `npm run docs:check`, `npm run typecheck`, `npm run verify:clean-checkout`, and `npm run release:preflight`.
2. The primary publish path remains `.github/workflows/release.yml` via `workflow_dispatch`, with local `npm run release` documented only as fallback.
3. After publish evidence is captured for Phase 12, the maintainer must run `bash scripts/smoke/verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations`.
4. The maintainer must review `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` and `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md`.
5. REL-09 is only complete when `13-REGISTRY-SMOKE.json` reports `"status": "passed"` with both runtime and initializer surfaces passing; if the file reports `"status": "blocked"` for `1.1.1`, the operator must execute the repair-release branch that republishes `1.1.2` and reruns smoke instead of closing the milestone.

Update `scripts/verify-docs.sh` in the existing exact-string style so docs verification enforces the presence of `13-REGISTRY-SMOKE.json`, `13-REGISTRY-SMOKE.md`, the exact `verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence` command, `workflow_dispatch`, `.github/workflows/release.yml`, and the blocked-versus-passed completion rule.</action>
  <acceptance_criteria>
    - `docs/RELEASE.md` contains `13-REGISTRY-SMOKE.json`
    - `docs/RELEASE.md` contains `13-REGISTRY-SMOKE.md`
    - `docs/RELEASE.md` contains `verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence`
    - `docs/RELEASE.md` contains `"status": "passed"`
    - `docs/RELEASE.md` contains `"status": "blocked"`
    - `docs/RELEASE.md` contains `1.1.2`
    - `docs/RELEASE.md` still contains `npm run release:preflight`
    - `docs/RELEASE.md` still contains `workflow_dispatch`
    - `docs/RELEASE.md` still contains `.github/workflows/release.yml`
    - `scripts/verify-docs.sh` contains `13-REGISTRY-SMOKE.json`
    - `scripts/verify-docs.sh` contains `13-REGISTRY-SMOKE.md`
    - `scripts/verify-docs.sh` contains `verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence`
    - `scripts/verify-docs.sh` still contains `workflow_dispatch`
    - `scripts/verify-docs.sh` still contains `.github/workflows/release.yml`
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check</automated>
  </verify>
  <done>The repo now has one maintained production release checklist that includes post-publish registry smoke, Phase 13 evidence review, and an explicit repair-release rule for a blocked `1.1.1` initializer outcome.</done>
</task>

</tasks>

<verification>
- `bash scripts/smoke/verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations` persists both evidence files
- `npm run docs:check` succeeds after the runbook update
- Treat `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` as the final REL-09 gate: `"status": "passed"` means the final smoked release is verified; `"status": "blocked"` means a repair release is still required and the phase is not done
</verification>

<success_criteria>
- OPER-07 is satisfied by one enforced `docs/RELEASE.md` checklist that spans preflight, publish, workflow confirmation, and post-publish registry smoke.
- REL-09 has checked-in Phase 13 evidence for the final smoked release, with a concrete `1.1.2` repair-release branch if the original `1.1.1` initializer smoke is blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-02-SUMMARY.md`
</output>

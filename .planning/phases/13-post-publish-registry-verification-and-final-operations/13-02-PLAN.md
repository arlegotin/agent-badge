---
phase: 13-post-publish-registry-verification-and-final-operations
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .changeset/13-registry-repair.md
  - .planning/phases/12-production-publish-execution/12-preflight.json
  - .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
  - .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md
  - .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json
  - .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md
  - docs/RELEASE.md
  - packages/core/package.json
  - packages/agent-badge/package.json
  - packages/create-agent-badge/package.json
  - scripts/verify-docs.sh
autonomous: true
requirements:
  - REL-09
  - OPER-07
user_setup:
  - service: npm
    why: "If the published `1.1.1` initializer smoke is blocked, the repair release path needs real npm publish authorization."
    env_vars:
      - name: NPM_TOKEN
        source: "GitHub repository secret `NPM_TOKEN` or maintainer npm auth configured for the chosen publish path"
    dashboard_config:
      - task: "Confirm the authenticated npm identity can publish `@legotin/agent-badge`, `@legotin/agent-badge-core`, and `create-agent-badge` patch versions"
        location: "npmjs.com -> package access / organization ownership"
  - service: GitHub
    why: "The preferred repair release path remains `.github/workflows/release.yml` via `workflow_dispatch`."
    env_vars:
      - name: GITHUB_TOKEN
        source: "Maintainer GitHub auth with permission to trigger/view Actions runs when the workflow path is used"
    dashboard_config:
      - task: "Confirm repository Actions access and the publish-capable `NPM_TOKEN` secret before attempting a repair release"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "Maintainers can run one checked-in post-publish smoke step against the current published version recorded in Phase 12 evidence and review durable evidence of runtime and initializer outcomes."
    - "The production release checklist covers registry preflight, publish execution, GitHub Actions confirmation, and post-publish registry smoke in one maintained document."
    - "If the current published initializer or runtime fails live smoke, the phase includes a concrete repair-release path that bumps the publishable packages, refreshes Phase 12 publish evidence, republishes, and reruns registry smoke before REL-09 can close."
  artifacts:
    - path: .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json
      provides: machine-readable post-publish registry smoke result for the Phase 12 release
      contains: '"status"'
    - path: .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md
      provides: human-readable post-publish registry smoke report
      contains: 'Runtime smoke:'
    - path: .changeset/13-registry-repair.md
      provides: exact patch-release input for a registry repair if the current published version smoke is blocked
      contains: '"@legotin/agent-badge": patch'
    - path: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
      provides: authoritative current published version used to drive the release checklist smoke command
      contains: '"version"'
    - path: docs/RELEASE.md
      provides: single production runbook including the Phase 13 post-publish smoke command and evidence review
      contains: '13-REGISTRY-SMOKE.md'
    - path: scripts/verify-docs.sh
      provides: exact-string enforcement for the final release checklist content
      contains: '13-REGISTRY-SMOKE.md'
  key_links:
    - from: docs/RELEASE.md
      to: scripts/smoke/verify-registry-install.sh
      via: "the runbook requires the repo-owned registry smoke command using the version currently recorded in Phase 12 publish evidence after publish and workflow confirmation"
      pattern: 'verify-registry-install.sh --version'
    - from: .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json
      to: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
      via: "Phase 13 evidence starts from the current published version recorded in Phase 12 evidence and must be rerun against the repaired patch version if the first smoke is blocked"
      pattern: '"version"'
    - from: .changeset/13-registry-repair.md
      to: packages/create-agent-badge/package.json
      via: "the contingent repair release bumps the publishable package versions before the rerun publish path executes"
      pattern: '1.1.2'
    - from: scripts/verify-docs.sh
      to: docs/RELEASE.md
      via: "docs verification enforces the final operator checklist and smoke-evidence references"
      pattern: '13-REGISTRY-SMOKE.md'
---

<objective>
Persist live registry smoke evidence for the published release and finalize the single production runbook.

Purpose: REL-09 is only credible when Phase 12’s published version is installed from npm and its real runtime/initializer effects are recorded, while OPER-07 requires the same repo-owned release checklist to include that final post-publish proof.
Output: `13-REGISTRY-SMOKE.json`, `13-REGISTRY-SMOKE.md`, and an updated `docs/RELEASE.md` plus `scripts/verify-docs.sh` that make Phase 13 smoke verification mandatory, driven by the current Phase 12 publish-evidence version, with a contingent `1.1.2` repair-release branch if the first smoke is blocked.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-VALIDATION.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-01-PLAN.md
@.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
@docs/RELEASE.md
@scripts/verify-docs.sh
@scripts/smoke/verify-registry-install.sh
@.github/workflows/release.yml

<interfaces>
Required smoke command from Plan 13-01:
```bash
bash scripts/smoke/verify-registry-install.sh \
  --version <current-version-from-12-PUBLISH-EVIDENCE.json> \
  --check-initializer \
  --write-evidence \
  --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations
```

Required evidence fields:
```json
{
  "status": "passed|blocked",
  "version": "<current-version>",
  "packages": ["@legotin/agent-badge@<current-version>", "@legotin/agent-badge-core@<current-version>", "create-agent-badge@<current-version>"],
  "runtime": {},
  "initializer": {},
  "blockingIssue": null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the first exact-version registry smoke and persist explicit `1.1.1` evidence</name>
  <files>.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md</files>
  <read_first>.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, scripts/smoke/verify-registry-install.sh, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-VALIDATION.md</read_first>
  <action>Use `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` as the authority for the first current published version. Before running the slow registry smoke, execute a fast pre-smoke assertion that parses the Phase 12 evidence and confirms all three package entries share the same version `1.1.1`; fail fast if the evidence versions diverge. Then run `bash scripts/smoke/verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations`. Do not substitute `latest`; this first smoke must target `1.1.1` exactly because that is the current version recorded in `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json`. Persist `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` and `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` from that run. The resulting JSON must explicitly include `version`, `status`, `packages`, `runtime`, `initializer`, and `blockingIssue`. If the first smoke is blocked, preserve that blocked `1.1.1` evidence exactly for the next task; do not repair or overwrite it inside this task.</action>
  <acceptance_criteria>
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` exists
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"version": "1.1.1"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"status"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"packages"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"runtime"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"initializer"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"blockingIssue"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"@legotin/agent-badge@1.1.1"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"@legotin/agent-badge-core@1.1.1"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"create-agent-badge@1.1.1"`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` contains `Status:`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` contains `Version: 1.1.1`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` contains `Runtime smoke:`
    - `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md` contains `Initializer smoke:`
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const evidence=JSON.parse(fs.readFileSync('.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json','utf8')); const versions=[...new Set(evidence.packages.map((pkg)=>pkg.version))]; if(versions.length!==1) throw new Error('publish evidence versions diverge'); if(versions[0]!=='1.1.1') throw new Error('expected initial published version 1.1.1');"</automated>
    <automated>set +e; bash scripts/smoke/verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations; status=$?; set -e; test "$status" -eq 0 -o "$status" -eq 1; test -f .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json; test -f .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md; node --input-type=module -e "import fs from 'node:fs'; const report=JSON.parse(fs.readFileSync('.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json','utf8')); if(report.version!=='1.1.1') throw new Error('expected 1.1.1 smoke'); if(!['passed','blocked'].includes(report.status)) throw new Error('unexpected status'); if(!Array.isArray(report.packages)||report.packages.length!==3) throw new Error('missing packages'); for (const name of ['@legotin/agent-badge@1.1.1','@legotin/agent-badge-core@1.1.1','create-agent-badge@1.1.1']) { if (!report.packages.includes(name)) throw new Error('missing package '+name); } if(typeof report.runtime!=='object' || report.runtime===null) throw new Error('missing runtime'); if(typeof report.initializer!=='object' || report.initializer===null) throw new Error('missing initializer'); if(!Object.hasOwn(report,'blockingIssue')) throw new Error('missing blockingIssue');"</automated>
  </verify>
  <done>Phase 13 has explicit first-pass evidence for the published `1.1.1` registry artifacts, including whether that initial smoke passed or was blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the contingent `1.1.2` repair-release branch when the first smoke is blocked</name>
  <files>.changeset/13-registry-repair.md, .planning/phases/12-production-publish-execution/12-preflight.json, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json</files>
  <read_first>.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, docs/RELEASE.md, package.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json, .github/workflows/release.yml</read_first>
  <action>If `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` reports `"status": "passed"` for `1.1.1`, do nothing in this task and preserve the existing `1.1.1` evidence as the final REL-09 proof. If the file reports `"status": "blocked"`, execute this concrete repair-release branch and do not end the phase until it finishes successfully:

1. Create `.changeset/13-registry-repair.md` with exactly these frontmatter entries: `"@legotin/agent-badge": patch`, `"@legotin/agent-badge-core": patch`, and `"create-agent-badge": patch`, plus body text stating the patch release repairs the published initializer registry behavior found by Phase 13 smoke.
2. Run `npm run changeset version` so `packages/core/package.json`, `packages/agent-badge/package.json`, and `packages/create-agent-badge/package.json` all move from `1.1.1` to `1.1.2`, and `packages/create-agent-badge/package.json` updates `"@legotin/agent-badge": "^1.1.2"`.
3. Re-run the release gates from the repaired source: `npm run docs:check`, `npm run typecheck`, `npm run verify:clean-checkout`, and `npm --silent run release:preflight -- --json > .planning/phases/12-production-publish-execution/12-preflight.json`.
4. Publish the repaired `1.1.2` packages using the same operator contract in `docs/RELEASE.md`: prefer `.github/workflows/release.yml` via `workflow_dispatch`, and use local `npm run release` only if the workflow path is unavailable.
5. Refresh `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` and `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md` with `npm run release:evidence`, using `--publish-path github-actions --workflow-run-url <exact URL> --workflow-run-id <exact ID> --workflow-run-conclusion success --published-at <ISO8601>` for the workflow path or `--publish-path local-cli --fallback-reason "<exact reason>" --published-at <ISO8601>` for local fallback. The refreshed JSON must show all three `packages[].version` values as `1.1.2`, and the refreshed Markdown must mention `1.1.2`.
6. Re-run `bash scripts/smoke/verify-registry-install.sh --version 1.1.2 --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations` and require the rewritten `13-REGISTRY-SMOKE.json` to report `"version": "1.1.2"` and `"status": "passed"`.

If the repair release cannot be published or the rerun smoke still reports `"status": "blocked"`, stop and leave the phase incomplete.</action>
  <acceptance_criteria>
    - if `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` contains `"version": "1.1.1"` and `"status": "passed"`, then `.changeset/13-registry-repair.md` does not need to exist and no package version bump is required
    - if the first smoke is blocked, `.changeset/13-registry-repair.md` exists and contains `"@legotin/agent-badge": patch`
    - if the first smoke is blocked, `.changeset/13-registry-repair.md` contains `"@legotin/agent-badge-core": patch`
    - if the first smoke is blocked, `.changeset/13-registry-repair.md` contains `"create-agent-badge": patch`
    - if the repair branch runs, `packages/core/package.json` contains `"version": "1.1.2"`
    - if the repair branch runs, `packages/agent-badge/package.json` contains `"version": "1.1.2"`
    - if the repair branch runs, `packages/create-agent-badge/package.json` contains `"version": "1.1.2"`
    - if the repair branch runs, `packages/create-agent-badge/package.json` contains `"@legotin/agent-badge": "^1.1.2"`
    - if the repair branch runs, `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` contains `"version": "1.1.2"` for all three published packages
    - if the repair branch runs, `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md` contains `1.1.2`
    - final REL-09 completion requires `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` to contain `"status": "passed"`
    - final REL-09 completion requires `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` to contain either `"version": "1.1.1"` or `"version": "1.1.2"`
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const report=JSON.parse(fs.readFileSync('.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json','utf8')); if(report.status!=='passed') throw new Error('final smoke must be passed'); if(report.version==='1.1.1') process.exit(0); if(report.version!=='1.1.2') throw new Error('unexpected final version'); const changeset=fs.readFileSync('.changeset/13-registry-repair.md','utf8'); for (const line of ['\"@legotin/agent-badge\": patch','\"@legotin/agent-badge-core\": patch','\"create-agent-badge\": patch']) { if(!changeset.includes(line)) throw new Error('missing '+line); } const core=JSON.parse(fs.readFileSync('packages/core/package.json','utf8')); const runtime=JSON.parse(fs.readFileSync('packages/agent-badge/package.json','utf8')); const init=JSON.parse(fs.readFileSync('packages/create-agent-badge/package.json','utf8')); if(core.version!=='1.1.2') throw new Error('core not bumped'); if(runtime.version!=='1.1.2') throw new Error('runtime not bumped'); if(init.version!=='1.1.2') throw new Error('initializer not bumped'); if(init.dependencies['@legotin/agent-badge']!=='^1.1.2') throw new Error('initializer dependency not bumped'); const evidence=JSON.parse(fs.readFileSync('.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json','utf8')); const versions=[...new Set(evidence.packages.map((pkg)=>pkg.version))]; if(versions.length!==1 || versions[0]!=='1.1.2') throw new Error('publish evidence not refreshed to 1.1.2'); const evidenceMd=fs.readFileSync('.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md','utf8'); if(!evidenceMd.includes('1.1.2')) throw new Error('publish evidence markdown not refreshed');"</automated>
  </verify>
  <done>The plan now has one explicit final-state gate: either the first `1.1.1` smoke passes, or a repair release is executed and the rerun `1.1.2` smoke passes before REL-09 can close.</done>
</task>

<task type="auto">
  <name>Task 3: Make the final release runbook require post-publish registry smoke and evidence review</name>
  <files>docs/RELEASE.md, scripts/verify-docs.sh</files>
  <read_first>docs/RELEASE.md, scripts/verify-docs.sh, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json, .planning/phases/13-post-publish-registry-verification-and-final-operations/13-RESEARCH.md, .github/workflows/release.yml</read_first>
  <action>Update `docs/RELEASE.md` so it becomes the single production release checklist for OPER-07, with the existing order preserved and one new post-publish section after workflow/fallback evidence capture. Derive `CURRENT_PUBLISHED_VERSION` from `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` after Task 2 completes: it is `1.1.1` when the first smoke passed and `1.1.2` when the repair-release branch ran. The updated runbook must include all of these exact points:

1. The required local gates remain `npm run docs:check`, `npm run typecheck`, `npm run verify:clean-checkout`, and `npm run release:preflight`.
2. The primary publish path remains `.github/workflows/release.yml` via `workflow_dispatch`, with local `npm run release` documented only as fallback.
3. After publish evidence is captured for Phase 12, the maintainer must run `bash scripts/smoke/verify-registry-install.sh --version CURRENT_PUBLISHED_VERSION --check-initializer --write-evidence --phase-dir .planning/phases/13-post-publish-registry-verification-and-final-operations`, where `CURRENT_PUBLISHED_VERSION` is the current version from `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json`.
4. The maintainer must review `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` and `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.md`.
5. REL-09 is only complete when `13-REGISTRY-SMOKE.json` reports `"status": "passed"` with both runtime and initializer surfaces passing; if the file reports `"status": "blocked"` for the current published version, the operator must execute the repair-release branch, refresh Phase 12 publish evidence, and rerun smoke against the new current version instead of closing the milestone.

Update `scripts/verify-docs.sh` in the existing exact-string style so docs verification enforces the presence of `13-REGISTRY-SMOKE.json`, `13-REGISTRY-SMOKE.md`, a `verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence` or `verify-registry-install.sh --version 1.1.2 --check-initializer --write-evidence` command that matches the current publish-evidence version, `workflow_dispatch`, `.github/workflows/release.yml`, and the blocked-versus-passed completion rule.</action>
  <acceptance_criteria>
    - `docs/RELEASE.md` contains `13-REGISTRY-SMOKE.json`
    - `docs/RELEASE.md` contains `13-REGISTRY-SMOKE.md`
    - `docs/RELEASE.md` contains `verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence` or `verify-registry-install.sh --version 1.1.2 --check-initializer --write-evidence`
    - `docs/RELEASE.md` contains `"status": "passed"`
    - `docs/RELEASE.md` contains `"status": "blocked"`
    - `docs/RELEASE.md` contains `1.1.2`
    - `docs/RELEASE.md` still contains `npm run release:preflight`
    - `docs/RELEASE.md` still contains `workflow_dispatch`
    - `docs/RELEASE.md` still contains `.github/workflows/release.yml`
    - `scripts/verify-docs.sh` contains `13-REGISTRY-SMOKE.json`
    - `scripts/verify-docs.sh` contains `13-REGISTRY-SMOKE.md`
    - `scripts/verify-docs.sh` contains `verify-registry-install.sh --version 1.1.1 --check-initializer --write-evidence` or `verify-registry-install.sh --version 1.1.2 --check-initializer --write-evidence`
    - `scripts/verify-docs.sh` still contains `workflow_dispatch`
    - `scripts/verify-docs.sh` still contains `.github/workflows/release.yml`
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const evidence=JSON.parse(fs.readFileSync('.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json','utf8')); const versions=[...new Set(evidence.packages.map((pkg)=>pkg.version))]; if(versions.length!==1) throw new Error('publish evidence versions diverge'); const version=versions[0]; if(!['1.1.1','1.1.2'].includes(version)) throw new Error('unexpected publish evidence version'); const release=fs.readFileSync('docs/RELEASE.md','utf8'); const verify=fs.readFileSync('scripts/verify-docs.sh','utf8'); const needle='verify-registry-install.sh --version '+version+' --check-initializer --write-evidence'; if(!release.includes(needle)) throw new Error('release docs not aligned to '+version); if(!verify.includes(version)) throw new Error('docs verifier missing current version '+version);"</automated>
    <automated>npm run docs:check</automated>
  </verify>
  <done>The repo now has one maintained production release checklist that includes post-publish registry smoke, Phase 13 evidence review, and an explicit repair-release rule for a blocked `1.1.1` initializer outcome.</done>
</task>

</tasks>

<verification>
- the first smoke task writes explicit `1.1.1` evidence with all required fields
- the repair task can only pass when the final `13-REGISTRY-SMOKE.json` status is `passed`
- `npm run docs:check` succeeds after the runbook update
- Treat `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-REGISTRY-SMOKE.json` as the final REL-09 gate: `"status": "passed"` means the final smoked release is verified; `"status": "blocked"` means a repair release is still required and the phase is not done
</verification>

<success_criteria>
- OPER-07 is satisfied by one enforced `docs/RELEASE.md` checklist that spans preflight, publish, workflow confirmation, and post-publish registry smoke.
- REL-09 has checked-in Phase 13 evidence for the final smoked release, with a concrete `1.1.2` repair-release branch if the original `1.1.1` initializer smoke is blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/13-post-publish-registry-verification-and-final-operations/13-02-SUMMARY.md`
</output>

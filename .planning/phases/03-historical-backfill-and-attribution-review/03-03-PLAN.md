---
phase: 03-historical-backfill-and-attribution-review
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - packages/core/src/scan/scan-report.ts
  - packages/core/src/scan/scan-report.test.ts
  - packages/core/src/scan/scan-state.ts
  - packages/core/src/scan/index.ts
  - packages/core/src/index.ts
  - packages/agent-badge/src/commands/scan.ts
  - packages/agent-badge/src/commands/scan.test.ts
  - packages/agent-badge/src/cli/main.ts
autonomous: true
requirements:
  - ATTR-02
  - ATTR-05
  - SCAN-05
must_haves:
  truths:
    - "The runtime CLI exposes a real `scan` command that performs the Phase 3 backfill and prints included totals, ambiguous sessions, and excluded sessions."
    - "Completed scans update provider `lastScannedAt` and preserve overrides without writing fake incremental cursors."
    - "Operator-facing output explains attribution evidence without printing raw absolute paths, transcript content, or other privacy-sensitive values."
  artifacts:
    - path: packages/agent-badge/src/commands/scan.ts
      provides: runtime Phase 3 scan command
      contains: "runScanCommand"
    - path: packages/core/src/scan/scan-report.ts
      provides: human-readable attribution report formatter
      contains: "formatScanReport"
    - path: packages/core/src/scan/scan-state.ts
      provides: completed-scan state writeback rules
      contains: "applyCompletedScanState"
    - path: packages/agent-badge/src/commands/scan.test.ts
      provides: SCAN-05 and override-reuse command coverage
      contains: "prints Ambiguous Sessions"
  key_links:
    - from: packages/agent-badge/src/commands/scan.ts
      to: packages/core/src/scan/full-backfill.ts
      via: "Phase 3 historical scan entrypoint"
      pattern: "runFullBackfillScan"
    - from: packages/agent-badge/src/commands/scan.ts
      to: packages/core/src/attribution/attribution-engine.ts
      via: "repo attribution classification"
      pattern: "attributeBackfillSessions"
    - from: packages/agent-badge/src/commands/scan.ts
      to: packages/core/src/scan/scan-state.ts
      via: "completed-scan checkpoint updates and override persistence"
      pattern: "applyCompletedScanState"
    - from: packages/core/src/scan/scan-report.ts
      to: packages/core/src/attribution/attribution-types.ts
      via: "included/ambiguous/excluded section rendering"
      pattern: "AttributionStatus|AttributedSession"
---

<objective>
Wire the Phase 3 backfill and attribution core into a user-facing `scan` command with safe report formatting and completed-scan state updates.

Purpose: SCAN-05 is the visible contract for this phase, and ATTR-05 is only useful in practice once a developer can run `scan`, apply include/exclude decisions, and see them reused on the next run.
Output: Scan formatter, state-write helpers, CLI wiring, and command tests that prove report content plus override reuse.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-01-PLAN.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-02-PLAN.md
@packages/agent-badge/src/cli/main.ts
@packages/agent-badge/src/commands/init.ts
@packages/core/src/config/config-schema.ts
@packages/core/src/state/state-schema.ts
@packages/core/src/scan/full-backfill.ts
@packages/core/src/attribution/attribution-engine.ts
@packages/core/src/attribution/override-store.ts

<interfaces>
From `packages/agent-badge/src/commands/init.ts`:

```ts
interface OutputWriter {
  write(chunk: string): unknown;
}
```

From `packages/core/src/state/state-schema.ts`:

```ts
export interface AgentBadgeState {
  checkpoints: {
    codex: { cursor: string | null; lastScannedAt: string | null };
    claude: { cursor: string | null; lastScannedAt: string | null };
  };
  overrides: { ambiguousSessions: Record<string, "include" | "exclude"> };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the scan report formatter with explicit redaction rules</name>
  <files>packages/core/src/scan/scan-report.ts, packages/core/src/scan/scan-report.test.ts</files>
  <read_first>packages/core/src/attribution/attribution-types.ts, packages/core/src/attribution/attribution-engine.ts, packages/core/src/scan/full-backfill.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Create `packages/core/src/scan/scan-report.ts` exporting `formatScanReport(result)` where `result` contains the repo summary, counts, attributed sessions, and any override actions applied during this run. The formatter must print exactly these top-level headings in order: `Included Totals`, `Ambiguous Sessions`, and `Excluded Sessions`. For each ambiguous or excluded row, print the stable session key `${provider}:${providerSessionId}`, the provider name, the evidence kinds, and the reason string, but do not print raw `cwdRealPath`, transcript file paths, or transcript text. In `packages/core/src/scan/scan-report.test.ts`, assert that formatted output contains the three headings, the session key format, and no raw placeholder path such as `/repo/main` or `/tmp/agent-badge-home-`.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/scan/scan-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/scan/scan-report.ts` contains `export function formatScanReport`
    - `packages/core/src/scan/scan-report.ts` contains `Included Totals`
    - `packages/core/src/scan/scan-report.ts` contains `Ambiguous Sessions`
    - `packages/core/src/scan/scan-report.ts` contains `Excluded Sessions`
    - `packages/core/src/scan/scan-report.test.ts` contains `does not print /repo/main`
    - `packages/core/src/scan/scan-report.test.ts` contains `does not print /tmp/agent-badge-home-`
  </acceptance_criteria>
  <done>Phase 3 report output is explicit enough to review attribution decisions without leaking private local evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Implement completed-scan state updates and the runtime scan command</name>
  <files>packages/core/src/scan/scan-state.ts, packages/core/src/scan/index.ts, packages/core/src/index.ts, packages/agent-badge/src/commands/scan.ts</files>
  <read_first>packages/core/src/state/state-schema.ts, packages/core/src/scan/full-backfill.ts, packages/core/src/attribution/attribution-engine.ts, packages/core/src/attribution/override-store.ts, packages/agent-badge/src/commands/init.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Create `packages/core/src/scan/scan-state.ts` exporting `applyCompletedScanState({ previousState, scanResult, now })` that updates `checkpoints.codex.lastScannedAt` and `checkpoints.claude.lastScannedAt` only for providers scanned successfully, preserves existing `cursor` values unless the scan result explicitly provides a concrete cursor, and preserves `overrides.ambiguousSessions` plus any include/exclude decisions applied this run. Export it from `packages/core/src/scan/index.ts` and `packages/core/src/index.ts`. Create `packages/agent-badge/src/commands/scan.ts` exporting `runScanCommand(options)` with options `{ cwd?: string; homeRoot?: string; stdout?: OutputWriter; includeSession?: string[]; excludeSession?: string[] }`. The command must read `.agent-badge/config.json` and `.agent-badge/state.json`, call `runFullBackfillScan()`, classify sessions through `attributeBackfillSessions()`, apply any explicit `--include-session` and `--exclude-session` keys through `applyAmbiguousSessionDecision()`, format the result with `formatScanReport()`, and write the updated state only after the full scan/report path succeeds. If an override key does not correspond to an ambiguous session in the current result, print a precise warning line and do not mutate state for that key.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/scan/scan-state.ts` contains `export function applyCompletedScanState`
    - `packages/core/src/scan/scan-state.ts` contains `lastScannedAt`
    - `packages/core/src/scan/scan-state.ts` contains `cursor`
    - `packages/agent-badge/src/commands/scan.ts` contains `export async function runScanCommand`
    - `packages/agent-badge/src/commands/scan.ts` contains `.agent-badge/config.json`
    - `packages/agent-badge/src/commands/scan.ts` contains `.agent-badge/state.json`
    - `packages/agent-badge/src/commands/scan.ts` contains `includeSession?: string[]`
    - `packages/agent-badge/src/commands/scan.ts` contains `excludeSession?: string[]`
    - `packages/agent-badge/src/commands/scan.ts` contains `formatScanReport`
  </acceptance_criteria>
  <done>The runtime now has a real scan command path that can apply explicit override decisions and update state only after a successful completed scan.</done>
</task>

<task type="auto">
  <name>Task 3: Wire `scan` into the CLI and prove SCAN-05 plus override reuse end to end</name>
  <files>packages/agent-badge/src/commands/scan.test.ts, packages/agent-badge/src/cli/main.ts</files>
  <read_first>packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/commands/scan.ts, packages/agent-badge/src/commands/init.test.ts, packages/core/src/scan/scan-report.ts, packages/core/src/scan/scan-state.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Update `packages/agent-badge/src/cli/main.ts` so the program registers a `scan` command with `.description(\"Scan local agent history and report attributed usage.\")`, plus Commander options `--include-session <provider:sessionId>` and `--exclude-session <provider:sessionId>` that forward to `runScanCommand()`. Create `packages/agent-badge/src/commands/scan.test.ts` using temp repo and provider fixtures to prove these exact behaviors: `prints Included Totals`, `prints Ambiguous Sessions`, `prints Excluded Sessions`, `reuses stored ambiguous overrides on a second scan`, and `does not rewrite checkpoints after a failed scan`. Assert that the command output contains the three required sections, that a stored include/exclude decision changes the next run's classification, and that failed runs leave the previous state file contents unchanged.</action>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/scan.test.ts packages/core/src/scan/scan-report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-badge/src/cli/main.ts` contains `.command("scan")`
    - `packages/agent-badge/src/cli/main.ts` contains `Scan local agent history and report attributed usage.`
    - `packages/agent-badge/src/cli/main.ts` contains `--include-session <provider:sessionId>`
    - `packages/agent-badge/src/cli/main.ts` contains `--exclude-session <provider:sessionId>`
    - `packages/agent-badge/src/commands/scan.test.ts` contains `prints Included Totals`
    - `packages/agent-badge/src/commands/scan.test.ts` contains `prints Ambiguous Sessions`
    - `packages/agent-badge/src/commands/scan.test.ts` contains `prints Excluded Sessions`
    - `packages/agent-badge/src/commands/scan.test.ts` contains `reuses stored ambiguous overrides on a second scan`
    - `packages/agent-badge/src/commands/scan.test.ts` contains `does not rewrite checkpoints after a failed scan`
  </acceptance_criteria>
  <done>SCAN-05 is proven through the actual CLI surface, including explicit ambiguity decisions and safe completed-scan state behavior.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` succeeds after wiring the scan command
- [ ] `npm test -- --run packages/core/src/scan/scan-report.test.ts packages/agent-badge/src/commands/scan.test.ts` succeeds
- [ ] `packages/agent-badge/src/commands/scan.ts` writes state only after successful scan completion
- [ ] `packages/core/src/scan/scan-report.ts` never prints raw absolute evidence paths
</verification>

<success_criteria>
- SCAN-05 is delivered through a real CLI command
- ATTR-05 is usable end to end through explicit include/exclude session keys
- Completed-scan state updates are conservative and do not fake incremental cursor semantics
</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-backfill-and-attribution-review/03-03-SUMMARY.md`
</output>

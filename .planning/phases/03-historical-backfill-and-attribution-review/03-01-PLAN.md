---
phase: 03-historical-backfill-and-attribution-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/providers/session-summary.ts
  - packages/core/src/providers/codex/codex-adapter.ts
  - packages/core/src/providers/claude/claude-jsonl.ts
  - packages/core/src/providers/claude/claude-adapter.ts
  - packages/core/src/scan/full-backfill.ts
  - packages/core/src/scan/full-backfill.test.ts
  - packages/core/src/scan/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - ATTR-02
must_haves:
  truths:
    - "A first-run full backfill scans only enabled providers and produces one deduped candidate per `${provider}:${providerSessionId}` before any publish flow exists."
    - "Normalized session summaries carry a privacy-safe transcript-correlation hint for later attribution without persisting transcript text or raw absolute transcript paths."
    - "Backfill preserves provider-separated counts and lineage metadata but does not invent parent-child token subtraction rules."
  artifacts:
    - path: packages/core/src/providers/session-summary.ts
      provides: Phase 3 attribution-ready session schema
      contains: "attributionHints"
    - path: packages/core/src/scan/full-backfill.ts
      provides: historical backfill orchestration entrypoint
      contains: "runFullBackfillScan"
    - path: packages/core/src/scan/full-backfill.test.ts
      provides: ATTR-02 regression coverage
      contains: "dedupes by provider plus providerSessionId"
    - path: packages/core/src/providers/claude/claude-jsonl.ts
      provides: privacy-safe transcript project correlation hint
      contains: "projectKey"
  key_links:
    - from: packages/core/src/scan/full-backfill.ts
      to: packages/core/src/repo/repo-fingerprint.ts
      via: "repo fingerprint resolved once per scan"
      pattern: "resolveRepoFingerprint"
    - from: packages/core/src/scan/full-backfill.ts
      to: packages/core/src/providers/codex/codex-adapter.ts
      via: "enabled-provider Codex scan"
      pattern: "scanCodexSessions"
    - from: packages/core/src/scan/full-backfill.ts
      to: packages/core/src/providers/claude/claude-adapter.ts
      via: "enabled-provider Claude scan"
      pattern: "scanClaudeSessions"
    - from: packages/core/src/providers/claude/claude-adapter.ts
      to: packages/core/src/providers/claude/claude-jsonl.ts
      via: "project-directory correlation key"
      pattern: "projectKey"
---

<objective>
Implement the first-run historical backfill pipeline that turns enabled provider adapters into one deduped Phase 3 candidate inventory.

Purpose: ATTR-02 requires a real full-scan entrypoint before Phase 3 can make attribution decisions or report trustworthy totals.
Output: Attribution-ready session summaries, a `runFullBackfillScan()` core service, and integration tests that prove enabled-provider backfill behavior.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md
@.planning/phases/02-repo-identity-and-provider-parsing/02-03-SUMMARY.md
@packages/core/src/config/config-schema.ts
@packages/core/src/repo/repo-fingerprint.ts
@packages/core/src/providers/session-summary.ts
@packages/core/src/providers/codex/codex-adapter.ts
@packages/core/src/providers/claude/claude-jsonl.ts
@packages/core/src/providers/claude/claude-adapter.ts
@packages/testkit/src/codex-fixtures.ts
@packages/testkit/src/claude-fixtures.ts
@packages/testkit/src/repo-fixtures.ts

<interfaces>
From `packages/core/src/providers/session-summary.ts`:

```ts
export interface NormalizedSessionSummary {
  provider: "codex" | "claude";
  providerSessionId: string;
  cwd: string | null;
  observedRemoteUrlNormalized: string | null;
  tokenUsage: { total: number; ... };
  lineage: { parentSessionId: string | null; kind: "root" | "child" | "unknown" };
}
```

From `packages/core/src/repo/repo-fingerprint.ts`:

```ts
export interface RepoFingerprint {
  readonly gitRoot: string;
  readonly gitRootRealPath: string;
  readonly originUrlNormalized: string | null;
  readonly aliasRemoteUrlsNormalized: string[];
  readonly aliasSlugs: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend normalized session summaries with attribution-ready local hints</name>
  <files>packages/core/src/providers/session-summary.ts, packages/core/src/providers/codex/codex-adapter.ts, packages/core/src/providers/claude/claude-jsonl.ts, packages/core/src/providers/claude/claude-adapter.ts</files>
  <read_first>packages/core/src/providers/session-summary.ts, packages/core/src/providers/codex/codex-adapter.ts, packages/core/src/providers/claude/claude-jsonl.ts, packages/core/src/providers/claude/claude-adapter.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Extend `packages/core/src/providers/session-summary.ts` so `normalizedSessionSummarySchema` includes an `attributionHints` object with exactly `cwdRealPath: string | null` and `transcriptProjectKey: string | null`. In `packages/core/src/providers/claude/claude-jsonl.ts`, add `projectKey` to `ClaudeProjectJsonlSession` by deriving it from the immediate directory name under `.claude/projects/`. Update `packages/core/src/providers/claude/claude-adapter.ts` so each summary sets `attributionHints.transcriptProjectKey` from `session.projectKey` and leaves `cwdRealPath` as `null`. Update `packages/core/src/providers/codex/codex-adapter.ts` so each summary includes `attributionHints: { cwdRealPath: null, transcriptProjectKey: null }`. Do not persist transcript file paths or any raw transcript text in the normalized summary.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/providers/session-summary.ts` contains `attributionHints`
    - `packages/core/src/providers/session-summary.ts` contains `cwdRealPath: z.string().min(1).nullable()`
    - `packages/core/src/providers/session-summary.ts` contains `transcriptProjectKey: z.string().min(1).nullable()`
    - `packages/core/src/providers/claude/claude-jsonl.ts` contains `readonly projectKey: string`
    - `packages/core/src/providers/claude/claude-adapter.ts` contains `transcriptProjectKey: session.projectKey`
    - `packages/core/src/providers/codex/codex-adapter.ts` contains `transcriptProjectKey: null`
  </acceptance_criteria>
  <done>Provider summaries now carry the minimum local-only hints Phase 3 needs for later transcript correlation without expanding the privacy surface.</done>
</task>

<task type="auto">
  <name>Task 2: Build the full historical backfill service and export it from core</name>
  <files>packages/core/src/scan/full-backfill.ts, packages/core/src/scan/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/config/config-schema.ts, packages/core/src/repo/repo-fingerprint.ts, packages/core/src/providers/session-summary.ts, packages/core/src/providers/codex/codex-adapter.ts, packages/core/src/providers/claude/claude-adapter.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Create `packages/core/src/scan/full-backfill.ts` exporting `runFullBackfillScan(options)` with the exact options `{ cwd: string; homeRoot: string; config: Pick<AgentBadgeConfig, "providers" | "repo"> }`. The service must resolve the current repo fingerprint with `resolveRepoFingerprint({ cwd, config })`, scan only enabled providers by calling `scanCodexSessions({ homeRoot })` and `scanClaudeSessions({ homeRoot })`, realpath any non-null session `cwd` into `attributionHints.cwdRealPath` when the path exists, dedupe results by the composite key `${session.provider}:${session.providerSessionId}`, and return a structured result containing `{ repo, sessions, scannedProviders, counts: { scannedSessions, dedupedSessions, byProvider } }`. Export the new service from `packages/core/src/scan/index.ts` and re-export `./scan/index.js` from `packages/core/src/index.ts`. Preserve provider lineage metadata as-is and do not subtract child-thread tokens from parents.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/scan/full-backfill.ts` contains `export async function runFullBackfillScan`
    - `packages/core/src/scan/full-backfill.ts` contains `resolveRepoFingerprint`
    - `packages/core/src/scan/full-backfill.ts` contains `scanCodexSessions`
    - `packages/core/src/scan/full-backfill.ts` contains `scanClaudeSessions`
    - `packages/core/src/scan/full-backfill.ts` contains `session.provider`
    - `packages/core/src/scan/full-backfill.ts` contains `session.providerSessionId`
    - `packages/core/src/scan/index.ts` contains `./full-backfill.js`
    - `packages/core/src/index.ts` contains `./scan/index.js`
  </acceptance_criteria>
  <done>The core package has one explicit first-run backfill entrypoint that Phase 3 and later phases can share.</done>
</task>

<task type="auto">
  <name>Task 3: Prove first-run backfill behavior with integration tests</name>
  <files>packages/core/src/scan/full-backfill.test.ts</files>
  <read_first>packages/core/src/scan/full-backfill.ts, packages/core/src/providers/codex/codex-adapter.ts, packages/core/src/providers/claude/claude-adapter.ts, packages/testkit/src/codex-fixtures.ts, packages/testkit/src/claude-fixtures.ts, packages/testkit/src/repo-fixtures.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Create `packages/core/src/scan/full-backfill.test.ts` that builds a temp repo plus temp provider home, runs `runFullBackfillScan()`, and proves three behaviors with exact test names: `dedupes by provider plus providerSessionId`, `scans only enabled providers`, and `preserves provider-separated counts for first-run backfill`. Use one fixture case where duplicate session objects are introduced by stubbing adapter output or by feeding repeated provider-session rows so the test proves the composite-key dedupe boundary explicitly. Assert that the result still contains provider lineage metadata and that no test snapshot or stringified output includes transcript text.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/scan/full-backfill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/scan/full-backfill.test.ts` contains `dedupes by provider plus providerSessionId`
    - `packages/core/src/scan/full-backfill.test.ts` contains `scans only enabled providers`
    - `packages/core/src/scan/full-backfill.test.ts` contains `preserves provider-separated counts for first-run backfill`
    - `packages/core/src/scan/full-backfill.test.ts` contains `lineage`
  </acceptance_criteria>
  <done>ATTR-02 is covered by an integration test that exercises the new full backfill entrypoint instead of trusting wiring by inspection.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` succeeds after adding attribution hints and the scan module
- [ ] `npm test -- --run packages/core/src/scan/full-backfill.test.ts` succeeds
- [ ] `packages/core/src/scan/full-backfill.ts` dedupes by composite provider-session key
- [ ] `packages/core/src/providers/session-summary.ts` exposes only privacy-safe attribution hints
</verification>

<success_criteria>
- ATTR-02 has one shared first-run backfill service
- Provider summaries carry the minimal local-only hints needed for later attribution
- Backfill remains conservative: enabled-provider scan, composite-key dedupe, preserved lineage, no speculative subagent arithmetic
</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-backfill-and-attribution-review/03-01-SUMMARY.md`
</output>

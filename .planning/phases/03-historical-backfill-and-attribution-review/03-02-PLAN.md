---
phase: 03-historical-backfill-and-attribution-review
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/core/src/attribution/attribution-types.ts
  - packages/core/src/attribution/attribution-engine.ts
  - packages/core/src/attribution/attribution-engine.test.ts
  - packages/core/src/attribution/override-store.ts
  - packages/core/src/attribution/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - ATTR-03
  - ATTR-04
  - ATTR-05
must_haves:
  truths:
    - "Attribution decisions are deterministic and evaluate repo-root, normalized remote, normalized cwd, transcript correlation, then persisted override reuse."
    - "Ambiguous sessions contribute zero to included totals until an explicit include or exclude override exists."
    - "Override persistence reuses the existing `overrides.ambiguousSessions` map and stores only stable session decision keys, not raw evidence or transcript data."
  artifacts:
    - path: packages/core/src/attribution/attribution-engine.ts
      provides: Phase 3 attribution decision engine
      contains: "attributeBackfillSessions"
    - path: packages/core/src/attribution/override-store.ts
      provides: stable ambiguous-session keying and override application
      contains: "buildAmbiguousSessionKey"
    - path: packages/core/src/attribution/attribution-engine.test.ts
      provides: ATTR-03/04/05 regression coverage
      contains: "reuses stored include and exclude overrides"
  key_links:
    - from: packages/core/src/attribution/attribution-engine.ts
      to: packages/core/src/repo/repo-fingerprint.ts
      via: "repo-root and remote comparison"
      pattern: "gitRootRealPath|originUrlNormalized|aliasRemoteUrlsNormalized"
    - from: packages/core/src/attribution/attribution-engine.ts
      to: packages/core/src/providers/session-summary.ts
      via: "normalized session evidence and attribution hints"
      pattern: "attributionHints|observedRemoteUrlNormalized|cwd"
    - from: packages/core/src/attribution/override-store.ts
      to: packages/core/src/state/state-schema.ts
      via: "ambiguous override reuse"
      pattern: "overrides\\.ambiguousSessions"
---

<objective>
Implement the deterministic attribution engine, ambiguity rules, and stable override persistence for Phase 3.

Purpose: ATTR-03, ATTR-04, and ATTR-05 are the credibility core of this phase; the scan inventory from `03-01` is only useful once it can be classified conservatively and repeated safely.
Output: Attribution types, a reusable decision engine, override helpers keyed by stable session IDs, and tests that prove ambiguity and override behavior.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md
@.planning/phases/03-historical-backfill-and-attribution-review/03-01-PLAN.md
@packages/core/src/repo/repo-fingerprint.ts
@packages/core/src/providers/session-summary.ts
@packages/core/src/state/state-schema.ts
@packages/core/src/scan/full-backfill.ts

<interfaces>
From `packages/core/src/state/state-schema.ts`:

```ts
export interface AgentBadgeState {
  checkpoints: { codex: { cursor: string | null; lastScannedAt: string | null }; claude: { cursor: string | null; lastScannedAt: string | null } };
  overrides: { ambiguousSessions: Record<string, "include" | "exclude"> };
}
```

From `packages/core/src/providers/session-summary.ts` after `03-01`:

```ts
export interface NormalizedSessionSummary {
  provider: "codex" | "claude";
  providerSessionId: string;
  cwd: string | null;
  observedRemoteUrlNormalized: string | null;
  attributionHints: {
    cwdRealPath: string | null;
    transcriptProjectKey: string | null;
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define attribution result types and deterministic evidence ordering</name>
  <files>packages/core/src/attribution/attribution-types.ts, packages/core/src/attribution/attribution-engine.ts, packages/core/src/attribution/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/repo/repo-fingerprint.ts, packages/core/src/providers/session-summary.ts, packages/core/src/scan/full-backfill.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Create `packages/core/src/attribution/attribution-types.ts` defining `AttributionEvidenceKind = "repo-root" | "git-remote" | "normalized-cwd" | "transcript-correlation" | "user-override"`, `AttributionStatus = "included" | "ambiguous" | "excluded"`, and an `AttributedSession` shape that includes `session`, `status`, `evidence`, `reason`, and `overrideApplied`. Create `packages/core/src/attribution/attribution-engine.ts` exporting `attributeBackfillSessions({ repo, sessions, overrides })`. The engine must classify each session using this order: exact repo root when `attributionHints.cwdRealPath === repo.gitRootRealPath`; exact remote when `observedRemoteUrlNormalized` equals `repo.originUrlNormalized` or an alias remote; normalized cwd when `cwdRealPath` starts with `${repo.gitRootRealPath}/`; transcript correlation when `attributionHints.transcriptProjectKey` equals the current repo realpath rewritten into Claude's hyphen-joined project-key form; then override reuse from the persisted map. If no evidence matches, mark the session `excluded`. If only weak evidence exists or conflicting weak evidence would make ownership unclear, mark it `ambiguous`. Export the new modules from `packages/core/src/attribution/index.ts` and re-export `./attribution/index.js` from `packages/core/src/index.ts`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/attribution/attribution-types.ts` contains `"repo-root" | "git-remote" | "normalized-cwd" | "transcript-correlation" | "user-override"`
    - `packages/core/src/attribution/attribution-types.ts` contains `"included" | "ambiguous" | "excluded"`
    - `packages/core/src/attribution/attribution-engine.ts` contains `export function attributeBackfillSessions`
    - `packages/core/src/attribution/attribution-engine.ts` contains `repo.gitRootRealPath`
    - `packages/core/src/attribution/attribution-engine.ts` contains `repo.originUrlNormalized`
    - `packages/core/src/attribution/attribution-engine.ts` contains `transcriptProjectKey`
    - `packages/core/src/index.ts` contains `./attribution/index.js`
  </acceptance_criteria>
  <done>Phase 3 now has a reusable attribution engine with explicit result types instead of hiding repo-matching rules in CLI code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stable ambiguous-session keys and override helpers</name>
  <files>packages/core/src/attribution/override-store.ts, packages/core/src/attribution/attribution-engine.ts</files>
  <read_first>packages/core/src/state/state-schema.ts, packages/core/src/attribution/attribution-types.ts, packages/core/src/attribution/attribution-engine.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-RESEARCH.md, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Create `packages/core/src/attribution/override-store.ts` exporting `buildAmbiguousSessionKey(session)`, `readAmbiguousSessionOverride(state, session)`, and `applyAmbiguousSessionDecision(state, sessionKey, decision)` where the stable key is exactly `${provider}:${providerSessionId}` and `decision` is `"include"` or `"exclude"`. Update `attributeBackfillSessions()` so a stored override becomes the final include/exclude status while preserving the raw evidence reason in the returned object. Do not store any raw `cwd`, transcript-path, or remote strings in the override map; store only the stable session key and the include/exclude choice.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/attribution/override-store.ts` contains `export function buildAmbiguousSessionKey`
    - `packages/core/src/attribution/override-store.ts` contains `session.provider`
    - `packages/core/src/attribution/override-store.ts` contains `session.providerSessionId`
    - `packages/core/src/attribution/override-store.ts` contains `applyAmbiguousSessionDecision`
    - `packages/core/src/attribution/attribution-engine.ts` contains `user-override`
    - `packages/core/src/attribution/attribution-engine.ts` contains `overrideApplied`
  </acceptance_criteria>
  <done>Explicit developer decisions can now be persisted and reused without inflating the stored state surface.</done>
</task>

<task type="auto">
  <name>Task 3: Prove ambiguity exclusion and override reuse with attribution tests</name>
  <files>packages/core/src/attribution/attribution-engine.test.ts</files>
  <read_first>packages/core/src/attribution/attribution-engine.ts, packages/core/src/attribution/override-store.ts, packages/core/src/repo/repo-fingerprint.ts, packages/core/src/state/state-schema.ts, .planning/phases/03-historical-backfill-and-attribution-review/03-VALIDATION.md</read_first>
  <action>Create `packages/core/src/attribution/attribution-engine.test.ts` that covers at least five behaviors with exact test names: `prefers repo-root over remote-only evidence`, `prefers normalized remote over cwd-only evidence`, `marks ambiguous sessions excluded from included totals`, `reuses stored include and exclude overrides`, and `does not persist raw cwd or transcript values in override keys`. Build test sessions directly as normalized summaries plus attribution hints instead of relying on the CLI. Assert that ambiguous sessions stay out of included totals until an override exists and that applying `include` or `exclude` decisions through the helper functions produces the same final outcome on repeated runs.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/attribution/attribution-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/attribution/attribution-engine.test.ts` contains `prefers repo-root over remote-only evidence`
    - `packages/core/src/attribution/attribution-engine.test.ts` contains `prefers normalized remote over cwd-only evidence`
    - `packages/core/src/attribution/attribution-engine.test.ts` contains `marks ambiguous sessions excluded from included totals`
    - `packages/core/src/attribution/attribution-engine.test.ts` contains `reuses stored include and exclude overrides`
    - `packages/core/src/attribution/attribution-engine.test.ts` contains `does not persist raw cwd or transcript values in override keys`
  </acceptance_criteria>
  <done>ATTR-03, ATTR-04, and ATTR-05 are protected by targeted unit tests that exercise the exact decision ordering and override semantics.</done>
</task>

</tasks>

<verification>
- [ ] `npm run typecheck` succeeds after adding the attribution module
- [ ] `npm test -- --run packages/core/src/attribution/attribution-engine.test.ts` succeeds
- [ ] `packages/core/src/attribution/attribution-engine.ts` classifies ambiguous sessions separately from included totals
- [ ] `packages/core/src/attribution/override-store.ts` stores only stable session keys plus include/exclude decisions
</verification>

<success_criteria>
- ATTR-03 is implemented as deterministic evidence ordering
- ATTR-04 excludes ambiguous sessions from included totals by default
- ATTR-05 reuses persisted overrides through a stable session key, not raw evidence blobs
</success_criteria>

<output>
After completion, create `.planning/phases/03-historical-backfill-and-attribution-review/03-02-SUMMARY.md`
</output>

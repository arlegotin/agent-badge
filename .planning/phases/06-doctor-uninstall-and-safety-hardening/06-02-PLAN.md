---
phase: 06-doctor-uninstall-and-safety-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/core/src/init/runtime-wiring.ts
  - packages/core/src/publish/github-gist-client.ts
  - packages/core/src/publish/publish-target.ts
  - packages/core/src/publish/index.ts
  - packages/agent-badge/src/commands/uninstall.ts
  - packages/agent-badge/src/commands/index.ts
  - packages/agent-badge/src/cli/main.ts
  - packages/agent-badge/src/commands/init.test.ts
  - packages/core/src/init/runtime-wiring.test.ts
autonomous: true
requirements:
  - OPER-05
  - SAFE-04
user_setup: []
must_haves:
  truths:
    - "uninstall removes local hook/scripts/gitignore integration without touching unrelated repo content."
    - "re-run `agent-badge init` after uninstall converges cleanly and never creates duplicate managed blocks."
    - "remote gist deletion/purge is only executed when `--purge-remote` is explicitly passed."
  artifacts:
    - path: packages/core/src/init/runtime-wiring.ts
      provides: inverse-managed wiring removal helpers for uninstall
      contains: "removeRepoLocalRuntimeWiring"
    - path: packages/core/src/publish/publish-target.ts
      provides: optional remote-purge helper
      contains: "deletePublishTarget"
    - path: packages/agent-badge/src/commands/uninstall.ts
      provides: uninstall command with safe defaults and flags
      contains: "runUninstallCommand"
    - path: packages/agent-badge/src/commands/uninstall.test.ts
      provides: uninstall CLI behavior and idempotency tests
      contains: "runUninstallCommand"
  key_links:
    - from: packages/agent-badge/src/commands/uninstall.ts
      to: packages/core/src/init/runtime-wiring.ts
      via: "marker-safe local runtime cleanup"
      pattern: "removeRepoLocalRuntimeWiring"
    - from: packages/core/src/init/runtime-wiring.ts
      to: packages/core/src/init/runtime-wiring.test.ts
      via: "single managed block removal proof"
      pattern: "# agent-badge:start"
    - from: packages/core/src/publish/publish-target.ts
      to: packages/core/src/publish/github-gist-client.ts
      via: "explicit remote purge when requested"
      pattern: "deleteGist"
---

<objective>
Create `agent-badge uninstall` and the inverse wiring logic required for safe cleanup and safe re-entry.

Purpose: OPER-05 and SAFE-04 require reversible integration removal and deterministic re-init behavior: uninstall should clean local hooks/scripts without collateral edits, and explicit flags should guard any remote deletion.
Output: uninstall command, core inverse-wiring function, remote purge helper, and tests proving idempotent re-init without duplicates.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-doctor-uninstall-and-safety-hardening/06-RESEARCH.md
@packages/core/src/init/runtime-wiring.ts
@packages/core/src/init/runtime-wiring.test.ts
@packages/core/src/publish/github-gist-client.ts
@packages/core/src/publish/publish-target.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/init.test.ts
@packages/agent-badge/src/commands/index.ts
@packages/agent-badge/src/cli/main.ts

<interfaces>
From `packages/core/src/init/runtime-wiring.ts`:

```ts
export async function applyRepoLocalRuntimeWiring(
  options: ApplyRepoLocalRuntimeWiringOptions
): Promise<RepoLocalRuntimeWiringResult>;
```

From `packages/core/src/init/runtime-wiring.ts` marker constants:

```ts
export const agentBadgeHookStartMarker = "# agent-badge:start";
export const agentBadgeHookEndMarker = "# agent-badge:end";
export const agentBadgeGitignoreStartMarker = "# agent-badge:gitignore:start";
export const agentBadgeGitignoreEndMarker = "# agent-badge:gitignore:end";
```

From `packages/core/src/publish/github-gist-client.ts`:

```ts
export interface GitHubGist {
  readonly id: string;
  readonly ownerLogin: string | null;
  readonly public: boolean;
  readonly files: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add core uninstall primitives for marker-safe runtime cleanup and optional remote purge</name>
  <files>packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/core/src/publish/github-gist-client.ts, packages/core/src/publish/github-gist-client.test.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/publish-target.test.ts, packages/core/src/publish/index.ts</files>
  <read_first>packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/core/src/publish/github-gist-client.ts, packages/core/src/publish/github-gist-client.test.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/publish-target.test.ts, packages/core/src/publish/index.ts</read_first>
  <action>In `packages/core/src/init/runtime-wiring.ts`, add `removeRepoLocalRuntimeWiring(options)` with input `{ cwd: string, packageManager: PackageManager }`. Reuse existing marker constants and `stripManagedHookBlock`/`stripManagedGitignoreBlock` logic to:
    - remove `package.json` scripts `agent-badge:init` and `agent-badge:refresh` when present
    - remove `devDependencies.agent-badge` only when value was injected by runtime tooling
    - remove managed blocks from `.gitignore` and `.git/hooks/pre-push`
    - when pre-push hook has only managed content, rewrite to `#!/bin/sh\n` and preserve executable bit
    - preserve unrelated package.json fields and unmanaged hook lines.
  In `packages/core/src/publish/github-gist-client.ts`, add `deleteGist` to `GitHubGistClient` and Octokit wrapper.
  In `packages/core/src/publish/publish-target.ts`, add `deletePublishTarget({ gistId, client })` that calls `client.deleteGist` and returns a typed bool-like result. Export in `packages/core/src/publish/index.ts`.
  Add tests in `packages/core/src/init/runtime-wiring.test.ts`, `packages/core/src/publish/github-gist-client.test.ts`, and `packages/core/src/publish/publish-target.test.ts`:
  - removing only managed hook blocks
  - keeping unrelated hook lines
  - removing marker-managed .gitignore entries only
  - delete method wiring exists and calls octokit `rest.gists.remove` path.</action>
  <acceptance_criteria>
    - `packages/core/src/init/runtime-wiring.ts` contains `removeRepoLocalRuntimeWiring`
    - `packages/core/src/init/runtime-wiring.ts` contains `agent-badge:init`
    - `packages/core/src/init/runtime-wiring.ts` contains `agent-badge:refresh`
    - `packages/core/src/init/runtime-wiring.ts` contains `agentBadgeHookStartMarker`
    - `packages/core/src/publish/github-gist-client.ts` contains `deleteGist`
    - `packages/core/src/publish/publish-target.ts` contains `deletePublishTarget`
    - `packages/core/src/publish/index.ts` contains `deletePublishTarget`
    - `packages/core/src/publish/github-gist-client.ts` contains `rest.gists.delete`
    - `packages/core/src/init/runtime-wiring.test.ts` contains `removeRepoLocalRuntimeWiring`
  </acceptance_criteria>
  <verify>npm test -- --run packages/core/src/init/runtime-wiring.test.ts packages/core/src/publish/github-gist-client.test.ts packages/core/src/publish/publish-target.test.ts packages/core/src/diagnostics/doctor.test.ts</verify>
  <done>Core uninstall primitives support deterministic, marker-safe local cleanup and explicit remote removal without touching unmanaged repo files.</done>
</task>

<task type="auto">
  <name>Task 2: Add `agent-badge uninstall` command and CLI registration</name>
  <files>packages/agent-badge/src/commands/uninstall.ts, packages/agent-badge/src/commands/uninstall.test.ts, packages/agent-badge/src/commands/index.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts</files>
  <read_first>packages/agent-badge/src/commands/uninstall.ts, packages/agent-badge/src/commands/uninstall.test.ts, packages/agent-badge/src/commands/index.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/publish/publish-target.ts, packages/core/src/publish/index.ts</read_first>
  <action>Create `runUninstallCommand` in `packages/agent-badge/src/commands/uninstall.ts` with options:
  - `cwd?`
  - `purgeRemote?`
  - `purgeConfig?`
  - `purgeState?`
  - `purgeLogs?`
  - `purgeCaches?`
  - `force?`
  - `env?`
  - `stdout?`
  
Default behavior must remove only local runtime integration artifacts (managed scripts, managed hook block, managed .gitignore entries) and local cache/logs, while preserving `config.json`/`state.json` unless purge flags are set. If `purgeRemote` is set, call `deletePublishTarget` and remove local publish ids from config when successful. Implement idempotent output with:
  - `- uninstall: start`
  - `- remove: ...`
  - `- keep-local: ...` when defaults are used
  - `- remote: preserved` unless explicit purge.
  Register `uninstall` in `cli/main.ts` with `--purge-remote`, `--purge-config`, `--purge-state`, `--purge-logs`, `--purge-cache`, `--force` and export command in `commands/index.ts`.
  Add tests in `packages/agent-badge/src/commands/uninstall.test.ts` for:
  - removes managed hook start/end markers on first run
  - preserves custom pre-push lines outside markers
  - defaults do not delete remote config/gist
  - `--purge-remote` clears gist association (or deletes gist via mock)</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/uninstall.ts` contains `runUninstallCommand`
    - `packages/agent-badge/src/commands/uninstall.ts` contains `--purge-remote`
    - `packages/agent-badge/src/commands/uninstall.ts` contains `default` in text output
    - `packages/agent-badge/src/commands/uninstall.ts` contains `removeRepoLocalRuntimeWiring`
    - `packages/agent-badge/src/commands/uninstall.ts` contains `removeConfig`
    - `packages/agent-badge/src/commands/uninstall.test.ts` contains `runUninstallCommand`
    - `packages/agent-badge/src/commands/uninstall.test.ts` contains `preserves .agent-badge/state.json`
    - `packages/agent-badge/src/cli/main.ts` contains `.command("uninstall")`
    - `packages/agent-badge/src/cli/main.ts` contains `--purge-remote`
    - `packages/agent-badge/src/commands/index.ts` contains `runUninstallCommand`
  </acceptance_criteria>
  <verify>npm test -- --run packages/agent-badge/src/commands/uninstall.test.ts packages/agent-badge/src/cli/main.test.ts</verify>
  <done>Uninstall becomes a safe, explicit command path with clear defaults and explicit remote deletion only under explicit flags.</done>
</task>

<task type="auto">
  <name>Task 3: Add re-init reconciliation tests for safe re-entry and no duplicate integration</name>
  <files>packages/agent-badge/src/commands/init.test.ts, packages/core/src/init/runtime-wiring.test.ts</files>
  <read_first>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/uninstall.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/agent-badge/src/commands/init.test.ts</read_first>
  <action>Extend `packages/agent-badge/src/commands/init.test.ts` with a regression that runs: `init` → `uninstall` → `init` in same repo and asserts:
  - exactly one managed hook block exists after re-run
  - `package.json` retains unmanaged scripts and devDependencies
  - `agent-badge:init` and `agent-badge:refresh` scripts are restored if missing
  - no duplicate `AGENT_BADGE_README_MARKER` insertion occurs across reruns
  - idempotent uninstall is re-runnable without throwing and returns stable summary.
  Add/adjust a minimal coverage test in `packages/core/src/init/runtime-wiring.test.ts` that calls the remove function twice and asserts second run produces warnings-only or empty updates with no unmanaged changes.
  These tests prove SAFE-04 behavior after uninstall/re-entry.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/init.test.ts` contains `runUninstallCommand`
    - `packages/agent-badge/src/commands/init.test.ts` contains `exactly one managed hook block`
    - `packages/agent-badge/src/commands/init.test.ts` contains `does not duplicate`
    - `packages/agent-badge/src/commands/init.test.ts` contains `agent-badge:init`
    - `packages/core/src/init/runtime-wiring.test.ts` contains `removeRepoLocalRuntimeWiring`
    - `packages/core/src/init/runtime-wiring.test.ts` contains `second run`
  </acceptance_criteria>
  <verify>npm test -- --run packages/agent-badge/src/commands/init.test.ts packages/core/src/init/runtime-wiring.test.ts</verify>
  <done>SAFE-04 is operationally verifiable: uninstall/re-init paths stay idempotent and cannot create duplicate managed hooks or scripts.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/agent-badge/src/commands/uninstall.test.ts packages/agent-badge/src/commands/init.test.ts packages/core/src/init/runtime-wiring.test.ts` succeeds
- Uninstall defaults leave local remote publish IDs intact and only remove them when `--purge-remote` is passed
- Re-running uninstall is safe and does not remove custom hook lines
</verification>

<success_criteria>
- OPER-05: `uninstall` removes local integration without destructive defaults and only purges remote publish state when explicitly requested.
- SAFE-04: repeat `init` after uninstall remains idempotent with no duplicated hooks/scripts/README markers.
</success_criteria>

<output>
After completion, create `.planning/phases/06-doctor-uninstall-and-safety-hardening/06-02-SUMMARY.md`
</output>

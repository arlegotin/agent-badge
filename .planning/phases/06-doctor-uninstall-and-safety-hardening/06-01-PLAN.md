---
phase: 06-doctor-uninstall-and-safety-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/diagnostics/doctor.ts
  - packages/core/src/diagnostics/doctor.test.ts
  - packages/core/src/diagnostics/index.ts
  - packages/core/src/index.ts
  - packages/agent-badge/src/commands/doctor.ts
  - packages/agent-badge/src/commands/doctor.test.ts
  - packages/agent-badge/src/commands/index.ts
  - packages/agent-badge/src/cli/main.ts
  - packages/agent-badge/src/cli/main.test.ts
autonomous: true
requirements:
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - "doctor runs by default in read-only mode and emits pass/warn/fail checks for git root, provider dirs, scan-readiness, gist auth/write, Shields URL, README badge marker, and hook wiring."
    - "doctor prints actionable remediation commands for each failing or warning-level check so reruns can be executed safely."
    - "doctor --json outputs a deterministic machine-readable result object with check IDs and status."
  artifacts:
    - path: packages/core/src/diagnostics/doctor.ts
      provides: typed doctor check contract and result shape
      contains: "runDoctorChecks"
    - path: packages/agent-badge/src/commands/doctor.ts
      provides: CLI command wrapper and output formatting
      contains: "runDoctorCommand"
    - path: packages/agent-badge/src/cli/main.ts
      provides: CLI registration for `agent-badge doctor`
      contains: "doctor"
  key_links:
    - from: packages/core/src/diagnostics/doctor.ts
      to: packages/core/src/init/preflight.ts
      via: "source of non-mutating repo and provider discovery state"
      pattern: "runInitPreflight"
    - from: packages/agent-badge/src/commands/doctor.ts
      to: packages/core/src/diagnostics/doctor.ts
      via: "delegates checks to typed core contract"
      pattern: "runDoctorChecks"
    - from: packages/agent-badge/src/cli/main.ts
      to: packages/agent-badge/src/commands/doctor.ts
      via: "invokes command action and JSON flag"
      pattern: "runDoctorCommand"
---

<objective>
Build the `doctor` command as the production recovery surface for initialization, provider scanning, publish wiring, badge placement, and hook state validation, with explicit fix guidance and optional write-probe checks.

Purpose: SAFE-03 requires a fast, repeatable operator recovery command that identifies common setup failures and tells operators exactly how to fix them, including publish auth/write and hook/README health.
Output: one robust `doctor` command, one core check service, and tests for both structured and human-readable output.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-doctor-uninstall-and-safety-hardening/06-RESEARCH.md
@packages/core/src/init/preflight.ts
@packages/core/src/init/github-auth.ts
@packages/core/src/init/provider-detection.ts
@packages/core/src/repo/git-context.ts
@packages/core/src/publish/github-gist-client.ts
@packages/core/src/publish/readme-badge.ts
@packages/core/src/init/runtime-wiring.ts
@packages/core/src/runtime/local-cli.ts
@packages/core/src/index.ts

<interfaces>
From `packages/core/src/init/preflight.ts`:

```ts
export interface InitPreflightResult {
  readonly cwd: string;
  readonly git: GitContext;
  readonly readme: InitReadmeStatus;
  readonly packageManager: InitPackageManagerStatus;
  readonly providers: ProviderDetectionResult;
  readonly githubAuth: GitHubAuthStatus;
  readonly existingScaffold: ExistingScaffoldStatus;
}

export async function runInitPreflight(
  options: RunInitPreflightOptions = {}
): Promise<InitPreflightResult>;
```

From `packages/core/src/publish/readme-badge.ts`:

```ts
export const AGENT_BADGE_README_START_MARKER = "<!-- agent-badge:start -->";
export const AGENT_BADGE_README_END_MARKER = "<!-- agent-badge:end -->";
```

From `packages/core/src/github-gist-client.ts`:

```ts
export interface GitHubGistClient {
  getGist(gistId: string): Promise<GitHubGist>;
  createPublicGist(input: CreatePublicGistInput): Promise<GitHubGist>;
  updateGistFile(input: UpdateGistFileInput): Promise<GitHubGist>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add core `doctor` check contracts and read-only health probes</name>
  <files>packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/core/src/diagnostics/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/init/preflight.ts, packages/core/src/init/github-auth.ts, packages/core/src/init/provider-detection.ts, packages/core/src/publish/github-gist-client.ts, packages/core/src/publish/readme-badge.ts, packages/core/src/repo/git-context.ts, packages/core/src/scan/refresh-cache.ts, packages/core/src/diagnostics/doctor.test.ts, packages/core/src/index.ts</read_first>
  <action>Implement `packages/core/src/diagnostics/doctor.ts` with `export type DoctorCheckStatus = "pass" | "warn" | "fail"`, `DoctorCheck`, `DoctorCheckResult`, and `export interface RunDoctorChecksOptions { cwd?: string; homeRoot?: string; env?: NodeJS.ProcessEnv; gistClient?: GitHubGistClient; runProbeWrite?: boolean; }`. Add `runDoctorChecks` that performs read-only checks (unless `runProbeWrite`) with concrete IDs: `git`, `providers`, `scan-access`, `publish-auth`, `publish-write`, `publish-shields`, `readme-badge`, `pre-push-hook`. For each check return `status`, `message`, `detail`, and `fix` suggestions only. Use existing helpers:
    - `runInitPreflight` for git/provider/auth/readme/package manager scaffolding status
    - `createGitHubGistClient` + `client.getGist` for gist read checks
    - `AGENT_BADGE_README_START_MARKER`/`AGENT_BADGE_README_END_MARKER` for README idempotency inspection
    - `agentBadgeHookStartMarker`/`agentBadgeHookEndMarker` + `.git/hooks/pre-push` for hook inspection
    - `http(s)://` GET to badge URL only when `--probe-write` is not needed? (no write side effects in default path).
  Add a fail-soft probe path for `publish-write` when `runProbeWrite` is true: fetch gist, then do a no-op `updateGistFile` call using current `agent-badge.json` content shape to validate write credentials without changing message semantics. Do not require this in default mode. Keep all returned diagnostics free of absolute filesystem paths and prompt/body text by design.
  Include summary fields in the returned object: total, passCount, warnCount, failCount, and `overallStatus` (`fail` when any fail exists, else `warn` when any warning exists, else `pass`). Export from `packages/core/src/diagnostics/index.ts` and `packages/core/src/index.ts`.</action>
  <acceptance_criteria>
    - `packages/core/src/diagnostics/doctor.ts` contains `runDoctorChecks`
    - `packages/core/src/diagnostics/doctor.ts` contains `DoctorCheckStatus`
    - `packages/core/src/diagnostics/doctor.ts` contains `check.id`
    - `packages/core/src/diagnostics/doctor.ts` contains `runProbeWrite`
    - `packages/core/src/diagnostics/doctor.ts` contains `overallStatus`
    - `packages/core/src/diagnostics/doctor.ts` contains `publish-write`
    - `packages/core/src/diagnostics/doctor.ts` contains `pre-push-hook`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `runDoctorChecks`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `publish auth`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `AGENT_BADGE_README_START_MARKER`
    - `packages/core/src/diagnostics/index.ts` contains `export * from "./doctor.js"`
  </acceptance_criteria>
  <verify>npm test -- --run packages/core/src/diagnostics/doctor.test.ts</verify>
  <done>Core doctor service produces structured, reproducible diagnostics with concrete, non-sensitive output and optional write-credential probe behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add `agent-badge doctor` command, output formats, and CLI wiring</name>
  <files>packages/agent-badge/src/commands/doctor.ts, packages/agent-badge/src/commands/doctor.test.ts, packages/agent-badge/src/commands/index.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts</files>
  <read_first>packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/index.ts, packages/agent-badge/src/commands/doctor.ts, packages/agent-badge/src/commands/doctor.test.ts, packages/agent-badge/src/commands/index.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts</read_first>
    <action>Create `runDoctorCommand` in `packages/agent-badge/src/commands/doctor.ts` with options:
  - `cwd?`
  - `homeRoot?`
  - `env?`
  - `json?`
  - `probeWrite?`
  - `gistClient?`

The command must parse `--json`, `--probe-write`, and call `runDoctorChecks`; print:
  - `agent-badge doctor`
  - `- <check-id>: pass|warn|fail`
  - `- Fix: <actionable command>`

Keep default mode read-only and mutation-free. In JSON mode write `JSON.stringify(result, null, 2)`. Register command in `packages/agent-badge/src/cli/main.ts` as `doctor` with options `--probe-write` and `--json`; add `runDoctorCommand` export in `packages/agent-badge/src/commands/index.ts`.
Add tests:
  - command returns structured result in json mode
  - doctor text output contains all required check IDs
  - at least one failing check prints a fix string
  - CLI test includes `doctor` and option flags `--probe-write` and `--json`</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/doctor.ts` contains `runDoctorCommand`
    - `packages/agent-badge/src/commands/doctor.ts` contains `--json`
    - `packages/agent-badge/src/commands/doctor.ts` contains `--probe-write`
    - `packages/agent-badge/src/commands/doctor.ts` contains `agent-badge doctor`
    - `packages/agent-badge/src/commands/doctor.test.ts` contains `runDoctorCommand`
    - `packages/agent-badge/src/commands/doctor.test.ts` contains `--json`
    - `packages/agent-badge/src/commands/doctor.test.ts` contains `Fix:`
    - `packages/agent-badge/src/cli/main.ts` contains `.command("doctor")`
    - `packages/agent-badge/src/commands/index.ts` contains `runDoctorCommand`
    - `packages/agent-badge/src/cli/main.test.ts` contains `doctor`
  </acceptance_criteria>
  <verify>npm test -- --run packages/agent-badge/src/commands/doctor.test.ts packages/agent-badge/src/cli/main.test.ts packages/core/src/diagnostics/doctor.test.ts</verify>
  <done>The CLI exposes a single `doctor` entrypoint that operators and automation can run for deterministic recovery guidance.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/doctor.test.ts packages/agent-badge/src/cli/main.test.ts` succeeds
- `runDoctorChecks` does not write files or mutate scaffold when `runProbeWrite` is not set
- `agent-badge doctor` checks and reports all required IDs: `git`, `providers`, `scan-access`, `publish-auth`, `publish-write`, `publish-shields`, `readme-badge`, `pre-push-hook`
</verification>

<success_criteria>
- SAFE-03 is met because `doctor` verifies repo initialization prerequisites, provider detection/access, publish auth/write, Shields availability, README badge marker state, and hook wiring.
- Fix guidance is actionable and grep-anchored in command output.
- No raw prompts/transcripts/absolute paths are added to diagnostics output.
</success_criteria>

<output>
After completion, create `.planning/phases/06-doctor-uninstall-and-safety-hardening/06-01-SUMMARY.md`
</output>

---
phase: 06-doctor-uninstall-and-safety-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - packages/core/src/logging/log.ts
  - packages/core/src/logging/index.ts
  - packages/agent-badge/src/commands/scan.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/agent-badge/src/commands/init.test.ts
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-04
user_setup: []
must_haves:
  truths:
    - "Scan, publish, and refresh commands write one newline-delimited log row per run under `.agent-badge/logs/`."
    - "Log rotation keeps only the newest bounded window of log files under `.agent-badge/logs/`."
    - "Published outputs and log rows contain only aggregate counts and operation metadata, never raw paths/transcripts/prompts."
    - "`init` and `publish` repeated calls remain idempotent and do not create duplicate managed artifacts."
  artifacts:
    - path: packages/core/src/logging/log.ts
      provides: log entry writer + rotation service
      contains: "appendAgentBadgeLog"
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: refresh run logging
      contains: "appendAgentBadgeLog"
    - path: packages/agent-badge/src/commands/publish.ts
      provides: publish run logging
      contains: "appendAgentBadgeLog"
  key_links:
    - from: packages/core/src/logging/log.ts
      to: packages/core/src/logging/log-entry.ts
      via: "validated log shape"
      pattern: "agentBadgeLogEntrySchema.parse"
    - from: packages/agent-badge/src/commands/refresh.ts
      to: packages/core/src/logging/log.ts
      via: "operation log emission"
      pattern: "refresh"
    - from: packages/agent-badge/src/commands/publish.ts
      to: packages/core/src/publish/publish-service.ts
      via: "same aggregate-only payload path"
      pattern: "publishBadgeToGist"
    - from: packages/agent-badge/src/commands/init.test.ts
      to: packages/core/src/logging/log.ts
      via: "idempotent setup/publish regression coverage"
      pattern: ".agent-badge/logs/"
---

<objective>
Build observability and hardening safeguards so phase-6 safety outcomes are traceable without exposing sensitive local details and remain bounded by disk constraints.

Purpose: SAFE-02 requires bounded logs under `.agent-badge/logs/`, and SAFE-01 requires all published and logged outputs to remain aggregate-only.
Output: new log writer/rotation service, command-level log emission for scan/publish/refresh, and regression tests proving idempotent setup/publish behavior without duplicates or sensitive leakage.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-doctor-uninstall-and-safety-hardening/06-RESEARCH.md
@packages/core/src/logging/log-entry.ts
@packages/core/src/logging/index.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/publish/badge-payload.ts
@packages/agent-badge/src/commands/scan.ts
@packages/agent-badge/src/commands/refresh.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/commands/init.test.ts

<interfaces>
From `packages/core/src/logging/log-entry.ts`:

```ts
export const agentBadgeLogEntrySchema = z
  .object({
    timestamp: z.string().datetime({ offset: true }),
    operation: z.string().min(1),
    status: z.enum(["success", "failure", "skipped"]),
    durationMs: z.number().int().nonnegative(),
    counts: z
      .object({
        scannedSessions: z.number().int().nonnegative(),
        attributedSessions: z.number().int().nonnegative(),
        ambiguousSessions: z.number().int().nonnegative(),
        publishedRecords: z.number().int().nonnegative()
      })
      .strict()
  })
  .strict();
```

From `packages/core/src/publish/publish-service.ts`:

```ts
export interface PublishBadgeIfChangedResult {
  readonly state: AgentBadgeState;
  readonly decision: "published" | "skipped";
}
```

From `packages/agent-badge/src/commands/scan.ts`:

```ts
export interface ScanCommandResult {
  readonly scan: RunFullBackfillScanResult;
  readonly attribution: AttributeBackfillSessionsResult;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded summary logging + rotation under `.agent-badge/logs/`</name>
  <read_first>packages/core/src/logging/log-entry.ts, packages/core/src/logging/index.ts, packages/core/src/logging/log.ts, packages/core/src/logging/log.test.ts</read_first>
  <files>packages/core/src/logging/log.ts, packages/core/src/logging/log.test.ts, packages/core/src/logging/index.ts</files>
  <action>Create `packages/core/src/logging/log.ts` with:
  - `const AGENT_BADGE_LOG_DIR = ".agent-badge/logs"`.
  - `export const AGENT_BADGE_LOG_MAX_FILES = 7`.
  - typed helper `buildLogEntry({ operation, status, startAtMs, counts })` that computes `durationMs`.
  - `appendAgentBadgeLog({ cwd, entry })` that validates `agentBadgeLogEntrySchema.parse(entry)`, creates `<cwd>/.agent-badge/logs` when missing, appends one JSONL line per run to a deterministic date-based file name (e.g., `agent-badge-<YYYYMMDD>.jsonl`), and returns written path.
  - `rotateLogFiles({ cwd, maxFiles })` that keeps only the newest `AGENT_BADGE_LOG_MAX_FILES` log files and removes oldest files by `mtime` when over limit.
  - `listAgentBadgeLogFiles({ cwd })` utility for testability.
  Export `appendAgentBadgeLog` from `packages/core/src/logging/index.ts`.
  Add tests in `packages/core/src/logging/log.test.ts` for:
  - strict schema validation rejects non-aggregate fields (e.g., `prompt`, `filename`, `rawPath`)
  - writes one valid NDJSON row
  - deletes oldest logs when more than max files are present</action>
  <acceptance_criteria>
    - `packages/core/src/logging/log.ts` contains `appendAgentBadgeLog`
    - `packages/core/src/logging/log.ts` contains `AGENT_BADGE_LOG_DIR`
    - `packages/core/src/logging/log.ts` contains `AGENT_BADGE_LOG_MAX_FILES`
    - `packages/core/src/logging/log.ts` contains `rotateLogFiles`
    - `packages/core/src/logging/index.ts` contains `appendAgentBadgeLog`
    - `packages/core/src/logging/log.test.ts` contains `non-aggregate`
    - `packages/core/src/logging/log.test.ts` contains `.agent-badge/logs`
    - `packages/core/src/logging/log.test.ts` contains `JSON.stringify`
  </acceptance_criteria>
  <verify>npm test -- --run packages/core/src/logging/log.test.ts</verify>
  <done>Command-level log writes are schema-safe, bounded, and persisted as summaries only.</done>
</task>

<task type="auto">
  <name>Task 2: Emit summary logs for `scan`, `publish`, and `refresh`</name>
  <read_first>packages/core/src/logging/log.ts, packages/core/src/logging/log-entry.ts, packages/agent-badge/src/commands/scan.ts, packages/agent-badge/src/commands/scan.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts</read_first>
  <files>packages/agent-badge/src/commands/scan.ts, packages/agent-badge/src/commands/scan.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts</files>
  <action>
  In `runScanCommand`, capture `startAt`, then after state write emit `appendAgentBadgeLog` with:
  - `operation: "scan"`
  - `status: "failure"` when command throws
  - `status: "success"` otherwise
  - `counts.scannedSessions` from `scan.counts.scannedSessions`
  - `counts.attributedSessions` from `attribution.counts.included`
  - `counts.ambiguousSessions` from `attribution.counts.ambiguous`
  - `counts.publishedRecords: 0`

  In `runRefreshCommand`, emit one log after refresh resolution with:
  - `operation: "refresh"`
  - `status: "success" | "failure" | "skipped"`
  - `counts.scannedSessions` from `result.summary.includedSessions + result.summary.ambiguousSessions + result.summary.excludedSessions`
  - `counts.attributedSessions` from `result.summary.includedSessions`
  - `counts.ambiguousSessions` from `result.summary.ambiguousSessions`
  - `counts.publishedRecords` from `publishDecision === "published" ? 1 : 0`
  For `failed-soft`, emit `failure` with persisted state snapshot where available.

  In `runPublishCommand`, emit:
  - `operation: "publish"`
  - `status: "success"` on `publishBadgeToGist` success
  - `status: "failure"` on error
  - `counts.attributedSessions` from derived scan totals.
  Add tests in command tests that mock `appendAgentBadgeLog` and assert the emitted log shapes for each command path, with `cwd` resolved from temporary fixture repos.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/scan.ts` contains `appendAgentBadgeLog`
    - `packages/agent-badge/src/commands/refresh.ts` contains `appendAgentBadgeLog`
    - `packages/agent-badge/src/commands/publish.ts` contains `appendAgentBadgeLog`
    - `packages/agent-badge/src/commands/scan.test.ts` contains `runScanCommand`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `appendAgentBadgeLog`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `appendAgentBadgeLog`
    - `packages/agent-badge/src/commands/publish.ts` contains `operation: "publish"`
  </acceptance_criteria>
  <verify>npm test -- --run packages/agent-badge/src/commands/scan.test.ts packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/publish.test.ts</verify>
  <done>Execution paths now emit aggregate-only logs for operational diagnosis and replayability.</done>
</task>

<task type="auto">
  <name>Task 3: Add privacy and idempotency regression tests for published outputs + log artifacts</name>
  <read_first>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/init.test.ts</read_first>
  <files>packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/init.test.ts</files>
  <action>Strengthen test coverage that enforces SAFE-01 and SAFE-04 across publish paths:
  - In `packages/core/src/publish/publish-service.test.ts`, inspect the `updateGistFile` payload and assert JSON keys are exactly `schemaVersion`, `label`, `message`, `color` (no `prompt`, `messages`, `transcripts`, `cwd`, `path`, `home`, or `provider`-path material) and that `message` contains only aggregate totals.
  - In `packages/agent-badge/src/commands/publish.test.ts`, add a regression that runs publish twice with unchanged aggregates and assert second run reuses `lastPublishedHash`/state idempotently and does not mutate README markers.
  - In `packages/agent-badge/src/commands/init.test.ts`, add an idempotency assert that `init` + `publish` + `init` still produces:
    - one README badge marker
    - one managed pre-push block
    - one `.agent-badge/logs/` directory
    and does not duplicate these artifacts.
  - Add a guard test that reads the latest log JSONL row and grep-fails if any line contains `/home/`, `prompt=`, `.jsonl`, or local absolute path patterns.
  </action>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-service.test.ts` contains `buildEndpointBadgePayload`
    - `packages/core/src/publish/publish-service.test.ts` contains `agent-badge.json`
    - `packages/core/src/publish/publish-service.test.ts` contains `schemaVersion`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `runPublishCommand`
    - `packages/agent-badge/src/commands/init.test.ts` contains `AGENT_BADGE_README_END_MARKER`
    - `packages/agent-badge/src/commands/init.test.ts` contains `exactly one managed hook block`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `lastPublishedHash`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `do not duplicate`
  </acceptance_criteria>
  <verify>npm test -- --run packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/init.test.ts</verify>
  <done>Published payloads and logs are verifiably aggregate-only, and repeated setup/publish flows remain idempotent without artifact duplication.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/logging/log.test.ts packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/scan.test.ts packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/publish.test.ts packages/agent-badge/src/commands/init.test.ts` succeeds
- Log files exist under `<repo>/.agent-badge/logs` and never include path/transcript/prompt values
- `runRefreshCommand` and `runPublishCommand` emit one `appendAgentBadgeLog` entry per invocation
- Re-running setup/publish in tests does not duplicate README badge markers, hook blocks, or managed publish associations
</verification>

<success_criteria>
- SAFE-02 is met: logs are summaries only and rotated under `.agent-badge/logs/`.
- SAFE-01 is met: payloads and logged diagnostics never include prompts, transcripts, raw filenames, or local filesystem paths.
- SAFE-04 is met across publish/setup: repeated `init` and `publish` calls do not duplicate marker-managed outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-doctor-uninstall-and-safety-hardening/06-03-SUMMARY.md`
</output>

---
phase: 01-workspace-and-init-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/core/src/config/config-schema.ts
  - packages/core/src/config/index.ts
  - packages/core/src/state/state-schema.ts
  - packages/core/src/state/index.ts
  - packages/core/src/logging/log-entry.ts
  - packages/core/src/logging/index.ts
  - packages/core/src/runtime/package-manager.ts
  - packages/core/src/runtime/local-cli.ts
  - packages/core/src/runtime/index.ts
  - packages/core/src/index.ts
  - packages/core/src/config/config-schema.test.ts
  - packages/core/src/state/state-schema.test.ts
  - packages/core/src/runtime/local-cli.test.ts
  - packages/testkit/src/repo-fixtures.ts
  - packages/testkit/src/provider-fixtures.ts
  - packages/testkit/src/index.ts
autonomous: true
requirements:
  - BOOT-03
  - BOOT-04
user_setup: []
must_haves:
  truths:
    - The project has one schema-backed source of truth for `.agent-badge` config, state, and log-summary files.
    - The core runtime can generate package-manager-aware local execution commands for the installed `agent-badge` CLI.
    - Test helpers exist for temp repos and stubbed provider homes so init behavior can be verified offline.
  artifacts:
    - path: packages/core/src/config/config-schema.ts
      provides: Zod schema and defaults for `.agent-badge/config.json`
      min_lines: 40
      contains: "agentBadgeConfigSchema"
    - path: packages/core/src/state/state-schema.ts
      provides: Zod schema and defaults for `.agent-badge/state.json`
      min_lines: 40
      contains: "agentBadgeStateSchema"
    - path: packages/core/src/runtime/local-cli.ts
      provides: Local CLI command builders for scripts and hooks
      contains: "getLocalAgentBadgeCommand"
    - path: packages/testkit/src/repo-fixtures.ts
      provides: Temporary repo helpers for init tests
      contains: "createRepoFixture"
    - path: packages/testkit/src/provider-fixtures.ts
      provides: Stubbed provider-home helpers for preflight tests
      contains: "createProviderFixture"
  key_links:
    - from: packages/core/src/runtime/local-cli.ts
      to: packages/core/src/runtime/package-manager.ts
      via: package-manager-specific command generation
      pattern: "detectPackageManager|getPackageManager"
    - from: packages/core/src/index.ts
      to: packages/core/src/config/config-schema.ts
      via: re-export surface for later init commands
      pattern: "config-schema|config"
    - from: packages/testkit/src/index.ts
      to: packages/testkit/src/repo-fixtures.ts
      via: shared fixture exports
      pattern: "repo-fixtures|createRepoFixture"
---

<objective>
Define the shared `.agent-badge` schemas, filesystem/runtime primitives, and reusable test helpers that the init flow depends on.

Purpose: Prevent the init path from inventing config shapes, local command strings, and fixture helpers ad hoc inside command code.
Output: Core schema modules, package-manager-aware CLI command builders, and reusable repo/provider fixture helpers with automated coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-init-foundation/01-RESEARCH.md
@.planning/phases/01-workspace-and-init-foundation/01-VALIDATION.md
@packages/core/package.json
@packages/core/src/index.ts
@packages/testkit/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement schema-backed config, state, and log models</name>
  <files>packages/core/src/config/config-schema.ts, packages/core/src/config/index.ts, packages/core/src/state/state-schema.ts, packages/core/src/state/index.ts, packages/core/src/logging/log-entry.ts, packages/core/src/logging/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/config/config-schema.ts, packages/core/src/config/index.ts, packages/core/src/state/state-schema.ts, packages/core/src/state/index.ts, packages/core/src/logging/log-entry.ts, packages/core/src/logging/index.ts, packages/core/src/index.ts, packages/core/package.json, .planning/phases/01-workspace-and-init-foundation/01-RESEARCH.md, .planning/ROADMAP.md</read_first>
  <action>Create `packages/core/src/config/config-schema.ts` exporting `agentBadgeConfigSchema`, `defaultAgentBadgeConfig`, and `parseAgentBadgeConfig`. The config schema must include `version: 1`, `providers.codex.enabled`, `providers.claude.enabled`, `badge.label`, `badge.mode`, `publish.provider`, `publish.gistId`, `publish.badgeUrl`, `refresh.prePush.enabled`, `refresh.prePush.mode`, and `privacy.aggregateOnly`. Create `packages/core/src/state/state-schema.ts` exporting `agentBadgeStateSchema`, `defaultAgentBadgeState`, and `parseAgentBadgeState` with keys `version`, `init.initialized`, `init.scaffoldVersion`, `init.lastInitializedAt`, `checkpoints.codex`, `checkpoints.claude`, `publish.status`, `publish.gistId`, `publish.lastPublishedHash`, and `overrides.ambiguousSessions`. Create `packages/core/src/logging/log-entry.ts` exporting `agentBadgeLogEntrySchema` with `timestamp`, `operation`, `status`, `durationMs`, and aggregate `counts`. Re-export these modules from the package-specific index files and from `packages/core/src/index.ts`. Keep all defaults privacy-safe by excluding transcript content, filenames, and local absolute paths.</action>
  <verify>npm test -- --run packages/core/src/config/config-schema.test.ts packages/core/src/state/state-schema.test.ts</verify>
  <acceptance_criteria>
    - packages/core/src/config/config-schema.ts contains `export const agentBadgeConfigSchema`
    - packages/core/src/config/config-schema.ts contains `aggregateOnly`
    - packages/core/src/state/state-schema.ts contains `export const agentBadgeStateSchema`
    - packages/core/src/state/state-schema.ts contains `ambiguousSessions`
    - packages/core/src/logging/log-entry.ts contains `export const agentBadgeLogEntrySchema`
    - packages/core/src/index.ts re-exports config, state, logging, and runtime modules
  </acceptance_criteria>
  <done>The core package has stable schemas and defaults for Phase 1 scaffolding plus later publish/checkpoint work.</done>
</task>

<task type="auto">
  <name>Task 2: Implement package-manager-aware local CLI command builders</name>
  <files>packages/core/src/runtime/package-manager.ts, packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/index.ts, packages/core/src/index.ts, packages/core/src/runtime/local-cli.test.ts</files>
  <read_first>packages/core/src/runtime/package-manager.ts, packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/index.ts, packages/core/src/index.ts, packages/core/package.json, package.json, .planning/phases/01-workspace-and-init-foundation/01-RESEARCH.md</read_first>
  <action>Create `packages/core/src/runtime/package-manager.ts` with a `packageManagerSchema` enum for `npm`, `pnpm`, `yarn`, and `bun`, plus a `detectPackageManager` helper that prefers lockfiles and falls back to `npm`. Create `packages/core/src/runtime/local-cli.ts` exporting `getLocalAgentBadgeCommand(packageManager)` and `getPrePushRefreshCommand(packageManager)` returning exact strings `npx --no-install agent-badge`, `pnpm exec agent-badge`, `yarn agent-badge`, or `bunx --bun agent-badge`, and pre-push variants that append `refresh --hook pre-push --fail-soft`. Re-export runtime helpers from `packages/core/src/runtime/index.ts` and `packages/core/src/index.ts`. Add `packages/core/src/runtime/local-cli.test.ts` asserting the exact command strings for all supported package managers.</action>
  <verify>npm test -- --run packages/core/src/runtime/local-cli.test.ts</verify>
  <acceptance_criteria>
    - packages/core/src/runtime/package-manager.ts contains `z.enum(["npm", "pnpm", "yarn", "bun"])`
    - packages/core/src/runtime/local-cli.ts contains `npx --no-install agent-badge`
    - packages/core/src/runtime/local-cli.ts contains `refresh --hook pre-push --fail-soft`
    - packages/core/src/runtime/local-cli.test.ts contains `pnpm exec agent-badge`
    - packages/core/src/runtime/local-cli.test.ts contains `bunx --bun agent-badge`
  </acceptance_criteria>
  <done>The project has one canonical way to generate repo-local CLI and hook commands across supported package managers.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixture helpers and automated coverage for schema/runtime primitives</name>
  <files>packages/core/src/config/config-schema.test.ts, packages/core/src/state/state-schema.test.ts, packages/testkit/src/repo-fixtures.ts, packages/testkit/src/provider-fixtures.ts, packages/testkit/src/index.ts</files>
  <read_first>packages/core/src/config/config-schema.test.ts, packages/core/src/state/state-schema.test.ts, packages/testkit/src/repo-fixtures.ts, packages/testkit/src/provider-fixtures.ts, packages/testkit/src/index.ts, packages/core/src/config/config-schema.ts, packages/core/src/state/state-schema.ts, packages/core/src/runtime/local-cli.ts, vitest.config.ts</read_first>
  <action>Create `packages/testkit/src/repo-fixtures.ts` exporting `createRepoFixture` and helpers for git/no-git repos with optional README files. Create `packages/testkit/src/provider-fixtures.ts` exporting `createProviderFixture` helpers for stubbed `~/.codex` and `~/.claude` homes. Update `packages/testkit/src/index.ts` to re-export these helpers. Add `packages/core/src/config/config-schema.test.ts` covering valid defaults and invalid missing keys, and `packages/core/src/state/state-schema.test.ts` covering default placeholders and rejection of raw transcript-style fields. Keep the tests purely local and filesystem-based so they can run without network access or real provider directories.</action>
  <verify>npm test -- --run</verify>
  <acceptance_criteria>
    - packages/testkit/src/repo-fixtures.ts contains `export async function createRepoFixture`
    - packages/testkit/src/provider-fixtures.ts contains `export async function createProviderFixture`
    - packages/core/src/config/config-schema.test.ts contains `defaultAgentBadgeConfig`
    - packages/core/src/state/state-schema.test.ts contains `defaultAgentBadgeState`
    - packages/testkit/src/index.ts re-exports `createRepoFixture` and `createProviderFixture`
  </acceptance_criteria>
  <done>Schema and runtime helpers are covered by deterministic tests, and later init tests have fixture primitives ready to use.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` succeeds
- [ ] `npm test -- --run` succeeds
- [ ] Schema and runtime tests prove exact command strings and default shapes
</verification>

<success_criteria>

- All tasks completed
- `.agent-badge` config and state have schema-backed defaults and parsers
- Package-manager-aware local CLI commands exist for scripts and pre-push hooks
- Test fixtures exist for temp repos and provider-home detection
- No schema or log model stores transcript text, filenames, or local absolute paths
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-and-init-foundation/01-02-SUMMARY.md`
</output>

---
phase: 01-workspace-and-init-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - packages/core/src/repo/git-context.ts
  - packages/core/src/init/provider-detection.ts
  - packages/core/src/init/github-auth.ts
  - packages/core/src/init/preflight.ts
  - packages/core/src/init/default-config.ts
  - packages/core/src/init/default-state.ts
  - packages/core/src/init/scaffold.ts
  - packages/core/src/init/index.ts
  - packages/core/src/init/preflight.test.ts
  - packages/core/src/init/scaffold.test.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
  - packages/agent-badge/src/cli/main.ts
  - packages/agent-badge/src/index.ts
  - packages/create-agent-badge/src/index.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - BOOT-03
  - BOOT-04
user_setup: []
must_haves:
  truths:
    - `agent-badge init` can inspect git state, README presence, package manager, provider availability, and GitHub auth before any repo mutation.
    - The scaffolding flow creates `.agent-badge/config.json`, `.agent-badge/state.json`, `.agent-badge/cache/`, and `.agent-badge/logs/` using schema-backed defaults.
    - Re-running init converges on the same scaffolded state instead of duplicating files or overwriting valid existing values.
    - The initializer package and runtime CLI both route through the same preflight and scaffolding services.
  artifacts:
    - path: packages/core/src/init/preflight.ts
      provides: Structured init preflight service
      min_lines: 60
      contains: "runInitPreflight"
    - path: packages/core/src/init/scaffold.ts
      provides: Idempotent `.agent-badge` scaffolding flow
      min_lines: 60
      contains: "applyAgentBadgeScaffold"
    - path: packages/agent-badge/src/commands/init.ts
      provides: Runtime CLI init command wiring
      contains: "runInitCommand"
    - path: packages/create-agent-badge/src/index.ts
      provides: Initializer entrypoint that reuses core init flow
      contains: "runCreateAgentBadge"
    - path: packages/core/src/init/scaffold.test.ts
      provides: Idempotency and scaffold coverage
      contains: "re-running init"
  key_links:
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/init/preflight.ts
      via: runtime init command invoking shared preflight
      pattern: "runInitPreflight"
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/init/scaffold.ts
      via: runtime init command invoking shared scaffold
      pattern: "applyAgentBadgeScaffold"
    - from: packages/create-agent-badge/src/index.ts
      to: packages/agent-badge/src/commands/init.ts
      via: initializer reusing runtime init orchestration
      pattern: "runInitCommand|runCreateAgentBadge"
    - from: packages/core/src/init/scaffold.ts
      to: packages/core/src/config/config-schema.ts
      via: schema-backed config writing
      pattern: "defaultAgentBadgeConfig|parseAgentBadgeConfig"
---

<objective>
Implement the actual Phase 1 init foundation: structured preflight checks, idempotent `.agent-badge` scaffolding, and shared runtime/initializer wiring.

Purpose: Turn the workspace and core primitives into a usable `init` path that later phases can extend without rewriting the command flow.
Output: Shared preflight/scaffold services, CLI and initializer entrypoints, and integration tests for no-mutation preflight plus idempotent re-runs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-init-foundation/01-RESEARCH.md
@packages/core/src/config/config-schema.ts
@packages/core/src/state/state-schema.ts
@packages/core/src/runtime/local-cli.ts
@packages/testkit/src/repo-fixtures.ts
@packages/testkit/src/provider-fixtures.ts
@packages/agent-badge/src/cli/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build structured init preflight services</name>
  <files>packages/core/src/repo/git-context.ts, packages/core/src/init/provider-detection.ts, packages/core/src/init/github-auth.ts, packages/core/src/init/preflight.ts, packages/core/src/init/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/repo/git-context.ts, packages/core/src/init/provider-detection.ts, packages/core/src/init/github-auth.ts, packages/core/src/init/preflight.ts, packages/core/src/init/index.ts, packages/core/src/index.ts, packages/core/src/config/config-schema.ts, packages/core/src/state/state-schema.ts, packages/core/src/runtime/package-manager.ts, packages/testkit/src/repo-fixtures.ts, packages/testkit/src/provider-fixtures.ts, .planning/phases/01-workspace-and-init-foundation/01-RESEARCH.md</read_first>
  <action>Create `packages/core/src/repo/git-context.ts` with helpers that report `{ isRepo, canInitialize, hasOrigin, blockingMessage }` without mutating the repo. Create `packages/core/src/init/provider-detection.ts` that checks whether `~/.codex` and `~/.claude` exist and returns booleans plus normalized home labels, not exported absolute paths. Create `packages/core/src/init/github-auth.ts` that reports `{ available, source }` using environment variables or a tiny injectable checker boundary instead of hard-coding networked auth calls. Create `packages/core/src/init/preflight.ts` exporting `runInitPreflight(options)` and `InitPreflightResult` with sections `git`, `readme`, `packageManager`, `providers`, `githubAuth`, and `existingScaffold`. Re-export the init modules from `packages/core/src/init/index.ts` and `packages/core/src/index.ts`.</action>
  <verify>npm test -- --run packages/core/src/init/preflight.test.ts</verify>
  <acceptance_criteria>
    - packages/core/src/init/preflight.ts contains `export async function runInitPreflight`
    - packages/core/src/init/preflight.ts contains `existingScaffold`
    - packages/core/src/repo/git-context.ts contains `canInitialize`
    - packages/core/src/init/provider-detection.ts contains `codex` and `claude`
    - packages/core/src/init/github-auth.ts contains `available` and `source`
  </acceptance_criteria>
  <done>There is one structured preflight result model for init that gathers facts without touching the target repo.</done>
</task>

<task type="auto">
  <name>Task 2: Implement schema-backed idempotent `.agent-badge` scaffolding and wire the CLI/initializer entrypoints</name>
  <files>packages/core/src/init/default-config.ts, packages/core/src/init/default-state.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/index.ts, packages/core/src/index.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/index.ts, packages/create-agent-badge/src/index.ts</files>
  <read_first>packages/core/src/init/default-config.ts, packages/core/src/init/default-state.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/index.ts, packages/core/src/index.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/index.ts, packages/create-agent-badge/src/index.ts, packages/core/src/config/config-schema.ts, packages/core/src/state/state-schema.ts, packages/core/src/runtime/local-cli.ts, packages/core/src/init/preflight.ts</read_first>
  <action>Create `packages/core/src/init/default-config.ts` and `packages/core/src/init/default-state.ts` that return schema-valid defaults for `.agent-badge/config.json` and `.agent-badge/state.json`. Create `packages/core/src/init/scaffold.ts` exporting `applyAgentBadgeScaffold({ cwd, preflight })` that ensures `.agent-badge/`, `.agent-badge/cache/`, and `.agent-badge/logs/` exist, writes `config.json` and `state.json` only when missing or incomplete, preserves existing valid values, and returns `{ created, reused, warnings }`. Add `packages/agent-badge/src/commands/init.ts` exporting `runInitCommand(options)` that calls `runInitPreflight`, prints a structured summary, and only then calls `applyAgentBadgeScaffold`. Update `packages/agent-badge/src/cli/main.ts` so the `init` command invokes `runInitCommand`. Update `packages/agent-badge/src/index.ts` to export `runInitCommand`. Update `packages/create-agent-badge/src/index.ts` to export `runCreateAgentBadge()` that delegates to the same init command path for the current working directory.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - packages/core/src/init/scaffold.ts contains `export async function applyAgentBadgeScaffold`
    - packages/core/src/init/scaffold.ts contains `.agent-badge/cache` and `.agent-badge/logs`
    - packages/agent-badge/src/commands/init.ts contains `runInitPreflight`
    - packages/agent-badge/src/commands/init.ts contains `applyAgentBadgeScaffold`
    - packages/agent-badge/src/cli/main.ts contains `runInitCommand`
    - packages/create-agent-badge/src/index.ts contains `export async function runCreateAgentBadge`
  </acceptance_criteria>
  <done>Both the runtime CLI and initializer now share one scaffold implementation that creates the required `.agent-badge` layout safely.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests for no-mutation preflight and idempotent re-entry</name>
  <files>packages/core/src/init/preflight.test.ts, packages/core/src/init/scaffold.test.ts, packages/agent-badge/src/commands/init.test.ts</files>
  <read_first>packages/core/src/init/preflight.test.ts, packages/core/src/init/scaffold.test.ts, packages/agent-badge/src/commands/init.test.ts, packages/core/src/init/preflight.ts, packages/core/src/init/scaffold.ts, packages/agent-badge/src/commands/init.ts, packages/testkit/src/repo-fixtures.ts, packages/testkit/src/provider-fixtures.ts, vitest.config.ts</read_first>
  <action>Create `packages/core/src/init/preflight.test.ts` covering existing git repos, non-git directories with init allowed, README present/absent cases, provider-home detection, and GitHub auth source detection through stubs. Create `packages/core/src/init/scaffold.test.ts` asserting that `.agent-badge/config.json`, `.agent-badge/state.json`, `.agent-badge/cache/`, and `.agent-badge/logs/` are created on first run and that re-running scaffold reports reused artifacts instead of duplicating content. Create `packages/agent-badge/src/commands/init.test.ts` covering the end-to-end init command flow with a temp repo and a blocking-path assertion for the non-git case when initialization is not allowed. Use the exact phrase `re-running init` in the idempotency test description so the summary and later verification can grep for it.</action>
  <verify>npm test -- --run</verify>
  <acceptance_criteria>
    - packages/core/src/init/preflight.test.ts contains `README`
    - packages/core/src/init/preflight.test.ts contains `GitHub auth`
    - packages/core/src/init/scaffold.test.ts contains `.agent-badge/config.json`
    - packages/core/src/init/scaffold.test.ts contains `re-running init`
    - packages/agent-badge/src/commands/init.test.ts contains `runInitCommand`
    - packages/agent-badge/src/commands/init.test.ts contains `non-git`
  </acceptance_criteria>
  <done>The init foundation is covered by automated tests that prove preflight is non-mutating and scaffold re-entry is idempotent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` succeeds
- [ ] `npm test -- --run` succeeds
- [ ] Preflight tests prove no target-repo mutation happens before scaffold execution
- [ ] Scaffold tests prove re-running init reuses existing artifacts without duplication
</verification>

<success_criteria>

- All tasks completed
- `agent-badge init` has a structured preflight stage with git, README, package manager, provider, and auth facts
- `.agent-badge/config.json`, `.agent-badge/state.json`, `.agent-badge/cache/`, and `.agent-badge/logs/` are created via shared core services
- Re-running init is convergent and does not silently overwrite valid user state
- The initializer package reuses the same shared init path as the runtime CLI
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-and-init-foundation/01-03-SUMMARY.md`
</output>

---
phase: 01-workspace-and-init-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - packages/core/src/runtime/local-cli.ts
  - packages/core/src/runtime/local-cli.test.ts
  - packages/core/src/init/runtime-wiring.ts
  - packages/core/src/init/runtime-wiring.test.ts
  - packages/core/src/init/index.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
autonomous: true
gap_closure: true
requirements:
  - BOOT-01
  - BOOT-02
  - BOOT-03
  - BOOT-04
user_setup: []
must_haves:
  truths:
    - After init, the target repo has repo-local `agent-badge` runtime wiring in `package.json` and `.git/hooks/pre-push` instead of only `.agent-badge/*` files.
    - The installed hook is a runnable failure-soft shell script on first-run repos and remains idempotent on reruns: rerunning init does not duplicate package scripts, dependency entries, or the managed hook block.
    - Runtime wiring uses package-manager-aware commands without relying on a global `agent-badge` install.
  artifacts:
    - path: packages/core/src/init/runtime-wiring.ts
      provides: Dedicated repo-local runtime wiring service for package scripts and git hooks
      contains: "applyRepoLocalRuntimeWiring"
    - path: packages/core/src/runtime/local-cli.ts
      provides: Safe package-script and hook command builders
      contains: "agent-badge:refresh"
    - path: packages/agent-badge/src/commands/init.ts
      provides: Init orchestration that applies runtime wiring after scaffold
      contains: "applyRepoLocalRuntimeWiring"
    - path: packages/core/src/init/runtime-wiring.test.ts
      provides: Idempotent runtime wiring coverage including first-run executable hook behavior
      contains: "pre-push"
    - path: packages/agent-badge/src/commands/init.test.ts
      provides: End-to-end init coverage for scripts, dependency entry, and hook installation
      contains: "agent-badge:refresh"
  key_links:
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/init/runtime-wiring.ts
      via: init invoking repo-local runtime integration after scaffold
      pattern: "applyRepoLocalRuntimeWiring"
    - from: packages/core/src/init/runtime-wiring.ts
      to: packages/core/src/runtime/local-cli.ts
      via: package-manager-specific script and hook template generation
      pattern: "agent-badge:refresh|pre-push"
    - from: packages/core/src/init/runtime-wiring.ts
      to: target repo package.json
      via: idempotent JSON merge for dependency and scripts
      pattern: "devDependencies|scripts"
    - from: packages/core/src/init/runtime-wiring.ts
      to: target repo .git/hooks/pre-push
      via: one managed agent-badge block preserved across reruns with shell preamble and executable mode on created hooks
      pattern: "agent-badge:start|agent-badge:end|#!/bin/sh"
---

<objective>
Close the remaining BOOT-03 verification gap by writing repo-local runtime dependency, script, and hook wiring during init.

Purpose: Make initialized repositories able to invoke `agent-badge` locally after dependency install without relying on a global binary or manual hook setup.
Output: A dedicated runtime-wiring service, safe package-manager-aware script/hook templates, and end-to-end coverage proving init writes them idempotently.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-init-foundation/01-RESEARCH.md
@.planning/phases/01-workspace-and-init-foundation/01-VERIFICATION.md
@.planning/phases/01-workspace-and-init-foundation/01-02-SUMMARY.md
@.planning/phases/01-workspace-and-init-foundation/01-03-SUMMARY.md
@packages/core/src/runtime/package-manager.ts
@packages/core/src/runtime/local-cli.ts
@packages/core/src/init/scaffold.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/package.json
@packages/create-agent-badge/package.json

<interfaces>
From `packages/core/src/runtime/package-manager.ts`:
```ts
export type PackageManager = z.infer<typeof packageManagerSchema>;
export function detectPackageManager(projectRoot: string): PackageManager;
```

From `packages/core/src/runtime/local-cli.ts`:
```ts
export function getLocalAgentBadgeCommand(packageManager: PackageManager): string;
export function getPrePushRefreshCommand(packageManager: PackageManager): string;
```

From `packages/agent-badge/src/commands/init.ts`:
```ts
export interface InitCommandResult {
  readonly preflight: InitPreflightResult;
  readonly scaffold: AgentBadgeScaffoldResult;
}

export async function runInitCommand(
  options?: RunInitCommandOptions
): Promise<InitCommandResult>;
```

Current package metadata:
```json
// packages/agent-badge/package.json
{ "name": "agent-badge", "version": "0.0.0" }

// packages/create-agent-badge/package.json
{ "dependencies": { "agent-badge": "file:../agent-badge" } }
```

Runtime dependency normalization for this plan:
- Source of truth is `packages/agent-badge/package.json` because it describes the runtime package the target repo must install.
- Treat `packages/create-agent-badge/package.json` `dependencies.agent-badge` as workspace-only bootstrap metadata; never copy its `file:` specifier into a target repo.
- Normalize the runtime dependency specifier to `^<version>` when `packages/agent-badge/package.json` contains a publishable semver version; otherwise fall back to `latest` when the version is missing, `0.0.0`, or otherwise non-publishable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create an idempotent runtime-wiring service and safe script/hook templates</name>
  <files>packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/local-cli.test.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/core/src/init/index.ts</files>
  <read_first>packages/core/src/runtime/package-manager.ts, packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/local-cli.test.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/index.ts, packages/agent-badge/package.json, packages/create-agent-badge/package.json, .planning/phases/01-workspace-and-init-foundation/01-RESEARCH.md, .planning/phases/01-workspace-and-init-foundation/01-VERIFICATION.md</read_first>
  <action>Extend `packages/core/src/runtime/local-cli.ts` with safe helpers for repo wiring, not just bare binary invocations. Define package-script commands using plain local-bin calls (for example `agent-badge init` and `agent-badge refresh --hook pre-push --fail-soft`) so script definitions do not recurse on package-manager aliases, and add a package-manager-specific hook runner command for invoking the refresh script from shell. Create `packages/core/src/init/runtime-wiring.ts` exporting `applyRepoLocalRuntimeWiring({ cwd, packageManager, runtimeDependencySpecifier })` that reads or creates `package.json`, adds `devDependencies.agent-badge` when missing, merges stable scripts such as `agent-badge:init` and `agent-badge:refresh`, and installs or updates one managed block inside `.git/hooks/pre-push` wrapped in marker comments like `agent-badge:start` / `agent-badge:end`. When the hook file is created for the first time, write a real shell script with a `#!/bin/sh` preamble and set executable permissions so Git can invoke it directly; when updating an existing hook, preserve unrelated content and ensure the resulting file remains executable. Preserve unrelated `package.json` fields and existing hook content outside the managed block, keep the hook failure-soft with `|| true`, and return structured `created`, `updated`, `reused`, and `warnings` arrays. Add test coverage that executes the generated first-run hook file in a temp repo and proves it is runnable even before real refresh behavior exists. Do not overload `scaffold.ts`; keep repo-local runtime wiring isolated so `.agent-badge` scaffold behavior remains easy to reason about.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/init/runtime-wiring.ts` exports `applyRepoLocalRuntimeWiring`
    - `packages/core/src/init/runtime-wiring.ts` manages both `package.json` and `.git/hooks/pre-push`
    - `packages/core/src/runtime/local-cli.ts` contains safe script/hook builders for `agent-badge:init` and `agent-badge:refresh`
    - `packages/core/src/init/runtime-wiring.test.ts` covers first-run creation and rerun idempotency
    - The managed hook content is delimited by stable marker comments so reruns update one block instead of appending duplicates
    - Newly created `.git/hooks/pre-push` files start with `#!/bin/sh` and are written with executable permissions
    - `packages/core/src/init/runtime-wiring.test.ts` proves a first-run repo can execute the generated hook file successfully
  </acceptance_criteria>
  <done>The repo has one dedicated core service that can write local runtime dependency/scripts/hooks predictably and idempotently.</done>
</task>

<task type="auto">
  <name>Task 2: Wire runtime integration into init and prove the target repo gains it</name>
  <files>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts</files>
  <read_first>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/runtime/local-cli.ts, packages/agent-badge/package.json, packages/create-agent-badge/package.json, .planning/phases/01-workspace-and-init-foundation/01-VERIFICATION.md</read_first>
  <action>Update `packages/agent-badge/src/commands/init.ts` so `runInitCommand()` derives `runtimeDependencySpecifier` from `packages/agent-badge/package.json` as the source of truth for the runtime package instead of hardcoding `latest` or reusing initializer workspace metadata. Normalize that value before calling `applyRepoLocalRuntimeWiring()`: when the runtime package version is a publishable semver, write `^<version>`; when it is missing, `0.0.0`, or otherwise non-publishable, fall back to `latest`. Explicitly ignore `packages/create-agent-badge/package.json` `dependencies.agent-badge` because its local `file:` specifier is workspace-only and must never leak into the target repo. Then call `applyRepoLocalRuntimeWiring()` after scaffold succeeds and after the git-bootstrap plan has ensured a real `.git` directory exists. Extend `InitCommandResult` and console output with a dedicated runtime-wiring summary so execution and later verification can distinguish `.agent-badge` scaffold results from repo wiring results. In `packages/agent-badge/src/commands/init.test.ts`, expand the end-to-end init test to assert the target repo gains `devDependencies.agent-badge`, script entries for `agent-badge:init` and `agent-badge:refresh`, and exactly one managed `pre-push` hook block. Add a rerun assertion that a second init does not duplicate dependency entries, scripts, or hook markers. Keep this plan narrowly scoped to wiring the call path; do not implement real `refresh` behavior here.</action>
  <verify>
    <automated>npm run typecheck && npm test -- --run packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/commands/init.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/init.ts` calls `applyRepoLocalRuntimeWiring()`
    - `packages/agent-badge/src/commands/init.ts` returns and prints a dedicated runtime-wiring result
    - `packages/agent-badge/src/commands/init.ts` derives the dependency spec from `packages/agent-badge/package.json` and never copies the initializer's `file:` specifier into target repos
    - `packages/agent-badge/src/commands/init.test.ts` asserts `devDependencies.agent-badge` is present after init
    - `packages/agent-badge/src/commands/init.test.ts` asserts `scripts.agent-badge:init` and `scripts.agent-badge:refresh` exist after init
    - `packages/agent-badge/src/commands/init.test.ts` asserts `.git/hooks/pre-push` contains exactly one managed agent-badge block across reruns
  </acceptance_criteria>
  <done>Initialized repos now gain repo-local runtime dependency/scripts/hook wiring automatically, and rerunning init converges instead of duplicating that wiring.</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck`
- Run `npm test -- --run packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/commands/init.test.ts`
- Run `npm test -- --run` before marking the plan complete
- Confirm init creates or updates `package.json` runtime wiring without destroying unrelated fields
- Confirm `.git/hooks/pre-push` contains one managed agent-badge block, preserves non-agent-badge content, starts with a shell preamble when newly created, and is executable
</verification>

<success_criteria>
- The second verification gap in `01-VERIFICATION.md` is closed
- Init writes repo-local runtime dependency and script/hook wiring into the target repo instead of only `.agent-badge/*`
- Re-running init does not duplicate dependency entries, scripts, or the managed hook block
- The runtime wiring remains package-manager-aware and failure-soft without depending on a global install, and newly created hooks are directly runnable by Git
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-and-init-foundation/01-05-SUMMARY.md`
</output>

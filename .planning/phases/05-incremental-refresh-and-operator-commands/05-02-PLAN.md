---
phase: 05-incremental-refresh-and-operator-commands
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - packages/core/src/config/config-schema.ts
  - packages/core/src/config/config-schema.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
  - packages/agent-badge/src/commands/status.ts
  - packages/agent-badge/src/commands/status.test.ts
  - packages/agent-badge/src/commands/config.ts
  - packages/agent-badge/src/commands/config.test.ts
  - packages/agent-badge/src/commands/index.ts
  - packages/agent-badge/src/cli/main.ts
  - packages/agent-badge/src/cli/main.test.ts
  - packages/agent-badge/src/index.ts
autonomous: true
requirements:
  - SCAN-04
  - OPER-02
  - OPER-03
  - OPER-04
user_setup: []
must_haves:
  truths:
    - "`agent-badge refresh` can rebuild local refresh state end to end from config, state, provider data, and the derived cache without requiring manual file edits."
    - "`agent-badge status` reads persisted aggregate refresh data plus provider/publish state and prints totals, enablement, publish state, and checkpoint timing clearly."
    - "`agent-badge config` can mutate supported post-init settings with validation while keeping aggregate-only publishing non-negotiable."
    - "The only privacy setting added in Phase 5 is local-output scoped; it does not allow raw transcripts, filenames, or absolute paths to leave the machine."
  artifacts:
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: end-to-end Phase 5 refresh command
      contains: "runRefreshCommand"
    - path: packages/agent-badge/src/commands/status.ts
      provides: operator status command
      contains: "runStatusCommand"
    - path: packages/agent-badge/src/commands/config.ts
      provides: config get/set command
      contains: "runConfigCommand"
    - path: packages/core/src/config/config-schema.ts
      provides: Phase 5 privacy/config surface
      contains: "privacy"
  key_links:
    - from: packages/agent-badge/src/commands/refresh.ts
      to: packages/core/src/scan/incremental-refresh.ts
      via: "refresh using the Phase 5 core scan contract"
      pattern: "runIncrementalRefresh"
    - from: packages/agent-badge/src/commands/refresh.ts
      to: packages/core/src/publish/publish-service.ts
      via: "refresh deciding whether to publish"
      pattern: "publishBadgeIfChanged"
    - from: packages/agent-badge/src/commands/status.ts
      to: packages/core/src/state/state-schema.ts
      via: "printing persisted refresh summary and checkpoints"
      pattern: "lastRefreshedAt|summary"
    - from: packages/agent-badge/src/commands/config.ts
      to: packages/core/src/config/config-schema.ts
      via: "validated post-init config mutations"
      pattern: "badge|providers|refresh|privacy"
---

<objective>
Turn the Phase 5 core refresh contract into real operator commands: `refresh`, `status`, and `config`.

Purpose: OPER-02, OPER-03, and OPER-04 only count if a developer can recover badge state, inspect the current repo/operator view quickly, and safely mutate supported settings after init.
Output: new runtime commands, CLI wiring, persisted config/state updates, and tests proving the operator UX and error boundaries.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-01-PLAN.md
@packages/core/src/config/config-schema.ts
@packages/core/src/state/state-schema.ts
@packages/core/src/scan/incremental-refresh.ts
@packages/core/src/scan/refresh-cache.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/runtime/local-cli.ts
@packages/agent-badge/src/commands/scan.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/cli/main.ts

<interfaces>
From `packages/core/src/scan/incremental-refresh.ts` after `05-01`:

```ts
export async function runIncrementalRefresh(options: {
  cwd: string;
  homeRoot: string;
  config: AgentBadgeConfig;
  state: AgentBadgeState;
  forceFull?: boolean;
}): Promise<{
  scanMode: "full" | "incremental";
  summary: {
    includedSessions: number;
    includedTokens: number;
    ambiguousSessions: number;
    excludedSessions: number;
  };
  providerCursors: Partial<Record<"codex" | "claude", string | null>>;
  cache: unknown;
}>;
```

From `packages/core/src/publish/publish-service.ts` after `05-01`:

```ts
export async function publishBadgeIfChanged(options: {
  config: Pick<AgentBadgeConfig, "badge" | "publish">;
  state: AgentBadgeState;
  includedTotals: { sessions: number; tokens: number };
  client: GitHubGistClient;
  now: string;
  skipIfUnchanged: boolean;
}): Promise<{ state: AgentBadgeState; decision: "published" | "skipped" }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the end-to-end `refresh` command with hook-aware and recovery modes</name>
  <files>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/index.ts, packages/agent-badge/src/index.ts</files>
  <read_first>packages/agent-badge/src/commands/scan.ts, packages/agent-badge/src/commands/publish.ts, packages/core/src/scan/incremental-refresh.ts, packages/core/src/scan/refresh-cache.ts, packages/core/src/publish/publish-service.ts, packages/core/src/state/state-schema.ts, .planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md, .planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md</read_first>
  <action>Create `packages/agent-badge/src/commands/refresh.ts` exporting `runRefreshCommand(options)` where `options` has exactly `cwd?: string`, `homeRoot?: string`, `env?: NodeJS.ProcessEnv`, `gistClient?: GitHubGistClient`, `stdout?: OutputWriter`, `hook?: "pre-push"`, `failSoft?: boolean`, and `forceFull?: boolean`. The command must read `.agent-badge/config.json` plus `.agent-badge/state.json`, call `runIncrementalRefresh({ cwd, homeRoot, config, state, forceFull })`, persist the updated `state.json` and `.agent-badge/cache/session-index.json`, and decide publish behavior like this: if `config.publish.gistId` or `config.publish.badgeUrl` is null, set `state.refresh.lastPublishDecision = "not-configured"` and skip remote work; otherwise call `publishBadgeIfChanged({ skipIfUnchanged: true })` with `includedTotals.sessions = summary.includedSessions` and `includedTotals.tokens = summary.includedTokens`. Print a summary beginning with `agent-badge refresh` and lines for `- Scan mode:`, `- Totals:`, `- Publish:`, and `- Last refresh:`. When `hook === "pre-push"`, keep output concise and action-oriented. When `failSoft === true` and any error is thrown after config/state load begins, print `- Refresh status: failed-soft` plus the error message and return a structured result instead of rethrowing. When `forceFull === true`, bypass incremental cursors/cache and rebuild the refresh summary from a full scan so the command can recover local badge state without manual file edits. Export the command from `packages/agent-badge/src/commands/index.ts` and `packages/agent-badge/src/index.ts`.</action>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/refresh.ts` contains `export async function runRefreshCommand`
    - `packages/agent-badge/src/commands/refresh.ts` contains `hook?: "pre-push"`
    - `packages/agent-badge/src/commands/refresh.ts` contains `failSoft?: boolean`
    - `packages/agent-badge/src/commands/refresh.ts` contains `forceFull?: boolean`
    - `packages/agent-badge/src/commands/refresh.ts` contains `agent-badge refresh`
    - `packages/agent-badge/src/commands/refresh.ts` contains `Refresh status: failed-soft`
    - `packages/agent-badge/src/commands/refresh.ts` contains `.agent-badge/cache/session-index.json`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `rebuilds refresh state with --force-full`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `returns a soft failure for pre-push mode`
    - `packages/agent-badge/src/index.ts` contains `runRefreshCommand`
  </acceptance_criteria>
  <done>Developers can now refresh badge state end to end through one command, including a safe full rebuild path and a hook-friendly failure-soft path.</done>
</task>

<task type="auto">
  <name>Task 2: Add the `status` command over persisted refresh and publish state</name>
  <files>packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts</files>
  <read_first>packages/agent-badge/src/commands/refresh.ts, packages/core/src/state/state-schema.ts, packages/core/src/config/config-schema.ts, packages/core/src/publish/publish-target.ts, .planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md, .planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md</read_first>
  <action>Create `packages/agent-badge/src/commands/status.ts` exporting `runStatusCommand({ cwd?, stdout? })`. The command must read `.agent-badge/config.json` and `.agent-badge/state.json` only; it must not rescan provider data or make network calls. Print exactly these headings/lines in order: `agent-badge status`, `- Totals:`, `- Providers:`, `- Publish:`, `- Last refresh:`, and `- Checkpoints:`. `- Totals:` must show `includedSessions` plus `includedTokens` from `state.refresh.summary`, and when the summary is null it must print `unavailable (run \`agent-badge refresh\`)`. `- Providers:` must show `codex=enabled|disabled` and `claude=enabled|disabled`. `- Publish:` must show the current `state.publish.status`, whether a gist is configured, and the stored `lastPublishedAt` when present. `- Last refresh:` must show `state.refresh.lastRefreshedAt` plus `state.refresh.lastScanMode`. `- Checkpoints:` must show each provider's `lastScannedAt` timestamp and avoid printing raw cursor strings or local provider paths. When `config.privacy.output === "minimal"`, keep the same headings but omit optional low-signal detail such as `lastPublishedHash` or gist identifiers; when it is `"standard"`, include those extra operator details if present. Add tests covering the configured, deferred, no-refresh-yet, and minimal-output cases.</action>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/status.ts` contains `export async function runStatusCommand`
    - `packages/agent-badge/src/commands/status.ts` contains `agent-badge status`
    - `packages/agent-badge/src/commands/status.ts` contains `- Totals:`
    - `packages/agent-badge/src/commands/status.ts` contains `- Providers:`
    - `packages/agent-badge/src/commands/status.ts` contains `- Publish:`
    - `packages/agent-badge/src/commands/status.ts` contains `- Last refresh:`
    - `packages/agent-badge/src/commands/status.ts` contains `- Checkpoints:`
    - `packages/agent-badge/src/commands/status.ts` contains `privacy.output === "minimal"`
    - `packages/agent-badge/src/commands/status.ts` contains `unavailable (run \`agent-badge refresh\`)`
    - `packages/agent-badge/src/commands/status.test.ts` contains `prints configured publish status and refresh totals`
    - `packages/agent-badge/src/commands/status.test.ts` contains `prints unavailable totals before the first refresh`
    - `packages/agent-badge/src/commands/status.test.ts` contains `omits optional publish detail in minimal privacy mode`
  </acceptance_criteria>
  <done>Operators can inspect the repo’s current badge state quickly without triggering scans or publish side effects.</done>
</task>

<task type="auto">
  <name>Task 3: Add the `config` command, whitelist safe mutations, and wire all Phase 5 commands into the CLI</name>
  <files>packages/core/src/config/config-schema.ts, packages/core/src/config/config-schema.test.ts, packages/agent-badge/src/commands/config.ts, packages/agent-badge/src/commands/config.test.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts</files>
  <read_first>packages/core/src/config/config-schema.ts, packages/core/src/config/config-schema.test.ts, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/status.ts, .planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md, .planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md</read_first>
  <action>Extend `packages/core/src/config/config-schema.ts` so `privacy` contains both `aggregateOnly: z.literal(true)` and `output: z.enum(["standard", "minimal"])`, with the default set to `{ aggregateOnly: true, output: "standard" }`. Create `packages/agent-badge/src/commands/config.ts` exporting `runConfigCommand({ cwd?, stdout?, action?, key?, value? })` where `action` supports `"get"` and `"set"`. `config get` with no key must print the current supported settings; `config set` must support exactly these keys: `providers.codex.enabled`, `providers.claude.enabled`, `badge.label`, `badge.mode`, `refresh.prePush.enabled`, `refresh.prePush.mode`, and `privacy.output`. Any attempt to set `privacy.aggregateOnly` to a value other than `true` must throw the exact error `privacy.aggregateOnly must remain true because agent-badge only publishes aggregate data.`. Any unknown key must throw `Unsupported config key: <key>`. Register these CLI surfaces in `packages/agent-badge/src/cli/main.ts`: `refresh` with options `--hook <name>`, `--fail-soft`, and `--force-full`; `status`; `config`; `config get [key]`; and `config set <key> <value>`. Add CLI main tests proving the new commands are registered and config tests proving supported mutations persist while unsafe privacy changes are rejected.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/config/config-schema.test.ts packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/cli/main.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/config/config-schema.ts` contains `output: z.enum(["standard", "minimal"])`
    - `packages/core/src/config/config-schema.ts` contains `aggregateOnly: z.literal(true)`
    - `packages/agent-badge/src/commands/config.ts` contains `export async function runConfigCommand`
    - `packages/agent-badge/src/commands/config.ts` contains `Unsupported config key:`
    - `packages/agent-badge/src/commands/config.ts` contains `privacy.aggregateOnly must remain true because agent-badge only publishes aggregate data.`
    - `packages/agent-badge/src/cli/main.ts` contains `.command("refresh")`
    - `packages/agent-badge/src/cli/main.ts` contains `.command("status")`
    - `packages/agent-badge/src/cli/main.ts` contains `.command("config")`
    - `packages/agent-badge/src/cli/main.ts` contains `--force-full`
    - `packages/agent-badge/src/commands/config.test.ts` contains `rejects disabling aggregate-only publishing`
    - `packages/agent-badge/src/cli/main.test.ts` contains `refresh`
    - `packages/agent-badge/src/cli/main.test.ts` contains `status`
    - `packages/agent-badge/src/cli/main.test.ts` contains `config`
  </acceptance_criteria>
  <done>The runtime now has a complete Phase 5 operator surface: refresh, status, and validated post-init configuration changes with a safe privacy boundary.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --run packages/core/src/config/config-schema.test.ts packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/status.test.ts packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/cli/main.test.ts` succeeds
- [ ] `packages/agent-badge/src/commands/refresh.ts` supports `--force-full`, `--hook pre-push`, and `--fail-soft`
- [ ] `packages/agent-badge/src/commands/status.ts` reads persisted state only and never scans providers
- [ ] `packages/agent-badge/src/commands/config.ts` rejects disabling aggregate-only publishing
</verification>

<success_criteria>
- OPER-02 is delivered through a real refresh command with a no-manual-edits recovery path
- OPER-03 is delivered through a fast status command over persisted refresh/publish/checkpoint state
- OPER-04 is delivered through a safe config mutation surface that does not weaken the privacy model
</success_criteria>

<output>
After completion, create `.planning/phases/05-incremental-refresh-and-operator-commands/05-02-SUMMARY.md`
</output>

---
phase: 05-incremental-refresh-and-operator-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/state/state-schema.ts
  - packages/core/src/state/state-schema.test.ts
  - packages/core/src/scan/refresh-cache.ts
  - packages/core/src/scan/refresh-cache.test.ts
  - packages/core/src/scan/incremental-refresh.ts
  - packages/core/src/scan/incremental-refresh.test.ts
  - packages/core/src/scan/index.ts
  - packages/core/src/index.ts
  - packages/core/src/providers/codex/codex-adapter.ts
  - packages/core/src/providers/claude/claude-adapter.ts
  - packages/core/src/publish/publish-service.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/core/src/publish/index.ts
autonomous: true
requirements:
  - SCAN-04
  - PUBL-05
  - OPER-03
user_setup: []
must_haves:
  truths:
    - "Phase 5 refresh computes current totals from provider-backed incremental inputs plus a derived local cache, not by rerunning a full historical rescan once valid checkpoints exist."
    - "Provider cursors advance only after successful provider scans and remain opaque serialized provider contracts instead of invented timestamps."
    - "Publish skipping compares the exact serialized endpoint badge payload hash against `state.publish.lastPublishedHash`."
    - "Persisted refresh summaries and session cache entries remain aggregate-only or stable-session-key based; they never store transcripts, filenames, cwd paths, or raw attribution evidence."
  artifacts:
    - path: packages/core/src/scan/incremental-refresh.ts
      provides: core incremental refresh orchestration
      contains: "runIncrementalRefresh"
    - path: packages/core/src/scan/refresh-cache.ts
      provides: derived session-index cache helpers
      contains: ".agent-badge/cache/session-index.json"
    - path: packages/core/src/state/state-schema.ts
      provides: refresh summary and publish timestamp state contract
      contains: "lastRefreshedAt"
    - path: packages/core/src/publish/publish-service.ts
      provides: hash-aware publish decision helper
      contains: "publishBadgeIfChanged"
  key_links:
    - from: packages/core/src/scan/incremental-refresh.ts
      to: packages/core/src/providers/codex/codex-adapter.ts
      via: "Codex incremental session input"
      pattern: "scanCodexSessionsIncremental"
    - from: packages/core/src/scan/incremental-refresh.ts
      to: packages/core/src/providers/claude/claude-adapter.ts
      via: "Claude incremental session input"
      pattern: "scanClaudeSessionsIncremental"
    - from: packages/core/src/scan/incremental-refresh.ts
      to: packages/core/src/scan/refresh-cache.ts
      via: "merging changed sessions into the derived cache"
      pattern: "readRefreshCache|writeRefreshCache"
    - from: packages/core/src/publish/publish-service.ts
      to: packages/core/src/publish/badge-payload.ts
      via: "exact endpoint-payload hashing"
      pattern: "buildEndpointBadgePayload"
---

<objective>
Define the Phase 5 core refresh contract before adding new commands: provider-owned cursors, a derived session cache, persisted aggregate refresh summaries, and diff-aware publish decisions.

Purpose: SCAN-04 and PUBL-05 are foundational contracts. If this plan gets the cursor/cache/hash rules wrong, every later `refresh`, `status`, `config`, and hook behavior will drift or double count.
Output: a reusable incremental refresh service, privacy-safe refresh cache/state schemas, and a publish helper that can skip unchanged badge uploads safely.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md
@packages/core/src/state/state-schema.ts
@packages/core/src/scan/scan-state.ts
@packages/core/src/scan/full-backfill.ts
@packages/core/src/providers/codex/codex-adapter.ts
@packages/core/src/providers/codex/codex-sql.ts
@packages/core/src/providers/claude/claude-adapter.ts
@packages/core/src/providers/claude/claude-jsonl.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/publish/badge-payload.ts
@packages/core/src/publish/index.ts
@packages/core/src/index.ts

<interfaces>
From `packages/core/src/state/state-schema.ts`:

```ts
checkpoints: {
  codex: { cursor: string | null; lastScannedAt: string | null };
  claude: { cursor: string | null; lastScannedAt: string | null };
}
publish: {
  status: "idle" | "deferred" | "pending" | "published" | "error";
  gistId: string | null;
  lastPublishedHash: string | null;
}
```

From `packages/core/src/publish/publish-service.ts`:

```ts
export async function publishBadgeToGist(
  options: PublishBadgeToGistOptions
): Promise<AgentBadgeState>;
```

From `packages/core/src/scan/full-backfill.ts`:

```ts
export async function runFullBackfillScan(
  options: RunFullBackfillScanOptions
): Promise<RunFullBackfillScanResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the refresh cache, refresh summary state, and provider incremental scan contract</name>
  <files>packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/scan/refresh-cache.ts, packages/core/src/scan/refresh-cache.test.ts, packages/core/src/scan/incremental-refresh.ts, packages/core/src/scan/incremental-refresh.test.ts, packages/core/src/scan/index.ts, packages/core/src/index.ts, packages/core/src/providers/codex/codex-adapter.ts, packages/core/src/providers/claude/claude-adapter.ts</files>
  <read_first>packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/scan/scan-state.ts, packages/core/src/scan/full-backfill.ts, packages/core/src/providers/codex/codex-adapter.ts, packages/core/src/providers/codex/codex-sql.ts, packages/core/src/providers/claude/claude-adapter.ts, packages/core/src/providers/claude/claude-jsonl.ts, packages/core/src/index.ts, .planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md, .planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md</read_first>
  <action>Extend `packages/core/src/state/state-schema.ts` with a new `refresh` object containing exactly `lastRefreshedAt: isoDateTimeSchema.nullable()`, `lastScanMode: z.enum(["full", "incremental"]).nullable()`, `lastPublishDecision: z.enum(["published", "skipped", "deferred", "not-configured", "failed"]).nullable()`, and `summary: z.object({ includedSessions: z.number().int().nonnegative(), includedTokens: z.number().int().nonnegative(), ambiguousSessions: z.number().int().nonnegative(), excludedSessions: z.number().int().nonnegative() }).nullable()`. Extend `publish` with `lastPublishedAt: isoDateTimeSchema.nullable()`, update defaults, and add schema tests for the new fields. Create `packages/core/src/scan/refresh-cache.ts` with the exact cache file path constant `".agent-badge/cache/session-index.json"` plus `readRefreshCache({ cwd })`, `writeRefreshCache({ cwd, cache })`, and `buildRefreshCacheKey(session)` using the stable key `${provider}:${providerSessionId}`. Cache entries must store only `provider`, `providerSessionId`, `updatedAt`, `status`, `includedSessions`, and `includedTokens`; they must not store `cwd`, `observedRemoteUrl`, transcript hints, or raw evidence arrays. In `packages/core/src/providers/codex/codex-adapter.ts`, add `scanCodexSessionsIncremental({ homeRoot, cursor })` that returns `{ sessions, cursor, mode }` where `mode` is `"incremental"` when the cursor is usable or `"full"` when the function has to fall back. In `packages/core/src/providers/claude/claude-adapter.ts`, add the matching `scanClaudeSessionsIncremental({ homeRoot, cursor })` contract. Then create `packages/core/src/scan/incremental-refresh.ts` exporting `runIncrementalRefresh({ cwd, homeRoot, config, state, forceFull })` that uses provider incremental scan functions when possible, falls back to `runFullBackfillScan()` when `forceFull === true` or cached cursor state is missing/invalid, recomputes attribution for changed sessions, merges changed session outcomes into the derived cache, computes the exact refresh summary counts, and returns `scanMode`, `summary`, `providerCursors`, and `cache`. Export the new modules from `packages/core/src/scan/index.ts` and `packages/core/src/index.ts`.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/state/state-schema.test.ts packages/core/src/scan/refresh-cache.test.ts packages/core/src/scan/incremental-refresh.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/state/state-schema.ts` contains `lastRefreshedAt`
    - `packages/core/src/state/state-schema.ts` contains `lastScanMode`
    - `packages/core/src/state/state-schema.ts` contains `lastPublishDecision`
    - `packages/core/src/state/state-schema.ts` contains `lastPublishedAt`
    - `packages/core/src/scan/refresh-cache.ts` contains `.agent-badge/cache/session-index.json`
    - `packages/core/src/scan/refresh-cache.ts` contains `buildRefreshCacheKey`
    - `packages/core/src/scan/refresh-cache.ts` contains `${provider}:${providerSessionId}`
    - `packages/core/src/providers/codex/codex-adapter.ts` contains `scanCodexSessionsIncremental`
    - `packages/core/src/providers/claude/claude-adapter.ts` contains `scanClaudeSessionsIncremental`
    - `packages/core/src/scan/incremental-refresh.ts` contains `export async function runIncrementalRefresh`
    - `packages/core/src/scan/incremental-refresh.ts` contains `forceFull`
  </acceptance_criteria>
  <done>Phase 5 now has a concrete incremental-scan contract, a derived local cache for delta-safe totals, and a persisted refresh summary/status surface for later commands.</done>
</task>

<task type="auto">
  <name>Task 2: Add a hash-aware publish helper that can skip unchanged badge uploads</name>
  <files>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/index.ts</files>
  <read_first>packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/core/src/publish/badge-payload.ts, packages/core/src/publish/github-gist-client.ts, packages/core/src/state/state-schema.ts, packages/core/src/scan/incremental-refresh.ts, .planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md, .planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md</read_first>
  <action>Update `packages/core/src/publish/publish-service.ts` so the existing `publishBadgeToGist()` remains available for current callers, but add a new helper `publishBadgeIfChanged({ config, state, includedTotals, client, now, skipIfUnchanged })`. The helper must build the endpoint payload through `buildEndpointBadgePayload()`, serialize it with `JSON.stringify(payload, null, 2) + "\\n"`, hash it with `createHash("sha256")`, and return `{ state, decision }` where `decision` is exactly `"published"` or `"skipped"`. When `skipIfUnchanged === true` and the new hash equals `state.publish.lastPublishedHash`, do not call `client.updateGistFile`, keep `publish.lastPublishedAt` unchanged, and return `decision: "skipped"`. When the hash differs, update the deterministic file `agent-badge.json`, set `publish.status = "published"`, set `publish.lastPublishedHash` to the new hash, set `publish.lastPublishedAt = now`, and return `decision: "published"`. Keep `publishBadgeToGist()` as a wrapper that computes `includedTotals` from `scan` plus `attribution` and delegates to `publishBadgeIfChanged({ skipIfUnchanged: false })` so existing Phase 4 call sites stay valid during this plan. Export `publishBadgeIfChanged` from `packages/core/src/publish/index.ts`.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/publish-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/publish/publish-service.ts` contains `publishBadgeIfChanged`
    - `packages/core/src/publish/publish-service.ts` contains `skipIfUnchanged`
    - `packages/core/src/publish/publish-service.ts` contains `decision: "skipped"`
    - `packages/core/src/publish/publish-service.ts` contains `lastPublishedAt`
    - `packages/core/src/publish/publish-service.ts` contains `agent-badge.json`
    - `packages/core/src/publish/publish-service.test.ts` contains `skips remote update when the payload hash matches`
    - `packages/core/src/publish/publish-service.test.ts` contains `publishes when the label changes`
    - `packages/core/src/publish/index.ts` contains `publishBadgeIfChanged`
  </acceptance_criteria>
  <done>The core publish layer can now make a correct visible-change decision without breaking existing publish/init callers.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --run packages/core/src/state/state-schema.test.ts packages/core/src/scan/refresh-cache.test.ts packages/core/src/scan/incremental-refresh.test.ts packages/core/src/publish/publish-service.test.ts` succeeds
- [ ] `packages/core/src/scan/incremental-refresh.ts` falls back to full scan only when `forceFull` is set or a provider cursor/cache is unusable
- [ ] `packages/core/src/scan/refresh-cache.ts` stores only stable session keys and aggregate contribution fields
- [ ] `packages/core/src/publish/publish-service.ts` skips `client.updateGistFile` when the serialized payload hash matches
</verification>

<success_criteria>
- SCAN-04 has a core service contract with real provider-owned cursors and no fake checkpoint semantics
- PUBL-05 has a safe exact-payload hash rule instead of a totals-only shortcut
- Phase 5 now has persisted refresh summary data that later `status` output can read without a full historical rescan
</success_criteria>

<output>
After completion, create `.planning/phases/05-incremental-refresh-and-operator-commands/05-01-SUMMARY.md`
</output>

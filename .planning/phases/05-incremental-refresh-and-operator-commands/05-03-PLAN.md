---
phase: 05-incremental-refresh-and-operator-commands
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - packages/core/src/runtime/local-cli.ts
  - packages/core/src/runtime/local-cli.test.ts
  - packages/core/src/init/runtime-wiring.ts
  - packages/core/src/init/runtime-wiring.test.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
  - packages/agent-badge/src/commands/config.ts
  - packages/agent-badge/src/commands/config.test.ts
  - packages/agent-badge/src/commands/refresh.test.ts
autonomous: true
requirements:
  - OPER-01
  - OPER-02
  - OPER-04
user_setup: []
must_haves:
  truths:
    - "The default managed `pre-push` path still runs `agent-badge refresh --hook pre-push --fail-soft` and remains bypassable with normal Git `--no-verify` behavior."
    - "Refresh automation settings changed through `config` reconcile the repo-local script and managed hook block instead of leaving init-time wiring stale."
    - "Disabling pre-push automation removes only the managed hook block while preserving unrelated hook content."
    - "Strict mode is an explicit opt-in that removes the failure-soft `|| true` behavior from the managed hook path without changing the default."
  artifacts:
    - path: packages/core/src/runtime/local-cli.ts
      provides: exact refresh script and hook command builders
      contains: "agent-badge refresh --hook pre-push --fail-soft"
    - path: packages/core/src/init/runtime-wiring.ts
      provides: config-aware runtime wiring reconciliation
      contains: "refresh.prePush"
    - path: packages/agent-badge/src/commands/config.ts
      provides: post-init hook reconciliation after config changes
      contains: "applyRepoLocalRuntimeWiring"
    - path: packages/core/src/init/runtime-wiring.test.ts
      provides: hook marker and fail-soft/strict regression coverage
      contains: "agent-badge:start"
  key_links:
    - from: packages/core/src/init/runtime-wiring.ts
      to: packages/core/src/runtime/local-cli.ts
      via: "managed script and hook command generation"
      pattern: "getAgentBadgeRefreshScriptCommand|getPrePushRefreshCommand"
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/init/runtime-wiring.ts
      via: "init-time wiring based on persisted refresh config"
      pattern: "applyRepoLocalRuntimeWiring"
    - from: packages/agent-badge/src/commands/config.ts
      to: packages/core/src/init/runtime-wiring.ts
      via: "config-driven wiring reconciliation"
      pattern: "applyRepoLocalRuntimeWiring"
---

<objective>
Finish Phase 5 by making the installed runtime wiring honor the new refresh/config semantics without breaking the Phase 1 init contract.

Purpose: OPER-01 is not done when `refresh` exists; it is done when the managed `pre-push` path stays fast, failure-soft by default, configurable after init, and idempotent across reruns.
Output: config-aware refresh script builders, managed hook reconciliation, and integration tests proving default, strict, disabled, and rerun behavior.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-01-PLAN.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-02-PLAN.md
@packages/core/src/runtime/local-cli.ts
@packages/core/src/init/runtime-wiring.ts
@packages/core/src/init/runtime-wiring.test.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/init.test.ts
@packages/agent-badge/src/commands/config.ts
@packages/agent-badge/src/commands/refresh.ts

<interfaces>
From `packages/core/src/runtime/local-cli.ts` before this plan:

```ts
export function getAgentBadgeRefreshScriptCommand(): string {
  return "agent-badge refresh --hook pre-push --fail-soft";
}
```

From `packages/core/src/init/runtime-wiring.ts` before this plan:

```ts
export async function applyRepoLocalRuntimeWiring(
  options: ApplyRepoLocalRuntimeWiringOptions
): Promise<RepoLocalRuntimeWiringResult>;
```

From `packages/core/src/config/config-schema.ts` after `05-02`:

```ts
refresh: {
  prePush: {
    enabled: boolean;
    mode: "fail-soft" | "strict";
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make runtime wiring config-aware for fail-soft, strict, and disabled pre-push modes</name>
  <files>packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/local-cli.test.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts</files>
  <read_first>packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/local-cli.test.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/core/src/config/config-schema.ts, .planning/phases/05-incremental-refresh-and-operator-commands/05-RESEARCH.md, .planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md</read_first>
  <action>Update `packages/core/src/runtime/local-cli.ts` so `getAgentBadgeRefreshScriptCommand()` becomes `getAgentBadgeRefreshScriptCommand(mode: "fail-soft" | "strict" = "fail-soft")` and returns the exact string `agent-badge refresh --hook pre-push --fail-soft` for `"fail-soft"` or `agent-badge refresh --hook pre-push` for `"strict"`. Keep `agentBadgeRefreshScriptName = "agent-badge:refresh"` unchanged. Update `getPrePushRefreshCommand()` so it still invokes the script through the detected package manager. Then extend `packages/core/src/init/runtime-wiring.ts` so `applyRepoLocalRuntimeWiring({ cwd, packageManager, runtimeDependencySpecifier, refresh })` manages `scripts["agent-badge:refresh"]` from `refresh.prePush.mode`, installs a managed hook block only when `refresh.prePush.enabled === true`, writes `<runner> agent-badge:refresh || true` for fail-soft mode, writes `<runner> agent-badge:refresh` for strict mode, and removes only the marker-bounded managed block when pre-push is disabled. If the managed block is removed and no other hook content remains, rewrite the file to exactly `#!/bin/sh\n` and keep it executable. Preserve unrelated `package.json` fields and unrelated hook content outside the managed markers. Add tests covering default fail-soft wiring, strict-mode wiring, disabled-mode managed-block removal, and idempotent reruns.</action>
  <verify>
    <automated>npm test -- --run packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/runtime/local-cli.ts` contains `getAgentBadgeRefreshScriptCommand(mode: "fail-soft" | "strict" = "fail-soft")`
    - `packages/core/src/runtime/local-cli.ts` contains `agent-badge refresh --hook pre-push --fail-soft`
    - `packages/core/src/runtime/local-cli.ts` contains `agent-badge refresh --hook pre-push`
    - `packages/core/src/init/runtime-wiring.ts` contains `refresh.prePush.enabled`
    - `packages/core/src/init/runtime-wiring.ts` contains `refresh.prePush.mode`
    - `packages/core/src/init/runtime-wiring.ts` contains `#!/bin/sh`
    - `packages/core/src/init/runtime-wiring.ts` contains `|| true`
    - `packages/core/src/init/runtime-wiring.test.ts` contains `writes a strict pre-push hook without fail-soft fallback`
    - `packages/core/src/init/runtime-wiring.test.ts` contains `removes only the managed hook block when pre-push is disabled`
    - `packages/core/src/runtime/local-cli.test.ts` contains `returns the exact pre-push refresh command for strict mode`
  </acceptance_criteria>
  <done>The repo-local runtime wiring now reflects the configured pre-push mode intentionally instead of freezing the init-time default forever.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile runtime wiring from init and config flows, then prove the managed hook contract end to end</name>
  <files>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/commands/config.ts, packages/agent-badge/src/commands/config.test.ts, packages/agent-badge/src/commands/refresh.test.ts</files>
  <read_first>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/commands/config.ts, packages/agent-badge/src/commands/config.test.ts, packages/agent-badge/src/commands/refresh.ts, packages/core/src/init/runtime-wiring.ts, packages/core/src/runtime/package-manager.ts, .planning/phases/05-incremental-refresh-and-operator-commands/05-02-PLAN.md, .planning/phases/05-incremental-refresh-and-operator-commands/05-VALIDATION.md</read_first>
  <action>Update `packages/agent-badge/src/commands/init.ts` so `runInitCommand()` passes the persisted `config.refresh` object into `applyRepoLocalRuntimeWiring()` instead of relying on an implicit default script/hook mode. Update `packages/agent-badge/src/commands/config.ts` so any successful change to `refresh.prePush.enabled` or `refresh.prePush.mode` immediately calls `applyRepoLocalRuntimeWiring()` to reconcile `package.json#scripts.agent-badge:refresh` and `.git/hooks/pre-push` in the current repo. Use the repo's detected package manager for this reconciliation, not a hard-coded `npm` assumption. Extend tests so init still creates exactly one managed pre-push block by default, config flips from fail-soft to strict update the script and hook in place, disabling pre-push removes only the managed block, re-enabling it restores one managed block, and direct hook execution still invokes the refresh command path. In `packages/agent-badge/src/commands/refresh.test.ts`, add a regression that hook-mode refresh output stays concise and that fail-soft mode swallows errors while strict mode surfaces them.</action>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/commands/refresh.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/init.ts` contains `config.refresh`
    - `packages/agent-badge/src/commands/init.ts` contains `applyRepoLocalRuntimeWiring`
    - `packages/agent-badge/src/commands/config.ts` contains `refresh.prePush.enabled`
    - `packages/agent-badge/src/commands/config.ts` contains `refresh.prePush.mode`
    - `packages/agent-badge/src/commands/config.ts` contains `applyRepoLocalRuntimeWiring`
    - `packages/agent-badge/src/commands/init.test.ts` contains `creates exactly one managed pre-push block by default`
    - `packages/agent-badge/src/commands/config.test.ts` contains `updates the managed hook to strict mode`
    - `packages/agent-badge/src/commands/config.test.ts` contains `removes only the managed block when pre-push is disabled`
    - `packages/agent-badge/src/commands/config.test.ts` contains `restores one managed block when pre-push is re-enabled`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `keeps pre-push output concise`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `surfaces hook errors in strict mode`
  </acceptance_criteria>
  <done>The installed automation path now stays aligned with config changes and preserves the default failure-soft contract without duplicating or orphaning managed hook content.</done>
</task>

</tasks>

<verification>
- [ ] `npm test -- --run packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/commands/refresh.test.ts` succeeds
- [ ] Default init wiring still writes a managed `pre-push` path that eventually runs `agent-badge refresh --hook pre-push --fail-soft`
- [ ] Strict mode removes the `|| true` fallback from the managed hook block
- [ ] Disabling pre-push removes only the marker-bounded managed content and preserves unrelated hook lines
</verification>

<success_criteria>
- OPER-01 is satisfied by real managed-hook behavior, not just by a refresh command existing
- Refresh automation remains failure-soft by default and configurable after init
- Config changes converge the repo-local script and hook state instead of leaving stale wiring behind
</success_criteria>

<output>
After completion, create `.planning/phases/05-incremental-refresh-and-operator-commands/05-03-SUMMARY.md`
</output>

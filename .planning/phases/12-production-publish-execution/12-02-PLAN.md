---
phase: 12-production-publish-execution
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/phases/12-production-publish-execution/12-preflight.json
  - .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md
  - .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
  - docs/RELEASE.md
autonomous: false
requirements:
  - REL-08
user_setup:
  - service: npm
    why: "The first production publish and post-publish registry reads require real publish authorization for `agent-badge`, `create-agent-badge`, and `@agent-badge/core`."
    env_vars:
      - name: NPM_TOKEN
        source: "GitHub repository secret `NPM_TOKEN` or maintainer npm auth configured for the explicitly chosen publish path"
    dashboard_config:
      - task: "Confirm the authenticated npm identity can publish the three target packages at version `1.1.0`"
        location: "npmjs.com -> package access / organization ownership"
  - service: GitHub
    why: "The primary production publish path uses `.github/workflows/release.yml` and requires workflow trigger/view access plus release secret availability."
    env_vars:
      - name: GITHUB_TOKEN
        source: "Maintainer GitHub CLI auth or token able to trigger/view Actions runs when the workflow path is used"
    dashboard_config:
      - task: "Confirm repository Actions access and that the `NPM_TOKEN` secret remains configured for `.github/workflows/release.yml`"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions"
must_haves:
  truths:
    - "The current source is published through the explicitly chosen production path after a successful final preflight from the same commit."
    - "The resulting release evidence records the exact commit, publish path, workflow or fallback metadata, and registry-visible versions for `agent-badge`, `create-agent-badge`, and `@agent-badge/core`."
    - "Any operator correction discovered during the real publish is captured concretely in docs instead of remaining an undocumented manual recovery step."
  artifacts:
    - path: .planning/phases/12-production-publish-execution/12-preflight.json
      provides: exact final preflight record captured immediately before publish
      contains: '"overallStatus"'
    - path: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
      provides: machine-readable record of the production publish result
      contains: '"publishPath"'
    - path: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md
      provides: human-readable production publish evidence report
      contains: 'Published commit:'
  key_links:
    - from: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
      to: .planning/phases/12-production-publish-execution/12-preflight.json
      via: "release evidence points back to the exact preflight report used before the irreversible publish step"
      pattern: '"preflightPath"'
    - from: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json
      to: packages/agent-badge/package.json
      via: "release evidence records the manifest-derived package inventory and expected version for the shipped source"
      pattern: '"agent-badge"'
    - from: .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md
      to: docs/RELEASE.md
      via: "final evidence reflects the documented production publish path and any concrete operator correction made during execution"
      pattern: 'Publish path:'
---

<objective>
Execute and document the first production publish from current source.

Purpose: REL-08 is only complete once the maintainer performs the real publish with live credentials and records trustworthy evidence of what was shipped and what the registry/workflow reported.
Output: the final preflight JSON, the completed publish-evidence artifacts, and any exact release-doc correction required by the real operator experience.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-production-publish-execution/12-RESEARCH.md
@.planning/phases/12-production-publish-execution/12-VALIDATION.md
@.planning/phases/12-production-publish-execution/12-01-PLAN.md
@package.json
@docs/RELEASE.md
@scripts/release/preflight.ts
@scripts/release/capture-publish-evidence.ts
@.github/workflows/release.yml
@packages/core/package.json
@packages/agent-badge/package.json
@packages/create-agent-badge/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the final preflight from current source and execute the chosen production publish path</name>
  <files>.planning/phases/12-production-publish-execution/12-preflight.json</files>
  <read_first>docs/RELEASE.md, .github/workflows/release.yml, package.json, scripts/release/preflight.ts, .planning/phases/12-production-publish-execution/12-RESEARCH.md, .planning/phases/12-production-publish-execution/12-01-PLAN.md, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json</read_first>
  <action>From the exact commit intended for release, run `npm run docs:check`, `npm run typecheck`, `npm run verify:clean-checkout`, and then `npm run release:preflight -- --json > .planning/phases/12-production-publish-execution/12-preflight.json`. Confirm the preflight report is for the same source commit and that `"overallStatus"` is not `"blocked"` before continuing. Use `.github/workflows/release.yml` as the primary publish authority: trigger or monitor that workflow from the documented path, record the exact workflow run URL, run ID, conclusion, and completion timestamp, and wait until it finishes successfully. Only if the workflow path cannot be used, execute the explicit local fallback from `docs/RELEASE.md` with real npm auth and capture the exact fallback reason instead of silently switching paths. Do not publish from a different commit than the one verified by the saved preflight report.</action>
  <acceptance_criteria>
    - `.planning/phases/12-production-publish-execution/12-preflight.json` exists
    - `.planning/phases/12-production-publish-execution/12-preflight.json` contains `"overallStatus"`
    - `.planning/phases/12-production-publish-execution/12-preflight.json` contains `"packages"`
    - the operator record includes an exact workflow run URL and run ID when `publishPath` is `github-actions`
    - the operator record includes an exact fallback reason when `publishPath` is `local-cli`
    - the publish is executed only after `npm run docs:check`, `npm run typecheck`, `npm run verify:clean-checkout`, and `npm run release:preflight -- --json` all complete from the release commit
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/12-production-publish-execution/12-preflight.json && rg -n '"overallStatus"|"packages"' .planning/phases/12-production-publish-execution/12-preflight.json</automated>
  </verify>
  <done>The release commit has one exact preflight record, and the real production publish has been run through the primary workflow path or an explicitly documented fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Write the final publish evidence and codify any real operator correction</name>
  <files>.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md, .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json, docs/RELEASE.md</files>
  <read_first>docs/RELEASE.md, .planning/phases/12-production-publish-execution/12-preflight.json, scripts/release/capture-publish-evidence.ts, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json</read_first>
  <action>After the production publish completes, run the exact post-publish registry reads `npm view agent-badge version dist-tags.latest --json`, `npm view create-agent-badge version dist-tags.latest --json`, and `npm view @agent-badge/core version dist-tags.latest --json`, then run `npm run release:evidence -- --phase-dir .planning/phases/12-production-publish-execution --publish-path github-actions --preflight-json .planning/phases/12-production-publish-execution/12-preflight.json --workflow-run-url <exact URL> --workflow-run-id <exact ID> --workflow-run-conclusion success --published-at <ISO8601>` when the workflow path was used. If the explicit local fallback path was used, run the same command with `--publish-path local-cli --fallback-reason <exact reason>` and omit workflow fields.

Use the preflight file and evidence output to verify every publishable package version before final handoff: each registry value in `registryResults` must match the corresponding `intendedVersion` from `.planning/phases/12-production-publish-execution/12-preflight.json` (or a declared `registryGap` correction explicitly documented in `docs/RELEASE.md` if the exact version differs). If a gap appears and is intentional, block completion until `docs/RELEASE.md` contains the concrete operator correction and approval record, then re-run `npm run release:evidence` with corrected metadata.

Confirm the generated `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` and `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md` record the release commit, package inventory, publish path, preflight file, and registry-visible results. If the real publish exposed an operator-facing gap in `docs/RELEASE.md`, update the doc with the exact corrected step or prerequisite discovered during execution; do not add vague retrospective notes.</action>
  <acceptance_criteria>
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` exists
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` contains `"publishPath"`
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` contains `"gitSha"`
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` contains `"registryResults"`
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` contains `"agent-badge"`
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md` contains `Publish path:`
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md` contains `Published commit:`
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md` contains `Registry results:`
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md` contains `agent-badge`
    - `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` package version results are compared against `.planning/phases/12-production-publish-execution/12-preflight.json` intended versions before marking Phase 2 complete
    - `docs/RELEASE.md` contains any exact operator correction discovered during the real publish, or remains unchanged when no correction was needed
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json && test -f .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md && node --input-type=module -e "import fs from 'node:fs'; const preflight=JSON.parse(fs.readFileSync('.planning/phases/12-production-publish-execution/12-preflight.json','utf8')); const evidence=JSON.parse(fs.readFileSync('.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json','utf8')); const expected=new Map(preflight.packages.map(p=>[p.packageName,p.intendedVersion])); let ok=true; for(const r of evidence.registryResults){const expectedVersion=expected.get(r.package); if (!expectedVersion || r.version!==expectedVersion){ok=false; break;}} if(!ok) throw new Error('Registry results do not match preflight intended versions.');"</automated>
    <automated>rg -n '"publishPath"|"gitSha"|"registryResults"|"agent-badge"' .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json && rg -n 'Publish path:|Published commit:|Registry results:|agent-badge' .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md</automated>
  </verify>
  <done>The repo contains a trustworthy, phase-scoped release record for the first production publish, and the release checklist reflects any exact operator correction learned during execution.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/12-production-publish-execution/12-preflight.json && rg -n '"overallStatus"|"packages"' .planning/phases/12-production-publish-execution/12-preflight.json`
- `test -f .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json && test -f .planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md`
- `npm view agent-badge version dist-tags.latest --json`
- `npm view create-agent-badge version dist-tags.latest --json`
- `npm view @agent-badge/core version dist-tags.latest --json`
</verification>

<success_criteria>
- REL-08 is satisfied by a real publish from current source with an exact preflight record and a checked-in evidence artifact.
- The resulting release record shows what commit shipped, which path was used, and what the npm registry reported for the three published packages.
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-publish-execution/12-02-SUMMARY.md`
</output>

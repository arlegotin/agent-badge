---
phase: 12-production-publish-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/RELEASE.md
  - scripts/release/capture-publish-evidence.ts
  - scripts/release/capture-publish-evidence.test.ts
  - scripts/verify-docs.sh
autonomous: true
requirements:
  - REL-08
user_setup: []
must_haves:
  truths:
    - "Maintainers have one explicit production publish path with a documented checkpoint after successful preflight and before the irreversible publish trigger."
    - "The repo can write a structured release-evidence artifact that records git SHA, package inventory, chosen publish path, preflight source, workflow metadata, and post-publish registry-visible results."
    - "The release checklist and docs gate enforce the required evidence-capture steps so the first production publish does not rely on ad hoc notes."
  artifacts:
    - path: package.json
      provides: repo-owned entrypoint for writing structured publish evidence
      contains: '"release:evidence": "tsx scripts/release/capture-publish-evidence.ts"'
    - path: scripts/release/capture-publish-evidence.ts
      provides: deterministic release-evidence writer for Phase 12 publish records
      contains: 'workflowRunUrl'
    - path: docs/RELEASE.md
      provides: maintainer-facing production publish instructions with required evidence files
      contains: '12-PUBLISH-EVIDENCE'
    - path: scripts/verify-docs.sh
      provides: deterministic docs enforcement for the production publish evidence contract
      contains: '12-PUBLISH-EVIDENCE'
  key_links:
    - from: package.json
      to: scripts/release/capture-publish-evidence.ts
      via: "root release-evidence script delegates to the repo-owned evidence writer"
      pattern: 'release:evidence'
    - from: scripts/release/capture-publish-evidence.ts
      to: packages/agent-badge/package.json
      via: "evidence writer derives the publishable package inventory from workspace manifests"
      pattern: 'packages/agent-badge/package.json'
    - from: docs/RELEASE.md
      to: .github/workflows/release.yml
      via: "maintainer release instructions point at the checked-in workflow as the primary production publish path"
      pattern: '.github/workflows/release.yml'
---

<objective>
Align the production publish path and release-evidence capture with the real operator workflow.

Purpose: REL-08 is not satisfied by Phase 11 readiness alone. Before the first real publish, the repo needs one explicit operator path and one structured evidence contract that records exactly what source was shipped and what the workflow/registry reported.
Output: a repo-owned `release:evidence` helper, tested evidence serialization, and release docs that require evidence capture around the real publish step.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-production-publish-execution/12-RESEARCH.md
@.planning/phases/12-production-publish-execution/12-VALIDATION.md
@package.json
@docs/RELEASE.md
@scripts/verify-docs.sh
@scripts/release/preflight.ts
@scripts/release/preflight.test.ts
@.github/workflows/release.yml
@.changeset/config.json
@packages/core/package.json
@packages/agent-badge/package.json
@packages/create-agent-badge/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a repo-owned release-evidence writer and root script for Phase 12 publish records</name>
  <files>package.json, scripts/release/capture-publish-evidence.ts, scripts/release/capture-publish-evidence.test.ts</files>
  <read_first>package.json, scripts/release/preflight.ts, scripts/release/preflight.test.ts, .planning/phases/12-production-publish-execution/12-RESEARCH.md, .changeset/config.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json</read_first>
  <action>Add a root script `"release:evidence": "tsx scripts/release/capture-publish-evidence.ts"` to `package.json`. Create `scripts/release/capture-publish-evidence.ts` as a repo-owned TypeScript entrypoint that accepts exact CLI flags `--phase-dir`, `--publish-path`, `--preflight-json`, `--workflow-run-url`, `--workflow-run-id`, `--workflow-run-conclusion`, `--published-at`, and optional `--fallback-reason`. The script must derive the publishable package inventory from `packages/core/package.json`, `packages/agent-badge/package.json`, and `packages/create-agent-badge/package.json`; capture `git rev-parse HEAD`; read the preflight JSON file produced by `npm run release:preflight -- --json`; run `npm view <package> version dist-tags.latest --json` for `@agent-badge/core`, `agent-badge`, and `create-agent-badge`; and write both `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.json` and `.planning/phases/12-production-publish-execution/12-PUBLISH-EVIDENCE.md`. The JSON must include exactly `publishPath`, `gitSha`, `preflightPath`, `publishedAt`, `packages`, `registryResults`, and workflow fields when `--publish-path github-actions` is used, plus optional `fallbackReason` when `--publish-path local-cli` is used. The Markdown must print lines beginning with `Publish path:`, `Published commit:`, `Preflight file:`, `Workflow run:`, `Fallback reason:`, and `Registry results:`. Create `scripts/release/capture-publish-evidence.test.ts` with deterministic coverage that stubs child-process calls and verifies required fields, manifest-derived package inventory, Git SHA capture, optional fallback reason persistence, and omission of workflow fields for local fallback mode.</action>
  <acceptance_criteria>
    - `package.json` contains `"release:evidence": "tsx scripts/release/capture-publish-evidence.ts"`
    - `scripts/release/capture-publish-evidence.ts` contains `--phase-dir`
    - `scripts/release/capture-publish-evidence.ts` contains `--publish-path`
    - `scripts/release/capture-publish-evidence.ts` contains `--preflight-json`
    - `scripts/release/capture-publish-evidence.ts` contains `workflowRunUrl`
    - `scripts/release/capture-publish-evidence.ts` contains `git rev-parse`
    - `scripts/release/capture-publish-evidence.ts` contains `npm view`
    - `scripts/release/capture-publish-evidence.ts` contains `12-PUBLISH-EVIDENCE.json`
    - `scripts/release/capture-publish-evidence.ts` contains `12-PUBLISH-EVIDENCE.md`
    - `scripts/release/capture-publish-evidence.ts` contains `fallbackReason`
    - `scripts/release/capture-publish-evidence.test.ts` contains `describe("capture publish evidence"`
    - `scripts/release/capture-publish-evidence.test.ts` contains `publishPath`
    - `scripts/release/capture-publish-evidence.test.ts` contains `fallbackReason`
    - `scripts/release/capture-publish-evidence.test.ts` contains `workflowRunUrl`
    - `scripts/release/capture-publish-evidence.test.ts` contains `agent-badge`
    - `scripts/release/capture-publish-evidence.test.ts` contains `create-agent-badge`
    - `scripts/release/capture-publish-evidence.test.ts` contains `@agent-badge/core`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run scripts/release/capture-publish-evidence.test.ts</automated>
  </verify>
  <done>The repo can generate a deterministic Phase 12 release-evidence record from current repo state plus exact publish metadata, without relying on handwritten notes.</done>
</task>

<task type="auto">
  <name>Task 2: Make the release checklist require preflight capture, workflow path selection, and evidence recording</name>
  <files>docs/RELEASE.md, scripts/verify-docs.sh</files>
  <read_first>docs/RELEASE.md, scripts/verify-docs.sh, package.json, scripts/release/capture-publish-evidence.ts, .github/workflows/release.yml, .planning/phases/12-production-publish-execution/12-RESEARCH.md</read_first>
  <action>Update `docs/RELEASE.md` so the production publish checklist becomes explicit after the existing local gates: save `npm run release:preflight -- --json` to `.planning/phases/12-production-publish-execution/12-preflight.json`; stop if the report is `OVERALL: blocked`; treat `.github/workflows/release.yml` as the primary production publish path; document that the operator must trigger or monitor that workflow and record the run URL/ID; and require `npm run release:evidence -- --phase-dir .planning/phases/12-production-publish-execution --publish-path github-actions --preflight-json .planning/phases/12-production-publish-execution/12-preflight.json --workflow-run-url <url> --workflow-run-id <id> --workflow-run-conclusion success --published-at <ISO8601>` after a successful publish. Document local `npm run release` only as an explicit fallback when the workflow path cannot be used, and require `--fallback-reason <text>` in that case. Update `scripts/verify-docs.sh` in the existing exact-string style so docs verification enforces `12-preflight.json`, `12-PUBLISH-EVIDENCE.md`, `npm run release:evidence`, `.github/workflows/release.yml`, `workflow_dispatch`, and the continued presence of `npm run release:preflight` and `NPM_TOKEN` guidance.</action>
  <acceptance_criteria>
    - `docs/RELEASE.md` contains `12-preflight.json`
    - `docs/RELEASE.md` contains `12-PUBLISH-EVIDENCE.md`
    - `docs/RELEASE.md` contains `npm run release:evidence`
    - `docs/RELEASE.md` contains `.github/workflows/release.yml`
    - `docs/RELEASE.md` contains `workflow_dispatch`
    - `docs/RELEASE.md` contains `--fallback-reason`
    - `scripts/verify-docs.sh` contains `12-preflight.json`
    - `scripts/verify-docs.sh` contains `12-PUBLISH-EVIDENCE.md`
    - `scripts/verify-docs.sh` contains `npm run release:evidence`
    - `scripts/verify-docs.sh` contains `.github/workflows/release.yml`
    - `scripts/verify-docs.sh` contains `workflow_dispatch`
    - `scripts/verify-docs.sh` still contains `npm run release:preflight`
    - `scripts/verify-docs.sh` still contains `NPM_TOKEN`
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check</automated>
  </verify>
  <done>The maintainer checklist now tells the operator exactly how to capture final preflight output, use the primary workflow publish path, and persist a structured evidence record for the first production release.</done>
</task>

</tasks>

<verification>
- `npm test -- --run scripts/release/capture-publish-evidence.test.ts` succeeds
- `npm run docs:check` succeeds
- `npm run release:evidence -- --phase-dir .planning/phases/12-production-publish-execution --publish-path github-actions --preflight-json .planning/phases/12-production-publish-execution/12-preflight.json --workflow-run-url https://github.com/example/repo/actions/runs/1 --workflow-run-id 1 --workflow-run-conclusion success --published-at 2026-03-31T00:00:00Z` is documented and writes both evidence artifact formats when given stubbed inputs
</verification>

<success_criteria>
- REL-08 has an explicit operator path and evidence contract before the first real publish starts.
- The repo can record the publish commit, chosen path, workflow metadata, and visible registry results without ad hoc manual note taking.
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-publish-execution/12-01-SUMMARY.md`
</output>

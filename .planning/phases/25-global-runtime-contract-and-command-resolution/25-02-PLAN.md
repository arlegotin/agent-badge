---
phase: 25-global-runtime-contract-and-command-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - "25-01"
files_modified:
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
  - packages/agent-badge/src/commands/config.ts
  - packages/agent-badge/src/commands/config.test.ts
  - packages/agent-badge/src/commands/status.ts
  - packages/agent-badge/src/commands/status.test.ts
  - packages/agent-badge/src/commands/uninstall.test.ts
  - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
  - scripts/smoke/verify-registry-install.sh
autonomous: true
requirements:
  - DIST-03
  - AUTO-01
must_haves:
  truths:
    - "Operator-facing commands that create or inspect managed automation report the shared runtime prerequisite with one consistent `Shared runtime:` vocabulary."
    - "Init and config keep Phase 25 compatibility behavior, but the generated repo contract they describe is the shared `agent-badge` runtime, not package-manager wrapper execution."
    - "Release-readiness and smoke coverage assert the new shared hook contract while preserving the Phase 25 boundary that repo-local runtime installation removal belongs to Phase 26."
    - "Uninstall and compatibility tests cover both the new shared hook block and legacy hook blocks without disturbing user-owned custom lines."
  artifacts:
    - path: packages/agent-badge/src/commands/status.ts
      provides: "operator-visible shared runtime status line"
      contains: "Shared runtime:"
    - path: packages/agent-badge/src/commands/init.ts
      provides: "init-time shared runtime prerequisite reporting"
      contains: "Shared runtime:"
    - path: packages/agent-badge/src/commands/config.ts
      provides: "config-time shared runtime prerequisite reporting for hook rewrites"
      contains: "Shared runtime:"
    - path: scripts/smoke/verify-registry-install.sh
      provides: "Phase 25 contract assertions for generated managed hook content"
      contains: "agent-badge refresh --hook pre-push --hook-policy"
  key_links:
    - from: packages/agent-badge/src/commands/init.ts
      to: packages/core/src/runtime/shared-cli.ts
      via: shared runtime inspection and remediation formatting
      pattern: "Shared runtime:"
    - from: packages/agent-badge/src/commands/status.ts
      to: packages/core/src/runtime/shared-cli.ts
      via: status reporting
      pattern: "Shared runtime:"
    - from: scripts/smoke/verify-registry-install.sh
      to: .git/hooks/pre-push
      via: generated hook assertion
      pattern: "agent-badge refresh --hook pre-push --hook-policy"
---

<objective>
Propagate the shared-runtime contract through operator surfaces and lock it in with compatibility-focused regression proof.

Purpose: Make the new Phase 25 runtime contract observable to operators and mechanically enforced in tests without prematurely doing the Phase 26 initializer-removal work.
Output: Shared-runtime reporting in user-facing commands, updated compatibility regressions, and smoke assertions that pin the new managed hook contract.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-global-runtime-contract-and-command-resolution/25-RESEARCH.md
@.planning/phases/25-global-runtime-contract-and-command-resolution/25-VALIDATION.md
@.planning/phases/01-workspace-and-init-foundation/01-05-SUMMARY.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-03-SUMMARY.md
@.planning/phases/06-doctor-uninstall-and-safety-hardening/06-02-SUMMARY.md
@.planning/phases/13-post-publish-registry-verification-and-final-operations/13-01-SUMMARY.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-02-SUMMARY.md
@packages/core/src/runtime/shared-cli.ts
@packages/core/src/init/runtime-wiring.ts
@packages/core/src/diagnostics/doctor.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/config.ts
@packages/agent-badge/src/commands/status.ts
@packages/agent-badge/src/commands/uninstall.test.ts
@packages/agent-badge/src/commands/release-readiness-matrix.test.ts
@scripts/smoke/verify-registry-install.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface shared runtime availability and remediation in init, config, and status</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/25-global-runtime-contract-and-command-resolution/25-RESEARCH.md
    - .planning/phases/25-global-runtime-contract-and-command-resolution/25-VALIDATION.md
    - packages/core/src/runtime/shared-cli.ts
    - packages/core/src/diagnostics/doctor.ts
    - packages/agent-badge/src/commands/init.ts
    - packages/agent-badge/src/commands/init.test.ts
    - packages/agent-badge/src/commands/config.ts
    - packages/agent-badge/src/commands/config.test.ts
    - packages/agent-badge/src/commands/status.ts
    - packages/agent-badge/src/commands/status.test.ts
  </read_first>
  <files>packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/commands/config.ts, packages/agent-badge/src/commands/config.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts</files>
  <behavior>
    - Test 1: `init`, `config`, and `status` each emit one `- Shared runtime:` line driven by the core Phase 25 probe result.
    - Test 2: when the shared runtime is missing, the commands surface the same remediation vocabulary from core and do not print machine-local absolute paths.
    - Test 3: `init` and `config` keep Phase 25 compatibility behavior by reporting the prerequisite without removing package.json mutation or the legacy initializer install path reserved for Phase 26.
  </behavior>
  <action>Use the new `packages/core/src/runtime/shared-cli.ts` exports from Plan 25-01 in the operator surfaces that create or inspect managed automation: `packages/agent-badge/src/commands/init.ts`, `packages/agent-badge/src/commands/config.ts`, and `packages/agent-badge/src/commands/status.ts`. Add a stable `- Shared runtime:` output line to each command. For `init`, print the runtime result after scaffold/runtime wiring so the operator sees whether managed hooks will resolve `agent-badge` from PATH on future pushes. For `config`, print the same runtime result whenever the command rewrites pre-push automation so DIST-03 is actionable at the moment hook behavior changes. For `status`, expose the current shared runtime state in a privacy-safe way using the exact same remediation text shape as init/config. Reuse the core remediation helper instead of duplicating strings, and keep the Phase 25 boundary explicit in code behavior: do not remove package.json mutation, do not remove the repo-local install compatibility path from `create-agent-badge`, and do not introduce absolute-path persistence into tracked repo artifacts.</action>
  <acceptance_criteria>
    - `rg -n "Shared runtime:" packages/agent-badge/src/commands/init.ts packages/agent-badge/src/commands/config.ts packages/agent-badge/src/commands/status.ts` returns at least one match in each file.
    - `rg -n "npm install -g @legotin/agent-badge|pnpm add -g @legotin/agent-badge|bun add -g @legotin/agent-badge" packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/commands/status.test.ts` returns matches proving one shared remediation vocabulary.
    - `rg -n "node_modules/.bin|npx --no-install|pnpm exec" packages/agent-badge/src/commands/init.ts packages/agent-badge/src/commands/config.ts packages/agent-badge/src/commands/status.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/commands/status.test.ts</automated>
  </verify>
  <done>Init, config, and status all describe the same shared-runtime prerequisite and remediation path, and none of them reintroduce wrapper-specific command assumptions into operator output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update compatibility regressions and smoke assertions to enforce the new managed hook contract</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/25-global-runtime-contract-and-command-resolution/25-RESEARCH.md
    - .planning/phases/25-global-runtime-contract-and-command-resolution/25-VALIDATION.md
    - packages/core/src/init/runtime-wiring.ts
    - packages/core/src/diagnostics/doctor.ts
    - packages/agent-badge/src/commands/uninstall.test.ts
    - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
    - packages/create-agent-badge/src/index.test.ts
    - scripts/smoke/verify-registry-install.sh
  </read_first>
  <files>packages/agent-badge/src/commands/uninstall.test.ts, packages/agent-badge/src/commands/release-readiness-matrix.test.ts, scripts/smoke/verify-registry-install.sh</files>
  <behavior>
    - Test 1: release-readiness assertions verify that managed hooks contain direct `agent-badge refresh --hook pre-push --hook-policy ...` commands and no package-manager runner content.
    - Test 2: uninstall compatibility coverage proves both the new direct shared hook block and the legacy `agent-badge:refresh` block are removed without deleting unrelated custom hook lines.
    - Test 3: smoke assertions verify the generated repo contract, not a package-manager wrapper contract, while preserving the current Phase 25 compatibility install path.
    - Test 4: release-readiness matrix coverage exercises npm, pnpm, Yarn, and Bun repo contexts so the shared runtime contract is proven across supported package-manager lockfile combinations.
  </behavior>
  <action>Update the compatibility and proof layer to encode the real Phase 25 contract instead of the legacy wrapper assumptions. In `packages/agent-badge/src/commands/release-readiness-matrix.test.ts`, replace expectations that focus only on `packageScripts["agent-badge:refresh"]` with assertions over `.git/hooks/pre-push` content so the managed hook contract is pinned to the direct shared command, and add explicit repo fixtures or scenario coverage for npm, pnpm, Yarn, and Bun lockfile contexts. In `packages/agent-badge/src/commands/uninstall.test.ts`, add coverage for uninstalling a repo whose managed hook already uses the new direct shared command, while preserving the existing legacy `agent-badge:refresh` cleanup regression. In `scripts/smoke/verify-registry-install.sh`, add explicit contract assertions over the generated hook content so smoke verification proves AUTO-01 directly. Keep the current repo-local install checks and `packages/create-agent-badge/src/index.test.ts` compatibility coverage intact in this phase; Phase 26 owns removing repo-local runtime installation and default manifest mutation.</action>
  <acceptance_criteria>
    - `rg -n "agent-badge refresh --hook pre-push --hook-policy" packages/agent-badge/src/commands/release-readiness-matrix.test.ts scripts/smoke/verify-registry-install.sh` returns at least one match in each file.
    - `rg -n "npm run --silent agent-badge:refresh|pnpm run --silent agent-badge:refresh|yarn run agent-badge:refresh|bun run agent-badge:refresh" packages/agent-badge/src/commands/release-readiness-matrix.test.ts scripts/smoke/verify-registry-install.sh` returns no matches.
    - `rg -n "echo custom-check" packages/agent-badge/src/commands/uninstall.test.ts` returns at least one match in the new direct-hook cleanup regression, proving custom lines remain intact.
    - `rg -n "installRepoLocalRuntime|Failed to install repo-local @legotin/agent-badge" packages/create-agent-badge/src/index.test.ts` still returns existing compatibility assertions; this task must not delete them.
    - `rg -n "package-lock.json|pnpm-lock.yaml|yarn.lock|bun.lock|bun.lockb" packages/agent-badge/src/commands/release-readiness-matrix.test.ts` returns lockfile coverage for npm, pnpm, Yarn, and Bun repo contexts.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/uninstall.test.ts packages/agent-badge/src/commands/release-readiness-matrix.test.ts packages/create-agent-badge/src/index.test.ts && bash -n scripts/smoke/verify-registry-install.sh</automated>
  </verify>
  <done>The proof layer now enforces the shared managed-hook contract, uninstall covers both new and legacy hook bodies, and Phase 25 still preserves the initializer compatibility path that Phase 26 will later remove.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core runtime probe -> command output | Low-level runtime availability is surfaced to operators and must stay consistent and privacy-safe. |
| legacy repo artifacts -> compatibility tests | Existing repos may still contain old managed hook bodies or package-script wiring. |
| smoke assertions -> persisted evidence | Phase-owned smoke checks must enforce the contract without leaking local file-system details into saved artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | I | `packages/agent-badge/src/commands/init.ts`, `config.ts`, `status.ts` | mitigate | Reuse the core remediation builder exactly and keep output generic; do not print resolved executable locations, home paths, or `node_modules/.bin` paths in user-facing command output. |
| T-25-02-02 | T | `packages/agent-badge/src/commands/uninstall.test.ts`, `release-readiness-matrix.test.ts` | mitigate | Add dual-contract regressions so test coverage explicitly distinguishes the new shared hook body from legacy `agent-badge:refresh` content and prevents silent false-green compatibility drift. |
| T-25-02-03 | D | `scripts/smoke/verify-registry-install.sh` | mitigate | Assert the generated hook contract directly while keeping the Phase 25 compatibility install path intact, so smoke verification does not fail for out-of-scope Phase 26 changes or misreport wrapper assumptions as the canonical contract. |
</threat_model>

<verification>
Run the Phase 25 command-surface and compatibility suite:

- `npm test -- --run packages/agent-badge/src/commands/init.test.ts packages/agent-badge/src/commands/config.test.ts packages/agent-badge/src/commands/status.test.ts`
- `npm test -- --run packages/agent-badge/src/commands/uninstall.test.ts packages/agent-badge/src/commands/release-readiness-matrix.test.ts packages/create-agent-badge/src/index.test.ts && bash -n scripts/smoke/verify-registry-install.sh`
</verification>

<success_criteria>
- `init`, `config`, and `status` expose one consistent shared-runtime prerequisite line and remediation path.
- Compatibility tests and release-readiness proof assert the new direct shared hook contract instead of package-manager wrapper execution.
- Uninstall coverage proves both new and legacy managed hook blocks are removable without harming user-owned hook lines.
- Phase 25 does not remove the repo-local install compatibility path or default manifest mutation that Phase 26 still owns.
</success_criteria>

<output>
After completion, create `.planning/phases/25-global-runtime-contract-and-command-resolution/25-global-runtime-contract-and-command-resolution-02-SUMMARY.md`
</output>

---
phase: 25-global-runtime-contract-and-command-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/runtime/shared-cli.ts
  - packages/core/src/runtime/shared-cli.test.ts
  - packages/core/src/runtime/local-cli.ts
  - packages/core/src/runtime/local-cli.test.ts
  - packages/core/src/runtime/index.ts
  - packages/core/src/init/runtime-wiring.ts
  - packages/core/src/init/runtime-wiring.test.ts
  - packages/core/src/diagnostics/doctor.ts
  - packages/core/src/diagnostics/doctor.test.ts
autonomous: true
requirements:
  - DIST-03
  - AUTO-01
must_haves:
  truths:
    - "Core code can probe whether a shared `agent-badge` executable is available on PATH without using package-manager exec wrappers."
    - "Managed pre-push hook content invokes `agent-badge refresh --hook pre-push --hook-policy ...` directly and no longer routes through repo package-manager script runners."
    - "Diagnostics recognize both the new shared hook contract and the legacy `agent-badge:refresh` contract during the migration window."
    - "Missing-runtime remediation is privacy-safe and never requires embedding machine-local absolute paths in tracked repo artifacts."
  artifacts:
    - path: packages/core/src/runtime/shared-cli.ts
      provides: "typed shared-runtime probe, command builder, and remediation renderer"
      exports:
        - inspectSharedRuntime
        - buildSharedRuntimeRemediation
        - getSharedAgentBadgeCommand
    - path: packages/core/src/init/runtime-wiring.ts
      provides: "shared-first managed hook writer with compatibility-safe package script retention"
      contains: "command -v agent-badge"
    - path: packages/core/src/diagnostics/doctor.ts
      provides: "single-read diagnostics that accept shared and legacy hook wiring"
      contains: "managedContent.includes(\"agent-badge:refresh\")"
  key_links:
    - from: packages/core/src/init/runtime-wiring.ts
      to: packages/core/src/runtime/shared-cli.ts
      via: createManagedHookBlock
      pattern: "command -v agent-badge|agent-badge refresh --hook pre-push --hook-policy"
    - from: packages/core/src/diagnostics/doctor.ts
      to: packages/core/src/runtime/shared-cli.ts
      via: shared remediation helper
      pattern: "buildSharedRuntimeRemediation|agent-badge:refresh"
---

<objective>
Establish the core shared-runtime contract and make the managed hook writer emit the new single canonical command path.

Purpose: Satisfy the Phase 25 contract boundary in core first so later command-surface work reuses one PATH-based runtime probe, one remediation vocabulary, and one hook format.
Output: A new shared-runtime helper module, direct shared hook generation, dual-read diagnostics, and focused regression coverage.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-global-runtime-contract-and-command-resolution/25-RESEARCH.md
@.planning/phases/25-global-runtime-contract-and-command-resolution/25-VALIDATION.md
@.planning/phases/01-workspace-and-init-foundation/01-05-SUMMARY.md
@.planning/phases/05-incremental-refresh-and-operator-commands/05-03-SUMMARY.md
@.planning/phases/06-doctor-uninstall-and-safety-hardening/06-01-SUMMARY.md
@.planning/phases/18-auth-hook-and-publish-readiness-hardening/18-02-SUMMARY.md
@packages/core/src/runtime/local-cli.ts
@packages/core/src/runtime/local-cli.test.ts
@packages/core/src/runtime/package-manager.ts
@packages/core/src/init/runtime-wiring.ts
@packages/core/src/init/runtime-wiring.test.ts
@packages/core/src/diagnostics/doctor.ts
@packages/core/src/diagnostics/doctor.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shared PATH-based runtime contract in core</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/25-global-runtime-contract-and-command-resolution/25-RESEARCH.md
    - .planning/phases/25-global-runtime-contract-and-command-resolution/25-VALIDATION.md
    - packages/core/src/runtime/local-cli.ts
    - packages/core/src/runtime/local-cli.test.ts
    - packages/core/src/runtime/package-manager.ts
    - packages/core/src/runtime/index.ts
  </read_first>
  <files>packages/core/src/runtime/shared-cli.ts, packages/core/src/runtime/shared-cli.test.ts, packages/core/src/runtime/local-cli.ts, packages/core/src/runtime/local-cli.test.ts, packages/core/src/runtime/index.ts</files>
  <behavior>
    - Test 1: `inspectSharedRuntime()` returns `{ status: "available" }` with the reported version when `agent-badge --version` succeeds via `spawnSync(..., { shell: false })`.
    - Test 2: `inspectSharedRuntime()` returns `{ status: "missing" }` on `ENOENT`, and `buildSharedRuntimeRemediation()` includes exactly `npm install -g @legotin/agent-badge`, `pnpm add -g @legotin/agent-badge`, `bun add -g @legotin/agent-badge`, plus a generic PATH fix note.
    - Test 3: shared command builders return plain `agent-badge` commands for init and refresh and never return `npx --no-install`, `pnpm exec`, `yarn run`, `bun run`, `bunx --bun`, or `node_modules/.bin`.
    - Test 4: `runtime-wiring.test.ts` exercises npm, pnpm, Yarn, and Bun repo contexts and proves the generated `pre-push` hook contract is invariant across those lockfile/package-manager combinations.
  </behavior>
  <action>Implement a new `packages/core/src/runtime/shared-cli.ts` module that defines the typed shared-runtime probe and remediation contract for the Phase 25 shared-runtime boundary and DIST-03. Start by defining exported result types in that file, then implement `inspectSharedRuntime()` with `spawnSync("agent-badge", ["--version"], { shell: false, env, encoding: "utf8", stdio: "pipe", windowsHide: true })`. Centralize the operator guidance in one remediation builder that stays generic and privacy-safe: list the npm, pnpm, and bun global/user-scope install commands and tell the operator to ensure the corresponding bin directory is on PATH, but do not resolve or print machine-local absolute paths. Update `packages/core/src/runtime/local-cli.ts` so the canonical command builders for Phase 25 return plain `agent-badge ...` strings; keep only compatibility exports or constants that other code still reads in this phase, and do not leave any package-manager wrapper table in the shared-runtime code path. Re-export the new module from `packages/core/src/runtime/index.ts`. Extend `packages/core/src/init/runtime-wiring.test.ts` in this plan so lockfile-driven repo fixtures for npm, pnpm, Yarn, and Bun each assert the same shared-first hook body and the same package-manager-neutral command contract.</action>
  <acceptance_criteria>
    - `rg -n "spawnSync\\(\"agent-badge\", \\[\"--version\"\\]" packages/core/src/runtime/shared-cli.ts` returns at least one match.
    - `rg -n "npm install -g @legotin/agent-badge|pnpm add -g @legotin/agent-badge|bun add -g @legotin/agent-badge" packages/core/src/runtime/shared-cli.ts` returns all three install lines.
    - `rg -n "npx --no-install|pnpm exec|yarn run|bun run|bunx --bun|node_modules/.bin" packages/core/src/runtime/shared-cli.ts packages/core/src/runtime/local-cli.ts` returns no matches in the shared command-builder path.
    - `rg -n "export \\* from \"\\./shared-cli\\.js\"" packages/core/src/runtime/index.ts` returns exactly one match.
    - `rg -n "packageManager: \"npm\"|packageManager: \"pnpm\"|packageManager: \"yarn\"|packageManager: \"bun\"" packages/core/src/init/runtime-wiring.test.ts` returns matches for all four package-manager contexts.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/runtime/shared-cli.test.ts packages/core/src/runtime/local-cli.test.ts packages/core/src/init/runtime-wiring.test.ts</automated>
  </verify>
  <done>The repo has one exported shared-runtime probe/remediation contract, and core command builders now encode the Phase 25 shared `agent-badge` invocation model instead of package-manager exec wrappers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire hook generation and diagnostics to single-write shared hooks with legacy-read compatibility</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/25-global-runtime-contract-and-command-resolution/25-RESEARCH.md
    - .planning/phases/25-global-runtime-contract-and-command-resolution/25-VALIDATION.md
    - packages/core/src/runtime/shared-cli.ts
    - packages/core/src/init/runtime-wiring.ts
    - packages/core/src/init/runtime-wiring.test.ts
    - packages/core/src/diagnostics/doctor.ts
    - packages/core/src/diagnostics/doctor.test.ts
    - packages/agent-badge/src/commands/init.test.ts
  </read_first>
  <files>packages/core/src/init/runtime-wiring.ts, packages/core/src/init/runtime-wiring.test.ts, packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts</files>
  <behavior>
    - Test 1: a newly generated fail-soft hook contains a `command -v agent-badge >/dev/null 2>&1` guard, prints the shared remediation text when missing, and runs `agent-badge refresh --hook pre-push --hook-policy fail-soft || true`.
    - Test 2: a strict hook contains the same direct command without `|| true`.
    - Test 3: doctor passes for a direct shared hook block and still treats legacy `agent-badge:refresh` marker blocks as valid compatibility wiring during the migration window.
    - Test 4: npm, pnpm, Yarn, and Bun repo fixtures all generate the same direct shared hook contract even though package-manager detection still exists elsewhere in the repo.
  </behavior>
  <action>Update `packages/core/src/init/runtime-wiring.ts` so Phase 25 becomes single-write for AUTO-01: the managed hook block must call `agent-badge refresh --hook pre-push --hook-policy {mode}` directly, preceded by a shared-runtime availability guard that checks `command -v agent-badge >/dev/null 2>&1` and prints the shared remediation text when the executable is missing. Keep the managed hook marker contract unchanged. Do not embed resolved executable paths or any machine-local absolute paths in the generated hook. Do not remove `package.json` script management or `@legotin/agent-badge` dependency management in this phase; keep those package.json mutations intact only for current compatibility, but make the hook independent of `npm run --silent agent-badge:refresh` and other package-manager runners. Update `packages/core/src/diagnostics/doctor.ts` to use the shared remediation builder and to remain dual-read: accept both the new direct shared hook block and the legacy `agent-badge:refresh` block, but report the new shared-runtime remediation when hook wiring is missing or malformed.</action>
  <acceptance_criteria>
    - `rg -n "command -v agent-badge >/dev/null 2>&1" packages/core/src/init/runtime-wiring.ts` returns at least one match.
    - `rg -n "agent-badge refresh --hook pre-push --hook-policy" packages/core/src/init/runtime-wiring.ts` returns at least one match.
    - `rg -n "npm run --silent agent-badge:refresh|pnpm run --silent agent-badge:refresh|yarn run agent-badge:refresh|bun run agent-badge:refresh" packages/core/src/init/runtime-wiring.ts` returns no matches.
    - `rg -n "managedContent.includes\\(\"agent-badge:refresh\"\\)" packages/core/src/diagnostics/doctor.ts` returns at least one match.
    - `rg -n "npm install -g @legotin/agent-badge|pnpm add -g @legotin/agent-badge|bun add -g @legotin/agent-badge" packages/core/src/diagnostics/doctor.ts` returns matches from the shared remediation path.
    - `rg -n "packageManager: \"npm\"|packageManager: \"pnpm\"|packageManager: \"yarn\"|packageManager: \"bun\"" packages/core/src/init/runtime-wiring.test.ts` returns matches for all four package-manager contexts.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/runtime/shared-cli.test.ts packages/core/src/init/runtime-wiring.test.ts packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/init.test.ts</automated>
  </verify>
  <done>Managed hooks are shared-runtime-first, privacy-safe, and explicit about missing-runtime remediation, while diagnostics still understand legacy hook/script wiring during the migration window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/state -> hook writer | Persisted repo settings influence generated shell hook content. |
| machine PATH -> runtime probe | Local environment determines whether the shared `agent-badge` executable is resolvable. |
| existing hook content -> diagnostics | Doctor must inspect potentially stale or manually edited hook content without misclassifying it as healthy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | T | `packages/core/src/init/runtime-wiring.ts` | mitigate | Build hook content only from fixed templates plus validated `refresh.prePush.mode`; do not interpolate arbitrary commands, package-manager runner strings, or absolute executable paths. |
| T-25-01-02 | I | `packages/core/src/runtime/shared-cli.ts` | mitigate | Keep remediation generic and install/PATH-oriented; never emit resolved home-directory paths, cache paths, or `node_modules/.bin` locations. |
| T-25-01-03 | D | `packages/core/src/diagnostics/doctor.ts` | mitigate | Treat only known shared-hook and legacy-hook markers as valid; warn/fail with explicit remediation when markers are malformed or refresh invocation is absent. |
</threat_model>

<verification>
Run the focused Phase 25 core suite:

- `npm test -- --run packages/core/src/runtime/shared-cli.test.ts packages/core/src/runtime/local-cli.test.ts`
- `npm test -- --run packages/core/src/runtime/shared-cli.test.ts packages/core/src/init/runtime-wiring.test.ts packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/init.test.ts`
</verification>

<success_criteria>
- Shared runtime probing, remediation, and command building live in one exported core module.
- Generated pre-push hooks use the direct shared command contract and no package-manager wrapper runners.
- Doctor accepts both shared and legacy managed hook contracts and reuses the shared remediation vocabulary.
- No generated hook content embeds machine-local absolute executable paths.
</success_criteria>

<output>
After completion, create `.planning/phases/25-global-runtime-contract-and-command-resolution/25-global-runtime-contract-and-command-resolution-01-SUMMARY.md`
</output>

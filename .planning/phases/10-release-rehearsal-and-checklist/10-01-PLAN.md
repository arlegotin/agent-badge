---
phase: 10-release-rehearsal-and-checklist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke/verify-packed-install.sh
autonomous: true
requirements:
  - PACK-03
user_setup: []
must_haves:
  truths:
    - "`npm run smoke:pack` succeeds from a cleaned tree because the script satisfies its own build precondition before packing tarballs."
    - "The packed-install proof resolves exactly one tarball for `@agent-badge/core`, `agent-badge`, and `create-agent-badge`, with no `agent-badge-*.tgz` overlap against `agent-badge-core-*.tgz`."
    - "The smoke rehearsal still installs into a fresh temporary project under `/tmp` with an isolated npm cache and proves both package imports plus both CLI `--help` surfaces."
  artifacts:
    - path: scripts/smoke/verify-packed-install.sh
      provides: standalone build-aware packed-install rehearsal for all publishable tarballs
      contains: 'npm run build'
    - path: package.json
      provides: repo-owned smoke entrypoint for maintainers and CI
      contains: '"smoke:pack": "bash scripts/smoke/verify-packed-install.sh"'
    - path: scripts/verify-clean-checkout.sh
      provides: higher-level clean-checkout verifier that composes with the standalone smoke rehearsal
      contains: 'npm_config_cache="${NPM_CACHE_DIR}" npm run smoke:pack'
  key_links:
    - from: package.json
      to: scripts/smoke/verify-packed-install.sh
      via: "root smoke:pack entrypoint delegates to the repo-owned packed-install rehearsal"
      pattern: '"smoke:pack": "bash scripts/smoke/verify-packed-install.sh"'
    - from: scripts/smoke/verify-packed-install.sh
      to: packages/agent-badge/package.json
      via: "packs and installs the exact runtime tarball before import and CLI checks"
      pattern: 'npm pack --workspace packages/agent-badge'
    - from: scripts/smoke/verify-packed-install.sh
      to: packages/create-agent-badge/package.json
      via: "packs and installs the exact initializer tarball before import and CLI checks"
      pattern: 'npm pack --workspace packages/create-agent-badge'
    - from: scripts/smoke/verify-packed-install.sh
      to: packages/core/package.json
      via: "packs and installs the exact core tarball needed by the published runtime dependency graph"
      pattern: 'npm pack --workspace packages/core'
---

<objective>
Prove the packed-install release rehearsal from current source, even on a cleaned tree and constrained machine.

Purpose: PACK-03 requires trustworthy proof that locally packed tarballs can be installed into a clean temporary project and that both exported CLIs still work from the installed artifacts.
Output: one hardened smoke script that rebuilds when needed, resolves the exact tarballs, and validates imports plus CLI help output from a fresh install.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-release-rehearsal-and-checklist/10-RESEARCH.md
@.planning/phases/10-release-rehearsal-and-checklist/10-VALIDATION.md
@.planning/phases/07-release-readiness/07-02-SUMMARY.md
@.planning/phases/09-package-metadata-and-tarball-integrity/09-02-SUMMARY.md
@package.json
@scripts/check-packages.mjs
@scripts/verify-clean-checkout.sh
@scripts/smoke/verify-packed-install.sh
@.github/workflows/ci.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `smoke:pack` safe as a standalone rehearsal by satisfying the build precondition before packing</name>
  <files>scripts/smoke/verify-packed-install.sh</files>
  <read_first>.planning/STATE.md, .planning/phases/10-release-rehearsal-and-checklist/10-RESEARCH.md, .planning/phases/09-package-metadata-and-tarball-integrity/09-02-SUMMARY.md, package.json, scripts/check-packages.mjs, scripts/verify-clean-checkout.sh, scripts/smoke/verify-packed-install.sh</read_first>
  <action>Update `scripts/smoke/verify-packed-install.sh` so `npm run smoke:pack` is a safe standalone maintainer command on a cleaned tree. Per the 2026-03-31 finding that standalone `npm_config_cache=/tmp/... npm run smoke:pack` produced package.json-only tarballs on an unbuilt tree, satisfy the build precondition inside this script before any `npm pack --workspace ...` calls. Run `npm run build` from the repo root so the publishable `dist/**` artifacts exist for `packages/core`, `packages/agent-badge`, and `packages/create-agent-badge`, while keeping the existing `/tmp` scratch directory pattern and `NPM_CACHE_DIR="${npm_config_cache:-${WORK_DIR}/npm-cache}"`. Do not weaken or duplicate the Phase 09 tarball allowlist logic from `scripts/check-packages.mjs`, and do not assume `verify:clean-checkout` has already run.</action>
  <acceptance_criteria>
    - `scripts/smoke/verify-packed-install.sh` contains `npm run build`
    - `scripts/smoke/verify-packed-install.sh` contains `npm pack --workspace packages/core`
    - `scripts/smoke/verify-packed-install.sh` contains `npm pack --workspace packages/agent-badge`
    - `scripts/smoke/verify-packed-install.sh` contains `npm pack --workspace packages/create-agent-badge`
    - `scripts/smoke/verify-packed-install.sh` contains `mktemp -d "${TMPDIR:-/tmp}/agent-badge-pack-smoke.XXXXXX"`
    - `scripts/smoke/verify-packed-install.sh` contains `NPM_CACHE_DIR="${npm_config_cache:-${WORK_DIR}/npm-cache}"`
    - `scripts/smoke/verify-packed-install.sh` runs `npm run build` before the first `npm pack --workspace` command
  </acceptance_criteria>
  <verify>
    <automated>find packages -type d -name dist -prune -exec rm -rf {} + && find packages -type f -name '*.tsbuildinfo' -delete && npm_config_cache=/tmp/agent-badge-npm-cache bash scripts/smoke/verify-packed-install.sh</automated>
  </verify>
  <done>`npm run smoke:pack` now rebuilds what it needs and passes from a cleaned tree instead of producing package.json-only tarballs.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve exact tarball identities and keep the packed-install proof diagnosable</name>
  <files>scripts/smoke/verify-packed-install.sh</files>
  <read_first>scripts/smoke/verify-packed-install.sh, package.json, scripts/verify-clean-checkout.sh, .planning/phases/07-release-readiness/07-02-SUMMARY.md, .planning/phases/10-release-rehearsal-and-checklist/10-VALIDATION.md</read_first>
  <action>In `scripts/smoke/verify-packed-install.sh`, replace the overlapping install glob contract with exact tarball resolution before `npm install`. Resolve the three tarballs into separate variables such as `CORE_TARBALL`, `AGENT_BADGE_TARBALL`, and `CREATE_AGENT_BADGE_TARBALL`, fail fast with a clear error if any tarball is missing or ambiguous, and install only those three resolved paths. Add terse operator-facing output for `PACK_DIR`, `INSTALL_DIR`, `NPM_CACHE_DIR`, and the exact tarball basenames before the install so failures are diagnosable on constrained machines. Preserve the actual proof surface from Phase 07: ESM imports of `agent-badge` and `create-agent-badge` plus both installed CLI `--help` checks. Do not add docs or workflow changes in this plan.</action>
  <acceptance_criteria>
    - `scripts/smoke/verify-packed-install.sh` contains `CORE_TARBALL=`
    - `scripts/smoke/verify-packed-install.sh` contains `AGENT_BADGE_TARBALL=`
    - `scripts/smoke/verify-packed-install.sh` contains `CREATE_AGENT_BADGE_TARBALL=`
    - `scripts/smoke/verify-packed-install.sh` does not contain `"${PACK_DIR}"/agent-badge-*.tgz`
    - `scripts/smoke/verify-packed-install.sh` contains `import('agent-badge')`
    - `scripts/smoke/verify-packed-install.sh` contains `import('create-agent-badge')`
    - `scripts/smoke/verify-packed-install.sh` contains `./node_modules/.bin/agent-badge --help`
    - `scripts/smoke/verify-packed-install.sh` contains `./node_modules/.bin/create-agent-badge --help`
    - `scripts/smoke/verify-packed-install.sh` prints `PACK_DIR`, `INSTALL_DIR`, and `NPM_CACHE_DIR` before `npm install`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'CORE_TARBALL|AGENT_BADGE_TARBALL|CREATE_AGENT_BADGE_TARBALL|npm run build|npm pack --workspace packages/core|npm pack --workspace packages/agent-badge|npm pack --workspace packages/create-agent-badge|import\\x28\\x27agent-badge\\x27\\x29|import\\x28\\x27create-agent-badge\\x27\\x29|agent-badge --help|create-agent-badge --help|PACK_DIR|INSTALL_DIR|NPM_CACHE_DIR' scripts/smoke/verify-packed-install.sh</automated>
  </verify>
  <done>The smoke rehearsal installs the exact three tarballs, exposes enough context to debug failures quickly, and still proves both package imports and both CLI binaries from the installed artifacts.</done>
</task>

</tasks>

<verification>
- `find packages -type d -name dist -prune -exec rm -rf {} + && find packages -type f -name '*.tsbuildinfo' -delete && npm_config_cache=/tmp/agent-badge-npm-cache bash scripts/smoke/verify-packed-install.sh` succeeds
- `scripts/smoke/verify-packed-install.sh` no longer installs via the overlapping `"${PACK_DIR}"/agent-badge-*.tgz` glob
- The smoke rehearsal still verifies both installed package imports and both CLI `--help` surfaces from the temporary project
</verification>

<success_criteria>
- PACK-03 is satisfied by a standalone, exact, and constrained-environment-safe packed-install rehearsal.
- Maintainers can trust `npm run smoke:pack` as focused proof for tarball installability instead of a script that silently depends on earlier build state.
</success_criteria>

<output>
After completion, create `.planning/phases/10-release-rehearsal-and-checklist/10-01-SUMMARY.md`
</output>

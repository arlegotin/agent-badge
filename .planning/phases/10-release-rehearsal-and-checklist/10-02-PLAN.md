---
phase: 10-release-rehearsal-and-checklist
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - docs/RELEASE.md
  - README.md
  - scripts/verify-docs.sh
autonomous: true
requirements:
  - OPER-06
user_setup: []
must_haves:
  truths:
    - "Maintainers can follow one discoverable release checklist that points to `npm run verify:clean-checkout` as the canonical full rehearsal on constrained machines."
    - "The release checklist explicitly covers isolated npm cache usage, `/tmp` scratch-space guidance, and live publish-time `npm view` checks for `agent-badge`, `create-agent-badge`, and `@agent-badge/core`."
    - "The checklist is linked from the README and enforced by the repo's existing docs verification gate."
  artifacts:
    - path: docs/RELEASE.md
      provides: single maintainer-facing constrained-environment release checklist
      contains: 'npm run verify:clean-checkout'
    - path: README.md
      provides: discoverable link to the maintainer release checklist
      contains: 'docs/RELEASE.md'
    - path: scripts/verify-docs.sh
      provides: deterministic verification that the release checklist exists and keeps its required commands
      contains: 'docs/RELEASE.md'
  key_links:
    - from: docs/RELEASE.md
      to: scripts/verify-clean-checkout.sh
      via: "the checklist sends maintainers through the repo-owned clean-checkout rehearsal rather than a handwritten command sequence"
      pattern: 'npm run verify:clean-checkout'
    - from: scripts/verify-docs.sh
      to: docs/RELEASE.md
      via: "docs verification enforces the required release-checklist content"
      pattern: 'docs/RELEASE.md'
    - from: README.md
      to: docs/RELEASE.md
      via: "repo landing page links directly to the maintainer release checklist"
      pattern: 'docs/RELEASE.md'
---

<objective>
Finalize one maintainer-facing release checklist that mirrors the proven local rehearsal and publish-time preflight.

Purpose: OPER-06 requires a single documented sequence that covers constrained npm-cache usage, low-disk `/tmp` workflows, and live package-name verification immediately before publish.
Output: one release-maintainer document, one README link, and one docs verification update that keeps the checklist mechanically enforced.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-release-rehearsal-and-checklist/10-RESEARCH.md
@.planning/phases/10-release-rehearsal-and-checklist/10-VALIDATION.md
@.planning/phases/07-release-readiness/07-03-SUMMARY.md
@package.json
@README.md
@docs/QUICKSTART.md
@docs/MANUAL-GIST.md
@scripts/verify-docs.sh
@scripts/verify-clean-checkout.sh
@scripts/smoke/verify-packed-install.sh
@.github/workflows/ci.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author one maintainer release checklist grounded in the repo-owned rehearsal commands</name>
  <files>docs/RELEASE.md</files>
  <read_first>.planning/STATE.md, .planning/phases/10-release-rehearsal-and-checklist/10-RESEARCH.md, package.json, scripts/verify-clean-checkout.sh, scripts/smoke/verify-packed-install.sh, .github/workflows/ci.yml, .github/workflows/release.yml, README.md, docs/QUICKSTART.md</read_first>
  <action>Create `docs/RELEASE.md` as the single maintainer-facing release checklist. Keep it grounded in repo-owned commands instead of ad-hoc shell prose: document `npm run docs:check`, `npm run typecheck`, and `npm run verify:clean-checkout` as the canonical full rehearsal, with `npm run smoke:pack` called out only as the focused rerun when the packed-install proof fails. Include the constrained-machine guidance already reflected in `.planning/STATE.md`: use an isolated `npm_config_cache` under `/tmp`, prefer temporary scratch space outside `/Volumes/git`, and treat the clean-checkout verifier as the main sequence. For publish-time preflight, explicitly document live registry checks `npm view agent-badge`, `npm view create-agent-badge`, and `npm view @agent-badge/core`, instructing maintainers to run them immediately before publish because registry state is time-sensitive and the 2026-03-31 findings are not timeless facts. Keep publish aligned with the current workflow-driven path and `npm run release`; do not redesign release automation.</action>
  <acceptance_criteria>
    - `docs/RELEASE.md` contains `# Release Checklist`
    - `docs/RELEASE.md` contains `npm run docs:check`
    - `docs/RELEASE.md` contains `npm run typecheck`
    - `docs/RELEASE.md` contains `npm run verify:clean-checkout`
    - `docs/RELEASE.md` contains `npm run smoke:pack`
    - `docs/RELEASE.md` contains `npm_config_cache`
    - `docs/RELEASE.md` contains `/tmp`
    - `docs/RELEASE.md` contains `npm view agent-badge`
    - `docs/RELEASE.md` contains `npm view create-agent-badge`
    - `docs/RELEASE.md` contains `npm view @agent-badge/core`
    - `docs/RELEASE.md` contains `npm run release`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'npm run docs:check|npm run typecheck|npm run verify:clean-checkout|npm run smoke:pack|npm_config_cache|/tmp|npm view agent-badge|npm view create-agent-badge|npm view @agent-badge/core|npm run release' docs/RELEASE.md</automated>
  </verify>
  <done>`docs/RELEASE.md` is the single maintainable checklist for constrained-environment release rehearsal and live publish-time preflight.</done>
</task>

<task type="auto">
  <name>Task 2: Make the release checklist discoverable and enforce it through the existing docs gate</name>
  <files>README.md, scripts/verify-docs.sh</files>
  <read_first>README.md, scripts/verify-docs.sh, docs/RELEASE.md, docs/QUICKSTART.md, docs/MANUAL-GIST.md, .planning/phases/07-release-readiness/07-03-SUMMARY.md</read_first>
  <action>Update `README.md` so the documentation index links to `docs/RELEASE.md` with a maintainer-oriented label such as `Release Checklist`. Update `scripts/verify-docs.sh` in the same exact style established in Phase 07: add `docs/RELEASE.md` to the required-files array and add exact `rg -n` content assertions for the checklist's critical strings `npm run verify:clean-checkout`, `npm view agent-badge`, `npm view create-agent-badge`, `npm view @agent-badge/core`, `npm_config_cache`, and `/tmp`. Keep the existing docs checks intact and additive; do not introduce new docs tooling or alter CI workflows in this plan.</action>
  <acceptance_criteria>
    - `README.md` contains `docs/RELEASE.md`
    - `README.md` contains `Release Checklist`
    - `scripts/verify-docs.sh` contains `docs/RELEASE.md`
    - `scripts/verify-docs.sh` contains `npm run verify:clean-checkout`
    - `scripts/verify-docs.sh` contains `npm view agent-badge`
    - `scripts/verify-docs.sh` contains `npm view create-agent-badge`
    - `scripts/verify-docs.sh` contains `npm view @agent-badge/core`
    - `scripts/verify-docs.sh` contains `npm_config_cache`
    - `scripts/verify-docs.sh` contains `/tmp`
    - `scripts/verify-docs.sh` still contains `docs/QUICKSTART.md`
    - `scripts/verify-docs.sh` still contains `docs/MANUAL-GIST.md`
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check</automated>
  </verify>
  <done>The maintainer release checklist is easy to find from the README and mechanically protected by the existing docs verification contract.</done>
</task>

</tasks>

<verification>
- `npm run docs:check` succeeds with `docs/RELEASE.md` wired into the required docs set
- `README.md` links to `docs/RELEASE.md`
- `docs/RELEASE.md` points maintainers to `npm run verify:clean-checkout` for the full rehearsal and includes the three live `npm view` checks immediately before publish
</verification>

<success_criteria>
- OPER-06 is satisfied by one discoverable, constrained-environment release checklist grounded in the repo's existing commands and workflows.
- Publish-time preflight now includes live package-name checks for `agent-badge`, `create-agent-badge`, and `@agent-badge/core` instead of stale recorded assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-release-rehearsal-and-checklist/10-02-SUMMARY.md`
</output>

---
phase: 09-package-metadata-and-tarball-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - package.json
  - scripts/check-packages.mjs
  - scripts/verify-clean-checkout.sh
autonomous: true
requirements:
  - PACK-02
user_setup: []
must_haves:
  truths:
    - "The repo has an automated pack gate that fails if a publishable tarball contains anything outside `dist/**` plus `package.json`."
    - "Each publishable package still includes the runtime entrypoints required for imports and CLI execution."
    - "Tarball integrity checks use the repo's isolated npm cache pattern so cache-permission issues do not masquerade as packaging regressions."
  artifacts:
    - path: scripts/check-packages.mjs
      provides: package-content gate over npm dry-run JSON output
      contains: 'const publishablePackages = ['
    - path: package.json
      provides: repo-owned tarball integrity verification entrypoint
      contains: '"pack:check"'
    - path: scripts/verify-clean-checkout.sh
      provides: release-critical verification path that reuses the strict tarball integrity gate
      contains: 'npm_config_cache="${NPM_CACHE_DIR}" npm run pack:check'
  key_links:
    - from: scripts/check-packages.mjs
      to: package.json
      via: "root pack:check script delegates tarball validation to the repo-owned checker"
      pattern: "check-packages.mjs"
    - from: scripts/check-packages.mjs
      to: packages/agent-badge/package.json
      via: "the allowed file set must still include the CLI runtime entrypoint"
      pattern: '"bin"'
    - from: scripts/check-packages.mjs
      to: packages/create-agent-badge/package.json
      via: "the allowed file set must still include the initializer runtime entrypoint"
      pattern: '"dist/index.js"'
---

<objective>
Turn package tarball integrity into an enforced repository gate instead of a manual inspection step.

Purpose: PACK-02 requires proof that published tarballs exclude non-runtime artifacts and still include the runtime/build/CLI files needed for execution.
Output: a repo-owned pack checker script, a root pack command that fails on forbidden content, and clean-checkout verification that reuses the stricter gate.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-package-metadata-and-tarball-integrity/09-RESEARCH.md
@.planning/phases/09-package-metadata-and-tarball-integrity/09-VALIDATION.md
@package.json
@scripts/verify-clean-checkout.sh
@scripts/smoke/verify-packed-install.sh
@packages/core/package.json
@packages/agent-badge/package.json
@packages/create-agent-badge/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a strict pack-output checker that enforces the allowed tarball surface for each publishable package</name>
  <files>scripts/check-packages.mjs, package.json</files>
  <read_first>package.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json, scripts/smoke/verify-packed-install.sh</read_first>
  <action>Create `scripts/check-packages.mjs` as an ESM Node script that shells out to `npm pack --workspace <workspace> --dry-run --json` for `packages/core`, `packages/agent-badge`, and `packages/create-agent-badge`, parses the returned `files[].path` arrays, and fails with a non-zero exit code when any file path is not under `dist/` and is not exactly `package.json`. Require exact runtime entrypoints in the parsed output: `packages/core` must include `dist/index.js` and `dist/index.d.ts`; `packages/agent-badge` must include `dist/index.js`, `dist/index.d.ts`, `dist/cli/main.js`, and `dist/cli/main.d.ts`; `packages/create-agent-badge` must include `dist/index.js` and `dist/index.d.ts`. Update the root `package.json` script `"pack:check"` to run `node scripts/check-packages.mjs` instead of chaining raw `npm pack --dry-run` commands.</action>
  <acceptance_criteria>
    - `scripts/check-packages.mjs` contains `const publishablePackages = [`
    - `scripts/check-packages.mjs` contains `packages/core`
    - `scripts/check-packages.mjs` contains `packages/agent-badge`
    - `scripts/check-packages.mjs` contains `packages/create-agent-badge`
    - `scripts/check-packages.mjs` contains `dist/cli/main.js`
    - `scripts/check-packages.mjs` contains `package.json`
    - `scripts/check-packages.mjs` contains `process.exitCode = 1`
    - `package.json` contains `"pack:check": "node scripts/check-packages.mjs"`
    - `package.json` does not contain `npm pack --workspace packages/core --dry-run --json &&`
  </acceptance_criteria>
  <verify>
    <automated>npm_config_cache=/tmp/agent-badge-npm-cache npm run pack:check</automated>
  </verify>
  <done>The repo has a deterministic pack checker that enforces the allowed tarball surface for all three publishable packages.</done>
</task>

<task type="auto">
  <name>Task 2: Keep clean-checkout verification aligned with the strict isolated-cache tarball gate</name>
  <files>scripts/verify-clean-checkout.sh</files>
  <read_first>scripts/verify-clean-checkout.sh, package.json, scripts/check-packages.mjs, scripts/smoke/verify-packed-install.sh</read_first>
  <action>Update `scripts/verify-clean-checkout.sh` only as needed so the clean-checkout path clearly reuses the stricter root `pack:check` command with `npm_config_cache="${NPM_CACHE_DIR}"`. Preserve the existing order `npm run build`, `npm test -- --run`, `npm_config_cache="${NPM_CACHE_DIR}" npm run pack:check`, and `npm_config_cache="${NPM_CACHE_DIR}" npm run smoke:pack`, but make the script comments or structure explicit enough that pack validation is understood as the tarball-integrity gate before the broader smoke install step.</action>
  <acceptance_criteria>
    - `scripts/verify-clean-checkout.sh` contains `npm run build`
    - `scripts/verify-clean-checkout.sh` contains `npm test -- --run`
    - `scripts/verify-clean-checkout.sh` contains `npm_config_cache="${NPM_CACHE_DIR}" npm run pack:check`
    - `scripts/verify-clean-checkout.sh` contains `npm_config_cache="${NPM_CACHE_DIR}" npm run smoke:pack`
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/verify-clean-checkout.sh && rg -n 'npm run build|npm test -- --run|npm_config_cache="\\$\\{NPM_CACHE_DIR\\}" npm run pack:check|npm_config_cache="\\$\\{NPM_CACHE_DIR\\}" npm run smoke:pack' scripts/verify-clean-checkout.sh</automated>
  </verify>
  <done>Release-critical verification continues to use the isolated-cache pack gate before smoke-install validation.</done>
</task>

</tasks>

<verification>
- `npm_config_cache=/tmp/agent-badge-npm-cache npm run pack:check` succeeds
- The pack checker fails if any publishable package emits a non-`dist/**` path other than `package.json`
- `scripts/verify-clean-checkout.sh` still routes through the isolated-cache pack gate before smoke-install validation
</verification>

<success_criteria>
- PACK-02 is enforced by a deterministic tarball-content gate instead of manual dry-run inspection.
- The repo's release-critical verification path keeps package-content validation separate from broader smoke-install behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-package-metadata-and-tarball-integrity/09-02-SUMMARY.md`
</output>

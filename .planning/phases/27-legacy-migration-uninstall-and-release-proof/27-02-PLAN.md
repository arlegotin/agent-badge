---
phase: 27-legacy-migration-uninstall-and-release-proof
plan: 02
type: execute
wave: 2
depends_on:
  - "27-01"
files_modified:
  - README.md
  - docs/INSTALL.md
  - docs/QUICKSTART.md
  - docs/CLI.md
  - docs/HOW-IT-WORKS.md
  - docs/TROUBLESHOOTING.md
  - docs/CONFIGURATION.md
  - docs/AUTH.md
  - docs/UNINSTALL.md
  - docs/FAQ.md
  - docs/MANUAL-GIST.md
  - docs/RECOVERY.md
  - scripts/verify-docs.sh
  - packages/agent-badge/src/cli/main.ts
  - packages/agent-badge/src/cli/main.test.ts
autonomous: true
requirements:
  - MIG-02
must_haves:
  truths:
    - "Default onboarding docs describe `npm init agent-badge@latest` as the global-first or shared-runtime path that leaves minimal repo artifacts, not as a repo-local runtime installer."
    - "Uninstall and recovery docs explain the current default behavior: remove managed repo wiring first, preserve config/state and gist linkage unless purge flags are used, and rely on the shared runtime story for re-entry."
    - "Docs verification locks in the shared-runtime vocabulary so repo-local wrapper guidance cannot drift back into the default operator story."
  artifacts:
    - path: README.md
      provides: "top-level onboarding truth for the default initializer path"
      contains: "shared runtime"
    - path: docs/CLI.md
      provides: "current command and setup contract"
      contains: "global or user-scoped"
    - path: docs/UNINSTALL.md
      provides: "current uninstall and reinstall guidance"
      contains: "preserves data unless you explicitly ask it to purge more"
    - path: scripts/verify-docs.sh
      provides: "fixed-string validation for the new operator vocabulary"
      contains: "npm init agent-badge@latest"
  key_links:
    - from: README.md
      to: docs/INSTALL.md
      via: default install narrative
      pattern: "shared runtime"
    - from: docs/CLI.md
      to: packages/agent-badge/src/cli/main.ts
      via: command-help vocabulary alignment
      pattern: "uninstall"
    - from: docs/UNINSTALL.md
      to: packages/agent-badge/src/commands/uninstall.ts
      via: default uninstall behavior and purge flags
      pattern: "- uninstall: start"
---

<objective>
Update docs and command help for global-first installation.

Purpose: Close the operator-communication half of MIG-02 by making README, user docs, uninstall guidance, and any CLI help surface describe the current shared-runtime/minimal-artifact model consistently.
Output: Updated onboarding and recovery docs plus docs verification that locks the new vocabulary in place.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-legacy-migration-uninstall-and-release-proof/27-RESEARCH.md
@.planning/phases/27-legacy-migration-uninstall-and-release-proof/27-VALIDATION.md
@README.md
@docs/INSTALL.md
@docs/QUICKSTART.md
@docs/CLI.md
@docs/HOW-IT-WORKS.md
@docs/TROUBLESHOOTING.md
@docs/CONFIGURATION.md
@docs/AUTH.md
@docs/UNINSTALL.md
@docs/FAQ.md
@docs/MANUAL-GIST.md
@docs/RECOVERY.md
@scripts/verify-docs.sh
@packages/agent-badge/src/cli/main.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/status.ts
@packages/agent-badge/src/commands/uninstall.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite README and primary user docs around the shared-runtime default path</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/27-legacy-migration-uninstall-and-release-proof/27-RESEARCH.md
    - .planning/phases/27-legacy-migration-uninstall-and-release-proof/27-VALIDATION.md
    - README.md
    - docs/INSTALL.md
    - docs/QUICKSTART.md
    - docs/CLI.md
    - docs/HOW-IT-WORKS.md
    - packages/agent-badge/src/commands/init.ts
    - packages/agent-badge/src/commands/status.ts
  </read_first>
  <files>README.md, docs/INSTALL.md, docs/QUICKSTART.md, docs/CLI.md, docs/HOW-IT-WORKS.md</files>
  <behavior>
    - Doc 1: the default `npm init agent-badge@latest` story says repo-owned scaffold plus shared-runtime/global or user-scoped CLI expectations, not repo-local runtime installation.
    - Doc 2: setup examples and command summaries no longer tell npm-initialized users to use `npx --no-install` or a repo-local wrapper as the default follow-up path.
    - Doc 3: direct package installation remains available only as an explicit alternative path and is labeled as such.
  </behavior>
  <action>Rewrite `README.md`, `docs/INSTALL.md`, `docs/QUICKSTART.md`, `docs/CLI.md`, and `docs/HOW-IT-WORKS.md` so the default narrative is: `npm init agent-badge@latest` writes repo-owned `.agent-badge/*`, README badge markup when available, managed `.gitignore` entries, and the direct shared-runtime hook, but it does not install repo-local `@legotin/agent-badge`, managed `agent-badge:init` or `agent-badge:refresh` scripts, or repo-local `node_modules` by default. Replace phrases such as `repo-local runtime`, `local runtime`, and `Use the repo-local wrapper that matches your package manager` with shared-runtime/global-first wording. Keep direct package installation only as a clearly labeled alternative path, and make sure the example final setup lines reflect the real init output from `packages/agent-badge/src/commands/init.ts`, especially `repo setup complete` and shared-runtime remediation wording.</action>
  <acceptance_criteria>
    - `rg -n "repo-local runtime|local runtime" README.md docs/INSTALL.md docs/QUICKSTART.md docs/CLI.md docs/HOW-IT-WORKS.md` returns no matches.
    - `rg -n "shared runtime|global or user-scoped|global-first" README.md docs/INSTALL.md docs/QUICKSTART.md docs/CLI.md docs/HOW-IT-WORKS.md` returns matches across the updated docs.
    - `rg -n "Use the repo-local wrapper that matches your package manager" docs/CLI.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check</automated>
  </verify>
  <done>Primary onboarding docs tell one consistent truth about the default initializer path and no longer describe a repo-local runtime as the default outcome.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align recovery, uninstall, FAQ, and docs verification with the current install model</name>
  <read_first>
    - .planning/phases/27-legacy-migration-uninstall-and-release-proof/27-RESEARCH.md
    - docs/TROUBLESHOOTING.md
    - docs/CONFIGURATION.md
    - docs/AUTH.md
    - docs/UNINSTALL.md
    - docs/FAQ.md
    - docs/MANUAL-GIST.md
    - docs/RECOVERY.md
    - scripts/verify-docs.sh
    - packages/agent-badge/src/cli/main.ts
    - packages/agent-badge/src/commands/uninstall.ts
  </read_first>
  <files>docs/TROUBLESHOOTING.md, docs/CONFIGURATION.md, docs/AUTH.md, docs/UNINSTALL.md, docs/FAQ.md, docs/MANUAL-GIST.md, docs/RECOVERY.md, scripts/verify-docs.sh, packages/agent-badge/src/cli/main.ts, packages/agent-badge/src/cli/main.test.ts</files>
  <behavior>
    - Doc 1: uninstall and recovery guidance matches current default behavior: remove managed wiring first, preserve config/state/remote unless purge flags say otherwise, and reconnect through shared-runtime flows.
    - Doc 2: FAQ and auth docs no longer claim `create-agent-badge` installs the runtime package into the repo or tell users to rely on `npx --no-install` after default init.
    - Doc 3: `scripts/verify-docs.sh` enforces the updated strings so the repo-local vocabulary cannot reappear silently.
  </behavior>
  <action>Update `docs/TROUBLESHOOTING.md`, `docs/CONFIGURATION.md`, `docs/AUTH.md`, `docs/UNINSTALL.md`, `docs/FAQ.md`, `docs/MANUAL-GIST.md`, and `docs/RECOVERY.md` so the recovery and reinstall story follows the shared-runtime/default-init contract established in Phases 25 and 26. In `docs/UNINSTALL.md`, keep the real default output lines from `runUninstallCommand()` and rewrite reinstall guidance so it does not imply repo-local runtime ownership is recreated by default. In `docs/FAQ.md`, replace the sentence that says `create-agent-badge` installs `@legotin/agent-badge` with the actual current behavior from `packages/create-agent-badge/src/index.ts`, which is a thin initializer entrypoint. Update `scripts/verify-docs.sh` fixed-string checks to enforce the new phrasing. If `packages/agent-badge/src/cli/main.ts` still exposes command descriptions that contradict the docs, align them and add or update `packages/agent-badge/src/cli/main.test.ts` coverage accordingly.</action>
  <acceptance_criteria>
    - `rg -n "which then installs the actual runtime package" docs/FAQ.md` returns no matches.
    - `rg -n "npx --no-install agent-badge init" docs/AUTH.md docs/UNINSTALL.md docs/TROUBLESHOOTING.md docs/RECOVERY.md` returns no matches.
    - `rg -n "preserve config/state/remote unless purge flags are set|original publisher machine|shared runtime" docs/UNINSTALL.md docs/RECOVERY.md docs/MANUAL-GIST.md docs/TROUBLESHOOTING.md` returns matches proving the updated guidance is explicit.
    - `rg -n "require_fixed" scripts/verify-docs.sh` still returns matches, and at least one fixed-string expectation references the new shared-runtime/global-first wording.
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check && npm test -- --run packages/agent-badge/src/cli/main.test.ts</automated>
  </verify>
  <done>Secondary docs, uninstall guidance, FAQ, and docs verification all reinforce the same global-first/shared-runtime contract as the primary onboarding pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation truth -> docs | Public docs must reflect the actual shared-runtime/minimal-artifact contract rather than historical behavior. |
| uninstall and recovery output -> human action | Operators rely on the docs to decide whether to reconnect, reinstall, purge, or migrate from the original publisher machine. |
| docs verification -> release confidence | Fixed-string checks are the backstop that prevents stale repo-local vocabulary from returning unnoticed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | D | README and user docs | mitigate | Remove stale repo-local wrapper guidance and replace it with the shared-runtime/global-first contract everywhere the default path is described. |
| T-27-02-02 | T | `docs/UNINSTALL.md`, `docs/RECOVERY.md`, `docs/MANUAL-GIST.md` | mitigate | Keep default uninstall and original-publisher migration guidance tied to the current command behavior so docs do not cause destructive or misleading recovery actions. |
| T-27-02-03 | I | `scripts/verify-docs.sh` | mitigate | Encode fixed checks for the new vocabulary so later edits cannot silently revert to the old repo-local model. |
</threat_model>

<verification>
Run the docs and help alignment checks:

- `npm run docs:check`
- `npm test -- --run packages/agent-badge/src/cli/main.test.ts`
</verification>

<success_criteria>
- README and user docs describe the default initializer path as shared-runtime/global-first and minimal-artifact.
- Uninstall, FAQ, auth, troubleshooting, and recovery pages no longer imply repo-local runtime ownership after default init.
- Docs verification enforces the updated operator vocabulary.
</success_criteria>

<output>
After completion, create `.planning/phases/27-legacy-migration-uninstall-and-release-proof/27-02-SUMMARY.md`
</output>

---
phase: 27-legacy-migration-uninstall-and-release-proof
plan: 03
type: execute
wave: 3
depends_on:
  - "27-01"
  - "27-02"
files_modified:
  - scripts/smoke/verify-registry-install.sh
  - scripts/smoke/verify-packed-install.sh
  - scripts/verify-clean-checkout.sh
  - docs/maintainers/RELEASE.md
  - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
  - packages/create-agent-badge/src/index.test.ts
autonomous: true
requirements:
  - MIG-02
must_haves:
  truths:
    - "Registry smoke treats `npm init agent-badge@latest` as a minimal-artifact initializer path and explicitly rejects repo-local runtime ownership as a required outcome."
    - "Packed-install smoke remains a valid direct-runtime proof path, but it is clearly separated from the initializer contract."
    - "Release and clean-temp verification instructions tell maintainers exactly which smoke path proves the initializer contract and which one proves direct runtime package installability."
  artifacts:
    - path: scripts/smoke/verify-registry-install.sh
      provides: "published-version initializer proof for minimal repo artifacts"
      contains: "initializer missing .agent-badge/config.json"
    - path: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      provides: "source-tree scenario proof for minimal-artifact init outcomes"
      contains: "expectNoManagedRuntimeManifestOwnership"
    - path: scripts/smoke/verify-packed-install.sh
      provides: "clean temp direct-runtime install smoke"
      contains: "./node_modules/.bin/agent-badge --help"
    - path: docs/maintainers/RELEASE.md
      provides: "maintainer instructions for release-proof smoke and evidence capture"
      contains: "verify-registry-install.sh"
  key_links:
    - from: scripts/smoke/verify-registry-install.sh
      to: .git/hooks/pre-push
      via: direct shared hook assertion in the initializer path
      pattern: "command -v agent-badge >/dev/null 2>&1"
    - from: scripts/smoke/verify-registry-install.sh
      to: packages/create-agent-badge/src/index.ts
      via: initializer smoke expectations
      pattern: "npm init \"agent-badge@"
    - from: docs/maintainers/RELEASE.md
      to: scripts/smoke/verify-registry-install.sh
      via: documented published-version smoke command
      pattern: "--check-initializer"
---

<objective>
Extend registry and clean-temp smoke coverage for minimal repo artifacts.

Purpose: Finish the proof half of MIG-02 by making release-smoke verification assert the current initializer contract and by separating that proof cleanly from direct runtime install smoke.
Output: Updated smoke scripts, release-readiness regression coverage, and maintainer release instructions that certify the right distribution model.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-legacy-migration-uninstall-and-release-proof/27-RESEARCH.md
@.planning/phases/27-legacy-migration-uninstall-and-release-proof/27-VALIDATION.md
@package.json
@scripts/smoke/verify-registry-install.sh
@scripts/smoke/verify-packed-install.sh
@scripts/verify-clean-checkout.sh
@docs/maintainers/RELEASE.md
@packages/create-agent-badge/src/index.ts
@packages/create-agent-badge/src/index.test.ts
@packages/agent-badge/src/commands/release-readiness-matrix.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite initializer smoke and regression proof around the minimal-artifact contract</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/27-legacy-migration-uninstall-and-release-proof/27-RESEARCH.md
    - .planning/phases/27-legacy-migration-uninstall-and-release-proof/27-VALIDATION.md
    - scripts/smoke/verify-registry-install.sh
    - packages/create-agent-badge/src/index.ts
    - packages/create-agent-badge/src/index.test.ts
    - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
  </read_first>
  <files>scripts/smoke/verify-registry-install.sh, packages/create-agent-badge/src/index.test.ts, packages/agent-badge/src/commands/release-readiness-matrix.test.ts</files>
  <behavior>
    - Test 1: after `npm init agent-badge@${VERSION}`, initializer smoke asserts `.agent-badge/config.json`, `.agent-badge/state.json`, and the managed direct shared hook, but does not require `node_modules/.bin/agent-badge` or `npx --no-install` to work inside the target repo.
    - Test 2: if `package.json` exists after initializer smoke, it contains no managed `agent-badge:init`, no managed `agent-badge:refresh`, and no managed `@legotin/agent-badge` dependency created by default.
    - Test 3: release-readiness or initializer tests in source form encode the same minimal-artifact truth so source-tree proof and published-version smoke cannot drift apart.
  </behavior>
  <action>Update `scripts/smoke/verify-registry-install.sh` so its initializer section treats `npm init agent-badge@${VERSION}` as a pure initializer path: keep the existing assertions for `.agent-badge/config.json`, `.agent-badge/state.json`, `.git/hooks/pre-push`, the shared-runtime PATH guard, and the direct `agent-badge refresh --hook pre-push --hook-policy fail-soft || true` command, but delete the assertions that require `node_modules/.bin/agent-badge` or `npx --no-install agent-badge --help` after init. Add explicit checks that, when `package.json` exists, `scripts.agent-badge:init`, `scripts.agent-badge:refresh`, and `devDependencies.@legotin/agent-badge` are absent. Mirror that contract in `packages/create-agent-badge/src/index.test.ts` and `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` so the source-tree tests prove the same minimal-artifact outcome.</action>
  <acceptance_criteria>
    - `rg -n "node_modules/.bin/agent-badge|npx --no-install agent-badge --help" scripts/smoke/verify-registry-install.sh` returns no matches.
    - `rg -n "agent-badge refresh --hook pre-push --hook-policy fail-soft \\|\\| true|command -v agent-badge >/dev/null 2>&1" scripts/smoke/verify-registry-install.sh` returns matches.
    - `rg -n "expectNoManagedRuntimeManifestOwnership|does not require a target package.json after initialization" packages/agent-badge/src/commands/release-readiness-matrix.test.ts packages/create-agent-badge/src/index.test.ts` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts packages/create-agent-badge/src/index.test.ts && bash -n scripts/smoke/verify-registry-install.sh</automated>
  </verify>
  <done>Initializer smoke and source-tree regression tests both certify the minimal-artifact contract instead of the historical repo-local binary contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Separate direct runtime smoke from initializer proof in clean-temp and release workflows</name>
  <read_first>
    - .planning/phases/27-legacy-migration-uninstall-and-release-proof/27-RESEARCH.md
    - scripts/smoke/verify-packed-install.sh
    - scripts/verify-clean-checkout.sh
    - docs/maintainers/RELEASE.md
    - package.json
  </read_first>
  <files>scripts/smoke/verify-packed-install.sh, scripts/verify-clean-checkout.sh, docs/maintainers/RELEASE.md</files>
  <behavior>
    - Doc 1: packed-install smoke is explicitly documented as direct runtime install proof, not proof that the npm initializer creates repo-local runtime ownership.
    - Doc 2: maintainers are told to run published-version registry smoke with `--check-initializer` when certifying a release.
    - Doc 3: clean-checkout or local smoke entrypoints remain clear about which contract they certify.
  </behavior>
  <action>Update `scripts/smoke/verify-packed-install.sh`, `scripts/verify-clean-checkout.sh`, and `docs/maintainers/RELEASE.md` so the repo has two clearly separated smoke stories. Keep `verify-packed-install.sh` as the direct runtime package proof that installs tarballs into a clean temp project and runs `./node_modules/.bin/agent-badge --help` and `./node_modules/.bin/create-agent-badge --help`, but add comments or wording that make it clear this proves explicit package installation, not the default initializer artifact contract. In `docs/maintainers/RELEASE.md`, make the published-version registry smoke step the authoritative initializer proof by keeping `verify-registry-install.sh --check-initializer --write-evidence` front and center. If `scripts/verify-clean-checkout.sh` needs a clarifying comment or step ordering adjustment to distinguish packed-install smoke from published-version registry smoke, make that explicit without adding network-dependent behavior to the local clean-checkout script.</action>
  <acceptance_criteria>
    - `rg -n "verify-registry-install.sh .*--check-initializer|REGISTRY-SMOKE" docs/maintainers/RELEASE.md` returns matches.
    - `rg -n "./node_modules/.bin/agent-badge --help|./node_modules/.bin/create-agent-badge --help" scripts/smoke/verify-packed-install.sh` returns matches.
    - `rg -n "smoke:pack" scripts/verify-clean-checkout.sh` returns a match, and any added comment or wording around that step distinguishes it from published-version registry smoke.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/smoke/verify-packed-install.sh && bash -n scripts/verify-clean-checkout.sh && npm run docs:check</automated>
  </verify>
  <done>Release instructions and local clean-temp smoke paths clearly separate direct runtime package proof from initializer minimal-artifact proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published package behavior -> smoke scripts | Smoke checks must certify the real initializer and runtime contracts, not stale expectations. |
| source-tree regression tests -> release docs | Maintainers rely on source tests and documented smoke commands to decide whether the release is truthful. |
| local clean-temp proof -> published-version proof | Packed-install smoke and registry initializer smoke cover different contracts and must not be conflated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | I | `scripts/smoke/verify-registry-install.sh` | mitigate | Remove repo-local binary expectations and assert the initializer's minimal-artifact contract directly, including hook content and manifest absence checks. |
| T-27-03-02 | D | `scripts/smoke/verify-packed-install.sh`, `scripts/verify-clean-checkout.sh` | mitigate | Preserve direct-runtime smoke as a separate path and document exactly what it proves so clean-temp checks do not give false confidence about the initializer. |
| T-27-03-03 | R | `docs/maintainers/RELEASE.md` | mitigate | Keep the release checklist explicit about the published-version registry smoke command and the evidence artifacts that prove the initializer contract. |
</threat_model>

<verification>
Run the proof-surface checks:

- `npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts packages/create-agent-badge/src/index.test.ts`
- `bash -n scripts/smoke/verify-registry-install.sh`
- `bash -n scripts/smoke/verify-packed-install.sh`
- `bash -n scripts/verify-clean-checkout.sh`
- `npm run docs:check`
</verification>

<success_criteria>
- Registry smoke no longer certifies repo-local runtime ownership as a required initializer outcome.
- Packed-install smoke remains explicit direct-runtime proof and is distinguished from initializer proof in docs and scripts.
- Maintainer release instructions point at the correct published-version smoke command and evidence artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/27-legacy-migration-uninstall-and-release-proof/27-03-SUMMARY.md`
</output>

---
phase: 19-recovery-paths-and-production-reliability-verification
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
  - docs/QUICKSTART.md
  - docs/TROUBLESHOOTING.md
  - docs/MANUAL-GIST.md
  - docs/RECOVERY.md
  - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
  - scripts/smoke/verify-recovery-flow.sh
  - .planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md
  - .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.md
  - .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json
autonomous: false
requirements:
  - CTRL-02
  - CTRL-03
user_setup:
  - "A local environment for this repo that can inspect the currently configured gist and reproduce the real `auth-missing` stale failed-publish state before recovery."
  - "A shell session where GitHub auth can be restored for the final live recovery proof."
must_haves:
  truths:
    - "One canonical runbook tells operators how to recover from stale failed publish, gist-target repair, missing local contributor, partial shared metadata, and team-coordination scenarios using the same command vocabulary shown by the CLI."
    - "Phase-owned evidence proves the real stale failed-publish state moved to healthy shared publish state through supported CLI flows on this repo."
    - "Automated scenario coverage and human/UAT artifacts together lock the operator messaging so docs cannot drift from actual command output."
  artifacts:
    - path: docs/RECOVERY.md
      provides: canonical recovery runbook that maps symptoms to exact supported commands
      contains: stale after failed publish
    - path: scripts/smoke/verify-recovery-flow.sh
      provides: repo-owned recovery evidence capture entrypoint
      contains: agent-badge status
    - path: .planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md
      provides: live stale-to-healthy verification checklist and captured outcomes
      contains: status:
    - path: .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json
      provides: machine-readable pre-recovery and post-recovery evidence for the real repo flow
      contains: postRecovery
    - path: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      provides: scenario-level regression coverage for supported recovery messaging
      contains: stale failed publish recovery
  key_links:
    - from: docs/RECOVERY.md
      to: packages/agent-badge/src/commands/status.ts
      via: "the runbook uses the same `agent-badge refresh` and `agent-badge init --gist-id <id>` recovery commands surfaced by status"
      pattern: agent-badge refresh
    - from: docs/RECOVERY.md
      to: packages/core/src/diagnostics/doctor.ts
      via: "the runbook mirrors the same recovery-path categories surfaced by doctor"
      pattern: Recovery path
    - from: scripts/smoke/verify-recovery-flow.sh
      to: .planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md
      via: "scripted evidence and human UAT capture the same stale-before and healthy-after checkpoints"
      pattern: agent-badge doctor
---

<objective>
Add the production runbook and proof artifacts that verify the real stale-badge failure-and-recovery path end to end.

Purpose: Phase 19 is not complete when the recovery route exists only in code. Operators need one canonical runbook, and the repo needs durable evidence that the real repo can move from stale failed publish back to healthy shared mode through the supported CLI flow.
Output: one canonical recovery doc, linked operator docs, scenario-level regression coverage, a repo-owned recovery evidence script, and a phase-owned live UAT artifact.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RESEARCH.md
@.planning/phases/19-recovery-paths-and-production-reliability-verification/19-VALIDATION.md
@.planning/phases/19-recovery-paths-and-production-reliability-verification/19-01-PLAN.md
@README.md
@docs/QUICKSTART.md
@docs/TROUBLESHOOTING.md
@docs/MANUAL-GIST.md
@packages/agent-badge/src/commands/release-readiness-matrix.test.ts
@scripts/release/capture-publish-evidence.ts
@scripts/smoke/verify-registry-install.sh
@.agent-badge/config.json
@.agent-badge/state.json

<current_failure_target>
Use the real current repo state as the live verification target:
- `.agent-badge/state.json` contains `publish.status = "error"`
- `.agent-badge/state.json` contains `lastFailureCode = "auth-missing"`
- `.agent-badge/state.json` contains `lastAttemptChangedBadge = "yes"`
- `.agent-badge/state.json` contains `publish.mode = "shared"`
- `.agent-badge/state.json` contains `refresh.lastPublishDecision = "failed"`
- `.agent-badge/config.json` still points at gist `f9f1989fe5ddd0f04e25df81c6dd051e`
</current_failure_target>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the canonical recovery runbook and align README/Quickstart/Troubleshooting/Manual Gist to it</name>
  <files>docs/RECOVERY.md, README.md, docs/QUICKSTART.md, docs/TROUBLESHOOTING.md, docs/MANUAL-GIST.md</files>
  <read_first>README.md, docs/QUICKSTART.md, docs/TROUBLESHOOTING.md, docs/MANUAL-GIST.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RESEARCH.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-01-PLAN.md</read_first>
  <action>Create `docs/RECOVERY.md` as the canonical operator runbook for Phase 19. The runbook must include exact sections for: stale failed publish (`agent-badge refresh`), gist target repair (`agent-badge init --gist-id <id>`), missing local contributor (`agent-badge refresh`), partial shared metadata (`agent-badge init`), and team-coordination-only states such as stale contributors or original-publisher migration. Include the exact user-facing phrases `stale after failed publish`, `Recovery path`, `agent-badge refresh`, and `agent-badge init --gist-id <id>`. Update `README.md`, `docs/QUICKSTART.md`, `docs/TROUBLESHOOTING.md`, and `docs/MANUAL-GIST.md` so they point at `docs/RECOVERY.md` as the canonical flow instead of maintaining divergent long-form recovery instructions. Keep the existing aggregate-only and local-first positioning intact.</action>
  <acceptance_criteria>
    - `docs/RECOVERY.md` contains `stale after failed publish`
    - `docs/RECOVERY.md` contains `agent-badge refresh`
    - `docs/RECOVERY.md` contains `agent-badge init --gist-id <id>`
    - `docs/RECOVERY.md` contains `missing local contributor`
    - `README.md` contains `docs/RECOVERY.md`
    - `docs/QUICKSTART.md` contains `RECOVERY.md`
    - `docs/TROUBLESHOOTING.md` contains `RECOVERY.md`
    - `docs/MANUAL-GIST.md` contains `RECOVERY.md`
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check</automated>
  </verify>
  <done>The repo has one canonical recovery runbook and the public/operator docs point to it instead of drifting copies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scenario coverage and the phase-owned recovery evidence harness</name>
  <files>packages/agent-badge/src/commands/release-readiness-matrix.test.ts, scripts/smoke/verify-recovery-flow.sh, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json</files>
  <read_first>packages/agent-badge/src/commands/release-readiness-matrix.test.ts, scripts/release/capture-publish-evidence.ts, scripts/smoke/verify-registry-install.sh, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RESEARCH.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-01-PLAN.md, .agent-badge/state.json, .agent-badge/config.json</read_first>
  <action>Extend `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` with a scenario-level recovery check that locks the exact supported stale-failed-publish messaging introduced by Plan 01, using the phrase `stale failed publish recovery`. Add a new repo-owned script `scripts/smoke/verify-recovery-flow.sh` that supports both `--dry-run` and live execution. In `--dry-run` mode the script must validate argument parsing, emit the exact `agent-badge status` / `agent-badge doctor` / supported recovery command sequence it would run, and write temp-output Markdown plus JSON evidence files without mutating publish state. In live mode the script must capture the same sequence against the real repo and write phase-owned evidence files. Create `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md`, `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.md`, and `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json` as concrete artifact templates with explicit fields for `pre_recovery`, `recovery_command`, `post_recovery`, `status`, and `source: scripts/smoke/verify-recovery-flow.sh`. The harness must be shaped for phase-owned evidence capture, not generic release publishing.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `stale failed publish recovery`
    - `scripts/smoke/verify-recovery-flow.sh` contains `--dry-run`
    - `scripts/smoke/verify-recovery-flow.sh` contains `agent-badge status`
    - `scripts/smoke/verify-recovery-flow.sh` contains `agent-badge doctor`
    - `scripts/smoke/verify-recovery-flow.sh` contains `agent-badge refresh`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `recovery_command:`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `source: scripts/smoke/verify-recovery-flow.sh`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `pre_recovery`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `post_recovery`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json` contains `postRecovery`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json` contains `source`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts</automated>
    <automated>PHASE19_DRY_RUN_DIR=/tmp/agent-badge-phase19-dry-run bash scripts/smoke/verify-recovery-flow.sh --dry-run --phase-dir /tmp/agent-badge-phase19-dry-run && test -f /tmp/agent-badge-phase19-dry-run/19-RECOVERY-EVIDENCE.json && rg -n '"source"|"preRecovery"|"postRecovery"' /tmp/agent-badge-phase19-dry-run/19-RECOVERY-EVIDENCE.json</automated>
  </verify>
  <done>The repo has both automated scenario coverage and concrete phase-owned artifacts for recording the real stale failed-publish recovery flow.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Execute the real stale-to-healthy recovery flow and record pass/fail evidence</name>
  <files>.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json</files>
  <read_first>scripts/smoke/verify-recovery-flow.sh, docs/RECOVERY.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RESEARCH.md, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-01-PLAN.md, .agent-badge/state.json, .agent-badge/config.json</read_first>
  <action>After auth is restored in the local environment, run the real recovery flow through the harness created in Task 2 and record the result in the phase artifacts. Invoke `bash scripts/smoke/verify-recovery-flow.sh --phase-dir .planning/phases/19-recovery-paths-and-production-reliability-verification` so the proof comes from one repeatable entrypoint instead of ad-hoc shell history. The current diagnosed path should remain the primary expected flow: capture pre-recovery `agent-badge status` and `agent-badge doctor`, run `agent-badge refresh`, then capture post-recovery `agent-badge status` and `agent-badge doctor` again. If the diagnosis has changed by execution time and the harness routes to `agent-badge init` or `agent-badge init --gist-id <id>` instead, record that exact supported command instead of forcing refresh. Persist the command used, the pre-recovery stale/auth-missing state, the post-recovery healthy shared-state output, timestamps, and final pass/fail outcome into `19-HUMAN-UAT.md`, `19-RECOVERY-EVIDENCE.md`, and `19-RECOVERY-EVIDENCE.json`. This task is not complete until the harness-written artifacts show the real repo moved from an unhealthy state to a healthy shared publish state through the supported CLI flow.</action>
  <acceptance_criteria>
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `status: passed`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `recovery_command: agent-badge`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `source: scripts/smoke/verify-recovery-flow.sh`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `pre_recovery`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `post_recovery`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md` contains `Shared mode: shared | health=healthy`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.md` contains `Pre-recovery status`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.md` contains `Post-recovery doctor`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json` contains `"status": "passed"`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json` contains `"source": "scripts/smoke/verify-recovery-flow.sh"`
    - `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json` contains `"postRecovery"`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke/verify-recovery-flow.sh --phase-dir .planning/phases/19-recovery-paths-and-production-reliability-verification && test -f .planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md && test -f .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.md && test -f .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json && rg -n 'status: passed|recovery_command: agent-badge|source: scripts/smoke/verify-recovery-flow.sh|pre_recovery|post_recovery|Shared mode: shared \\| health=healthy' .planning/phases/19-recovery-paths-and-production-reliability-verification/19-HUMAN-UAT.md && rg -n 'Pre-recovery status|Post-recovery doctor|agent-badge|verify-recovery-flow.sh' .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.md && rg -n '\"status\": \"passed\"|\"postRecovery\"|\"recoveryCommand\"|\"source\": \"scripts/smoke/verify-recovery-flow.sh\"' .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json</automated>
  </verify>
  <done>The phase artifacts prove the real repo moved from stale failed publish to healthy shared mode through the supported recovery command sequence.</done>
</task>

</tasks>

<verification>
- `npm run docs:check` succeeds
- `npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts` succeeds
- `docs/RECOVERY.md` and `19-HUMAN-UAT.md` describe the same supported stale failed-publish flow that Phase 19-01 surfaces in CLI output
- `test -f .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json && rg -n '"status": "passed"|"postRecovery"' .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RECOVERY-EVIDENCE.json` succeeds after the live proof run
</verification>

<success_criteria>
- Operators have one canonical runbook for recovery that matches the CLI output exactly.
- The repo has a phase-owned evidence path for proving stale failed publish -> supported recovery command -> healthy shared state on the real repo.
- Recovery documentation and proof stay specific to the supported CLI flows instead of manual state edits or vague troubleshooting prose.
</success_criteria>

<output>
After completion, create `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-02-SUMMARY.md`
</output>

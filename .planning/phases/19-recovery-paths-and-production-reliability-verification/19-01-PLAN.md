---
phase: 19-recovery-paths-and-production-reliability-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/publish/recovery-plan.ts
  - packages/core/src/publish/recovery-plan.test.ts
  - packages/core/src/publish/index.ts
  - packages/core/src/diagnostics/doctor.ts
  - packages/core/src/diagnostics/doctor.test.ts
  - packages/agent-badge/src/commands/status.ts
  - packages/agent-badge/src/commands/status.test.ts
  - packages/agent-badge/src/commands/refresh.ts
  - packages/agent-badge/src/commands/refresh.test.ts
  - packages/agent-badge/src/commands/init.ts
  - packages/agent-badge/src/commands/init.test.ts
autonomous: true
requirements:
  - CTRL-02
user_setup: []
must_haves:
  truths:
    - "A repo in publish error state exposes one explicit supported recovery path through CLI output without requiring manual edits to `.agent-badge/state.json`."
    - "Recovery routing keeps `publish-readiness`, `publish-trust`, and `shared-health` as separate source signals while producing one canonical operator action plan."
    - "Successful `agent-badge refresh` and `agent-badge init` repairs clear previously diagnosed publish-error ambiguity and report the same recovery vocabulary surfaced by `status` and `doctor`."
  artifacts:
    - path: packages/core/src/publish/recovery-plan.ts
      provides: canonical recovery-plan contract built from readiness, trust, and shared-health facts
      contains: deriveRecoveryPlan
    - path: packages/core/src/diagnostics/doctor.ts
      provides: detailed supported recovery output for unhealthy publish and shared-state scenarios
      contains: Recovery path
    - path: packages/agent-badge/src/commands/status.ts
      provides: concise recovery summary in normal operator status output
      contains: - Recovery:
    - path: packages/agent-badge/src/commands/refresh.ts
      provides: post-repair summary when retrying a stale failed publish
      contains: - Recovery result:
    - path: packages/agent-badge/src/commands/init.ts
      provides: post-repair summary when reconnecting gist targets or repairing shared metadata
      contains: - Recovery result:
  key_links:
    - from: packages/core/src/publish/recovery-plan.ts
      to: packages/core/src/diagnostics/doctor.ts
      via: "doctor renders canonical recovery actions from the shared recovery-plan helper instead of re-deriving fix text"
      pattern: deriveRecoveryPlan
    - from: packages/core/src/publish/recovery-plan.ts
      to: packages/agent-badge/src/commands/status.ts
      via: "status prints the same primary recovery action that doctor reports in detailed mode"
      pattern: Recovery:
    - from: packages/core/src/publish/recovery-plan.ts
      to: packages/agent-badge/src/commands/refresh.ts
      via: "refresh success output closes the diagnosed stale publish state using the same recovery vocabulary"
      pattern: Recovery result
    - from: packages/core/src/publish/recovery-plan.ts
      to: packages/agent-badge/src/commands/init.ts
      via: "init repair output closes gist-target and shared-metadata recovery states using the same recovery vocabulary"
      pattern: Recovery result
---

<objective>
Create the canonical recovery-routing layer and operator-facing supported repair flows for publish error state and stale shared state.

Purpose: Phase 19 needs one explicit answer to "what should I run now?" based on the existing readiness, trust, and shared-health models. This plan keeps scope in core recovery routing plus the command surfaces that already own repair behavior.
Output: one canonical recovery-plan helper, status and doctor output that surface exact supported recovery commands, and refresh/init repair summaries that confirm when recovery succeeded.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-recovery-paths-and-production-reliability-verification/19-RESEARCH.md
@.planning/phases/19-recovery-paths-and-production-reliability-verification/19-VALIDATION.md
@.agent-badge/config.json
@.agent-badge/state.json
@packages/core/src/publish/publish-readiness.ts
@packages/core/src/publish/publish-trust.ts
@packages/core/src/publish/shared-health.ts
@packages/core/src/publish/index.ts
@packages/core/src/diagnostics/doctor.ts
@packages/agent-badge/src/commands/status.ts
@packages/agent-badge/src/commands/refresh.ts
@packages/agent-badge/src/commands/init.ts

<state_snapshot>
From .agent-badge/state.json and .agent-badge/config.json:
```json
{
  "publish": {
    "status": "error",
    "lastAttemptOutcome": "failed",
    "lastAttemptChangedBadge": "yes",
    "lastFailureCode": "auth-missing",
    "mode": "shared"
  },
  "refresh": {
    "lastPublishDecision": "failed"
  },
  "config": {
    "publish": {
      "gistId": "f9f1989fe5ddd0f04e25df81c6dd051e"
    },
    "refresh": {
      "prePush": {
        "mode": "strict"
      }
    }
  }
}
```
</state_snapshot>

<interfaces>
From packages/core/src/publish/publish-readiness.ts:
```typescript
export interface PublishReadinessReport {
  readonly status: PublishReadinessStatus;
  readonly summary: string;
  readonly fix: readonly string[];
}
```

From packages/core/src/publish/publish-trust.ts:
```typescript
export interface PublishTrustReport {
  readonly status: PublishTrustStatus;
  readonly lastRefreshedAt: AgentBadgeState["refresh"]["lastRefreshedAt"];
  readonly lastPublishedAt: AgentBadgeState["publish"]["lastPublishedAt"];
}
```

From packages/core/src/publish/shared-health.ts:
```typescript
export interface SharedPublishHealthReport {
  readonly mode: SharedPublishHealthMode;
  readonly status: SharedPublishHealthStatus;
  readonly issues: SharedPublishIssueId[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the canonical recovery-plan helper and use it in status and doctor</name>
  <files>packages/core/src/publish/recovery-plan.ts, packages/core/src/publish/recovery-plan.test.ts, packages/core/src/publish/index.ts, packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts</files>
  <read_first>packages/core/src/publish/publish-readiness.ts, packages/core/src/publish/publish-trust.ts, packages/core/src/publish/shared-health.ts, packages/core/src/publish/index.ts, packages/core/src/diagnostics/doctor.ts, packages/core/src/diagnostics/doctor.test.ts, packages/agent-badge/src/commands/status.ts, packages/agent-badge/src/commands/status.test.ts, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RESEARCH.md, .agent-badge/state.json, .agent-badge/config.json</read_first>
  <action>Create `packages/core/src/publish/recovery-plan.ts` and export it from `packages/core/src/publish/index.ts`. Define one canonical helper named `deriveRecoveryPlan()` that consumes the already-separated publish inputs and returns exact supported recovery actions without mutating state. The helper must distinguish at least these concrete scenarios using exact command strings: `agent-badge refresh` for stale failed publish and missing local contributor, `agent-badge init` for repairing missing shared overrides or partial shared metadata, `agent-badge init --gist-id <id>` for reconnecting a valid public gist target, and a team-coordination path for stale contributors, conflicting observations, or original-publisher migration. Wire `packages/core/src/diagnostics/doctor.ts` to render detailed `Recovery path` output from `deriveRecoveryPlan()` instead of composing freehand fix lists for these scenarios, while preserving doctor's read-only default behavior. Wire `packages/agent-badge/src/commands/status.ts` to print one concise line exactly prefixed `- Recovery:` whenever the repo is not fully healthy, using the same primary action string returned by the helper. Add focused tests first for the current live repo state (`auth-missing` + `stale-failed-publish` + shared mode), missing local contributor, partial shared state, and a team-coordination-only stale contributor case.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/recovery-plan.ts` contains `export function deriveRecoveryPlan`
    - `packages/core/src/publish/recovery-plan.ts` contains `agent-badge refresh`
    - `packages/core/src/publish/recovery-plan.ts` contains `agent-badge init --gist-id <id>`
    - `packages/core/src/diagnostics/doctor.ts` contains `Recovery path`
    - `packages/agent-badge/src/commands/status.ts` contains `- Recovery:`
    - `packages/core/src/diagnostics/doctor.test.ts` contains `auth-missing stale shared publish uses refresh as the supported recovery path`
    - `packages/agent-badge/src/commands/status.test.ts` contains `prints a concise recovery line for stale failed publish`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/recovery-plan.test.ts packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/status.test.ts</automated>
  </verify>
  <done>The repo has one canonical recovery-plan contract and both status and doctor surface exact supported recovery commands for unhealthy states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teach init and refresh to report when supported recovery actually closed the diagnosed state</name>
  <files>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts</files>
  <read_first>packages/agent-badge/src/commands/refresh.ts, packages/agent-badge/src/commands/refresh.test.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/core/src/publish/recovery-plan.ts, packages/core/src/publish/publish-state.ts, packages/core/src/publish/publish-service.ts, .planning/phases/19-recovery-paths-and-production-reliability-verification/19-RESEARCH.md, .agent-badge/state.json</read_first>
  <action>Update `packages/agent-badge/src/commands/refresh.ts` and `packages/agent-badge/src/commands/init.ts` so they explicitly report when a supported repair path closed a previously diagnosed unhealthy state. In refresh, after a successful publish retry or local contributor recreation, print a line exactly prefixed `- Recovery result:` followed by a concrete phrase such as `healthy after agent-badge refresh`. In init, after reconnecting a gist target or repairing shared metadata, print a line exactly prefixed `- Recovery result:` followed by a concrete phrase such as `healthy after agent-badge init` or `healthy after agent-badge init --gist-id <id>`. Reuse `deriveRecoveryPlan()` so the pre-repair diagnosed action and post-repair success wording are aligned. Do not add manual state editing paths, and do not make doctor or status mutate state. Add tests first that prove refresh clears the current auth-missing stale publish case after a successful retry and that init reports gist-target or shared-metadata repair with the same supported recovery vocabulary.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/refresh.ts` contains `- Recovery result:`
    - `packages/agent-badge/src/commands/init.ts` contains `- Recovery result:`
    - `packages/agent-badge/src/commands/refresh.test.ts` contains `reports healthy after agent-badge refresh when stale failed publish is repaired`
    - `packages/agent-badge/src/commands/init.test.ts` contains `reports healthy after agent-badge init --gist-id <id> when the publish target is repaired`
    - `packages/agent-badge/src/commands/init.test.ts` contains `reports healthy after agent-badge init when shared metadata is repaired`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/init.test.ts</automated>
  </verify>
  <done>Refresh and init confirm when the supported recovery path actually restored the repo to a healthy publish or shared state.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/recovery-plan.test.ts packages/core/src/diagnostics/doctor.test.ts packages/agent-badge/src/commands/status.test.ts` succeeds
- `npm test -- --run packages/agent-badge/src/commands/refresh.test.ts packages/agent-badge/src/commands/init.test.ts` succeeds
- `status`, `doctor`, `refresh`, and `init` all use the same supported recovery vocabulary for the same unhealthy state
</verification>

<success_criteria>
- Operators can see the exact supported recovery command for stale failed publish and stale shared-state problems without reading docs first.
- The supported local recovery flows stay limited to existing repair commands instead of manual state edits or destructive reset operations.
- Successful repair commands make it obvious that the prior unhealthy state has been closed.
</success_criteria>

<output>
After completion, create `.planning/phases/19-recovery-paths-and-production-reliability-verification/19-01-SUMMARY.md`
</output>

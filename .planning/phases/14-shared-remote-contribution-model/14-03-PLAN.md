---
phase: 14-shared-remote-contribution-model
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - packages/core/src/publish/shared-badge-aggregation.test.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - docs/HOW-IT-WORKS.md
  - docs/PRIVACY.md
  - scripts/verify-docs.sh
autonomous: true
requirements:
  - TEAM-03
  - TEAM-01
  - TEAM-02
user_setup: []
must_haves:
  truths:
    - "Phase 14 shared publish remains aggregate-only in public docs and tests even though the remote model now contains per-publisher contribution files and a shared overrides file."
    - "Malformed or truncated shared files fail safely in tests instead of silently corrupting derived badge totals."
    - "Docs verification enforces the new shared publish wording so the public privacy promise cannot drift away from implementation."
  artifacts:
    - path: packages/core/src/publish/shared-badge-aggregation.test.ts
      provides: direct proof that badge endpoint files are derived from contributor records rather than one local aggregate snapshot
      contains: 'derives the badge payload from remote contributor totals'
    - path: packages/core/src/publish/publish-service.test.ts
      provides: safety regressions for malformed or privacy-unsafe shared files
      contains: 'rejects a shared overrides file that contains a raw provider session key'
    - path: docs/HOW-IT-WORKS.md
      provides: operator-facing explanation of shared contributor files and derived badge payloads
      contains: 'agent-badge-contrib-'
    - path: docs/PRIVACY.md
      provides: explicit shared-state privacy contract for contributor files and shared override digests
      contains: 'agent-badge-overrides.json'
    - path: scripts/verify-docs.sh
      provides: exact-string enforcement for the shared publish docs contract
      contains: 'agent-badge-contrib-'
  key_links:
    - from: packages/core/src/publish/shared-badge-aggregation.test.ts
      to: packages/core/src/publish/publish-service.ts
      via: "aggregation tests prove derived badge files reflect remote contributor totals after shared publish wiring lands"
      pattern: 'deriveSharedIncludedTotals'
    - from: docs/HOW-IT-WORKS.md
      to: docs/PRIVACY.md
      via: "the public user-facing flow and privacy model must describe the same shared remote files and the same forbidden outbound data"
      pattern: 'agent-badge-overrides.json'
    - from: scripts/verify-docs.sh
      to: docs/PRIVACY.md
      via: "docs verification locks the shared-state privacy wording in place"
      pattern: 'agent-badge-contrib-'
---

<objective>
Finish Phase 14 with explicit privacy, safety, and docs enforcement for the shared model.

Purpose: TEAM-03 is not satisfied by shared publish wiring alone. The repo needs direct regression tests that prove badge files are derived from remote contributor records, direct safety tests that reject malformed shared files, and docs that precisely describe the new public gist contents without weakening the aggregate-only privacy promise.
Output: shared aggregation and privacy regression tests plus updated public docs and docs verification strings for the new contributor-file model.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md
@.planning/phases/14-shared-remote-contribution-model/14-VALIDATION.md
@.planning/phases/14-shared-remote-contribution-model/14-01-PLAN.md
@.planning/phases/14-shared-remote-contribution-model/14-02-PLAN.md
@packages/core/src/publish/shared-model.ts
@packages/core/src/publish/shared-merge.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/publish/publish-service.test.ts
@packages/agent-badge/src/commands/publish.test.ts
@docs/HOW-IT-WORKS.md
@docs/PRIVACY.md
@scripts/verify-docs.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit aggregation and malformed-shared-file safety tests</name>
  <files>packages/core/src/publish/shared-badge-aggregation.test.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.test.ts</files>
  <read_first>packages/core/src/publish/shared-model.ts, packages/core/src/publish/shared-merge.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, packages/agent-badge/src/commands/publish.test.ts, .planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md, .planning/phases/14-shared-remote-contribution-model/14-VALIDATION.md</read_first>
  <action>Create `packages/core/src/publish/shared-badge-aggregation.test.ts` with at least two explicit cases: `derives the badge payload from remote contributor totals` and `does not need the local includedTotals snapshot to contain every remote contributor`. Extend `packages/core/src/publish/publish-service.test.ts` with a case named `rejects a shared overrides file that contains a raw provider session key` by feeding an `agent-badge-overrides.json` payload whose `overrides` object contains a key like `codex:session-1`; the service must throw instead of publishing. Extend `packages/agent-badge/src/commands/publish.test.ts` so command-level output and persisted state assertions still exclude `/Users/example/project`, `codex:session-1`, `transcriptProjectKey`, and `prompt` after shared publishing is enabled.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/shared-badge-aggregation.test.ts` contains `derives the badge payload from remote contributor totals`
    - `packages/core/src/publish/shared-badge-aggregation.test.ts` contains `does not need the local includedTotals snapshot to contain every remote contributor`
    - `packages/core/src/publish/publish-service.test.ts` contains `rejects a shared overrides file that contains a raw provider session key`
    - `packages/core/src/publish/publish-service.test.ts` contains `codex:session-1`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `transcriptProjectKey`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `prompt`
    - `packages/agent-badge/src/commands/publish.test.ts` does not contain `/Users/example/project`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/shared-badge-aggregation.test.ts packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts</automated>
  </verify>
  <done>The shared publish model now has direct regression coverage for derived badge aggregation, malformed shared-file rejection, and command-level privacy boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Update public docs and lock the shared publish contract with docs verification</name>
  <files>docs/HOW-IT-WORKS.md, docs/PRIVACY.md, scripts/verify-docs.sh</files>
  <read_first>docs/HOW-IT-WORKS.md, docs/PRIVACY.md, scripts/verify-docs.sh, .planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md, packages/core/src/publish/shared-model.ts</read_first>
  <action>Update `docs/HOW-IT-WORKS.md` so the `What Gets Published` and publish-flow sections explicitly say the gist now contains derived badge endpoint files plus deterministic shared-state files named `agent-badge-contrib-<publisher>.json` and `agent-badge-overrides.json`. State plainly that contributor files contain only aggregate totals plus opaque publisher ids and freshness metadata, and that shared override entries use opaque digest keys rather than raw provider session ids. Update `docs/PRIVACY.md` so `Aggregate-only publishing` explicitly says badge endpoint payloads remain aggregate-only and shared-state files still never publish prompts, transcript text, filenames, or local paths. Keep the forbidden outbound data list intact and add explicit lines that raw `provider:providerSessionId` values are not published in shared overrides. Update `scripts/verify-docs.sh` in the existing exact-string style so docs verification requires `agent-badge-contrib-<publisher>.json`, `agent-badge-overrides.json`, `opaque digest`, and `provider:providerSessionId` in the appropriate docs files.</action>
  <acceptance_criteria>
    - `docs/HOW-IT-WORKS.md` contains `agent-badge-contrib-<publisher>.json`
    - `docs/HOW-IT-WORKS.md` contains `agent-badge-overrides.json`
    - `docs/HOW-IT-WORKS.md` contains `opaque publisher ids`
    - `docs/HOW-IT-WORKS.md` contains `opaque digest`
    - `docs/PRIVACY.md` contains `agent-badge-overrides.json`
    - `docs/PRIVACY.md` contains `provider:providerSessionId`
    - `docs/PRIVACY.md` contains `prompt text`
    - `docs/PRIVACY.md` contains `local paths`
    - `scripts/verify-docs.sh` contains `agent-badge-contrib-<publisher>.json`
    - `scripts/verify-docs.sh` contains `agent-badge-overrides.json`
    - `scripts/verify-docs.sh` contains `opaque digest`
    - `scripts/verify-docs.sh` contains `provider:providerSessionId`
  </acceptance_criteria>
  <verify>
    <automated>npm run docs:check</automated>
  </verify>
  <done>The public docs and docs gate now describe the shared publish model precisely enough that Phase 14 cannot silently drift into a weaker privacy contract.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/shared-badge-aggregation.test.ts packages/core/src/publish/publish-service.test.ts packages/agent-badge/src/commands/publish.test.ts` succeeds
- `npm run docs:check` succeeds
</verification>

<success_criteria>
- TEAM-03 has direct automated coverage and public documentation, not only implementation intent.
- Shared badge totals are proven to be derived from contributor files.
- The docs gate enforces the new shared publish contract.
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-remote-contribution-model/14-03-SUMMARY.md`
</output>

---
phase: 14-shared-remote-contribution-model
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - packages/core/src/publish/github-gist-client.ts
  - packages/core/src/publish/github-gist-client.test.ts
  - packages/core/src/publish/publish-service.ts
  - packages/core/src/publish/publish-service.test.ts
  - packages/core/src/state/state-schema.ts
  - packages/core/src/state/state-schema.test.ts
  - packages/core/src/init/default-state.ts
  - packages/core/src/init/scaffold.ts
  - packages/core/src/init/scaffold.test.ts
  - packages/agent-badge/src/commands/publish.ts
  - packages/agent-badge/src/commands/publish.test.ts
  - packages/agent-badge/src/commands/init.ts
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
user_setup: []
must_haves:
  truths:
    - "Shared publish updates one deterministic contribution file for the local publisher id, updates one deterministic shared overrides file, rereads the gist, then rewrites badge endpoint files from the authoritative remote contributor set."
    - "Local runtime state persists an opaque publisher id and shared publish mode without exposing GitHub login, machine name, cwd paths, transcript keys, or prompts."
    - "Both `agent-badge publish` and the publish step inside `agent-badge init` use the same shared publish orchestration instead of separate last-writer-wins logic."
  artifacts:
    - path: packages/core/src/publish/github-gist-client.ts
      provides: gist reads with file content/truncation data needed for shared-state loading
      contains: 'truncated'
    - path: packages/core/src/publish/publish-service.ts
      provides: shared publish orchestration for contributor-file update, overrides update, remote reread, and derived badge rewrite
      contains: 'agent-badge-contrib-'
    - path: packages/core/src/state/state-schema.ts
      provides: persisted local publisher identity and shared publish mode
      contains: 'publisherId'
    - path: packages/core/src/init/scaffold.ts
      provides: scaffold reconciliation that preserves shared publish metadata across reruns
      contains: 'publisherId'
    - path: packages/agent-badge/src/commands/publish.test.ts
      provides: command-level regression coverage for shared publish state persistence
      contains: 'Publish mode: shared'
  key_links:
    - from: packages/core/src/publish/github-gist-client.ts
      to: packages/core/src/publish/publish-service.ts
      via: "publish-service needs gist file contents and truncation markers to load contributor and override records safely"
      pattern: 'content'
    - from: packages/core/src/state/state-schema.ts
      to: packages/core/src/init/scaffold.ts
      via: "scaffold reconciliation must preserve or initialize shared publish metadata using the same persisted shape"
      pattern: 'publisherId'
    - from: packages/agent-badge/src/commands/publish.ts
      to: packages/core/src/publish/publish-service.ts
      via: "command output and state persistence reuse the shared publish service instead of reimplementing merge logic"
      pattern: 'publishBadgeToGist'
---

<objective>
Wire the new per-publisher shared model into the live gist publish path.

Purpose: the Phase 14 contract is only useful if runtime publishing actually stops being single-writer. This plan converts the current aggregate overwrite flow into a shared contributor-file update followed by derived badge regeneration, while persisting one opaque local publisher identity that survives reruns.
Output: shared publish service wiring, gist content reads, state/init persistence for `publisherId`, and command-level coverage proving shared mode is used by both `publish` and `init`.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md
@.planning/phases/14-shared-remote-contribution-model/14-VALIDATION.md
@.planning/phases/14-shared-remote-contribution-model/14-01-PLAN.md
@packages/core/src/publish/shared-model.ts
@packages/core/src/publish/shared-merge.ts
@packages/core/src/publish/github-gist-client.ts
@packages/core/src/publish/github-gist-client.test.ts
@packages/core/src/publish/publish-service.ts
@packages/core/src/publish/publish-service.test.ts
@packages/core/src/state/state-schema.ts
@packages/core/src/state/state-schema.test.ts
@packages/core/src/init/default-state.ts
@packages/core/src/init/scaffold.ts
@packages/core/src/init/scaffold.test.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/publish.ts
@packages/agent-badge/src/commands/publish.test.ts

<interfaces>
From packages/core/src/publish/github-gist-client.ts:
```typescript
export interface GitHubGistClient {
  getGist(gistId: string): Promise<GitHubGist>;
  createPublicGist(input: CreatePublicGistInput): Promise<GitHubGist>;
  updateGistFile(input: UpdateGistFileInput): Promise<GitHubGist>;
}
```

From packages/core/src/publish/publish-service.ts:
```typescript
export async function publishBadgeIfChanged(
  options: PublishBadgeIfChangedOptions
): Promise<PublishBadgeIfChangedResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend gist reads and the core publish service for contributor-file shared publishing</name>
  <files>packages/core/src/publish/github-gist-client.ts, packages/core/src/publish/github-gist-client.test.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts</files>
  <read_first>packages/core/src/publish/shared-model.ts, packages/core/src/publish/shared-merge.ts, packages/core/src/publish/github-gist-client.ts, packages/core/src/publish/github-gist-client.test.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, .planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md</read_first>
  <action>Update `packages/core/src/publish/github-gist-client.ts` so `getGist()` returns file content and truncation metadata rather than only filenames. Replace the current `files: string[]` shape with a map keyed by filename where each entry includes `filename`, `content`, and `truncated`. Update `normalizeGist()` and `packages/core/src/publish/github-gist-client.test.ts` accordingly. Then update `packages/core/src/publish/publish-service.ts` to use the Phase 14 shared model: load existing contributor records from filenames beginning with `agent-badge-contrib-`, load the deterministic `agent-badge-overrides.json` file when present, write the local contributor record for `state.publish.publisherId`, write the shared overrides file derived from local overrides using `buildSharedOverrideDigest()`, reread the gist, derive merged totals from all contributor records with `deriveSharedIncludedTotals()`, and only then rewrite `agent-badge.json` plus the preview badge files. If any contributor or override file reports `truncated: true`, throw instead of silently using partial data. Update `publish-service.test.ts` so one test proves two remote contributor files produce a derived shared total, one test proves the local publisher record replaces its own prior file without deleting another publisher file, and one test proves truncated shared files are rejected.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/github-gist-client.ts` contains `content`
    - `packages/core/src/publish/github-gist-client.ts` contains `truncated`
    - `packages/core/src/publish/publish-service.ts` contains `agent-badge-contrib-`
    - `packages/core/src/publish/publish-service.ts` contains `agent-badge-overrides.json`
    - `packages/core/src/publish/publish-service.ts` contains `buildSharedOverrideDigest`
    - `packages/core/src/publish/publish-service.ts` contains `deriveSharedIncludedTotals`
    - `packages/core/src/publish/publish-service.ts` contains `truncated`
    - `packages/core/src/publish/publish-service.test.ts` contains `derives shared totals from two remote contributor files`
    - `packages/core/src/publish/publish-service.test.ts` contains `replaces the local publisher contribution without deleting another publisher file`
    - `packages/core/src/publish/publish-service.test.ts` contains `rejects truncated shared contributor or overrides files`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/github-gist-client.test.ts packages/core/src/publish/publish-service.test.ts</automated>
  </verify>
  <done>The core gist publish path now has the data it needs to load remote contributor files safely and regenerate badge totals from shared state instead of one local aggregate snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist opaque publisher identity and shared mode in local state and scaffold reconciliation</name>
  <files>packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/init/default-state.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/scaffold.test.ts</files>
  <read_first>packages/core/src/state/state-schema.ts, packages/core/src/state/state-schema.test.ts, packages/core/src/init/default-state.ts, packages/core/src/init/scaffold.ts, packages/core/src/init/scaffold.test.ts, .planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md</read_first>
  <action>Extend `packages/core/src/state/state-schema.ts` so `publish` persists the exact new fields `publisherId: string | null` and `mode: "legacy" | "shared"`. Keep the existing `status`, `gistId`, `lastPublishedHash`, and `lastPublishedAt` fields. Update `defaultAgentBadgeState` so `publisherId` defaults to `null` and `mode` defaults to `"legacy"`. Update `packages/core/src/init/default-state.ts` to preserve those defaults. Update `packages/core/src/init/scaffold.ts` so state reconciliation preserves an existing `publisherId`, preserves an existing `mode`, and does not drop either field during init reruns or invalid-state recovery. Add or update tests in `state-schema.test.ts` and `scaffold.test.ts` so they explicitly prove `publisherId` survives parse/reconcile, invalid extra publish fields still reject, and init reruns do not rotate a preexisting `publisherId`.</action>
  <acceptance_criteria>
    - `packages/core/src/state/state-schema.ts` contains `publisherId`
    - `packages/core/src/state/state-schema.ts` contains `z.enum(["legacy", "shared"])`
    - `packages/core/src/state/state-schema.ts` contains `mode: "legacy"`
    - `packages/core/src/state/state-schema.test.ts` contains `publisherId`
    - `packages/core/src/state/state-schema.test.ts` contains `mode: "shared"`
    - `packages/core/src/init/scaffold.ts` contains `publisherId`
    - `packages/core/src/init/scaffold.ts` contains `mode`
    - `packages/core/src/init/scaffold.test.ts` contains `does not rotate an existing publisherId`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/state/state-schema.test.ts packages/core/src/init/scaffold.test.ts</automated>
  </verify>
  <done>The runtime now has one stable, opaque local publisher identity and a persisted shared-mode flag that survive init reruns and scaffold reconciliation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route publish and init command flows through the shared publish model and persist shared-mode output</name>
  <files>packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/init.ts</files>
  <read_first>packages/core/src/publish/publish-service.ts, packages/core/src/state/state-schema.ts, packages/agent-badge/src/commands/publish.ts, packages/agent-badge/src/commands/publish.test.ts, packages/agent-badge/src/commands/init.ts, .planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md</read_first>
  <action>Update `packages/agent-badge/src/commands/publish.ts` and the publish path inside `packages/agent-badge/src/commands/init.ts` to rely on the shared publish service from Task 1. When `publishBadgeToGist()` returns, persist the returned `state.publish.publisherId` and `state.publish.mode`. Update command output so successful shared publishes print `- Publish mode: shared` in addition to the existing badge URL, publish status, and lastPublishedHash lines. Extend `packages/agent-badge/src/commands/publish.test.ts` to prove the persisted state now includes `publisherId` and `mode: "shared"` after a successful publish, and prove the printed output contains `Publish mode: shared` without leaking raw provider session ids or local paths.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/publish.ts` contains `Publish mode: shared`
    - `packages/agent-badge/src/commands/publish.ts` contains `publisherId`
    - `packages/agent-badge/src/commands/init.ts` contains `publishBadgeToGist`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `mode: "shared"`
    - `packages/agent-badge/src/commands/publish.test.ts` contains `Publish mode: shared`
    - `packages/agent-badge/src/commands/publish.test.ts` does not contain `/Users/example/project`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/publish.test.ts</automated>
  </verify>
  <done>The user-facing publish entrypoints now persist and surface shared mode while reusing one shared publish implementation across init and publish.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/github-gist-client.test.ts packages/core/src/publish/publish-service.test.ts` succeeds
- `npm test -- --run packages/core/src/state/state-schema.test.ts packages/core/src/init/scaffold.test.ts` succeeds
- `npm test -- --run packages/agent-badge/src/commands/publish.test.ts` succeeds
</verification>

<success_criteria>
- Shared publishing no longer treats one local aggregate payload as the source of truth.
- One opaque local publisher id is persisted and reused across publishes.
- Init and publish both flow through the same shared publish implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-remote-contribution-model/14-02-SUMMARY.md`
</output>

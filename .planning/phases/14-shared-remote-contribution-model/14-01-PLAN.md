---
phase: 14-shared-remote-contribution-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/publish/shared-model.ts
  - packages/core/src/publish/shared-merge.ts
  - packages/core/src/publish/shared-model.test.ts
  - packages/core/src/publish/shared-merge.test.ts
  - packages/core/src/publish/index.ts
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-03
user_setup: []
must_haves:
  truths:
    - "Phase 14 defines canonical shared remote state as deterministic per-publisher contribution files plus one deterministic shared overrides file, not one mutable aggregate source-of-truth file."
    - "Shared badge totals in Phase 14 are derived from contributor records, while the existing badge endpoint files remain stable render outputs for Shields."
    - "Repo-level shared override references are privacy-safe and do not expose raw `provider:providerSessionId`, cwd realpaths, transcript keys, filenames, or local paths."
  artifacts:
    - path: packages/core/src/publish/shared-model.ts
      provides: strict schemas and filename helpers for Phase 14 remote shared files
      contains: 'agent-badge-contrib-'
    - path: packages/core/src/publish/shared-merge.ts
      provides: deterministic contributor-record merge and derived-total helpers
      contains: 'deriveSharedIncludedTotals'
    - path: packages/core/src/publish/shared-model.test.ts
      provides: schema and privacy regression coverage for public shared-state files
      contains: 'agent-badge-overrides.json'
    - path: packages/core/src/publish/shared-merge.test.ts
      provides: contributor replacement and totals recomputation coverage
      contains: 'replaces an existing contributor record with the same publisherId'
    - path: packages/core/src/publish/index.ts
      provides: public exports for shared-model and merge helpers
      contains: 'shared-model'
  key_links:
    - from: packages/core/src/publish/shared-model.ts
      to: packages/core/src/publish/shared-merge.ts
      via: "merge helpers consume the strict contributor and override record types defined for remote public files"
      pattern: 'SharedContributorRecord'
    - from: packages/core/src/publish/shared-model.test.ts
      to: packages/core/src/publish/shared-model.ts
      via: "schema tests prove the filename helpers and privacy-safe override keys match the shared model contract"
      pattern: 'buildSharedOverrideDigest'
    - from: packages/core/src/publish/shared-merge.test.ts
      to: packages/core/src/publish/shared-merge.ts
      via: "merge tests prove contributor-slot replacement and total derivation are deterministic"
      pattern: 'deriveSharedIncludedTotals'
---

<objective>
Define the Phase 14 shared remote contract before any publish wiring changes land.

Purpose: TEAM-01 through TEAM-03 cannot be implemented safely until the repo has explicit schemas, deterministic file naming, opaque shared override keys, and pure merge helpers that encode the per-publisher remote model recommended by `14-RESEARCH.md`.
Output: new shared-model and shared-merge modules with direct tests and exported helpers that later plans can wire into publish and init flows.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md
@.planning/phases/14-shared-remote-contribution-model/14-VALIDATION.md
@packages/core/src/publish/publish-service.ts
@packages/core/src/publish/publish-service.test.ts
@packages/core/src/publish/index.ts
@packages/core/src/attribution/override-store.ts
@packages/core/src/state/state-schema.ts
@docs/HOW-IT-WORKS.md
@docs/PRIVACY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict schemas and deterministic file helpers for per-publisher contributions and shared overrides</name>
  <files>packages/core/src/publish/shared-model.ts, packages/core/src/publish/shared-model.test.ts, packages/core/src/publish/index.ts</files>
  <read_first>packages/core/src/publish/index.ts, packages/core/src/publish/publish-service.ts, packages/core/src/attribution/override-store.ts, packages/core/src/state/state-schema.ts, .planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md, docs/HOW-IT-WORKS.md, docs/PRIVACY.md</read_first>
  <action>Create `packages/core/src/publish/shared-model.ts` and export all of the following exact contracts. Add `AGENT_BADGE_CONTRIB_GIST_FILE_PREFIX = "agent-badge-contrib-"`, `AGENT_BADGE_OVERRIDES_GIST_FILE = "agent-badge-overrides.json"`, and a `buildContributorGistFileName(publisherId: string): string` helper that returns `agent-badge-contrib-${publisherId}.json`. Add `sharedContributorRecordSchema` with exact fields `schemaVersion: 1`, `publisherId: string`, `updatedAt: datetime`, and `totals: { sessions: int >= 0, tokens: int >= 0, estimatedCostUsdMicros: int >= 0 | null }`. Add `sharedOverridesRecordSchema` with exact fields `schemaVersion: 1` and `overrides: Record<string, { decision: "include" | "exclude"; updatedAt: datetime; updatedByPublisherId: string }>`. Export `parseSharedContributorRecord()`, `parseSharedOverridesRecord()`, `isContributorGistFileName()`, and `buildSharedOverrideDigest(sessionKey: string): string`. `buildSharedOverrideDigest()` must use `createHash("sha256")` from `node:crypto` and return a `sha256:`-prefixed hex digest so public shared override keys never contain raw `provider:providerSessionId` values. Export the new helpers from `packages/core/src/publish/index.ts`. Add `packages/core/src/publish/shared-model.test.ts` that proves the contributor filename helper emits `agent-badge-contrib-abc123.json`, shared override digests start with `sha256:`, shared override digests do not contain `codex:session-1`, and both schemas reject extra fields such as `cwd`, `transcriptProjectKey`, `fileName`, `prompt`, or `localPath`.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/shared-model.ts` contains `agent-badge-contrib-`
    - `packages/core/src/publish/shared-model.ts` contains `agent-badge-overrides.json`
    - `packages/core/src/publish/shared-model.ts` contains `buildContributorGistFileName`
    - `packages/core/src/publish/shared-model.ts` contains `buildSharedOverrideDigest`
    - `packages/core/src/publish/shared-model.ts` contains `createHash("sha256")`
    - `packages/core/src/publish/shared-model.ts` contains `schemaVersion: z.literal(1)`
    - `packages/core/src/publish/shared-model.ts` contains `updatedByPublisherId`
    - `packages/core/src/publish/shared-model.test.ts` contains `agent-badge-contrib-abc123.json`
    - `packages/core/src/publish/shared-model.test.ts` contains `sha256:`
    - `packages/core/src/publish/shared-model.test.ts` contains `codex:session-1`
    - `packages/core/src/publish/shared-model.test.ts` contains `localPath`
    - `packages/core/src/publish/index.ts` contains `shared-model`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/shared-model.test.ts</automated>
  </verify>
  <done>The repo has a strict, privacy-safe remote file contract for Phase 14 and later plans can rely on deterministic contributor and shared override filenames.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic merge and totals helpers for contributor-slot replacement</name>
  <files>packages/core/src/publish/shared-merge.ts, packages/core/src/publish/shared-merge.test.ts, packages/core/src/publish/index.ts</files>
  <read_first>packages/core/src/publish/shared-model.ts, packages/core/src/publish/publish-service.ts, packages/core/src/publish/publish-service.test.ts, .planning/phases/14-shared-remote-contribution-model/14-RESEARCH.md, .planning/phases/14-shared-remote-contribution-model/14-VALIDATION.md</read_first>
  <action>Create `packages/core/src/publish/shared-merge.ts` with pure helpers that operate only on contributor and override records from `shared-model.ts`. Export `replaceContributorRecord(records, nextRecord)` that replaces the existing record with the same `publisherId` or appends it if absent. Export `deriveSharedIncludedTotals(records)` that returns `{ sessions, tokens, estimatedCostUsdMicros }` by summing every contributor record and returning `estimatedCostUsdMicros: null` only when every contributor record has `null`; otherwise sum non-null values exactly. Export `mergeSharedOverrides(baseOverrides, nextOverrides)` that returns one overrides record where exact shared override keys from `nextOverrides` overwrite the same keys in `baseOverrides`, while untouched keys are preserved. Add `packages/core/src/publish/shared-merge.test.ts` with explicit coverage for: replacing an existing contributor record with the same `publisherId`, preserving a different publisher record, summing two contributors into one derived total, preserving `null` cost when all contributors have `null`, and overwriting only the matching shared override digest while leaving a second digest untouched. Export the merge helpers from `packages/core/src/publish/index.ts`.</action>
  <acceptance_criteria>
    - `packages/core/src/publish/shared-merge.ts` contains `replaceContributorRecord`
    - `packages/core/src/publish/shared-merge.ts` contains `deriveSharedIncludedTotals`
    - `packages/core/src/publish/shared-merge.ts` contains `mergeSharedOverrides`
    - `packages/core/src/publish/shared-merge.ts` contains `estimatedCostUsdMicros`
    - `packages/core/src/publish/shared-merge.test.ts` contains `replaces an existing contributor record with the same publisherId`
    - `packages/core/src/publish/shared-merge.test.ts` contains `preserves a second contributor record`
    - `packages/core/src/publish/shared-merge.test.ts` contains `preserves null estimatedCostUsdMicros when every contributor is null`
    - `packages/core/src/publish/shared-merge.test.ts` contains `overwrites only the matching shared override digest`
    - `packages/core/src/publish/index.ts` contains `shared-merge`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/core/src/publish/shared-merge.test.ts</automated>
  </verify>
  <done>The repo has pure merge semantics for Phase 14 that are specific enough for execution and narrow enough to stay out of Phase 15 deduplication territory.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/core/src/publish/shared-model.test.ts` succeeds
- `npm test -- --run packages/core/src/publish/shared-merge.test.ts` succeeds
</verification>

<success_criteria>
- Phase 14 has a concrete remote contract based on per-publisher files and one shared overrides file.
- Shared override keys are privacy-safe by construction.
- Pure merge helpers exist before publish-service rewiring begins.
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-remote-contribution-model/14-01-SUMMARY.md`
</output>

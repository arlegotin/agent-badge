# Deferred Items

- 2026-03-31 (07-02 Task 1 verification): `npm run build` fails in `packages/core/src/publish/github-gist-client.ts` with `TS2352` (`Octokit` cast mismatch; missing `gists.remove` on typed interface). Discovery is outside current task-owned files and was not introduced by Task 1 manifest/tsconfig edits.

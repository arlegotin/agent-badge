---
phase: 07-release-readiness
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - docs/QUICKSTART.md
  - docs/ATTRIBUTION.md
  - docs/PRIVACY.md
  - docs/TROUBLESHOOTING.md
  - docs/MANUAL-GIST.md
  - scripts/verify-docs.sh
  - package.json
  - README.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - REL-03
  - REL-02
user_setup: []
must_haves:
  truths:
    - "Public docs include quickstart, attribution model, privacy model, troubleshooting, and manual gist connection guidance."
    - "CI validates docs presence/content and runs pack smoke checks as part of release-readiness gating."
    - "Repository README links directly to the new docs set for discoverability."
  artifacts:
    - path: docs/QUICKSTART.md
      provides: install/init/first publish walkthrough
      contains: "# Quickstart"
    - path: docs/PRIVACY.md
      provides: explicit aggregate-only privacy boundary
      contains: "Aggregate-only publishing"
    - path: scripts/verify-docs.sh
      provides: deterministic docs presence/content verification
      contains: "set -euo pipefail"
    - path: .github/workflows/ci.yml
      provides: docs and pack-smoke gates integrated into CI
      contains: "npm run docs:check"
  key_links:
    - from: package.json
      to: scripts/verify-docs.sh
      via: "docs:check script entrypoint"
      pattern: '"docs:check": "bash scripts/verify-docs.sh"'
    - from: .github/workflows/ci.yml
      to: package.json
      via: "invokes docs and smoke scripts in CI"
      pattern: "npm run docs:check"
    - from: README.md
      to: docs/QUICKSTART.md
      via: "documentation index links for external users"
      pattern: "docs/QUICKSTART.md"
---

<objective>
Ship the public documentation set and final docs/smoke CI gates needed for external release confidence.

Purpose: REL-03 is documentation-complete release readiness, and REL-02 requires docs validation to run in CI with the rest of release checks.
Output: five docs pages, one docs verification script, README documentation links, and CI job updates for docs/smoke gates.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-release-readiness/07-RESEARCH.md
@.planning/phases/07-release-readiness/07-VALIDATION.md
@README.md
@package.json
@.github/workflows/ci.yml
@scripts/smoke/verify-packed-install.sh
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/doctor.ts

<interfaces>
From `packages/agent-badge/src/commands/init.ts` output text patterns:

```ts
"- Publish target: deferred"
"- Badge setup deferred: ..."
"- README badge: updated ..."
```

From `packages/agent-badge/src/commands/doctor.ts` intent:

```ts
doctor command emits actionable fix guidance for setup and publish issues
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author REL-03 documentation set and add README doc links</name>
  <files>docs/QUICKSTART.md, docs/ATTRIBUTION.md, docs/PRIVACY.md, docs/TROUBLESHOOTING.md, docs/MANUAL-GIST.md, README.md</files>
  <read_first>docs/QUICKSTART.md, docs/ATTRIBUTION.md, docs/PRIVACY.md, docs/TROUBLESHOOTING.md, docs/MANUAL-GIST.md, README.md, .planning/REQUIREMENTS.md, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/doctor.ts</read_first>
  <action>Create the five docs files with concrete top-level headings and required release content: (1) `docs/QUICKSTART.md` with `# Quickstart`, sections `Install`, `Initialize`, `Refresh`, `Publish`, and exact commands `npm init agent-badge@latest`, `agent-badge status`, `agent-badge refresh`, `agent-badge publish`; (2) `docs/ATTRIBUTION.md` with `# Attribution Model` and explicit evidence order `exact repo root -> exact remote -> normalized cwd -> transcript correlation -> persisted override`; (3) `docs/PRIVACY.md` with `# Privacy Model`, `Aggregate-only publishing`, and explicit forbidden outbound data list `prompt text`, `raw transcript`, `filename`, `local absolute path`; (4) `docs/TROUBLESHOOTING.md` with `# Troubleshooting` and concrete recovery steps for `non-git directory`, `no GitHub auth`, `deferred publish target`, and `provider directory not found`; (5) `docs/MANUAL-GIST.md` with `# Manual Gist Connection` and step-by-step `agent-badge init --gist-id <id>` reconnection flow plus expected badge URL format. Update `README.md` by adding a `## Documentation` section with links to all five docs paths.</action>
  <acceptance_criteria>
    - `docs/QUICKSTART.md` contains `# Quickstart`
    - `docs/QUICKSTART.md` contains `npm init agent-badge@latest`
    - `docs/QUICKSTART.md` contains `agent-badge refresh`
    - `docs/ATTRIBUTION.md` contains `# Attribution Model`
    - `docs/ATTRIBUTION.md` contains `exact repo root -> exact remote -> normalized cwd -> transcript correlation -> persisted override`
    - `docs/PRIVACY.md` contains `# Privacy Model`
    - `docs/PRIVACY.md` contains `Aggregate-only publishing`
    - `docs/PRIVACY.md` contains `local absolute path`
    - `docs/TROUBLESHOOTING.md` contains `# Troubleshooting`
    - `docs/TROUBLESHOOTING.md` contains `non-git directory`
    - `docs/MANUAL-GIST.md` contains `# Manual Gist Connection`
    - `docs/MANUAL-GIST.md` contains `agent-badge init --gist-id <id>`
    - `README.md` contains `## Documentation`
    - `README.md` contains `docs/QUICKSTART.md`
    - `README.md` contains `docs/MANUAL-GIST.md`
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/QUICKSTART.md && test -f docs/ATTRIBUTION.md && test -f docs/PRIVACY.md && test -f docs/TROUBLESHOOTING.md && test -f docs/MANUAL-GIST.md && rg -n "Quickstart|Attribution Model|Privacy Model|Troubleshooting|Manual Gist Connection" docs/*.md</automated>
  </verify>
  <done>REL-03 docs corpus exists with concrete operational guidance and is discoverable from README.</done>
</task>

<task type="auto">
  <name>Task 2: Add docs verification script and wire docs/smoke gates into CI</name>
  <files>scripts/verify-docs.sh, package.json, .github/workflows/ci.yml</files>
  <read_first>scripts/verify-docs.sh, package.json, .github/workflows/ci.yml, scripts/smoke/verify-packed-install.sh, docs/QUICKSTART.md, docs/ATTRIBUTION.md, docs/PRIVACY.md, docs/TROUBLESHOOTING.md, docs/MANUAL-GIST.md</read_first>
  <action>Create `scripts/verify-docs.sh` (`#!/usr/bin/env bash`, `set -euo pipefail`) that enforces docs presence and required content via exact checks: file existence for five docs plus `rg -n` checks for `npm init agent-badge@latest`, evidence-order string, `Aggregate-only publishing`, and `agent-badge init --gist-id <id>`. Add root `package.json` script `"docs:check": "bash scripts/verify-docs.sh"`. Update `.github/workflows/ci.yml` by adding a `release-readiness` job on Node `24.x` that runs `npm ci`, `npm run docs:check`, `npm run pack:check`, and `npm run smoke:pack` (using the smoke script from Plan 07-02). Keep existing matrix validate and scenario-matrix jobs intact; this new job is additive and must fail the workflow on docs or smoke regressions.</action>
  <acceptance_criteria>
    - `scripts/verify-docs.sh` contains `#!/usr/bin/env bash`
    - `scripts/verify-docs.sh` contains `set -euo pipefail`
    - `scripts/verify-docs.sh` contains `docs/QUICKSTART.md`
    - `scripts/verify-docs.sh` contains `docs/ATTRIBUTION.md`
    - `scripts/verify-docs.sh` contains `docs/PRIVACY.md`
    - `scripts/verify-docs.sh` contains `docs/TROUBLESHOOTING.md`
    - `scripts/verify-docs.sh` contains `docs/MANUAL-GIST.md`
    - `scripts/verify-docs.sh` contains `Aggregate-only publishing`
    - `scripts/verify-docs.sh` contains `agent-badge init --gist-id <id>`
    - `package.json` contains `"docs:check": "bash scripts/verify-docs.sh"`
    - `.github/workflows/ci.yml` contains `release-readiness`
    - `.github/workflows/ci.yml` contains `npm run docs:check`
    - `.github/workflows/ci.yml` contains `npm run pack:check`
    - `.github/workflows/ci.yml` contains `npm run smoke:pack`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-docs.sh && rg -n "release-readiness|npm run docs:check|npm run pack:check|npm run smoke:pack" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI now enforces docs integrity and tarball smoke validation as release-readiness gates.</done>
</task>

</tasks>

<verification>
- `bash scripts/verify-docs.sh` succeeds
- `.github/workflows/ci.yml` includes `release-readiness` job with docs and smoke checks
- `README.md` includes links to the five release docs pages
</verification>

<success_criteria>
- REL-03 is satisfied by complete public docs for quickstart, attribution, privacy, troubleshooting, and manual gist flow.
- REL-02 is extended to include docs and pack-smoke checks in CI.
</success_criteria>

<output>
After completion, create `.planning/phases/07-release-readiness/07-03-SUMMARY.md`
</output>

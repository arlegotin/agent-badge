---
phase: 07-release-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/core/package.json
  - packages/agent-badge/package.json
  - packages/create-agent-badge/package.json
  - packages/core/tsconfig.json
  - packages/agent-badge/tsconfig.json
  - scripts/smoke/verify-packed-install.sh
  - .github/workflows/release.yml
autonomous: true
requirements:
  - REL-01
  - REL-02
user_setup:
  - service: npm
    why: "Release workflow publishes workspace packages via Changesets."
    env_vars:
      - name: NPM_TOKEN
        source: "GitHub repository secret `NPM_TOKEN` (fallback when trusted publishing is not configured)"
    dashboard_config:
      - task: "Configure npm trusted publishing for this GitHub repository (preferred over long-lived tokens)"
        location: "npmjs.com -> Package settings -> Trusted Publishers"
must_haves:
  truths:
    - "Packed tarballs install in a clean temporary project and runtime imports/binaries resolve successfully."
    - "Published package manifests no longer contain runtime-breaking `file:../...` dependency specifiers."
    - "Release automation exists as a separate workflow from CI and runs build/test/pack smoke gates before publish."
  artifacts:
    - path: scripts/smoke/verify-packed-install.sh
      provides: deterministic pack/install/import smoke validation
      contains: "npm pack --workspace"
    - path: packages/agent-badge/package.json
      provides: publish-safe dependency specifier for @agent-badge/core
      contains: '"@agent-badge/core": "^0.0.0"'
    - path: .github/workflows/release.yml
      provides: changesets-driven release workflow
      contains: "changesets/action@v1"
  key_links:
    - from: scripts/smoke/verify-packed-install.sh
      to: packages/agent-badge/package.json
      via: "tarball install/import validation of published dependency graph"
      pattern: "import\\(\"agent-badge\"\\)"
    - from: .github/workflows/release.yml
      to: package.json
      via: "executes release and smoke scripts declared in root package scripts"
      pattern: "npm run release"
    - from: packages/create-agent-badge/package.json
      to: packages/agent-badge/package.json
      via: "initializer runtime dependency resolves through published agent-badge package"
      pattern: '"agent-badge": "\\^0.0.0"'
---

<objective>
Eliminate packaging/runtime release blockers and add release automation with pack-level smoke gates.

Purpose: REL-02 requires reproducible CI/release validation without hidden local assumptions, and REL-01 requires install/runtime behavior to hold in clean environments.
Output: publish-safe package manifests, pack/install smoke script, and release workflow with pre-publish gates.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-release-readiness/07-RESEARCH.md
@.planning/phases/07-release-readiness/07-VALIDATION.md
@package.json
@packages/core/package.json
@packages/agent-badge/package.json
@packages/create-agent-badge/package.json
@packages/core/tsconfig.json
@packages/agent-badge/tsconfig.json
@packages/create-agent-badge/src/index.ts
@packages/agent-badge/src/index.ts
@.changeset/config.json

<interfaces>
From `packages/create-agent-badge/src/index.ts`:

```ts
export async function runCreateAgentBadge(
  options: RunCreateAgentBadgeOptions = {}
): Promise<InitCommandResult>;
```

From `packages/agent-badge/src/index.ts`:

```ts
export { runInitCommand } from "./commands/init.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace runtime-breaking workspace file dependencies with publish-safe package contracts</name>
  <files>package.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json, packages/core/tsconfig.json, packages/agent-badge/tsconfig.json</files>
  <read_first>package.json, packages/core/package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json, packages/core/tsconfig.json, packages/agent-badge/tsconfig.json, .changeset/config.json</read_first>
  <action>Update package manifests to remove publish-time `file:` runtime dependencies: set `packages/agent-badge/package.json` dependency `"@agent-badge/core"` to `"^0.0.0"` (not `file:../core`), set `packages/create-agent-badge/package.json` dependency `"agent-badge"` to `"^0.0.0"` and remove direct `"@agent-badge/core"` dependency, and add `"publishConfig": { "access": "public" }` to `packages/core/package.json`. In root `package.json`, add scripts `pack:check` (`npm pack --workspace packages/core --dry-run --json && npm pack --workspace packages/agent-badge --dry-run --json && npm pack --workspace packages/create-agent-badge --dry-run --json`), `smoke:pack` (`bash scripts/smoke/verify-packed-install.sh`), `changeset` (`changeset`), and `release` (`changeset publish`). Update `packages/core/tsconfig.json` and `packages/agent-badge/tsconfig.json` with `exclude: ["src/**/*.test.ts"]` so test artifacts are not emitted into published `dist/` payloads.</action>
  <acceptance_criteria>
    - `packages/agent-badge/package.json` contains `"@agent-badge/core": "^0.0.0"`
    - `packages/agent-badge/package.json` does not contain `file:../core`
    - `packages/create-agent-badge/package.json` contains `"agent-badge": "^0.0.0"`
    - `packages/create-agent-badge/package.json` does not contain `file:../agent-badge`
    - `packages/create-agent-badge/package.json` does not contain `"@agent-badge/core"`
    - `packages/core/package.json` contains `"publishConfig"`
    - `package.json` contains `"pack:check"`
    - `package.json` contains `"smoke:pack"`
    - `package.json` contains `"release": "changeset publish"`
    - `packages/core/tsconfig.json` contains `"exclude"`
    - `packages/core/tsconfig.json` contains `"src/**/*.test.ts"`
    - `packages/agent-badge/tsconfig.json` contains `"exclude"`
    - `packages/agent-badge/tsconfig.json` contains `"src/**/*.test.ts"`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run pack:check</automated>
  </verify>
  <done>Workspace packages are publish-safe and pack inspection is available as a repeatable root script gate.</done>
</task>

<task type="auto">
  <name>Task 2: Add tarball smoke script and release workflow with pre-publish gates</name>
  <files>scripts/smoke/verify-packed-install.sh, .github/workflows/release.yml</files>
  <read_first>scripts/smoke/verify-packed-install.sh, .github/workflows/release.yml, package.json, packages/agent-badge/package.json, packages/create-agent-badge/package.json, packages/core/package.json</read_first>
  <action>Create `scripts/smoke/verify-packed-install.sh` (`#!/usr/bin/env bash`, `set -euo pipefail`) that: (1) packs `packages/core`, `packages/agent-badge`, and `packages/create-agent-badge` into a temp directory using `npm pack --workspace ... --pack-destination ...`; (2) creates a clean temp install project with `npm init -y`; (3) installs those tarballs with one `npm install`; (4) verifies runtime imports with `node --input-type=module -e "import('agent-badge').then(...)"` and `import('create-agent-badge')`; and (5) verifies binaries with `./node_modules/.bin/agent-badge --help` and `./node_modules/.bin/create-agent-badge --help`. Create `.github/workflows/release.yml` with `workflow_dispatch` and `push` on `main`, `actions/checkout@v6`, `actions/setup-node@v6` on Node `24.x`, and steps for `npm ci`, `npm run typecheck`, `npm run build`, `npm test -- --run`, `npm run pack:check`, `npm run smoke:pack`, followed by `changesets/action@v1` using `version: npm run changeset version` and `publish: npm run release`. Set workflow permissions `contents: write`, `pull-requests: write`, and `id-token: write` and wire `GITHUB_TOKEN` plus optional `NPM_TOKEN` env for publish execution.</action>
  <acceptance_criteria>
    - `scripts/smoke/verify-packed-install.sh` contains `#!/usr/bin/env bash`
    - `scripts/smoke/verify-packed-install.sh` contains `set -euo pipefail`
    - `scripts/smoke/verify-packed-install.sh` contains `npm pack --workspace packages/core`
    - `scripts/smoke/verify-packed-install.sh` contains `npm pack --workspace packages/agent-badge`
    - `scripts/smoke/verify-packed-install.sh` contains `npm pack --workspace packages/create-agent-badge`
    - `scripts/smoke/verify-packed-install.sh` contains `import('agent-badge')`
    - `scripts/smoke/verify-packed-install.sh` contains `import('create-agent-badge')`
    - `scripts/smoke/verify-packed-install.sh` contains `./node_modules/.bin/agent-badge --help`
    - `.github/workflows/release.yml` contains `name: Release`
    - `.github/workflows/release.yml` contains `workflow_dispatch`
    - `.github/workflows/release.yml` contains `changesets/action@v1`
    - `.github/workflows/release.yml` contains `npm run smoke:pack`
    - `.github/workflows/release.yml` contains `id-token: write`
    - `.github/workflows/release.yml` contains `NPM_TOKEN`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke/verify-packed-install.sh && test -f .github/workflows/release.yml</automated>
  </verify>
  <done>Release automation and pack smoke checks exist and can validate tarball install/runtime behavior before publish.</done>
</task>

</tasks>

<verification>
- `npm run pack:check` succeeds
- `bash scripts/smoke/verify-packed-install.sh` succeeds in a clean temporary install project
- `.github/workflows/release.yml` includes changesets publish flow and pre-publish validation commands
</verification>

<success_criteria>
- REL-02 packaging/release validation is encoded into repeatable scripts and workflow automation.
- REL-01 install/runtime reliability is proven through clean tarball smoke install/import checks.
</success_criteria>

<output>
After completion, create `.planning/phases/07-release-readiness/07-02-SUMMARY.md`
</output>

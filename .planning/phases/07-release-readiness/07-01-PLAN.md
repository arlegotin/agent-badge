---
phase: 07-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agent-badge/src/commands/release-readiness-matrix.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - REL-01
  - REL-02
user_setup: []
must_haves:
  truths:
    - "A fixture-backed test suite verifies fresh repo, existing repo rerun, provider combinations, missing README, missing origin, no auth, and idempotent re-init behavior."
    - "CI runs build and tests on Node 20.x, 22.x, and 24.x without requiring real ~/.codex or ~/.claude directories."
    - "The release-readiness scenario matrix runs in CI as an explicit gate, not an implied side effect."
  artifacts:
    - path: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      provides: explicit Phase 7 scenario matrix test harness
      contains: 'describe("release readiness scenario matrix"'
    - path: .github/workflows/ci.yml
      provides: node-matrix CI workflow for typecheck/build/tests and scenario matrix gate
      contains: "node-version: ['20.x', '22.x', '24.x']"
  key_links:
    - from: .github/workflows/ci.yml
      to: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      via: "explicit vitest file run command"
      pattern: "npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts"
    - from: packages/agent-badge/src/commands/release-readiness-matrix.test.ts
      to: packages/agent-badge/src/commands/init.ts
      via: "imports and executes runInitCommand against fixture repos"
      pattern: "runInitCommand"
---

<objective>
Build the Phase 7 scenario-matrix proof layer and baseline CI workflow so release readiness is measured automatically and repeatably.

Purpose: REL-01 and REL-02 require objective evidence that supported init/publish conditions work and that CI validation is fixture-only (no live provider home assumptions).
Output: one new matrix test file plus one CI workflow that runs typecheck/build/test on supported Node versions and executes the matrix gate explicitly.
</objective>

<execution_context>
@/Users/artemlegotin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/artemlegotin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-release-readiness/07-RESEARCH.md
@.planning/phases/07-release-readiness/07-VALIDATION.md
@package.json
@vitest.config.ts
@packages/agent-badge/src/commands/init.ts
@packages/agent-badge/src/commands/init.test.ts
@packages/testkit/src/repo-fixtures.ts
@packages/testkit/src/provider-fixtures.ts

<interfaces>
From `packages/agent-badge/src/commands/init.ts`:

```ts
export async function runInitCommand(
  options: RunInitCommandOptions = {}
): Promise<InitCommandResult>;
```

From `packages/testkit/src/repo-fixtures.ts`:

```ts
export async function createRepoFixture(
  options?: CreateRepoFixtureOptions
): Promise<RepoFixture>;
```

From `packages/testkit/src/provider-fixtures.ts`:

```ts
export async function createProviderFixture(
  options?: CreateProviderFixtureOptions
): Promise<ProviderFixture>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit release-readiness scenario matrix tests for REL-01</name>
  <files>packages/agent-badge/src/commands/release-readiness-matrix.test.ts</files>
  <read_first>packages/agent-badge/src/commands/release-readiness-matrix.test.ts, packages/agent-badge/src/commands/init.ts, packages/agent-badge/src/commands/init.test.ts, packages/agent-badge/src/commands/uninstall.ts, packages/testkit/src/repo-fixtures.ts, packages/testkit/src/provider-fixtures.ts</read_first>
  <action>Create `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` with `describe("release readiness scenario matrix", ...)` and a table-driven suite that executes `runInitCommand` using fixtures for every required REL-01 scenario: `fresh-repo`, `existing-repo-rerun`, `providers-codex-only`, `providers-claude-only`, `providers-both`, `no-readme`, `no-origin`, `no-auth`, and `idempotent-reinit`. Use deterministic gist client stubs (`getGist`, `createPublicGist`, `updateGistFile`, `deleteGist`) and explicit `homeRoot` fixture inputs from `createProviderFixture` so tests never touch real `~/.codex` or `~/.claude`. Add concrete assertions per row: publish state status (`published` or `deferred`), README marker count (`<!-- agent-badge:start -->` exactly once when README exists), managed hook marker count (`# agent-badge:start` exactly once after reruns), and no README file creation in the `no-readme` case.</action>
  <acceptance_criteria>
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `describe("release readiness scenario matrix"`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `fresh-repo`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `no-readme`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `no-origin`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `no-auth`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `idempotent-reinit`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `runInitCommand`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `createRepoFixture`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `createProviderFixture`
    - `packages/agent-badge/src/commands/release-readiness-matrix.test.ts` contains `<!-- agent-badge:start -->`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts</automated>
  </verify>
  <done>REL-01 scenario coverage is explicit, fixture-backed, and runnable as a single deterministic matrix test file.</done>
</task>

<task type="auto">
  <name>Task 2: Create baseline Node-matrix CI workflow with explicit scenario gate (REL-02)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml, package.json, vitest.config.ts, .planning/phases/07-release-readiness/07-VALIDATION.md</read_first>
  <action>Create `.github/workflows/ci.yml` with workflow name `CI`, triggers on `push` and `pull_request`, and a `validate` job that runs on `ubuntu-latest` with `strategy.matrix.node-version: ['20.x', '22.x', '24.x']`. Use `actions/checkout@v6` and `actions/setup-node@v6` with npm cache. Set `HOME` to `${{ runner.temp }}/agent-badge-home` and create that directory before test execution. Run the exact commands `npm ci`, `npm run typecheck`, `npm run build`, and `npm test -- --run`. Add a second job `scenario-matrix` on Node `24.x` that runs `npm ci` then `npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts` so REL-01 has an explicit CI gate.</action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `name: CI`
    - `.github/workflows/ci.yml` contains `pull_request`
    - `.github/workflows/ci.yml` contains `push`
    - `.github/workflows/ci.yml` contains `actions/checkout@v6`
    - `.github/workflows/ci.yml` contains `actions/setup-node@v6`
    - `.github/workflows/ci.yml` contains `node-version: ['20.x', '22.x', '24.x']`
    - `.github/workflows/ci.yml` contains `npm ci`
    - `.github/workflows/ci.yml` contains `npm run typecheck`
    - `.github/workflows/ci.yml` contains `npm run build`
    - `.github/workflows/ci.yml` contains `npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts`
    - `.github/workflows/ci.yml` contains `HOME: ${{ runner.temp }}/agent-badge-home`
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/ci.yml && rg -n "node-version: \\['20.x', '22.x', '24.x'\\]|scenario-matrix|release-readiness-matrix\\.test\\.ts" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI baseline is in place with supported Node versions and explicit scenario-matrix validation without live provider directory dependency.</done>
</task>

</tasks>

<verification>
- `npm test -- --run packages/agent-badge/src/commands/release-readiness-matrix.test.ts` succeeds
- `.github/workflows/ci.yml` includes both `validate` and `scenario-matrix` jobs
- CI commands use fixture-backed tests and do not rely on host machine provider directories
</verification>

<success_criteria>
- REL-01 is represented by a dedicated scenario-matrix test harness and CI gate.
- REL-02 baseline is satisfied for build/typecheck/test validation across supported Node versions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-release-readiness/07-01-SUMMARY.md`
</output>
